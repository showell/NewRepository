# Compiler -- open capabilities

Quire-domain backlog, same rules as the app registers: an entry says
what is still missing and nothing else, a closed entry is DELETED, and
a gap that is still real is never quietly dropped. There is no
platform-wide register; `docs/PM/CurrentPlan.md` carries the shape.

A deletion is proven seed-neutral only by the BINARY, never assumed: build the
candidate from the depot seed and compare it to that seed byte for byte outside
the signature window (offsets 40..135). COMPILER-64 came out identical and
shipped no seed; COMPILER-23's four unreferenced functions came out 228 bytes
smaller (main 24438), so code nothing references can still be present in the
shipped seed, and a removal that assumes otherwise lands a seed it never proved.

| # | Capability | State of the gap |
|---|---|---|
| COMPILER-72 | **The ARM64 backend resolves record field offsets through a hand-written name-to-index table, nothing at RUNTIME observes it, and 30 of its 110 entries are provably wrong for at least one record.** `a64-hardcoded-field-index` (`codex/plugs/arm64/Arm64CodeGen3.codex`) maps a field NAME to a slot number. It takes the type name and IGNORES it, so one name gets one index across every record in the tree. It is a FALLBACK: `a64-find-field-index-st` uses the record's own field list when the type carries one and reaches the table only when it does not, which is why the sabotage below passed and why the blast radius is bounded by that path rather than by the table's size. ARM64 is the only backend exposed: x86-64 derives the index from the record's field list (`find-record-field-index`), and RISC-V resolves through the field list, then type-defs, then learned fields (`rv-find-field-index-st`), with no hard-coded table at all. **HOW IT WAS FOUND (blu, 2026-09-08, during the `recv-buf` removal, main 24239, which shifted four entries):** the table was corrected, the arm64 plug rebuilt, `net-recv-heap` run cross to arm64, PASS; then ONE index was set to a wrong value, the plug rebuilt, the wrong value confirmed in `build-output/plug-source.codex`, and the arm PASSED AGAIN (L-VACUOUS). **THE RUNNER EXISTS: `build/check-a64-field-index.ps1`** (blu, 2026-09-08). It grades each entry against the field's position in its record's DECLARATION order, which is what the real path returns, so the sabotage that no arm could see now fails in one second with the file, line, table value and declared position named. Controlled in both directions before it was trusted: the sabotaged entry FAILS, and an entry whose name several records share passes, which is the instrument's honest boundary rather than a pass. **WHAT IT MEASURES, and the denominator matters: 110 live entries, 62 GRADED, 46 ambiguous, 2 naming no record in the tree.** An ambiguous entry cannot be graded because the table is keyed by name alone; **30 of those 46 name records that declare the field at DIFFERENT positions**, so the table's single answer is wrong for at least one of them by construction (`state` answers 0 while 83 records declare it at 0/1/2/3; `session` answers 0 while `NetResult` declares it at 1). That is the design flaw the runner cannot fix, and closing it means making the table type-aware or deleting it in favour of the type-defs path it already falls through to. **STILL OPEN, in order:** (1) the 30 conflicting entries above, and THE OBVIOUS EXPERIMENT IS PRICED OUT: the way COMPILER-9 settled its sibling table was to empty it and compare fail sets, but Renode is out (Damian, 2026-09-01), and the compile-only substitute (emit arm64 with the fallback at -1, diff the emitted bytes against a baseline emission) costs one arm64 cross compile per subject, MEASURED AT 66 SECONDS on 2026-09-08, so 668 cross-eligible subjects is about 12 hours per pass and 24 for the pair. Nobody should grant that for this. A targeted subset -- the arms touching the records the table actually names (net, virtio, PCI, HTTP) -- is about an hour per pass and is the version worth asking for if the answer is ever wanted; (2) CLOSED: the runner is WIRED, in the text-only cluster beside `check-cite-names` and `check-builtin-alloc`, by editing `codex/build/BuildScript.codex` and installing the emitted text (root ruled 2026-09-08 that `Build.md` governs and `build.ps1`'s no-hand-edit header is the stale sentence). Drift is 0, the shipped diff is one 19-line addition, and the phase was proven in the gate's own invocation form in both directions: clean it exits 0, and with `recv-cap` set to 9 it prints the file, line and declared position and exits 1; (3) CLOSED: `a64-hardcoded-field-index-disabled`, a second copy holding 94 entries that nothing called, is deleted (102 lines), which the plug rebuild and the arm64 cross arm both survive. It had already cost this lane one pointless correction before anybody noticed the name. Unowned. Whoever takes it should decide whether the subject belongs in `codex/plugs/plugs-backlog.md`, the way COMPILER-9 asks the same of the sibling ARM64 precedence table. |
| COMPILER-1 | **Runtime scope enforcement for effect grants** | Re-homed from CAPABILITY-REFINEMENT step 6 (moved to Done 2026-08-05). Compile-time scope enforcement is shipped and pinned (`lint-effect-scope` / `scope-grants` in the type checker; `codex/test/errors/scope-violation`, `scope-network-port`). **Correction 2026-08-11 (val): the earlier claim that "the runtime half was never built" is FALSE, verified against source.** Runtime scope enforcement IS built and sound on x86-64 for FileSystem (path prefix) and Network (host:port authority), and the effect-op path enforces the SCOPE, not just the cap bit. Read path: `__fs-read-servicer` (checks `cap-filesystem-read`) -> `fat16-servicer-read` -> `fat16-read-text` -> `fat16-read-bytes` -> `fat16-resolve-path`, which calls `fat16-scope-admits` reading `process-get-scope process-get-pid` (the CALLING process's scope; the servicer runs in-context, only flipping the elevation flag). Writes are symmetric via `fat16-create-file` (`fat16-create-directory`, `fat16-list-root`, `fat16-root-entries` gated too); network via `net-scope-admits` in `http-request` / `fetch-tls`. Scope is serialized into the CDX (`manifest-cap-bytes`: cap-id, direction, scope-len, scope-bytes) and loaded into the proc table (`proc-scope-offset` fs, `proc-net-scope-offset` net) on BOTH boot (`emit-set-boot-scope`) and verified-load (`ProcessCaps.apply-load-decision`); the load-time wiring is pinned by `loader-scope-test`, `loader-network-scope-test`, `process-caps-test`, `network-scope-{open,deny,spawn}`. **Real remnants:** (1) **ARM64 has no counterpart.** The whole process/capability kernel is absent on ARM64 (no proc table, no syscalls, no fs/net execution), so scope is silently unenforced there. This is a MAJOR effort and its own initiative, not a COMPILER-1 tail; a QEMU bed now exists. Full detail and the parity chain are tracked in `codex/plugs/plugs-backlog.md` 1.10. (2) **Runtime denial IS tested end-to-end (correction 2026-08-11; the earlier "no denial test" claim here was wrong, caught by reading the tests).** `scope-runtime-deny` boots a program granted `FileSystem.Read "OK"` on a real disk and asserts the out-of-scope write (False), read (<none>) and root listing (0) are all refused while in-scope ops succeed (re-run green here, seed AF4E14D9); `scope-runtime-spawn` asserts a spawned child inherits the scope and denial (10); `scope-runtime-open` is the no-scope control; `loader-scope-test` asserts the verified-load path installs the scope. No denial-test gap remains. The only combination not in a single test is a verified-load-NARROWED scope plus a runtime out-of-scope read in one program; it is covered compositionally (same `proc-scope-offset` cell, same `fat16-scope-admits`), and a composed integration test would be real but marginal. (3) **Architecture, revisit ONLY if it becomes structurally limiting (Damian ruling 2026-08-11):** enforcement is library-level -- the servicer routes through the gated `fat16-*` functions rather than the stub checking the path itself. There is no single chokepoint (`Fat16.codex:1805-1809` at head 2026-09-08, drifted from the 1073-1077 this row carried: "the gate is applied three times, and a fourth caller that reaches the disk another way needs a fourth"), so a future ungated door is a silent hole with no test to catch it. A syscall-boundary chokepoint would collapse the N gates to one, but is a seed-affecting change that stays redundant with gates that must remain for direct in-process callers. KEPT AS-IS: the burden is a clarity/discipline cost, not a structural break. Not blocking anything current. |

| COMPILER-7 | **Reconsider the `bounded` check's one over-refusal, after it has been in use for a while** | Damian, 2026-08-16. The `growing` inference (Cost Model 3.3 step 2, blu) scores **10 of 11** on `codex/test/cost/accumulator-corpus`, measured by ablation against the previous seed: 5 of 5 on the quadratic half, 5 of 6 on the negatives, table in `CostModel.md` section 8b. **This row said 9 of 10 until 2026-08-17, which was the score before rule 3 landed at main 16118 and took the corpus from ten entries to eleven; the count was stale, the row is not.** Still exactly one LINEAR entry refused, `n-fixed-appends`, which appends to a growing accumulator in a self call exactly four times however large the input gets and so has the shape without the cost. Ruled the cheap direction under abstain-toward-refusal (`docs/Designs/Active/Features/CostModel.md` section 5 q2, section 8) and paid in a declaration an author can decline to write. **Revisit once real chapters carry `bounded` declarations:** count how many callers the over-refusal actually bites in the tree (a refusal that never fires costs nothing; one that makes authors drop the declaration costs the guarantee). Any rule that lifts it must keep 5 of 5 on the quadratic half of the corpus, measured by the corpus and not argued. Not before the declaration has been in the tree for a cycle.  **CENSUSED 2026-09-01 (blu) and the revisit condition is UNMET, so do not open this yet.** "Once real chapters carry `bounded` declarations" -- they do not: a walk of 3,791 `.codex` files finds **72 `bounded` declarations and ALL 72 are in `codex/test/**`**, zero in `apps/`, `codex/foreword`, `codex/os`, `codex/compiler` or `codex/plugs`. So the over-refusal bites no caller today and costs nothing. Note the corpus is NOT where to look: `accumulator-corpus.codex` carries no `bounded` line at all, because it scores the INFERENCE by ablation rather than declarations. **RE-VERIFIED 2026-09-02 (blu), condition STILL UNMET, so this row stays parked.** A walk of 3,736 `.codex` files (`codex` and `apps`, excluding `build-output` and the generated `codex/build` shell scripts) finds no `bounded` DECLARATION outside `codex/test`. A declaration-shaped pattern returns ten hits outside it and **all ten are PROSE** (`punctual: no heap, no recursion, bounded instruction count` in the six `codex/foreword/punctual` chapters, `Section: Bounded Slots`, `Section: Bounded Window`, and two sentences), which is the trap this re-measure existed to catch: the word is common in prose and the declaration is not. Verdict: OPEN, correctly parked, not stale. |
| COMPILER-11 | **`noalias-note` has a positive arm and no negative arm, so "proven unaliased" and "nobody asked" are one output; DECIDED: no note at the not-proven arm** | From PR 66 finding 10 (plugs-backlog 1.27; confirmed reek 16195; red routed to root 2026-08-17). `emit-record-set-builtin` (`Emit/X86_64Builtins.codex:947`) and the field-store site (`X86_64.codex:1222`) call `noalias-note` (`X86_64Compound.codex:1891`), which raises the INFO `cdx-noalias-proven` (CDX4011) only when the base is a unique linear owner (`noalias-slots`, from `def.unique-params`) and returns the state untouched otherwise; `fc-store-barrier` raises the same code for a store through a fresh allocation. **Measured on the corpus, 2026-08-17 (root):** compiling the whole compiler (`build/output/quine-check/compiler.ir.log`, the gate's own log) raises CDX4011 ZERO times: the compiler has no linear-parameter record updates, so the positive arm never speaks on the largest program we have. A note at the not-proven arm would fire at every ordinary in-place store: 401 `__record-set` sites in `codex/compiler` (415 in the compiler's IR, plus 69 field stores), 2,729 more in `apps/`, 11 in `codex/test`. Every one of those is the documented concession (`DevelopersGuide.md`, "`__record-set` is not a functional update"; `mutable` records are unique-owned by the checker, CDX2062), written knowingly, so an info there would be an instrument that says the same thing at every site and is read by nobody (L-FALSIF): 400-plus lines in every compiler compile log. **Decision: the not-proven case gets NO note.** What the finding actually asks for is that the ABSENCE of CDX4011 must not be read as a safety claim, and that belongs in the code's own description: `CdxCodes.codex:358` should say "absence means not proven, not unsafe"; a description string is compiled into the seed, so it is folded into the next seed-affecting CL that touches `CdxCodes.codex` rather than spending a seed cycle on a sentence. **What would change the decision:** a consumer. If the IndependentRechecker or an audit ever wants a per-site verdict, emit the negative arm under `wire-trace` only, at `__record-set` sites whose base is neither fresh, nor linear, nor a `mutable` record, which is the only class where the concession is load-bearing. Not seed-affecting as decided. |
| COMPILER-14 | **`emit-record` had two record layouts and every reader used only the first; the second is now a refusal** | From Steve Howell, PR 67 (`gh pr view 67 -R damiant3/NewRepository`), routed by red 2026-08-17. `emit-record` (`Emit/X86_64Compound.codex:1839` at head 2026-09-08, drifted from the 1751 this row carried) laid a record literal out by `build-cce-byte-offsets` (width-prefixed sort key, cumulative widths) when `resolve-constructed-ty` answered a `RecordTy`, and otherwise by `emit-store-record-fields-by-list` (name-sorted rank times 8); `emit-field-access` and `emit-record-set-builtin` read by the first rule unconditionally, so a record built through the fallback with any field narrower than eight bytes was read at the wrong offsets (PR 67 probe: `Box = record { flag : Boolean, items : List Integer }`, `b.items` reading the boolean). Steve could not reach the fallback through `opening.codex`; his harness reached it only by skipping RESOLVE. **Root, 2026-08-17: the fallback is replaced by the refusal its neighbour `emit-field-access` already makes** (`st-add-error cdx-ir-error "emit-record: unresolved type for record literal"` and `ud2`), and `emit-store-record-fields-by-list`, `field-local-names`, `RecordLayoutPair` are deleted. Reachability was measured by that refusal under the gate: the compiler self-compile and the BVT never take it (a taken branch is a red gate). Not measured beyond the gate: the full battery and the apps; a red there with this message means the branch IS reachable and the close becomes "lay out by `cce-byte-offset-and-type` so the two rules cannot drift", not a restore of the by-list layout.  **THE APPS HALF IS STILL NOT MEASURED, and a green `-Internal` does NOT discharge it (blu, 2026-09-01).** `-Internal`'s `app-sweep` passes `-Sample 30`, so it sweeps a strided **30 of the 270** entry chapters; only the release gate sweeps all of them (`build/build.ps1` above `Measure-Phase 'app-sweep'`). Reading a green internal gate as "the apps are covered" overstates it ninefold (L-DENOM: the cap is not the corpus). So this row still waits on a release gate or a battery, neither of which is an agent command. **RE-VERIFIED 2026-09-08 (red): the row is correctly parked, and the line it quoted has both MOVED and CHANGED.** It is `build/build.ps1:1406`, not 1119, and it now reads `if ($Internal) { $sweepArgs = @(if ($tCompiler) { @('-Sample', '30') } else { @('-CiteScoped') }) }`, so the strided sample of 30 applies only when `$tCompiler` fires and the other arm is `-CiteScoped`. The row's conclusion is unaffected and is now true for a second reason: only a release gate or a battery sweeps every entry chapter, and `-Internal` is banned for a lane in any case. Both are outside what an agent may run, so nothing in this lane can discharge this row. Verdict: OPEN, blocked on a non-agent action, not stale. |
| COMPILER-16 | **The compiler's own warnings are gated by a ratchet now; the GATE still throws its self-compile log away.** | Unowned. **THE RATCHET IS BUILT:** `build/check-compiler-warnings.ps1` reads a self-compile log, counts `warning CDX####` per code against the dated `build/compiler-warning-baseline.txt`, and fails on a RISE, on a FALL not recorded in the same changelist, on any error in the log, and on a log it cannot read, because a check that passes when its input is absent is not a check. It boots nothing. All six arms were fired before it was called done (red 2026-09-08): clean passes, a doctored rise fails, a doctored fall fails, an unbaselined code fails as a rise from 0, an injected error fails, a missing log is refused. Its header carries the trap that a STAGE-2 log is the seed compiling your source and grades no change of yours; the stage-3 log is the one that does. **THE BASELINE: 8 warnings, 7 CDX3005 + 1 CDX6020, 0 errors** (2026-09-08, stage 3 of the COMPILER-71 candidate; unchanged across CDX2097 and the complete traversal landing). **WHAT REMAINS, and it is the retention half:** `Invoke-BuildCdx` in `build/build.ps1` writes the self-compile log to `GetTempFileName()` and keeps it only on FAILURE, so a green gate discards every warning it saw and there is nothing for the ratchet to read after the run that matters most. `build.ps1` is GENERATED from `codex/build/BuildScript.codex`, so both halves land in one CL (`Build.md` 187), and a LANE cannot verify the result: running the gate is Damian's (`-Internal` is banned), so whoever takes this needs his run or his say-so. **WHAT IS ALREADY DONE AND MUST NOT BE RE-DERIVED:** CDX6020 was a shape detector firing on 13 benign sites and is now a hazard detector gating on `ctor-field-mutation-hazard` (`Types/TypeCheckerInference.codex:1912`, drifted from the 1776 this row carried), which took the self-compile 13 to 1. Its control `docs/Test/Active/Cdx6020Hazard.codex` has THREE arms since COMPILER-71: a hazard, a safe arm, and `hazard-in-list`, whose read sits in a list literal so that it separates a complete traversal from a partial one; the first two cannot, because both reach their read through `AFieldAccess`. The surviving CDX6020 is the computed-value site in `X86_64Compound`, where the mutated record has no name to extract and warning is the deliberate conservative direction. The CDX2064 instance that started this row is fixed. **WHY IT IS WORTH A RUNNER AT ALL:** that CDX2064 was a live miscompilation the checker reported correctly on every build for as long as it existed, and nobody read it (L-BODY). The 7 CDX3005 are untouched and outside this unit. |
| COMPILER-17 | **The boot capability grant and revoke emitters build a 64-bit mask through `li` plus `or-rr`/`and-rr`, and nothing exercises a bit at 31 or above end to end.** `emit-grant-cap-mask`, `emit-grant-capability` and `emit-revoke-capability` (`Emit/X86_64Boot.codex:2800`, `:2810`, `:2821`) were rewritten from the sign-extending imm32 form (`or-ri`/`and-ri` is `alu-ri` writing `write-i32` under REX.W, so a bit-31 mask became `FFFFFFFF80000000` and a bit at 32 or above was dropped); `emit-check-capability` (`:3053`) is `bt r/m64, imm8` and was never wrong; the riscv and arm64 boot grants REFUSE a mask carrying bit 31 or above with `[UNSUPPORTED]` and emit no grant. The loader round trip is graded by `codex/test/ops/cap-word-64` (bit 31 and bit 40 through `peek-qword`/`poke-qword`, with a `poke-32` control row that answers 0, so a compiler that narrows the pair back to 32 bits fails it). **The emitter half is UNGRADED**, because no capability sits at bit 31: `codex/foreword/core/Capability.codex` assigns every bit 0 to 30 (`cap-rng : Integer = 30` is the last, measured 2026-09-08), so the NEXT capability lands on bit 31 and its row owes the fixture, a runtime arm that reads proc 0's cap word after boot and shows bit 31 grants only bit 31. Owner: root (HAL), with the first bit-31 capability. |
| COMPILER-22 | **Two CCE-to-Unicode paths are ungated and one is uncalled.** Every reader of `cce-to-unicode-byte-table` in `Emit/` combines the hi byte and the serial print path, `text-to-unicode-bytes` and the reverse `unicode-bytes-to-text` are graded (`codex/test/ops/tier0-cyrillic-print`, `text-to-unicode-bytes-tier0`, `unicode-bytes-roundtrip`, verified at head 2026-09-08). Still ungated: `__uefi_print`, `__uefi_print_no_nl` and `__uefi_read_file` (`Emit/X86_64Helpers.codex`), because nothing under `codex/test` runs under `-uefi` and the sidecar set cannot ask for it (L-NOGATE). Still uncalled: `__cce_to_unicode`, emitted into every binary by `X86_64Helpers.codex:208` and `codex/plugs/riscv/RiscVRuntime.codex:2838` with no caller anywhere in the tree (L-UNCALLED); deleting it is a design call nobody has taken. Unowned. |
| COMPILER-28 | **OPEN, and it is the narrow remainder of a fixed row: the SECOND narrowing swallow point.** The declaration-level half is FIXED on main 19731 (blu, 2026-08-25): a range on a base that cannot carry one is REFUSED at the declaration with CDX2054, verified at head 2026-09-07 in `Types/TypeChecker.codex` (`check-bounded-bases`) and `Core/CdxCodes.codex`. **What remains:** `lint-narrowing-check` opens on `IntegerTy` and falls through `is otherwise`, so it catches nothing on any OTHER route to a non-integer declared bound. It is UNREACHABLE by the declaration route, which is now refused earlier, and the file records this itself in the prose above `check-bounded-bases-def` (`Types/TypeChecker.codex:3529-3534` at head 2026-09-08, drifted from the 3527 this row carried). `lint-narrowing-check` is DEFINED in `Types/TypeCheckerInference.codex:1208`, not in `TypeChecker.codex`, which is where this row's wording sends a reader; `TypeChecker.codex:896` is a call site. Deliberately left open. **What it unblocks is live:** the compile-time backstop half of COMPILER-23's defect C is no longer impossible. That row was DELETED as closed in this audit; its repair is main 19813 and its guard is `codex/test/ops/unicode-bytes-roundtrip`, which is where to read before assuming a bounded domain cannot be declared. |
| COMPILER-74 | **A type variable no constraint reaches is never resolved, so a well-typed program whose IR still carries it is refused by a typed plug and runs on bare metal** | Steve Howell, PR (this row's carrier), measured with a hosted `codexir` at u58-candidate `77ed0cbf`. Specimen: `make-empty : Integer -> List a; make-empty (n) = []` used once as `list-length (make-empty 0)`. Nothing constrains the element: `opening`'s wire carries `(name "list-length" (fn (list (tvar 6)) int-default))` and, after inline-single-caller, `(list-expr (elems) (tvar 6))` -- one variable, consistently, with no binder in `opening` and no `forall`. Bare metal erases it and prints `0`; the zig plug refuses (`no element type for this empty list`). It is not the inliner: before its return-type pair landed the literal carried the helper's `(tvar 2)` instead, and the plug refused the same way. The gap is a per-definition finalization the checker does not have: after a definition's constraints are solved, a variable that is neither quantified by the signature nor bound by any constraint is defaulted (`int-default`, as literals already are), so no hole reaches the wire. The Rust front end at `rust-codex-compiler` does this (`default_ambiguous_vars`, commit `4d0c8e4`) and its IR for the specimen is `(list-expr (elems) int-default)`, which the plug builds. `codex/test/roc-alias-empty` is the same gap through the zig plug today. `ir-fidelity -Disagree` cannot see it: the def agrees with itself, and the atom is a legal `tvar`. Unowned. |
| COMPILER-30 | **`ErrorTy` is BOTH the type-failure atom and lowering's no-expectation sentinel, and the IR drops types the checker did resolve, so a plug cannot tell "the checker failed" from "nobody wrote it down"** | Steve Howell's note "Zig as the demanding customer" (2026-08-27, via Damian and red); both halves verified against source and by measurement by blu the same day. This is the named residue behind `codex/plugs/plugs-backlog.md` 1.96, where the Ada and Fortran ErrorTy arms guess a 64-bit integer. **Half one, the overload, is a source fact:** `IR/Lowering.codex:689` and `:707` (`lower-let`, `lower-let-rest`) pass `ErrorTy` as the EXPECTED-type argument to `lower-expr-at`, meaning "no expectation", and `:55` (`AFieldAccess`) does the same, while `ErrorTy` is simultaneously the atom the checker produces for a genuine type failure. One atom, two meanings, and the wire carries no marker separating them. **Half two, the fidelity gap, is measured on `codex/test/roc-fold-empty`**, which compiles clean (exit 0) and prints its expected `42`: its IR carries `(param "xs" (list error))` on the lambda parameter while THE SAME NAME inside that lambda's body carries `(name "xs" (list int-default))`, so the checker computed the element type and the IR failed to carry it to the declaration site. The same def also shows `(let "total" error ...)` against `(name "total" int-default)` two nodes later, and the empty literal itself as `(list-expr (elems) error)`. **By the discriminator Steve's note states -- did the checker compute an answer the IR failed to carry? -- this is UPSTREAM, in the compiler, not the plug's work.** Distinct from COMPILER-15, which is two specific `is otherwise -> []` sites in codegen with a residue measured as bounded at 2; this is a source-level empty literal in argument position, a third producer. Distinct from COMPILER-13, whose multi-parameter lambda defect is closed and guarded. **Note before re-measuring: `-IrUni` and `-IrCce` carry different IR since main 19558 (see COMPILER-12), so a measurement of the WIRE must not be taken from `-IrUni`; the figures above are `-IrUni` and are about what the CHECKER resolved rather than about the wire.** **THAT NOTE IS STALE AS OF blu's LIFT UNIFICATION (main 20176); measured 2026-08-27 by fester against seed `4341370C8FE5BAD6`, the two paths now carry the SAME IR and `-IrUni` is a sound wire measurement.** Not a length proxy: for four programs the `-IrCce` bytes were aligned position-by-position against the `-IrUni` characters and the map checked in BOTH directions, giving a clean bijection with zero inconsistencies (`lambda-param-type` 1153 positions / 42 distinct characters, `parametric-sum-pattern-binding` 1637, `empty-list-element-type` 1127, `bounded-int-derived-range` 1151; CCE is one code per character over this content and the single trailing byte is the newline, CCE 1). The lambda program is COMPILER-12's own discriminating subject, the one that would diverge if only the CDX path lifted: both paths now emit the lifted `__lam_0`. A stated blocker is the one sentence nobody re-tests, because its whole function is to stop people looking, so this was re-tested rather than inherited. The reserved work in this row is untouched by this note. **THE LAMBDA-PARAMETER CARRIER IS FIXED (Steve Howell's PR 93, absorbed by red in the CL carrying this edit; the design and every measurement are his).** `LambdaExpr` carries its lambda token through the parser's three construction sites, the desugarer passes `token-span` instead of `synthetic-span`, `infer-lambda` records the solved function type under that span, and `lower-lambda` asks for it through `lambda-expected-ty` ONLY when its expectation is `ErrorTy`, so the expectation-present path is untouched by construction. His measurements: matrix `error` cells 6 of 11 to 0 of 11 with case f (`Text`) recovering `text` rather than a default, case g honestly a type variable, both controls byte-identical; corpus clean 318 to 324, no program regressing, 570 of 577 emitted files byte-identical. **STILL OPEN in this row, none of it closed by the PR:** the `ErrorTy` overload itself (his explicit question upstream: should `ErrorTy` on the wire be reserved for actual type failures? -- a design call, rulings-queue shaped); the empty-list carrier (`roc-fold-empty` above); and two lambda sites still desugaring synthetic, `ForExpr`'s `map-list` lambda (`Desugarer.codex:78`, `var-tok` available) and instance-method synthesis (`:1455`, `m.name` available), left out of the PR because his matrix does not measure them. **RESERVED 2026-08-27 (red, from Steve Howell's comment on PR 93, ruled by Damian: grab his work, suspend ours): DO NOT TAKE THESE UP.** Steve reports measured fixes on his branch for the empty-list carrier (recorded under the literal's span, asked for only when the context supplies no list type; `roc-fold-empty` goes markers to match) and for the instance-method site (`token-span (m.name)`; his measurement shows `typeclass-smoke`/`typeclass-poly` refusing today, with a method lambda disagreeing with itself inside one node), plus two more in the same family: `subst-type-vars-from-arg` (`IR/LoweringTypes.codex`) learns through only its `TypeVar`/`ListTy`/`FunTy`/`TypeApply` arms while `SumTy`, `RecordTy`, `ConstructedTy`, `LinkedListTy`, `VectorTy`, `UnitTy` and `LinearTy` all land on `is otherwise -> target`, so the learning direction is blind where the applying direction (`subst-type-var-in-target`) already walks every variant; and `lower-nonempty-list` consults its elements only when the context offers NO type, so a wrong context wins unopposed (his `hamt-test` reading). All four are fixed and measured on his corpus arm with a bare-metal sweep running; they arrive as a second PR after the mirror push moves the pin, and red answered "send all four" on the PR. The ForExpr `map-list` site (`Desugarer.codex:78`) stays here, unmeasured on both sides. **THE ERRORTY OVERLOAD IS RULED (Damian, 2026-08-27 afternoon): SPLIT NOW.** `ErrorTy` on the wire is reserved for genuine type failures; a distinct no-expectation marker takes the sentinel meaning at every site that currently passes `ErrorTy` to mean "no expectation" (`IR/Lowering.codex:689`, `:707`, `:55` per the measurement above). Every plug inherits the answer; Steve Howell's second PR rebases onto the new wire and was told so on PR 93. When the split lands, `build/ir-fidelity` gains a new invariant for free: a clean compile carries zero `ErrorTy`. **THE SPLIT IS LANDED (fester, main 20316 compiler and seed, 20327 the plug arms) AND THAT FREE INVARIANT DOES NOT HOLD.** `NoExpectTy` took the sentinel meaning and `ErrorTy` kept only genuine type failures, but the promise in the sentence above was written from reading and is false in the tree. **Measured against the landed seed over all 607 programs in `codex/test`: 596 compile clean and 23 of those carry `error`, 251 occurrences of it at ONE site** -- `list-expr[2]` on an EMPTY list literal, which is `lower-empty-list`'s `is otherwise -> deck-record (IrList [] ErrorTy sp)` at `IR/Lowering.codex:1319`, Steve Howell's reserved fix and deliberately untouched here. A `let` binding one of those also records `error` as its own type, so the two sites travel together. The instrument is `build/ir-fidelity -Census` and the attribution is measured rather than read: ablating `:1319` alone to a concrete type removes both sites and moves nothing else. **`-Census -Calibrate` is the part that makes a reading worth anything** -- a positive arm that must find the atom and only in the program with no type expectation on its empty list, and a negative arm asking the same corpus for an atom outside the wire vocabulary, which must find nothing. A census that reports "no sites" is otherwise indistinguishable from a reader that cannot see. **Also worth knowing before quoting a number here: the constrained sibling is CLEAN, because `:1318` takes the `ListTy` arm and records the element type.** So the defect is an empty list literal whose type is not constrained at that point, not empty lists in general. Remaining open here: Steve's second PR, the `ForExpr` `map-list` site (`Desugarer.codex:78`, unmeasured on both sides), and the invariant itself once `:1319` is fixed. Open work, unowned. **CORRECTION 2026-08-28 (Steve, PR 93 comment 20:34Z, absorbed by root): the instance-method half of the reservation is WITHDRAWN.** He built `token-span (m.name)` and it does not hold: a method lambda is a record FIELD VALUE whose declared type supplies the expectation, so the no-expectation arm never fires there and his `typeclass-smoke` readings were UNCHANGED. The instance-method site stays unmeasured; the empty-list-carrier fix stays expected in his second PR. His replacement lead, convergent with our issue-94 reply's `Desugarer.codex:1377` pointer: `Showable-dict-Integer` carries `(args (tvar 511))` where the Boolean sibling instantiates, in a unit with no foralls at all. **THE RESERVATION IS CLOSED: THREE OF THE FOUR LANDED (Steve Howell, PR 101, absorbed by red 2026-09-01), THE FOURTH WITHDRAWN BY HIM AS MEASURED (above).** (1) The empty-list carrier: `infer-list` now files the fresh variable under the literal's span (`record-expr-type`), and `lower-empty-list` asks for it through `empty-list-element-source` ONLY when the context supplied no list type, so the `ListTy`/`LinkedListTy` arms are byte-for-byte what they were. (2) `subst-type-vars-from-arg` learns through `SumTy`, `RecordTy`, `ConstructedTy` (name-guarded, positional args), `LinkedListTy`, `VectorTy`, `UnitTy` (name-guarded) and `LinearTy`, so the learning direction walks what `subst-type-var-in-target` already walked. (3) `lower-nonempty-list` lets a witness from element 0 win over a context element type that still carries type variables, when the witness has no `error` and admits no widening (`list-elem-witnessed`). ONE DEPARTURE FROM THE PR, on R-COST grounds: his version lowered element 0 as a witness on EVERY non-empty literal, so a fully typed list lowered its first element twice (and a lambda there would lift twice); here the witness is computed only when the context carries type variables, which is the only case in which it can win, so every fully typed list is unchanged. MEASURED against a kernel built from this source (`Sut101`, unsigned, 45755F2C): `roc-fold-empty` wire `error` 6 to 0 with `(param "xs" (list error))` now `(list int-default)`, program still prints 42; `hamt-test` loses `tvar 25` (type variables with no `forall` in scope 17 to 16, three defs changed) and still matches its `.expected`; the changed compiler compiles its own source to a byte-identical binary in one pass; diagnostic census unchanged (CDX3005 7, CDX6020 13). STILL OPEN HERE: the ForExpr `map-list` site (`Desugarer.codex:78`, unmeasured on both sides); the `build/ir-fidelity` invariant "a clean compile carries zero `ErrorTy`" is MEASURED (fester, 2026-09-07, seed 39D40A84, 632 compiles in 255 s): **IT NOW HOLDS, and that half is CLOSED (red, 2026-09-08).** It did not when fester measured it: **1 of 615 clean compiles carried `error`, 8 occurrences over 5 sites, ALL in `lazy-smoke`** (`fn[2]` 3, `name[2]` 2, `field-access[3]` 1, `if[4]` 1, `let[2]` 1); 11 programs refused and are not in the denominator. That is down from the 23 of 596 / 251 occurrences this row recorded when the split landed, and the empty-list carrier that produced them is gone -- the specimen now lowers to `(list-expr (elems) (tvar 269))`. **NOT ATTRIBUTED: COMPILER-32's open item is `noexpect` in the SAME program at overlapping site names (`fn[2]`, `let[2]`), and whether the two share a root cause is unmeasured; they are different atoms and this census counted `error`.** The census was blocked first by an inverted `$calClosed` table and both halves are measured in the script's own comment; and a NEW instance of the family Steve names in the PR, `lower-apply-lambda-chain` after main 20706 typing `let "total"` in `roc-fold-empty` as a three-argument function on the wire (`(let "total" (fn (list int-default) (fn int-default ...)))`) while the name reads back an integer, which he reports bare metal reproduces and which refuses in his zig arm as `expected type i64, found CxFn3`. **THAT INSTANCE IS FIXED (verified red, 2026-09-08).** `lower-apply-lambda-chain` no longer asks `expected-or-recorded-ty`; it peels the recorded type once per applied argument (`peel-applied-return ... n`, `Ir/Lowering.codex:405`), which is the over-currying gone at its source, and `roc-fold-sum`, `roc-fold-count`, `roc-fold-product` and `roc-fold-empty` all compile OK and run PASS on the arm64 cross bed against seed `12B76F40E611053F`. **Measured behaviourally on purpose:** the CL that introduced the defect shipped green on a static IR census, which is the failure this whole row is about. His standing offer: a whole-IR property, "for every expression the checker resolved, does the wire carry the answer", built on his non-erasing arm, reported as one list rather than a sixth finding. |
| COMPILER-59 | **A desugared node built with `synthetic-span` CANNOT carry a recorded type, so any node whose lowering consults the type table silently gets `ErrorTy`** | Named by fester 2026-09-07 out of COMPILER-30's ForExpr residue (main 22584), and MEASURED before any design was written, which changed what the row says twice. **IT IS NOT A COLLISION.** The first reading was "150 sites share one span key, so records overwrite each other". False: the guard is symmetric and deliberate. `record-expr-type` (`Unifier.codex:149`) stores NOTHING when `is-synthetic-span`, `lookup-expr-type` (`:180`) answers `ErrorTy` for one, and `is-synthetic-span` is `span.file-id == 0`. There is no collision, no last-writer-wins and no dead storage: a synthetic span is excluded from the expression-type table on BOTH sides, by construction. So the defect is not corruption, it is ABSENCE, and it is only a defect where something asks. **SEVEN CONSUMERS ASK** (`IR/Lowering.codex`): `lower-lazy` :123, `lower-dict-placeholder` :234, `lower-class-method-ref` :266, `lower-name-normal` :279, `lower-apply-lambda-chain` :405, `expected-or-recorded-ty` :802, `empty-list-element-source` :1426. **THE COUNT WAS A SHAPE FLATTENED (L-ADJECTIVE): 150 sites, 32 EXPOSED.** Of the 150 `synthetic-span` uses in `Ast/Desugarer.codex`, only those building a node kind one of those seven lowers can lose anything: `AApplyExpr` 19, `ANameExpr` 10, `ALambdaExpr` 2, `ALazyExpr` 1. The other 118 build literals, types, params and patterns, which never consult the table and cannot lose a type this way. Anyone planning off "150" budgets nearly five times the work. **R-COST, and it runs the other way from the intuition:** a synthetic span costs NOTHING today, because the writer skips it; giving a site a real span ADDS one `ExprTypeEntry` per occurrence PER PROGRAM to `st.expr-types`, which is a sorted list read by `bsearch-expr-type-pos`. The cost is per occurrence in compiled source, not per site in the compiler, and it is unmeasured. **WHAT IS NOT MEASURABLE WITH THE PRESENT INSTRUMENT, stated so nobody reports a zero as an all-clear:** `ir-fidelity -Census` counts a wire ATOM, and this failure does not produce one. The ForExpr instance surfaced as a stale `(tvar 23)` where a concrete type belonged, which is legal-looking and uncensusable; the corpus census reads `error` 0 of 615 at head and says nothing about it. **THE COUNTING INSTRUMENT EXISTS AND THE COUNT IS 3 SITES OVER 2 PROGRAMS OF 615 (2026-09-07).** `ir-fidelity -Disagree` reads a def that DISAGREES WITH ITSELF: one name carrying `(tvar N)` at one site and a concrete type at another, which needs no binder because every name on the wire carries its own type (`Emit/IRTextEmitter.codex` 362, 387, 420, 446). Calibrated on three offline arms and on the pre-fix ForExpr compiler, where it flags the defect this row's proven instance names. `-Expect` makes the baseline mechanical; it is NOT wired into any gate. The sites: `typeclass-poly:7` `equals` -> `__lam_0` `y`, `typeclass-poly:19` `sort-key` -> `__lam_2` `x`, `typeclass-smoke:11` `to-text` -> `__lam_2` `b`. **NEITHER OBVIOUS FIX IS AVAILABLE, both foreclosed by measurement.** Resolving the expectation at `expected-or-recorded-ty` (`Lowering.codex:803`) changes nothing: the store has not solved those variables at lowering time, and `typeclass-poly` `__lam_0`'s body `x` is `(tvar 305)`, another variable rather than a concrete type. The recorded route the `ErrorTy` arm uses cannot be asked either, which is COMPILER-60. A whole-IR property comparing recorded type against wire cell (Steve Howell's standing offer on PR 101) is still the general form. **PROVEN INSTANCE, and the only one measured:** the ForExpr `map-list` name, fixed at main 22584 with a three-point ablation (depot DROPPED, span-on-the-lambda DROPPED, span-on-the-name CARRIED), guarded by `build/ir-fidelity/cases/for-comprehension-element-type`. That is 1 of the 32. **NO EDIT TO THE OTHER 31** (root, 2026-09-07: measure before design). Each needs its own ablation: a site whose consumer never takes the `ErrorTy` arm loses nothing, which is exactly why Steve WITHDREW the instance-method site after building the fix and measuring no change. Open work, unowned. |
| COMPILER-60 | **A lambda parameter carries no span, so nothing the checker recorded about it can ever be looked up** | `ALambdaExpr` is `(List Name) (AExpr) (SourceSpan)` (`Ast/AstNodes.codex:41`) and `Name` is `{ value : Text }` (`Core/Name.codex:4`), so a lambda parameter has one span for the whole lambda and none of its own; `lower-lambda-params` stamps every param with it (`IR/Lowering.codex:871-877`; all four citations in this sentence re-read at head 2026-09-08 and all four hold). `lower-def-params` takes `AParam`, which DOES carry a span (`AstNodes.codex`), so the def path already has what the lambda path lacks and the asymmetry is the whole row. MEASURED CONSEQUENCE, not a hypothesis: the three sites `ir-fidelity -Disagree` reads at head are all lambda parameters whose slot holds an unsolved `(tvar N)` while the body's uses of the same name are emitted resolved, and neither available repair reaches them (COMPILER-59 records both, foreclosed by evidence). The change is `ALambdaExpr` carrying `AParam`, the desugarer keeping the token span it drops at `desugar-lambda-param` (`Ast/Desugarer.codex:233`), and the checker recording under it so `lookup-expr-type` has an answer to give. **BUILT AND MEASURED 2026-09-07, NOT LANDED: it moves 2 of the 3 sites and REGRESSES a def.** Two candidates off the depot seed (`40D70FA13AD14646`, `83F52E4B2FB833B0`) both compile clean and both read 2 sites over 1 program against 3 over 2 at head: `typeclass-smoke` goes entirely clean and both `__lam_2` sites go, while `typeclass-poly`'s `convert` DEF, whose head wire is `(param "x" int-default) (param "y" int-default)`, comes back carrying `y` as `(tvar 312)`. It is NOT the def parameters' spans: returning `synth-bare-methods` and `synth-spec-methods` to `synthetic-span` and leaving only the lambda sites real changed nothing, same sites, same variables. The residue is the remaining lambda site, `typeclass-poly` `__lam_0` `x` `(tvar 302)` against `int-default`. Shape, R-COST count and the refuted reading: `docs/Designs/Active/Compiler/LambdaParamSpans.md`. Seed-affecting, node-shaped, and it touches the parser, the desugarer, the checker and lowering together. Open work, unowned. |
| COMPILER-33 | **The class-dictionary superclass field is an UNDER-APPLIED CONSTRUCTOR: `__super-<C>` is declared as a bare `<C>Dict` while `<C>Dict` now takes a type parameter.** `Ast/Desugarer.codex:1028` builds the field's type as `ANamedType ((cd.superclass) & "Dict")` with no argument, and `:1467` builds its VALUE as an untyped `ANameExpr`. Both re-read at head 2026-09-08 and both still hold; the line numbers this row carried (1000 and 1439-1440) had drifted. It is LATENT, not live: `codex/test/typeclass-poly` is the only subject in the corpus with a superclass (`class Equatable a => Sortable`) and it passes, so nothing forces the arity today. The exposure is a second superclass subject, or any change that makes the checker pin that field. | **BOTH ORIGINAL HALVES ARE CLOSED.** The NULLARY half was fixed 2026-09-01 (blu, main 21348, seed E5425317): `synth-class-type-defs` gave the Dict record a type parameter only when the class had more than one instance, while `synth-class-fields` copies method signatures that mention `a` regardless, so a single-instance class declared `(tparams)` empty and referred to a name nothing bound; binding it unconditionally types both dicts at `int-default`. The COMPOUND half is FIXED and VERIFIED AT HEAD 2026-09-07 (blu), which corrects this row rather than adding work: `parse-instance-type-head` (`Syntax/Parser.codex`) returns a `TypeExpr` via `parse-type` instead of answering the first token and skipping the rest, `InstanceDef.type-name` carries it, and the args-aware key the row called the hard part exists as `type-key-of` / `type-key-of-args` (`IR/Lowering.codex:729,737`), computed BESIDE `type-name-of` rather than replacing it, because all 11 of its callers want the bare name. **MEASURED, one claim run against the depot seed:** `codex/test/typeclass-compound-head` (blu 21390, main 21392) prints `plain-integer : int 7`, `compound-int : ints 3`, `compound-text : texts 2`, matching its `.expected` exactly. That fixture carries BOTH `instance Showable (List Integer)` and `instance Showable (List Text)`, which mangle to the same bare `List`, so the collision this row called unreachable is covered and green. The row previously asserted "the tree has never spelled a second compound head" while that fixture, produced by this row's own work, is the second one. From Steve Howell's issue 94 section 2; that issue stays OPEN because section 3 (`ErrorTy` is two facts in one spelling) is the free-vs-solved wire marker in `CurrentPlan`'s rulings queue and section 4 (the IR carries instantiations at call sites, not definitions) is COMPILER-24, whose dictionary-passing design is settled and not started. |
| COMPILER-32 | **OPEN, and it is BLOCKED on the backend rather than on lowering: `lower-lazy` still records the no-expectation sentinel, and it cannot come off until the function-valued force is right.** Verified at head: `lower-lazy` is at `IR/Lowering.codex:120`. The `handle[4]`, `with-timeout[5]`, `apply[3]` and propagated `let[2]` sites are FIXED and must not be re-derived. **What remains, and why it is not a one-line fix:** once the thunk lambda carries a real `FunTy` instead of the sentinel, `nested` breaks in EMIT rather than in lowering, because the forcing call takes `emit-apply`'s `is FunTy` arm, which is COMPILER-18 and COMPILER-20 territory. Removing the sentinel without the backend fix reproduces the revert of main 20762. **NO `error` REACHES THE WIRE HERE ANY MORE, and this row said otherwise until 2026-09-08 (red).** The removed sentence claimed `nested`'s value binding was `error` on the wire before and after; measured at head with the optimizer off, `lazy-smoke` carries `error` 0 and `noexpect` 65, `codex/test/cost/builtin-alloc` carries 0 and 2, the self-host carries 0 and 0, and every `__lz-*` binding in `lazy-smoke` is `int-default` 45, `noexpect` 3, `text` 3, the lazy-of-lazy case included. `lower-lazy`'s own recovery arm (main 22544) is what closed it. **THE PEEL HELPERS ANSWER `NoExpectTy` FOR A NON-ARROW (Damian's ruling 2026-09-08 on Steve Howell's issue 94, reversing red's no-flip; red CL 23803).** `peel-fun-param` and `peel-fun-return`'s `is otherwise` arms in `Types/CodexTypeHelpers.codex` answer the sentinel, so a free position is spelled as free and `ErrorTy` is left to genuine type failures. Blast radius, read then measured: every lowering reader of a peeled type folds `ErrorTy | NoExpectTy` into one arm, and the three `ErrorTy`-only readers a peeled value can reach (`lookup-overlay` through `lookup-type-split`, `binder-free`, `codex-emit-type`) are reached only when a lambda or a def has more parameters than its type has arrows, which no clean compile in the corpus does. Measured on the candidate: the compiler binary differs from the prior seed in exactly two code bytes (the two constructor tags) outside the hash and signature window; stage 2 == stage 3; `lazy-smoke` and `cost/builtin-alloc` wire byte-identical against the prior seed (`error` 0 and 0 on both); `build/ir-fidelity` 8 cases as expected; BVT 143 pass 0 fail. Five emitters fold both atoms into ONE arm (`is ErrorTy | NoExpectTy ->` in csharp, rust, ada, fortran, html), `recheck` alone distinguishes them on purpose, and zig, wasm, wgsl, arm64 and riscv have no arm for either, so both take the same default. The standing approach for a site that leaks stays: recover at the site, as `lower-lazy` does. |
| COMPILER-35 | **Nothing sweeps the front end for a diagnostic placed BELOW an early return that the guarded input is the one thing that triggers.** Two members are fixed and guarded, both in `Syntax/Lexer.codex`: `scan-string-body` (CDX7, `codex/test/errors/text-open-at-eol`) and `scan-char-literal` (`cdx-unterminated-char`, code 8, `codex/test/errors/char-open-at-eol`; an opening quote immediately after a letter or a digit is an apostrophe and raises nothing, because headings reach the scanner as ordinary tokens). The class is unswept: the parser, the desugarer and the checker carry the same `if <nothing-changed> then <state>` shape and no arm has asked whether each guard is reachable on the input it guards; a diagnostic that cannot fire reads exactly like one with nothing to report. Unowned. |
| COMPILER-62 | **CDX0008 refuses an apostrophe-quoted identifier in a prose block, and which apostrophes it refuses is not explained by column** | Found by reek 2026-09-07 while fixing the wgsl page module, which built 3x CDX0008 "Unterminated character literal", CODEGEN-HALTED, no IR emitted, on seed E103FF07EA9EE7B7. The three sites were quoted naga error messages inside prose: `error: Function [11] 'rf_nearest' is invalid`, the same for `earth_shade_pixel`, and `= Argument 'framebuf' at index 0 ...`. **The obvious explanation does not hold.** `DevelopersGuide.md:9` says prose is column 2, and column-2 prose apostrophes do NOT trip the rule (`Tint's`, `NAGA'S` on adjacent lines). The three that fail sit at 5 and 7 spaces, which would suggest anything past column 2 is lexed as code. But two other lines outside column 2 carry apostrophes and do NOT fail: `which can't be passed` at 9 spaces and `the loop's own exit condition` at 23. What fits the observation is a PAIR of apostrophes around an identifier rather than a lone one, which is a description and not a mechanism. **Not diagnosed, deliberately**: reek removed the trigger from wgsl by switching those three to backticks (main 23169), which is the convention that file already uses, rather than resting a fix on an unproven reading of the rule. Whoever takes this should decide whether a prose block should be lexed for character literals at all. CDX0008 arrived with 22739. wgsl ships in nothing today, so this was not a gate red. **Read beside COMPILER-55**: a prose line at exactly three columns is lexed as code, and that row's first measured bite (`unit-field-assign`, 2026-09-08) was CDX0008 on an apostrophe pair; the five- and seven-space lines here are another column and may be a second mechanism, unmeasured. |
**MEASURED 2026-09-01 (reek, taking red's lane): the multiply sweep is SMALL, and
the sizing step as written would have measured it as EMPTY.** red sized this
unbuilt and named the first step: build a trapping kernel in scratch and count
`!EXC=06` over `sha512-test`, `ed25519-test`, `crc32-test`, `foreword-random`,
the `cce-*` tests and a self-compile. That was done. The probe is the house trap
shape, `jcc 1 2` over `[15, 11]`, appended at the seven `IrMulInt` sites; the
kernel is 3,094,535 bytes against the seed's 3,091,608, so 2,927 bytes, and the
gate's 5 per cent drift guard is nowhere near it.

**Calibrated first, both directions.** Steve's own fixture
`4000000000 * 4000000000` traps under it, with `RSI=de0b6b3a76400000` in the
dump, which is the wrapped product the issue reports being printed silently;
`1000 * 1000` prints `1000000` and does not trap.

**Results, multiply only:**

| subject | result |
|---|---|
| the compiler's own self-compile | NO TRAP, exit 0, one pass |
| `sha512-test`, `ed25519-test`, `crc32-test` | no trap, output matches |
| `cce-roundtrip`, `cce-tier1`, `cce-tier2`, `cce-bounds-smoke`, `cce-builtin-bounds` | no trap, output matches |
| `forewords/foreword-random` | no trap, output matches, AND THE ROW IS VACUOUS |
| `Foreword chapter Random`, `mix-bits 12345 7`, called directly | **TRAPS** |

**The last two rows are the finding.** `codex/test/forewords/foreword-random`
CITES the Random chapter and prints `"Foreword/Random OK"`; it never calls the
RNG. So its green says nothing about the RNG, which is L-NAMED at the exact
point where the sizing step depends on it. Called directly, `mix-bits` traps at
once, and it must: it multiplies by 2654435769, 6364136223846793005 and
1442695040888963407, which is wrap-by-design and the clearest annotation
candidate in the tree.

**So the count to plan from is not zero and not "the bulk".** For MULTIPLY the
compiler itself is clean across a whole self-compile, and the hashes and CRC
are untouched because they add and xor rather than multiply, so a multiply-only
probe cannot speak to them at all. What needs `wrapping` is the mixers.
**Size the remaining sweep by probing the wrap-by-design SITES directly, never
by running tests named after them**, and note that add/sub are unprobed here and
are where SHA-512 and CRC will actually land.

**BLOCKED 2026-09-01 (reek), and it is a question for Damian, not a workaround.
THERE IS NO WAY TO SPELL 64-BIT WRAPPING ARITHMETIC TODAY, and 64-bit is what
every site this row must annotate needs.**

The ruled shape is `mul-int` trapping by default with `-wrapping` spellings for
`OvWrapping`. Reaching `OvWrapping` from source requires a BOUNDED band
(`Parser.codex` `parse-bound-between` is the only producer), and
`check-wrapping-band` refuses any band that is not exactly a hardware width.
`bounds-are-hw-width` (`Types/CodexTypeHelpers.codex:91`) answers True for u8,
i8, u16, i16, u32 and i32, and **False for HwU64 and HwI64**.

That refusal is deliberate and its reasoning is sound for a bounded FIELD, in
the prose beside it: the exact 64-bit bands are not writable as literals (u64's
hi and i64's lo both fall outside the signed 64-bit literal range) and a 64-bit
store narrows nothing, so there is no truncation for the band to coincide with.

**But this row does not want a store property, it wants an ARITHMETIC MODE**,
and the sites it must annotate are all full-width 64-bit: `mix-bits` multiplies
by 6364136223846793005 and 1442695040888963407 and relies on i64 wraparound, and
every other mixer in the list is the same shape.

Measured, not assumed. `Integer between -9223372036854775808 and
9223372036854775807 wrapping` is refused with CDX1073. A bare `Integer wrapping`
does not mean the mode at all: it parses as a type APPLICATION and fails
CDX2003 "Arithmetic operator requires Integer or Real" with CDX2001
"Type mismatch: Con:Integer[T271] vs Integer".

**So the annotation half cannot be written for a single one of the eighteen
sites, and without it the trapping half cannot land** (turning on the trap
without annotating the mixers red-lines the tree). The IR plumbing was built and
proven separately -- opcode, mode-selecting lowering, wire spelling both ways,
and arms at all 28 routing sites, compiling clean -- and was then REVERTED
rather than landed as dead code, since nothing can emit the new opcode at 64-bit
until this is answered.

**The choice is Damian's.** Allow a 64-bit wrapping band and solve the literal
spelling; or give the mode its own spelling that does not go through a band
(a bare `Integer wrapping` currently means something else, so it would have to
be taken); or re-shape the row. Whichever is chosen, add and sub inherit it.

**THE ANNOTATION SWEEP IS SIZED, 2026-09-01 (reek), and the syntax half is
already done.** `wrapping` is a REAL spelling today: `Parser.codex:188` maps the
token to `OvWrapping`, `CodexEmitter` prints it back, and `IRTextEmitter` already
distinguishes `ov-wrap` in the type. So nothing needs inventing at the source
level; what is missing is that lowering discards the mode.

**The tree declares it exactly ONCE**: `RankedTextSet.codex:144`,
`pos : Integer between 0 and 4294967295 wrapping`. The other eight matches for
the word in the foreword are prose using it in an English sentence, which is
worth naming because a grep for `wrapping` reads as nine adopters and is one.

**The multiply sites that will need it**, taken by searching for a multiply by a
constant large enough to overflow rather than by chapter name: the mixers and
LCGs. `Random` (`mix-bits`, three), `ElasticHash` (three), `FunnelHash` (three),
`Noise`, `Perlin`, `DiamondSquare`, `CellularAutomata`, `Steering`, `WorldGen`,
`SpatialHash`, `ImageTensor`, `Lz4`. Roughly eighteen expressions across twelve
chapters.

**Excluded deliberately, and this is where a careless sweep would go wrong:**
byte and word assembly multiplies (`TrueType` `a * 16777216 + b * 65536`,
`Gguf`/`SafeTensors` `hi * 4294967296`) cannot overflow for the byte-sized
inputs they take, and the fixed-point scalings (`Trig`, `Geodesic`, `Sampler`,
`GpuProxy`) are bounded by their callers. Annotating those would relabel correct
trapping arithmetic as wrapping and retire a real guarantee, which is the
opposite of what this row is for.

So the multiply unit is: an opcode carrying the mode, lowering selecting on it,
an x86-64 arm, and about eighteen annotations. Not a tree-wide sweep.

**PROBED 2026-09-02 (red), ADD/SUB/MUL TOGETHER, seed BE8B04B5, and two of the rows above are corrected.** Kernel: every IR-driven integer add, sub and mul site in `Emit/X86_64.codex` and `X86_64Compound.codex` (the `lea` immediates replaced by `mov`+`add-ri`/`sub-ri` so the flags exist) followed by `jno +2; ud2`, 29 sites, 3,151,761 bytes against the seed 3,149,077. Calibrated both directions: a small-value control prints, `4000000000 * 4000000000` and `9223372036854775807 + 1` each die `!EXC=06`. Results: `sha512-test` TRAPS, `ed25519-test` TRAPS (it cites Sha512); `crc32-test`, the five `cce-*` tests and `foreword-random` match their `.expected` with no trap. **The "self-compile NO TRAP" row above is VACUOUS (L-VACUOUS): a kernel compiled BY THE SEED carries no traps in its own code, so running it over the source tests nothing about the compiler's arithmetic.** The kernel it PRODUCES carries them, and that one compiling the same source dies at `memo-probe+0x3B` (invalid opcode) at 88 MB of heap: `Types/Unifier.codex:952`, `a * 2177342782468422677`, a Fibonacci hash of an address. **So the compiler is NOT clean; its mixers are on the list.** Sites measured or read, by chapter: `Unifier.codex` 930, 952, 993 (the same multiply, three sites) and 986 (`cons-mix`, `h * 1099511628211 + v`); `opening.codex:2205` (`disk-fnv`); `Sha512.codex:18` `w64-add` (EVERY SHA-512 add funnels through it, so the chapter is ONE signature, and Ed25519 reaches it by citation); `Random.codex:70-74` `mix-bits` (three multiplies and one add). The `sha512-test` trap RIP was not mapped to a site; `w64-add` is the only add in the chapter that can overflow. What the probe cannot say: whether Ed25519's own field arithmetic traps independently of Sha512 (the trap could be either), and what else in the compiler traps after `memo-probe` (the first trap ends the run; expect `cons-mix` and `disk-fnv` next).

Caveats on this measurement, so it is not read wider than it is: multiply only;
the self-compile is ONE pass against the trapping kernel and not a re-proved
fixed point; nothing was submitted and no seed moved.

**ADD/SUB UNIT BUILT 2026-09-02 (red), the same shape as multiply.** Plain `+` and `-` trap on overflow on x86-64 at every IR-driven site. The immediate paths drop `lea` for `mov`+`add-ri`/`sub-ri` followed by `jno +2; ud2` when the node type traps and keep `lea` when it wraps (`emit-binary-add-imm`/`sub-imm`, and the two tail-call immediate paths through `tail-add-imm`/`tail-sub-imm` in `X86_64Compound.codex`); the register forms (`emit-binary-op`, `inplace-int-op`, `emit-tco-binop`, `emit-binary-standard`, the staged pop-add) append `int-trap-after` on the node type, and `emit-reg-right-inplace` now receives the node type rather than the left operand's (the inliner rule the multiply unit paid for). The LIR refuses wrapping add/sub (`lir-op-wraps`), traps `LoAdd`/`LoSub` unconditionally in both ALU forms, loses its two `lea` fusions (they set no flags), and its aliased subtract parks the right operand in R8 and subtracts the right way round instead of `neg; add`, which raises OF on `r == i64-min` where `l - r` does not. `simp-fold-int` folds a trapping sum or difference only when it fits (`add-fold-fits`/`sub-fold-fits`) and never a wrapping one, and add/sub are no longer `simp-op-trapfree`. Wire: `add-int-wrapping`/`sub-int-wrapping` from the node type, parsed by `IRTextParser` to the plain ops (plugs 2.21). `Foreword chapter Wrap64` gains `w64-add`/`w64-sub`; `Sha512.codex` cites it and its own masked `w64-add`, the one signature every SHA-512 add funnels through, is gone. **No other annotation was needed**: the stage-2 self-compile (the trap-carrying compiler compiling the source, the run L-VACUOUS says is the only one that counts) ran clean the first time, because every accumulator the multiply unit annotated already carries the mode on its left operand and the `+` inherits it. Measured against stage 2 (stage 2 == stage 3 byte for byte, one pass; `sha512-test`, `ed25519-test`, `crc32-test`, `foreword-random`, the five `cce-*`, `hamt-test`, `mix-bits`, `int-mul-wrapping` and the new `codex/test/ops/int-add-wrapping` (funnel, parameter, immediate, in-band and small arms for both ops) match their sidecars; `9223372036854775807 + 1` in a plain function dies `!EXC=06` with `RDI=8000000000000000 RSI=7fffffffffffffff` after the small control printed. Size: stage 2 is 3,191,651 bytes against the seed's 3,155,718, 1.1 per cent, 4 bytes per trapping add or sub. The app sites are compile-checked by the gate and run only by the battery, as for multiply. **GATED AND LANDED (red, 2026-09-02): seed F185CB2E (3,191,641 B, signed, self-verified), `-Internal` green in 599 s, one-pass fixed point on the merged head at main 21784, BVT 408 lines 0 drift, oracle-vector 130/130. One red on the way, the multiply unit's shape again (L-CENSUS): `build/oracle-vector.ps1`'s generated `ballast` sums the IEEE bit patterns of negative doubles with plain `+` as a live-value sink, and the third term carries the sum past i64-min (trap at `ballast+0x191`, RSI holding the wrapped sum); it goes through `w64-add` now (blu's generator, authorized by root). The first green gate was then invalidated by reek's seed 21767 landing under it, and the unit was re-gated on the merged head.** **SPELLING UNIT (red, 2026-09-02, Damian's 10:35 ruling): a bare `Integer wrapping` is the i64 band in wrapping mode, hooked in `parse-type-args` before the identifier would be taken as a type argument (`is-bare-wrapping`, base spelled `Integer`, next token spelled `wrapping`); `-9223372036854775808` is a literal because the checker's unary arm accepts the one-past-max magnitude directly under the minus (`is-int-min-literal`) while the bare literal stays CDX2071, and lowering already folds the negation without a subtraction (`IrIntLit (-v)`, where `-v` of i64-min is i64-min); `vec-lane-literal` negates with unary minus rather than `0 - v` so a pathological lane index cannot trap the compiler. Fixture `codex/test/ops/int-wrapping-spelling` (both literal positions, bare-band add, sub and mul, a small control).** **A third census miss, found by fester's plug-selftest at 21846 (L-CENSUS): the evidence plug's `ev-u64-loop` assembled a little-endian u64 as `acc + byte * mul` with `mul * 256`, which reaches 2^64 on the eighth byte; a multiply trap latent since 21676, invisible to the gate because plug-selftest runs only when the plug changes and app-sweep only compiles. It is `bit-or`/`bit-shl` now (main 21855). A byte assembler is a shift, and the shape to grep for is a loop parameter multiplied by a power of two and fed back.** **A fourth, from reek's hosted runs the same afternoon: both classic-games subjects died in `rng-next`, the LCG `state * 1103515245 + 12345` on a plain `Integer` record field (`apps/games/classic/Rng.codex`), reproduced on bare metal at `rng-next+0x2e`. A census by CONSTANT over `apps/` found the same LCG in nine more chapters (codexmagic Token and DungeonRun, one chapter of the withheld solver, spark SparkDemo and ParticleSystem, fontexplorer FontModel, fontai FontTrain, fireworks, fishtank), each annotated on the operand that carries the state (a record field or the seed parameter) in the sweep CL. The app sites the multiply unit deferred to the battery are these; the gate compiles them and only a runner that executes them sees the trap.**
|
| COMPILER-41 | **OPEN, and UNMEASURED: whether the PLUGS share the `show`-on-`Real` digit substitution.** The x86-64 printer is FIXED at CL 21214 (copy-up 21215), verified at head: `codex/test/real-show-wide` matches its `.expected`, `d17` printing `12345678901234566.0` and `d18` `123456789012345680.0`, the two arms that were wrong. **What remains:** nobody has checked the plugs. The printer is hand-rolled in `Emit/X86_64TextHelpers.codex` and the plugs format reals through their target language, so the defect is x86-specific by construction -- but that is an argument and not a measurement, and this row exists to keep the difference. The shape to look for is a DIGIT SUBSTITUTION toward `0` at a fixed position, which is a place-value or divisor error in the digit loop, not precision loss; the 2^53 significand limit is separate and documented at `Emit/X86_64TextHelpers.codex:506`. |
| COMPILER-39 | **A declared export is never erased: `inline-single-caller` (and any pass that removes a definition) exempts every name a chapter declares exported, on every target** | From Steve Howell's issue 110 (2026-08-30), ruled by Damian 2026-09-01 ("exports exempt from single-caller is fine"), posted on the issue the same day. The pass is correct inside a whole program: `collect-once-defs` / `keep-single-caller` (`IR/Lowering.codex`, at `:2726`/`:2741`, verified at head 2026-09-08 with no export exemption built yet) inline a definition referenced exactly once and it never reaches the output. It is wrong at a language boundary, where a zig shim outside the transpiled program can call only what survives, and which definitions survive depends on how many OTHER places call them, invisible at the definition. His measurement: nine definitions erased across three ported games (Num 1, Truck 7, GuardRail 1), eight of them single-caller, decidable from the emitted module because a body that still contains calls is not a leaf. **The mechanism is the export declaration PR 112 introduced for wasm** (`plugs-backlog.md` 2.18, `wasm-exports`, taken): a chapter names its exports, and that list's scope extends from the wasm emitter to the pass pipeline, so the rule is predictable from the definition you are looking at. Not a diagnostic on erasure; inlining at the call site stays allowed; the definition reaching the output is what is guaranteed. Acceptance is his two-roads harness (`passes=text-plug` against `default-ir-pipeline`): a declared export appears on both roads, and an undeclared single-caller definition still does not. Seed-affecting. Steve's queue lane (reek, red's lane absorbed 2026-09-01). |
| COMPILER-45 | **`compile.ps1 -EscapeCheck` crashes on the seed compiling the compiler, so `KEEP-STAT` and every escape-check reading of the selfhost are unreachable** | Registered as COMPILER-44 at 21317 and renumbered 45 by root the same hour, blu having taken 44 at 21314. Measured 2026-09-01 (red) against depot seed 83C9E0B1: the selfhost unit under `-EscapeCheck` dies in `copy-sx-pos+0x1E` (general protection, RDI non-canonical) at heap 128.5 MB, and identically under the pre-change compiler, so it is not a regression of the memory campaign. Only the selfhost unit was measured. The counts the mode would print (`KEEP-STAT`) are read from the `CHECK-TAIL` phase-trace note instead. Nothing in any gate runs `-EscapeCheck` (L-NOGATE), which is how it stayed unnoticed. Start from the crash site: `copy-sx-pos` is the span copier, and a non-canonical pointer that deep reads as a span taken from reclaimed memory under the escape walk.  **REPRODUCED AND NARROWED 2026-09-02 (blu), against depot seed `BE8B04B5`, two selfhost runs.** The crash reproduces exactly: GP in `copy-sx-pos+0x1E`, `RDI 0x0038281100000000`, heap 125.9 MB (red measured 128.5 MB against 83C9E0B1, so the heap figure moves with the seed and the crash does not). **THE HEADLINE IS TOO BROAD AND SHOULD NOT BE ACTED ON AS WRITTEN: `-EscapeCheck` IS NOT UNREACHABLE, ONLY THE SELFHOST UNIT DIES.** Compiling `codex/test/ops/unicode-bytes-roundtrip` under `-EscapeCheck` exits 0, produces a 104,523-byte CDX, and prints the full escape report for every phase (PARSE 2 deck pointers into bivy, DESUGAR 6, SCOPE 1162, CHECK 1, LOWER 211, with all four PRECISE lines at 0 ptr and reach 12,048 / 23,185 / 48,625 / 31,051). So escape-check readings of ordinary chapters are available today and only the selfhost reading is blocked. **THE PHASE IS NAMED: it dies after `PH:SCOPE` and before `PH:CHECK`.** A second run under `-EscapeCheck -DebugMode` prints `DBG:frontend src=3115049`, then `PH:LEX`, `PH:PARSE`, `PH:DESUGAR`, `PH:SCOPE`, then the fault. **THE FAULTING VALUE IS DETERMINISTIC AND CONTENT-DERIVED, NOT A WILD ADDRESS, AND THAT IS THE MOST USEFUL FACT HERE.** Across the two runs `RDI` and `RBX` are byte-identical at `0x0038281100000000` while every neighbouring register MOVED with the extra `-DebugMode` allocations (`R12`/`RSI` 0x0E0CEC75 to 0x0E0CF22D, `R14` 0x05E16B99 to 0x05E17151, `R10` 0x083E69F9 to 0x083E71F9, `R15` 0x0D7535B9 to 0x0D753D41). A pointer into reclaimed memory would have moved with them. The value is a 32-bit quantity sitting in the HIGH half of the word (`0x382811 << 32`, low 32 bits all zero), which is the shape of a field read at the wrong width or offset, or of a value that took a `shl 32` it should not have, rather than of a stale pointer. That is a different first suspect from the row above it: the row said a span taken from reclaimed memory, and the register evidence argues against that reading without settling what it is. **WHAT IS UNMEASURED AND IS THE NEXT STEP, NAMED SO IT IS NOT GUESSED:** which call in the SCOPE window actually reaches `copy-sx-pos`. the window around `opening.codex:597` is that window and contains no `copy-sx-*` call at all; the nearest are the DESUGAR keep at `:561-567` (before the SCOPE marker) and the CHECK keep at `:706` (after it). The two candidates to separate first are `precise-escape-bag` at `:597`, which walks the AChapter graph through the runtime pmap table that only `-EscapeCheck` enables (`X86_64Compound.codex:1306` and following, the `pmap-escape-report-*` family), and a symbol misattribution in `seed/Codex.map` putting a neighbouring function under this name. Settle that before reading any more code, because both readings explain the symptom and only one survives a symbol check (L-MECHANISM: name the line the mechanism runs through, and grep it). Not chased further; blu had two box grants and spent them on the two runs above. |
| COMPILER-46 | **RE-MEASURED 2026-09-08 (fester) and it DOES NOT REPRODUCE: `build/test-disk-compile.ps1` passes end to end. What is left is a flaky step 2b, not a DISK-mode defect** | Three runs today against depot seed `D9CF240465C3D0BC`, staged as `build-output/bare-metal/Codex.cdx` because the harness takes the DEFAULT kernel and has no `-Kernel` parameter. Two passed outright: `DISK-OUT: OK OUT.CDX 89714`, the guest compiled `codex/test/field-range-proven.codex` off its own FAT16 volume, and the extracted CDX ran and printed `12`. The third run, ablated with a week-old seed (`AB13F36912D3CE57`, `//Codex/main/seed/Codex.cdx#760`), ALSO compiled clean on disk (`DISK-OUT: OK OUT.CDX 89682`), so the stale-kernel theory this row invited is REFUTED rather than confirmed. **The one failure of the three is a different defect and it is real: step 2b races the guest.** `Export-Fat16File` calls `OpenRead` on `disk-test.img` while the QEMU process still holds it, and the harness exits 1, which on a verdict line is indistinguishable from a DISK-mode failure. That is 1 of 3 runs. The harness is GENERATED (`codex/build/testdiskcompileScript.codex`), so a wait-for-exit or a retry goes through the generator. **What CANNOT be re-measured is the blindness this row registered first**, an error COUNT with four blank lines where the message should be: nothing fails on the DISK path at head, so that path is unobservable now. It is neither confirmed nor closed; whoever next sees a DISK compile fail should read whether a diagnostic arrives with it. The original 2026-09-01 measurement named no kernel, and the harness default is whatever ran last. |


| COMPILER-48 | **Four dependencies on the deck discipline that nothing declares (Steve Howell, issue 115)** | **Instance (5) TAKEN by fester 2026-09-02** (root, after the bytes-loss row): measure the runtime consequence with a bounded probe first, then decide guard + poke, control both ways. Instances (1) to (4) are unclaimed and stay reek's row. Filed 2026-09-02. All four RE-MEASURED against head by reek before filing, and one does not hold here. **(1) VERIFIED.** `Syntax/SyntaxNodes.codex` carries exactly one cite, `Foreword chapter Maybe`, and uses 45 `for .. in .. ->` comprehensions, which desugar to `map-list`; in a bundle not otherwise carrying that chapter it is 20x CDX3002. **(2) DOES NOT HOLD AT HEAD.** The report says `Syntax/Lexer.codex` defines `copy-sx-diag` and so does `SyntaxNodes`, colliding as CDX3006. Measured: the definition exists ONCE, at `SyntaxNodes.codex:554`; `Lexer.codex:127` and `Parser.codex:930` CALL it, and `Lexer.codex:122` names it in prose. No duplicate and no collision. What IS true, and is the same shape, is that Lexer cites nothing while using a SyntaxNodes definition. **(3) VERIFIED.** `Syntax/Lexer.codex` has ZERO `cites` lines and uses `deck-record` 60 times. Whether `deck-record` is the intrinsic or a 4-byte identity is decided by the DEFINING CHAPTER in `Emit/X86_64Chapter.codex`, and a cite is what pulls that chapter into a bundle, so a bundle without it gets the identity with nothing said. **(4) VERIFIED as to the calling convention.** `check-chapter` issues its own `__deck-exit` at `Types/TypeChecker.codex:2388` and `__deck-enter` at `:2393`, so it exits an extent it assumes its caller opened, and `opening.codex` is the caller that opens one. Nothing above the signature says a caller owes that. The consequence Steve reports (called bare the nesting counter goes to -1, every `deck-record` inside the walk silently stops being one, and the damage surfaces in a LATER phase) is HIS measurement and is not re-measured here. **(5) FIXED at head (fester, verified 2026-09-08):** `emit-build` (`Emit/EmitAllocator.codex:21-26`) calls `deck-reservation-guard` and publishes its top through `poke-qword-cell deck-reservation-top-cell` exactly as `build` does, so an emit extent is guarded against its own ceiling; the measurement that found emit unguarded (`PROBE48 ceil=449847088 p=6627864 top=16392728`, the ceiling 27.4x above emit's reservation) is what this closed. |

**FIX MEASURED, four arms, CL 22060 (fester, 2026-09-02).** `emit-build` takes both of `build`'s lines. (A) The fixed compiler compiles its own source to a HARD FIXED POINT, stage1 === stage2 byte-identical at `2E50AB28`, so emit does NOT exceed its own reservation and arming its real ceiling costs nothing -- the risk that this converts a working compile into a fault is refuted, not assumed. (B1) Fix plus a deliberately undersized emit reservation (`emit-build 65536` at `X86_64Chapter.codex:1248`) faults `!EXC=06` at `R10=0x091943B8`: the guard fires. (B2) **The same undersizing WITHOUT the fix exits 0, produces output, and raises no fault at all** -- emit overran its reservation by roughly 150 MB and nothing noticed. B1 against B2 isolates the behaviour to the two added lines, and B2 is the direct demonstration of the silent-corruption claim rather than an inference from the ceiling arithmetic. Remaining proof is the BVT as one granted run. The ask: teach `check-subset-cites` the dependency that is not a cite, since the emitter already computes the predicate; for (4) make `__deck-exit` refuse a non-positive counter, turning a silent -1 into a local failure; and for (5) decide whether an emit reservation should publish its own ceiling or deliberately inherit. All are COMPILER-47's shape: the mechanism degrades to a silent default instead of refusing. |

| COMPILER-50 | **`derive-deck-scale` clamps at 100 while a compiler-sized unit estimates 200, so the derivation cannot express the largest unit we have** | UNOWNED, raised by reek 2026-09-02 from the plugs 1.75 residue; read, not run. `derive-deck-scale` (`opening.codex:165`) is `est = unit-len * 100 * deck-scale-margin / deck-scale-anchor` with `deck-scale-margin` 2 and `deck-scale-anchor` 2,993,576, then `if est < 64 then 64 else if est > 100 then 100 else est`. A unit of anchor length estimates exactly **200** and is clamped to **100**, so every unit at or above HALF the anchor, **1,496,788 bytes**, gets the identical reservation: the derivation stops tracking length at half the anchor rather than at the anchor, and the file's own prose beside it says so. **Measured consequence, already paid:** the page's binary tab sent `CDX` with no `decks=`, derived, was clamped, and refused `CDX9002 Deck overflow in SCOPE` on a compiler-sized unit (`plugs-backlog.md`, the binary-tab 2.10). The page works around it with an explicit ladder, `DECKS = [12, 48, 125]` (`prism.html:693`), whose top rung is 25 per cent ABOVE the clamp the derivation can reach; so the only unit large enough to need more than the clamp is currently served by a hardcoded list in a web page rather than by the derivation. **THE PHASE IS ESTABLISHED FOR BOTH BANDS ALREADY, IN `opening.codex` ITSELF, and this row asked for a run that would re-derive what the tree records (fester, 2026-09-07, verified at head, no guest spent).** DESUGAR binds across the 29 units above 150 KB, estimate-to-requirement 1.24 to 1.71 (`opening.codex:81-84`). LOWER binds for the one unit already past the clamp: `codex/test/apps/foreword-all-compile` needs at least 131, its LOWER deck measured at 437,990,744 bytes at decks=200 on 2026-08-09, and at the 100 it would derive the CDX path refuses CDX9002 NAMING LOWER (`:140-147`). It compiles today only because `codex/test/apps/foreword-all-compile.flags` carries `decks=200`, which is on disk and is 11 bytes. So the only phase left unexplained is the page's SCOPE symptom, which came from the binary tab rather than from a compiler run, and `PARSE-KEEP` in `plugs-backlog` 1.71 remains stale for the reason that row gives. **A SEPARATE NUMBER HAS INVERTED SINCE IT WAS MEASURED AND `opening.codex` STILL CARRIES THE OLD DIRECTION.** Its prose (`:72-78`) records the anchor as LARGER than the true unit, 2,993,576 against 2,746,259 measured 2026-08-09, and names that as the harmful direction because it divides estimates down. Re-measured 2026-09-07 by the same route (`concat-codex-self.ps1`, default `-CodexDir codex\compiler`): the unit is **3,119,490 bytes**, now 125,914 ABOVE the anchor, so the staleness has flipped to the direction that multiplies estimates UP and is benign. The prose is wrong in a way that reads as a live hazard, and whoever takes the ruling should correct it in the same CL as the code, since a prose-only edit to `opening.codex` raises a seed question for a comment. **Nothing in the tree is a single FILE above half the anchor**: the units that reach it are concatenated ones, the compiler's own and `foreword-all-compile`, which is 14,545 bytes on disk. **The repair is a decision, not a patch:** raise the clamp, make the estimate non-linear above half the anchor (the prose records CHECK usage per unit byte between 21x and 167x and not monotonic, which is why the margin exists), or rule that the ladder carries it and the derivation is for small units only. Seed-affecting whichever way it goes. |

## COMPILER-55 -- OPEN, unowned (Steve Howell's issue 120, VERIFIED STATICALLY at head by blu 2026-09-07): a prose continuation indented to exactly THREE columns is lexed as CODE and joins the previous definition's expression

`Syntax/Parser.codex:1302` defines a prose line as `(current st).column == 2`,
exactly. The continuation tests are `column > 3`, at `:1316`
(`skip-prose-lines`), `:1400` (`collect-prose-with-text`) and `:1535`
(`parse-top-level`). Column THREE is neither: not a new prose line, not a
continuation, so it falls to the else and is returned as the start of code,
where it joins the preceding expression.

No guest was spent. The three call sites read as written above, and the gap is
arithmetic on the two constants rather than a claim about behaviour.

The fix is a decision, not a patch: either the continuation test becomes
`column > 2` (prose owns everything past the marker column) or the parser
refuses column 3 with a diagnostic. Both change what an existing corpus means,
so this wants a ruling before code. Steve's reproducer is three files differing
only in the indent of the continuation line.

FIRST MEASURED BITE (root, 2026-09-08, the Update 56 release gate): `check-errors`
went red on `codex/test/errors/unit-field-assign` because a three-column prose
line in it lexed as code and main 22739's CDX0008 fired on the `''` inside the
sentence. blu rewrote the test's prose; the defect above is unchanged and is
what the row still names.

## COMPILER-56 -- OPEN, unowned (Steve Howell's issue 126, routed by blu 2026-09-07, NOT verified on metal): the hosted compiler types every comparison EXPRESSION `ErrorTy` where bare metal says `boolean`

Measured by Steve against a tree that ALREADY CARRIES PR 117, so this is not a
regression of PR 117's fix and not closed by it. Six operators (`lt le gt ge eq
ne`) come back `error` from a hosted `codexir` and `boolean` through the seed,
with the IR otherwise identical. He states the qualifier himself: for `fib` the
emitted zig is byte-identical either way, so on that evidence the wrong type is
inert for codegen. PR 117's own history is the argument for not shrugging: there an ErrorTy binding cascaded until nine ladder rungs went red off one cause.

TWO CANDIDATE SITES ELIMINATED BY READING (blu, no guest): the whole `mcopy`
memo apparatus is unreachable on a hosted target after PR 117.
`check-batch-copy` is called only from `check-batch-close`, which now returns
early on `hosted-kind /= 0`; `tcons-lookup` (`Types/Unifier.codex:932`, the
only other raw-memory read in the type layer) is reached only through
`mcopy-text-content`, which is inside that same family. So the site is neither
the copier nor the memo, and whatever answers `ErrorTy` for a comparison
expression is somewhere else.

Verifying further needs a hosted build, which is why this routes rather than
resolves.

## COMPILER-57 -- OPEN, unowned (Steve Howell's issue 125, routed by blu 2026-09-07, NOT independently measured): 10 of 120 ordinary Real literals land one ULP from correctly rounded, in the front end

His measurement: 120 random doubles in (-1000, 1000), each written as its
shortest round-tripping decimal, compared through `real-to-bits` because `show`
on a `Real` is refused. An independent Rust front end gets 0 of 120 wrong;
Codex gets 10, worst gap 1 ULP. Every COMPUTED value in his two failing test
chapters is bit-identical across arms, so this is the literals and not the
arithmetic.

THIS IS THE ROUNDING HALF ONLY. The WRAP half of the same `__text_to_double`
-- past 19 digits the decimal accumulator wraps in one i64 -- was COMPILER-37,
and it is FIXED: a literal wider than an i64 is refused with CDX2073 rather
than read as a different number (main 21199, shipped in Update 54). The
refusal is pinned by `codex/test/errors/real-literal-overflow.codex` with a
`.failing` sidecar, and that chapter's own prose names Steve Howell's issue
106. COMPILER-37's row is absent because this register DELETES a closed entry,
which is its stated convention and not a lost row.

`codex/test/ops/real-literal-rounding` guards this half with twelve values all
below ten, so it is structurally blind to the 1-ULP cases. The coupling that
made Steve file rather than patch still holds: the zig plug's
`zig-p-cx-text-to-double-bits` mirrors the seed deliberately, so both parsers
move together or neither does.
