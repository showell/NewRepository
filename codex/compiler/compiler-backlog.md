# Compiler -- open capabilities

Quire-domain backlog, same rules as the app registers: an entry says
what is still missing and nothing else, a closed entry is DELETED, and
a gap that is still real is never quietly dropped. There is no
platform-wide register; `docs/PM/CurrentPlan.md` carries the shape.

| # | Capability | State of the gap |
|---|---|---|
| COMPILER-1 | **Runtime scope enforcement for effect grants** | Re-homed from CAPABILITY-REFINEMENT step 6 (moved to Done 2026-08-05). Compile-time scope enforcement is shipped and pinned (`lint-effect-scope` / `scope-grants` in the type checker; `codex/test/errors/scope-violation`, `scope-network-port`). **Correction 2026-08-11 (val): the earlier claim that "the runtime half was never built" is FALSE, verified against source.** Runtime scope enforcement IS built and sound on x86-64 for FileSystem (path prefix) and Network (host:port authority), and the effect-op path enforces the SCOPE, not just the cap bit. Read path: `__fs-read-servicer` (checks `cap-filesystem-read`) -> `fat16-servicer-read` -> `fat16-read-text` -> `fat16-read-bytes` -> `fat16-resolve-path`, which calls `fat16-scope-admits` reading `process-get-scope process-get-pid` (the CALLING process's scope; the servicer runs in-context, only flipping the elevation flag). Writes are symmetric via `fat16-create-file` (`fat16-create-directory`, `fat16-list-root`, `fat16-root-entries` gated too); network via `net-scope-admits` in `http-request` / `fetch-tls`. Scope is serialized into the CDX (`manifest-cap-bytes`: cap-id, direction, scope-len, scope-bytes) and loaded into the proc table (`proc-scope-offset` fs, `proc-net-scope-offset` net) on BOTH boot (`emit-set-boot-scope`) and verified-load (`ProcessCaps.apply-load-decision`); the load-time wiring is pinned by `loader-scope-test`, `loader-network-scope-test`, `process-caps-test`, `network-scope-{open,deny,spawn}`. **Real remnants:** (1) **ARM64 has no counterpart.** The whole process/capability kernel is absent on ARM64 (no proc table, no syscalls, no fs/net execution), so scope is silently unenforced there. This is a MAJOR effort and its own initiative, not a COMPILER-1 tail; a QEMU bed now exists. Full detail and the parity chain are tracked in `codex/plugs/plugs-backlog.md` 1.10. (2) **Runtime denial IS tested end-to-end (correction 2026-08-11; the earlier "no denial test" claim here was wrong, caught by reading the tests).** `scope-runtime-deny` boots a program granted `FileSystem.Read "OK"` on a real disk and asserts the out-of-scope write (False), read (<none>) and root listing (0) are all refused while in-scope ops succeed (re-run green here, seed AF4E14D9); `scope-runtime-spawn` asserts a spawned child inherits the scope and denial (10); `scope-runtime-open` is the no-scope control; `loader-scope-test` asserts the verified-load path installs the scope. No denial-test gap remains. The only combination not in a single test is a verified-load-NARROWED scope plus a runtime out-of-scope read in one program; it is covered compositionally (same `proc-scope-offset` cell, same `fat16-scope-admits`), and a composed integration test would be real but marginal. (3) **Architecture, revisit ONLY if it becomes structurally limiting (Damian ruling 2026-08-11):** enforcement is library-level -- the servicer routes through the gated `fat16-*` functions rather than the stub checking the path itself. There is no single chokepoint (`Fat16.codex:1073-1077` warns a new disk-reaching door needs its own gate), so a future ungated door is a silent hole with no test to catch it. A syscall-boundary chokepoint would collapse the N gates to one, but is a seed-affecting change that stays redundant with gates that must remain for direct in-process callers. KEPT AS-IS: the burden is a clarity/discipline cost, not a structural break. Not blocking anything current. |

| COMPILER-7 | **Reconsider the `bounded` check's one over-refusal, after it has been in use for a while** | Damian, 2026-08-16. The `growing` inference (Cost Model 3.3 step 2, blu) scores **10 of 11** on `codex/test/cost/accumulator-corpus`, measured by ablation against the previous seed: 5 of 5 on the quadratic half, 5 of 6 on the negatives, table in `CostModel.md` section 8b. **This row said 9 of 10 until 2026-08-17, which was the score before rule 3 landed at main 16118 and took the corpus from ten entries to eleven; the count was stale, the row is not.** Still exactly one LINEAR entry refused, `n-fixed-appends`, which appends to a growing accumulator in a self call exactly four times however large the input gets and so has the shape without the cost. Ruled the cheap direction under abstain-toward-refusal (`docs/Designs/Active/Features/CostModel.md` section 5 q2, section 8) and paid in a declaration an author can decline to write. **Revisit once real chapters carry `bounded` declarations:** count how many callers the over-refusal actually bites in the tree (a refusal that never fires costs nothing; one that makes authors drop the declaration costs the guarantee). Any rule that lifts it must keep 5 of 5 on the quadratic half of the corpus, measured by the corpus and not argued. Not before the declaration has been in the tree for a cycle. |
| COMPILER-9 | **The ARM64 shadow rule is derived from registration on both halves; the hand list that remains is the helper-versus-arm precedence table, and it is named that** | Census 2026-08-16 (reek), born from the `peek-16` root cause: `a64-check-builtin-collisions` warned from a hand chain of 126 names that missed 85 of the 104 the runtime registers. **Both halves are now derived (root, 2026-08-17, on `//Codex/root`; copy-up after MAIN OPEN).** The CHECK reads `func-names` past `runtime-funcs` (reek, main 16081). The DISPATCH in `a64-emit-direct-call` (`Arm64CodeGen2.codex`) now says: a USER def (`a64-find-name-index (st.func-names) name runtime-funcs >= 0`) wins over every emitter arm unless the name is inlined-here, which is the x86 rule and the one `RiscVCodeGen2.codex:836` already had; a user def named like an arm the old list did not carry no longer loses silently. **What the list turned out to be, measured, so nobody deletes it next:** for names the user never defines it decides helper-versus-arm (a registered name IN it is called by name, its runtime helper; a registered name absent from it takes the arm), and deriving that half as "every registered name" put eight arm64 bed answers wrong; it is kept as `a64-helper-by-name` with that prose. **A latent defect found by the same bed and closed in the same CL:** `a64-emit-user-fn-call` was a stale twin of the generic call tail without the more-than-eight-argument stack path (`a64-push-stack-args-pre`), reachable before only for listed builtins of small arity; the derived rule sent every user def through it and `goose-encode` faulted, `heap-frame-reclaim` read 15 for 43, six more; the twin is now the one call path and the tail delegates to it. Judged by the full arm64 bed against two controls (old dispatch with head `Arm64CodeGen3`): identical FAIL set (27, all pre-existing on head; blu's lane). Left, blu's lane: derive the precedence table itself, one name at a time with the bed as judge (which of helper and arm is right for each of the 105 registered names is unmeasured), and RISC-V has no such table because it always calls the registered helper by name. **SCOPED AND RE-MEASURED 2026-08-26 (blu). THREE OF THIS ROW'S NUMBERS WERE STALE AND THE TABLE IS AN EIGHTH OF ITS APPARENT SIZE.** Registered names are **109**, not 105. **THIS FIGURE WAS PUBLISHED AS 108 EARLIER THE SAME DAY AND THAT WAS ALSO WRONG; FOUR INSTRUMENTS GAVE 83, 103, 108 AND 109.** The count is the single most error-prone thing in this row, so take the method and not just the number. 83 came from a pattern matching only `a64-record-func st<N>`. 103 is the literals in `Arm64Runtime.codex`. 108 adds the two sites that pass a VARIABLE (`a64-emit-svc-stub`, `a64-emit-process-spawn-named`, carrying `block-sector-count`, `block-read-sector`, `block-write-sector`, `process-spawn`, `process-spawn-with-heap`) but was measured over that ONE FILE. 109 adds `__opening_print`, registered at `Arm64CodeGen3.codex:1570`, which a single-file census cannot see. **So: census the whole plug, exclude `a64-record-func-with-arity` (a `\b` after `func` matches it, and it is fed `def.name` from user IR rather than runtime helpers), and separate the DYNAMIC registrations** -- `Arm64CodeGen3.codex:1774` and `:1802` register `"__handler_" & op-name`, one per handler clause in the program being compiled, so they are per-program and not part of this table's domain at all. With `__opening_print` counted the split is **16 helper / 93 arm**, not 16/92; it is absent from the hand list, so the 106-inert result below is unaffected. **THE TABLE HELD 122 ENTRIES AND 106 OF THEM WERE INERT, NOW REMOVED (main pending).** A hit sends the name to `a64-find-func-offset`, which answers -1 for anything `func-names` does not carry (`Arm64CodeGen3.codex:1920`), so an entry naming an unregistered name falls through to the arm exactly as if absent -- and the spot-checked ones already had arms. **Measured, not reasoned:** all 106 deleted, plug rebuilt, full bed run -- identical FAIL set, identical classes, identical PASS_EXPECTED, nothing fixed and nothing broken. **The 16 that decide anything** are `__buf-read-bytes`, `__buf-write-byte`, `__buf-write-bytes`, `bit-and`, `bit-or`, `bit-shl`, `bit-shru`, `bit-xor`, `char-code`, `code-to-char`, `integer-to-text`, `list-snoc`, `print-line`, `text-concat-list`, `text-replace`, `text-split`; the other 92 registered names take the arm today. **THE BAR, AND IT IS BETTER THAN THE FEAR:** the bed is `build/test-cross-batch.ps1 -Arch arm64 -Jobs 8`, **502 tests** (its own header says 363), Renode at `C:\Renode`, ~14 min a run. Baseline is **48 FAIL**, not the 27 this row carries -- 29-31 in that era on a bed ~78 tests smaller, so it is not a regression and must not be read as one. **Two full runs at different box loads (13.9 and 17.9 min, compile phase 222 s against 365 s) produced IDENTICAL FAIL sets, identical per-test classes, zero movement**, so one run adjudicates an experiment and no averaging is needed. Compare the SET, never the count: a count lets one test start failing while another starts passing. **The 48 decompose into three classes and only one of them is this row's:** **A, 20 tests, `[UNSUPPORTED]` at compile** -- `char-encode` (11), `raw-bytes-to-text` (8), `vec-empty` (1). **These are NOT a precedence question and do not belong here:** both are BUILTINS (`Types/Builtins.codex:58` and `:214`) with arms in csharp, javascript, python, wasm and zig and NONE in arm64 or riscv, and a name with neither helper nor arm cannot be routed anywhere. That is a plug builtin-coverage gap for `codex/plugs/plugs-backlog.md`. **B, 18 tests, wrong answer** -- deterministic, and the only class where a helper-versus-arm mis-route could show; `effect-smoke` printing `args=1075847184` against an expected `42` has the shape of a pointer. **C, 10 tests, silent or starved** -- timing, and stable across both baselines rather than noisy. **So the remaining work is not a 108-name sweep.** One-at-a-time over 108 is ~25 hours of bed; the 18 class-B failures are a far better entry, because the bed already names which answers are wrong and the work runs backwards from a wrong answer to a mis-route instead of flipping names and hoping. `-Filter` takes a subset. **THE DERIVATION THIS ROW ASKED FOR IS DONE, AND THE ANSWER IS THAT THE TABLE IS NOT NEEDED (main pending, 2026-08-26, blu).** It did not take 108 experiments; it took two, because the question "which of helper and arm is right for each name" has a cheaper form: route them ALL one way and see whether the bed can tell. Removing the 106 inert entries changed nothing. Then emptying the remaining 16 as well, so every registered name takes its arm, changed nothing either -- **48 against 48, zero movement in either direction, on a baseline two prior runs had shown deterministic at the set level.** **The vacuity control is what makes that a result rather than an absence:** the 16 are heavily exercised, `bit-and` at 184 uses in the test corpus and 900 in the foreword, `integer-to-text` 410, `char-code` 91 and 244, so the bed reaches them constantly and still cannot distinguish the two routings. **THE ASYMMETRY IS THE FINDING.** Routing every name to its HELPER was measured 2026-08-17 and put eight answers wrong; routing every name to its ARM costs nothing. So the arm is the safe default and the helper is the exception, which is the reverse of what a hand list of 122 names implies, and it is why the earlier "derive it as every registered name" attempt failed -- it derived the dangerous direction. RISC-V arrives at the same place from the other side, with no table because it always calls the helper. **The mechanism is kept, the data is gone:** naming a registered function there still routes it by name, which is the escape hatch for an arm that turns out to be wrong, and a name goes in when a measurement wants it. **Stated honestly: the bed skips 135 tests, so this is "no routing difference the bed can observe", not "no difference".** **What is left of this row is the 15 unexplained class-B failures**, three having been split out as COMPILER-29 (an effect operation with an argument, and NOT a precedence defect). The rest of this row's history (six-def clause, list-tail, VirtioPci `poke-16`) is closed in `docs/Designs/Done/Compiler/ForewordShadows.md`. |
| COMPILER-11 | **`noalias-note` has a positive arm and no negative arm, so "proven unaliased" and "nobody asked" are one output; DECIDED: no note at the not-proven arm** | From PR 66 finding 10 (plugs-backlog 1.27; confirmed reek 16195; red routed to root 2026-08-17). `emit-record-set-builtin` (`Emit/X86_64Builtins.codex:863`) and the field-store site (`X86_64.codex:1222`) call `noalias-note` (`X86_64Compound.codex:1810`), which raises the INFO `cdx-noalias-proven` (CDX4011) only when the base is a unique linear owner (`noalias-slots`, from `def.unique-params`) and returns the state untouched otherwise; `fc-store-barrier` raises the same code for a store through a fresh allocation. **Measured on the corpus, 2026-08-17 (root):** compiling the whole compiler (`build/output/quine-check/compiler.ir.log`, the gate's own log) raises CDX4011 ZERO times: the compiler has no linear-parameter record updates, so the positive arm never speaks on the largest program we have. A note at the not-proven arm would fire at every ordinary in-place store: 401 `__record-set` sites in `codex/compiler` (415 in the compiler's IR, plus 69 field stores), 2,729 more in `apps/`, 11 in `codex/test`. Every one of those is the documented concession (`DevelopersGuide.md`, "`__record-set` is not a functional update"; `mutable` records are unique-owned by the checker, CDX2062), written knowingly, so an info there would be an instrument that says the same thing at every site and is read by nobody (L-FALSIF): 400-plus lines in every compiler compile log. **Decision: the not-proven case gets NO note.** What the finding actually asks for is that the ABSENCE of CDX4011 must not be read as a safety claim, and that belongs in the code's own description: `CdxCodes.codex:351` should say "absence means not proven, not unsafe"; a description string is compiled into the seed, so it is folded into the next seed-affecting CL that touches `CdxCodes.codex` rather than spending a seed cycle on a sentence. **What would change the decision:** a consumer. If the IndependentRechecker or an audit ever wants a per-site verdict, emit the negative arm under `wire-trace` only, at `__record-set` sites whose base is neither fresh, nor linear, nor a `mutable` record, which is the only class where the concession is load-bearing. Not seed-affecting as decided. |
| COMPILER-12 | **`lift-lambdas` runs on the CDX path only, so the plug wire carries lambdas that close over enclosing locals, and the wire format says nothing about it** | PR 66 finding 6 (plugs-backlog 1.27; verified root 2026-08-17 against source, routed here when plugs 1.17 closed). `codex/compiler/IR/LambdaLifting.codex:18` `lift-lambdas` has exactly ONE caller, `opening.codex:833`, inside the `cdx-chapter` path (its own LIFT phase and deck budget); `emit-ir-cce` (`opening.codex:1694`) emits `fe.ir` from `compile-frontend-ir` after `ir-prune-unreachable-roots` and never lifts. So every plug either implements closure capture itself or mis-emits a body silently, and Steve's zig emitter paid for a free-variable analysis with a shadowing rule that lifted IR makes unnecessary (the pass rewrites the use site as a partial application, which that plug already handled). Two closes, and the first is the one to take: (a) lift on the wire path too, one call to `lift-lambdas` in `emit-ir-cce` before the prune (or after, measure which; the pass has a deck ceiling argument to thread), seed-affecting, token; its runner is a plug-side arm: `test-input/lambda.codex` through a plug with NO closure support of its own must transpile a lambda over an enclosing local as a call to a lifted def, and `plug-oracle-test.ps1` must stay green on the plugs that already handle closures; (b) declare the contract in the wire format so a plug can refuse (`!UNSUPPORTED`), which is the honest fallback if (a) costs a deck the wire path does not have. **DECIDED 2026-08-17 (root, red's routing): (b), DECLARE; the wire does not lift.** Measured: the wire for `test-input/lambda.codex` carries `(def "make-adder" ... (lambda (params (param "x" int-default)) (binary add-int (name "x") (name "n"))))`, the captured `n` a bare `name` with no marker, and the wire path is LOWER plus the named passes only (no RESOLVE, no LIFT: those are `compile-frontend-cdx` phases, `opening.codex:819-841`). Census of the 55 plugs: 52 handle `IrLambda`; six carry their own capture/lifting machinery (`arm64`, `riscv`, `zig`, `t3isa`, `pascal`, `python`) and the rest lean on the target's own closures, which is correct for every closure-native target and is where the plugs that emit today actually are. Lifting on the wire path would hand every plug lifted `__lambda_N` defs and rewrite every capture site into a PARTIAL application, so it trades a burden six plugs already carry for a partial-application burden on all 55, and a plug that handles closures but not under-application would go from right to silently wrong (L-CAPABILITY-LOST shape); nothing measured asks for it. So the contract is declared where plug authors read, `DevelopersRulebook.md` codex.plugs ("What the wire carries"), and a plug for a closure-less target lifts or captures itself, as the six do. Re-open (a) only if a plug author measures a case the declaration cannot serve. Two adjacent findings from the same measurement, each its own row: `typescript` collapses the curried over-application `(make-adder 10) 32` to `make_adder(10, 32)` while `python` and `rust` emit `make_adder(10)(32)` (plugs-backlog 1.35); and the directly-applied inline lambda `(\x -> x + 100) 5` reaches the wire with `(param "x" error)`, a type the checker never resolved (COMPILER-13). |
| COMPILER-13 | **No arm would catch the multi-parameter lambda defect coming back** | The defect itself is CLOSED: root did the single-argument case (main 16326) and blu the nested-apply case (main 16374, seed 1E6BED13), so `((\x y -> x + y) 1) 2` now types every parameter instead of only its first. The account is CL 16371. **What is left is that nothing guards it.** Measured 2026-08-17: the OLD compiler ran all three shapes correctly, 105, 3 and 6, exactly as the new one does. The defect was never visible in x86-64 BEHAVIOUR at all; it lived only in the IR type annotations that cross the plug wire, where a parameter read `error`. So no x86 test can pin it and a behavioural test would be one pretending to guard something it cannot, which is why none was added. **The arm that would work is plug-level** -- emit the same source through a plug under both compilers and diff -- and it is UNMEASURED. Same shape `check-plug-types` records for its own subject: x86-64 never crosses this wire, so no x86 test will catch it. **Measured 2026-08-17 (blu), depot seed 1E6BED13: the wire is CLEAN for all three shapes.** `codex/plugs/test-input/lambda.codex` through `compile.ps1 -IrUni -Passes 'text-plug'` carries `(param "x" int-default)` at every lambda, including the directly-applied inline `(\x -> x + 100) 5` that COMPILER-12's row recorded as reaching the wire with `(param "x" error)`; that adjacent defect is closed by 16374 too, and COMPILER-12's row should not be read as still open on it. **The guard is still absent, and the diff-against-the-old-compiler arm the row asks for cannot be the guard: it expires the moment the old seed is replaced, which has already happened.** A durable arm is behavioural and belongs where the wire is already run rather than inspected -- `plug-oracle-arith.codex` has no lambda section, and `test-input/lambda.codex` is run by NOTHING automated (its only use in the tree is one manual measurement, plugs-backlog 605). **GUARD LANDED 2026-08-18 (blu): `plug-oracle-arith` has a Lambdas section and it is behavioural, so this row's gap is closed.** Seven rows: `lam-one` (single-parameter inline apply), `lam-two` (`((\x y -> x + y) a) b`, the exact shape of this defect), `lam-three` (catches a plug that types the first two parameters and not the third), `lam-mixed`, and `lam-capture` / `lam-capture-two`. **`lam-mixed`'s second parameter is a `Boolean` deliberately, and that choice is the whole point of the row:** with every parameter typed `Integer` a wrong or missing annotation on any parameter but the first still runs correctly, which is exactly how this class stayed invisible, so a same-typed row would be a guard pretending to guard. The capture rows exist because COMPILER-12 DECIDED the wire does not lift, which makes capture the plug's job and made it contract that nothing exercised. x86-64 truth measured, not predicted: 105, 3, 14, 7, -7, 15, 300; sidecar at 40 lines; gate green (`check-plug-builtins` and `check-plug-types` both clean, `SUT === stage1`, seed unchanged). **The census on its first run, all six wired plugs built and none skipped or stale: javascript and typescript PASS 40 of 40, and FOUR plugs fail, each a distinct defect nothing could see before because no lambda had ever been RUN through them.** Two passing is what says the subject is sound rather than impossible. `python` emits an N-ary lambda and applies it curried (`(lambda x, y: (x + y))(a)(b)`, `TypeError`), hitting all four multi-parameter rows and neither single-parameter one. `wasm` emits the lambda body inline referencing `local.get $x` for locals it never declares, so the WAT will not assemble; wasm has no native closures, so that plug must lift and does not. `csharp` raises CS0149 at six sites. `zig` captures `const k = 10` as `@TypeOf(k)`, which is `comptime_int`, so the env struct is comptime-only and has no runtime size. **Read the counts, not the line totals: the harness merges stderr, so a traceback shifts every later line and python reports 74 differing lines for one crash.** Routing: python, wasm and csharp are the plugs lane; **`codex/plugs/zig/**` is Steve Howell's and not the fleet's to change**, so that one is red's to route upstream. `plug-oracle-test.ps1` is standalone and NOT in the standing gate, so these four do not make main red. |
| COMPILER-14 | **`emit-record` had two record layouts and every reader used only the first; the second is now a refusal** | From Steve Howell, PR 67 (`gh pr view 67 -R damiant3/NewRepository`), routed by red 2026-08-17. `emit-record` (`Emit/X86_64Compound.codex:1751`) laid a record literal out by `build-cce-byte-offsets` (width-prefixed sort key, cumulative widths) when `resolve-constructed-ty` answered a `RecordTy`, and otherwise by `emit-store-record-fields-by-list` (name-sorted rank times 8); `emit-field-access` and `emit-record-set-builtin` read by the first rule unconditionally, so a record built through the fallback with any field narrower than eight bytes was read at the wrong offsets (PR 67 probe: `Box = record { flag : Boolean, items : List Integer }`, `b.items` reading the boolean). Steve could not reach the fallback through `opening.codex`; his harness reached it only by skipping RESOLVE. **Root, 2026-08-17: the fallback is replaced by the refusal its neighbour `emit-field-access` already makes** (`st-add-error cdx-ir-error "emit-record: unresolved type for record literal"` and `ud2`), and `emit-store-record-fields-by-list`, `field-local-names`, `RecordLayoutPair` are deleted. Reachability was measured by that refusal under the gate: the compiler self-compile and the BVT never take it (a taken branch is a red gate). Not measured beyond the gate: the full battery and the apps; a red there with this message means the branch IS reachable and the close becomes "lay out by `cce-byte-offset-and-type` so the two rules cannot drift", not a restore of the by-list layout. |
| COMPILER-15 | **An empty list literal in a `when` wildcard arm reaches the plug wire with element type `error`** | Found 2026-08-17 (blu) while measuring COMPILER-13's residue, verified against source. Two sites, both `is otherwise -> []`: `codex/compiler/Emit/X86_64Compound.codex:409` and `:413` in `tco-arg-reads` (declared `List Integer`), and `codex/compiler/IR/Lir.codex:909` in `lir-bind-pattern` (the `let tys` fallback). Both emit `(list-expr (elems) error)` on the wire. Measured by scanning the gate's own `build/output/quine-check/compiler.ir.log`, 16.0 MB, whole compiler: exactly **2** type slots read `error` and both are this shape, so the residue is small and bounded rather than systemic. A third `error` hit in that log is `(text-lit "(error ")` inside `IRTextEmitter.codex` itself and is a false positive of a naive scan, recorded so a re-run excludes it the same way. **The SOURCE pattern is wider than the defect and a re-runner must not close the gap from source alone:** `X86_64Compound.codex:888` is a third `is otherwise -> []` and the wire is clean at it, so the empty literal there does get a resolved element type. The wire is the authority; two sites, not three. Line numbers verified against main at CL 16394 (root's COMPILER-14 touched this same file and did not move them). **Invisible to x86-64, which needs no element type to build an empty list**, which is why nothing caught it; a statically typed target must name the element type before it can build the list, so zig would refuse it and C or Java would take a wrong guess and run. LATENT: no plug is known to emit from either site today. Whoever fixes it makes the arm resolve the empty literal against the arm's expected type rather than leaving `ErrorTy`. **Not seed-affecting to record; the fix is.** |
| COMPILER-16 | **The compiler's own self-compile emits 21 warnings that nothing gates and no retained log carries, and one of them was a live miscompilation for as long as it has been there** | Found 2026-08-18 (blu) while investigating issue 70's supposed CDX2064 blind spot. **The premise was half wrong and the correction is the finding.** CDX2064 DID fire on `X86_64Boot.codex:3216`, correctly, on every build: measured by compiling the pre-fix concatenated source (`build/output-20260818-012801/Codex.codex`, retained by chance) and capturing diagnostics, which gives `:22326:39: warning CDX2064: Field 'code-len' is read from 'st7' ...`, and line 22326 of that file is exactly `in let st9 = patch-jcc-at st8 (st7.code-len) loop-top`. The checker was right and had been saying so all along; nobody read it. **A warning nobody reads is not a guard** (L-BODY: if a thing is gated only by prose or by a human noticing, it is not gated). Two separate gaps follow. **(1) VISIBILITY, and it needs a ruling: nothing gates compiler warnings and `build/output` retains no diagnostic log for the self-compile**, only `canary-compile.log` and `cdx-sign.log`. The 21 are 13 x CDX6020, 7 x CDX3005, 1 x CDX2064. Whether CDX2064 should fail the build, or the log should merely be retained, is Damian's call and is in the rulings queue. **(2) A GENUINE BLIND SPOT, and it is the eight siblings rather than 3216.** `update-consumed` marks a variable stale only when the mutating call is bound in a `let`; the sibling shape `patch-jcc-at (st-append-code st5 (jcc cc-ne 0)) (st5.code-len) loop-top` puts the mutation in a sibling ARGUMENT of the same call as the stale read, and `scan-stale`'s `AApplyExpr` arm walks both arguments with the SAME consumed set, so it cannot see it. Extending it means threading the consumed set left to right across an argument list, which is sound because argument evaluation order was MEASURED left-to-right (probe: `two (addn s 10) (s.n)` answers 1111 left-to-right, 1101 right-to-left; it answered 1111). **The probe that proves the current checker works on the let-bound shape and not the sibling shape is `staleprobe.codex`, and any extension must keep the hoisted control silent** (arm warns, control does not). **UNAUDITED SURFACE, do not read this row as "one defect found and fixed":** the 13 CDX6020 (`mutation in constructor`) sites in the compiler are the SAME mutate-in-place family and each needs per-site judgement, not a sweep. Several look benign because the field read is an argument evaluated before the mutation applies (`13777`), and at least one reads a DIFFERENT binding than it mutates (`13846`: `state = __record-set st1 "load-local-toggle" (st.load-local-toggle + 1)` reads `st` while mutating `st1`). None of them has been judged. |
| COMPILER-17 | **The boot capability grant is emitted as a sign-extended imm32, so a capability at bit 31 would grant bits 31-63 and any bit at or above 32 is dropped silently** | Found 2026-08-18 (root) while scoping the HAL's next capability rows (I2C, ADC, Power would take bits 30, 31, 32: `cap-spi` is 29 and the table has no free bit below it). `emit-grant-cap-mask` (`Emit/X86_64Boot.codex:2506`) and `emit-grant-capability` (`:2514`) OR the mask into proc 0's cap word with `or-ri reg-rax mask`, and `alu-ri` (`X86_64Encoder.codex`) encodes the immediate as `write-i32` under REX.W, which the CPU sign-extends: a mask with bit 31 set becomes `FFFFFFFF80000000` (every capability from 31 up granted, L-CAPABILITY-LOST shape: the aggregate reads as "granted", nothing says too much was), and a mask whose only bit is 32 or higher writes its low 32 bits, zero, so the capability is never granted and every use of it is refused at runtime. Bit 30 (`I2c`) is the last safe one under the current emission. LATENT today: `cap-spi` 29 is the highest bit in the table, `capability-doors` proves the table and not the grant, and nothing in the tree declares a bit above 29. **The fix is small and seed-affecting:** load the mask with `li` (mov r64, imm64) into a scratch register and `or-rr`, in both emitters, plus the ARM64/RISC-V boot grants (`Arm64Runtime`/`RiscVRuntime` equivalents, unmeasured) -- and it is untestable until a capability sits at bit 31 or above, so it lands WITH the first such row (`Adc` at 31, `Power` at 32) and a runtime arm that reads proc 0's cap word after boot and shows bit 31 grants only bit 31 and bit 32 grants at all. **Sidestepped 2026-08-18 (root): I2c, Adc and Power took the table's unassigned bits 1, 2 and 13, so nothing sits above 29 and no emitter changed; the row stays LATENT for the next capability, which has no gap left below 30 (bit 30 is the last safe one).** The audit is wider than the two grant emitters: `emit-check-capability` and every `and-ri reg (shl-1 cap-bit)` check site share the imm32 shape, `ProcessCaps.codex read-process-grant` reads the word with `peek-32` (bit 32 and up invisible to the loader), and the RISC-V `rv-emit-or-into-cell` loads the mask into t0 with `rv-li`, whose own scratch for a #8xxxxxxx immediate is t0. Owner: root (HAL) when the next row needs it. |
| COMPILER-18 | **A partial-application closure carries no remaining-arity, so entering it with fewer arguments than it still wants corrupts silently -- and one-at-a-time entry is what the Rulebook says the language does** | From Steve Howell's zig-plug ladder, 2026-08-24 (ladder tag `closure-arity`). Line numbers checked against main at `5b8091e2`; `git diff bdf0049b..5b8091e2 -- codex/compiler/Emit/` is empty, so they also hold at the Update 49 pin where this was measured. Lane: the native x86-64 backend, seed-compiled, run on bare metal under QEMU, no plug anywhere in the path. **MEASURED.** (1) *Two spellings of one computation, one line apart, and the working one is yours.* `let h = add3 10 in show ((h 20) 12)` prints **42**; that is the shape of your own fixture at `codex/plugs/test-input/partial.codex:9-10`. Splitting the same work into two steps -- `let j = add3 10 in let g = j 20 in show (g 12)` -- faults in the same program, immediately after the first line prints. (2) *A wrong number with nothing to notice.* With `even-fn (n) = if n == 0 then add3 5 else odd-fn (n - 1)` and `odd-fn (n) = even-fn n`, both at `Integer -> (Integer, Integer -> Integer)`, `show (((even-fn 4) 20) 22)` prints **6291488** where 47 belongs. That is `0x600020`, a heap address printed as an Integer: no crash, no CDX code, no register dump. (3) *Control.* `add3` returns an Integer, is named by a partial application in every program in the series, and is correct in all of them. **READ FROM SOURCE.** A partial application is a heap object `[code-ptr][capture...]` of `(1 + num-captures) * 8` bytes (`emit-partial-application`, `Emit/X86_64Compound.codex:715-733`, size at `:727`). Its code pointer is a trampoline that shifts the incoming argument registers up by the capture count (`:703-707`), loads the captures beneath them (`:709-713`), and `jmp rax` to the target (`:724-725`). It therefore only works when entered with every remaining argument at once, and cannot re-close when given fewer -- **and the object stores no count, so no call site can discover what it wants.** Both entry paths reach it: `emit-apply` sends a global with more arguments than its arity to `emit-over-apply-extras` (`:154`, `:253-274`), which by construction emits one `mov rdi, <arg>` / `call rax` per argument (`:266-270`) and stores each result back as the next closure (`:272-273`); and it sends a FunTy result whose callee is a local to `emit-indirect-call` (`:166-169`, `:205-219`), which passes exactly the arguments written at that site. Measurement (1) is the second of those. **THIS IS THE DECLARED MODEL, NOT AN EXOTIC USE.** `docs/DevelopersRulebook.md:258-260` states it: a def "emitted n-ary is called flat at that arity, under-applied with one arrow per missing parameter, **over-applied by applying the rest one at a time**". COMPILER-12 already records the same shape on the plug side, warning that `(make-adder 10) 32` can go "right to silently wrong". And `codex/plugs/test-input/lambda.codex:19` drives the over-apply path on every build and is green -- because that closure wants exactly one more argument. The tree tests the boundary case and nothing past it. **NOT ESTABLISHED.** Why (2) needs the mutual recursion at all: the flat form `f (n) = add3 5` applied the same way prints 47, so something about the recursive shape is load-bearing and the ladder cannot say what. And separately, why appending a third statement to an unrelated two-statement program stops its *first* statement from printing. Both are real and neither is explained here. **THE ASK, and it is a ruling rather than a patch.** Repairing one caller only moves the boundary, since the arity is missing from the object rather than from any caller: the choice is whether the closure gains a remaining-arity word so the trampoline can re-close on under-application -- which is what the Rulebook's model above requires -- or whether under-application is instead refused where it is emitted. That is a representation decision and not a contributor's to make. On coverage, the shape belongs beside `codex/plugs/test-input/lambda.codex`, whose one-remaining case already passes; this PR does not add it, because accepting a red fixture is yours to decide. Seed-affecting. **RULED 2026-08-24: "give it the word" (Damian, via red). SCOPED AND MEASURED 2026-08-25 (blu), NOT IMPLEMENTED -- a second representation decision is open and is named at the end.** REPRODUCED first, on the seed compiler: the flat spelling prints 42 and the split spelling faults immediately after with `!EXC=08 CR2=fffffffffffffff8`. **WHY THE TWO SPELLINGS DIFFER, which the row above records as a mystery and is not one:** `flatten-apply` collapses `((h 20) 12)` into ONE flat apply carrying both arguments, which is exactly what the trampoline expects, while `let g = j 20` is an apply with one argument and the trampoline still shifts for two. The working spelling works by an accident of flattening, not by design, so it is not evidence the path is sound. **THE OBJECT ALONE CANNOT FIX IT.** x86-64 does not tell a callee how many arguments it received, so a remaining-arity word in the closure gives the trampoline something to compare against and nothing to compare WITH. The count has to reach it from the call site, and that is the open decision. **THE OBVIOUS CARRIER IS A LANDMINE: `r10` IS THE LIVE BUMP ALLOCATOR** (`X86_64Builtins.codex:589-593` advances it as a heap cursor; `X86_64Boot.codex:1287` saves it per process; `__alloc` itself is `mov rax, r10` / `add r10, rdi`). Putting an argument count there would corrupt the allocator silently. There is no other free register either: `rax` is the call target, `r11` the closure, `r15` has 99 uses across nine files, and `rbx`/`r12`/`r13`/`r14` are the tree emitter's locals. **TAGGING THE CLOSURE POINTER'S LOW BITS DOES NOT WORK, AND THE REASON IS WORTH KEEPING: heap pointers are NOT 8-byte aligned.** `__alloc` advances the cursor by the exact byte count requested with no rounding or masking, and `X86_64Helpers.codex:524-526` allocates `length + 8` bytes for text with no `shl`, so any odd-length text leaves the cursor unaligned and the next allocation returns an unaligned pointer. x86 tolerates unaligned 8-byte loads, which is why nothing has ever complained. The low three bits are real address bits. **ONE CHECK CAME BACK CLEAN and is recorded so it is not re-run:** the only non-partial-application shape reaching `emit-indirect-call` is the effect-handler resume closure, whose whole stub is `mov rax, rdi` / `ret` (`X86_64.codex:1427`) and which never reads `r11`. Lambdas are not a separate shape: `IrLambda` surviving to x86 emission is a hard error (`X86_64.codex:1201`), so lambdas are lifted and become partial applications, as does a bare function used as a value. **BLAST RADIUS:** five closure-entry sites (`X86_64Compound.codex:214`, `:267`, `:288`, `X86_64Builtins.codex:378`, `X86_64Helpers.codex:2844`) plus a family of trampolines per partial-application site, because the existing trampoline hard-codes its capture count in the shift and cannot re-close for a different one. **THERE IS NO OPEN DECISION. ONE DESIGN SURVIVES, and this paragraph said the opposite until 2026-08-25.** It called the carrier "Damian's decision" and left a compile-time alternative standing beside it. Both were wrong. The ruling in `CurrentPlan.md`'s ruled-items list is that **the trampoline re-closes on under-application**, which excludes re-closing at compile time from the callee's static type -- that is the call site re-closing, not the trampoline, and it traps rather than re-closes wherever the arity is not static. The register route and the tagged-pointer route are excluded by the two measurements above. What is left is one design and it is the one to build. **PASS THE COUNT ON THE STACK:** the caller does `push <count>` before `call rax`; at trampoline entry `[rsp]` is the return address and `[rsp+8]` is the count; the exact-arity path slides the return address up over the count, drops 8 from `rsp`, and jumps to the target exactly as today, using `rax` as the scratch since it is reloaded with the target anyway; the under-application path reads the count and re-closes. It is reentrant, needs no free register, and assumes nothing about alignment -- which is what makes it the only survivor rather than merely the preferred one. **Common ground under both, already ruled and independently gateable: the object gains the word and captures move from `[8 + i*8]` to `[16 + i*8]`.** The reproducer is four lines and is in the row's measurement (1); write it before touching anything. **BUILT 2026-08-25 (blu) AND IT IS RED, ON COST. THE WORD IN THE OBJECT IS NOT AFFORDABLE AND THE MEASUREMENT IS THE FINDING.** The layout half is correct: the reproducer still prints `flat: 42` under the new layout and R10 lands 16 bytes further on, exactly two closures' worth. It is the SIZE that fails. `build/build.ps1 -Internal` goes red at the STAGE-2 compile with `CDX9002: Deck overflow in LOWER`, isolated by compiling one snapshot of the concatenated source under three kernels: seed and Sut (both old-layout runtimes, since Sut is seed-emitted) agree to the byte at `LOWER used=189,324,144`, and stage 1 -- the first binary that actually CARRIES the new layout -- needs `335,997,424`. `-Decks 200` clears it, which is what says budget rather than corruption. **A scaling probe falsified the obvious explanation and is worth more than the red (L-SABOTAGE).** Re-running the whole chain with a THREE-word header instead of two predicted another jump of the same size in LOWER and did not get one: LOWER went 189,324,144 -> 335,997,424 -> 337,231,552, so the second 8 bytes cost 1,234,128 where the first cost 146,673,280 and LOWER's growth is mostly not per-object. The phases that ARE linear name the real cost exactly, the two deltas identical to the byte: **CHECK-RESOLVE 353,424,952 -> 488,626,600 -> 623,828,248, +135,201,648 per header word, which is 16,900,206 partial-application objects allocated in that one phase**, and CHECK +20,876,576 per word, another 2,609,572. **So the compiler allocates roughly twenty million partial applications per self-compile, and the great majority must be ZERO-CAPTURE -- a bare function used as a value, whose object today is a single 8-byte word.** The ruled word does not add 8 bytes to a rare object; it DOUBLES the hottest small object in the compiler. R-COST assessed this change by inspection as "8 bytes per partial application, negligible" and that was wrong in the way R-COST exists to catch: the per-object delta was right and nobody asked how many objects. **THE DESIGN THAT COSTS NOTHING PER OBJECT, AND IT IS THE ONE BEING BUILT (Damian, 2026-08-25, on the measurement above: do the right thing given the new information, the ruling "was merely in agreement with your prescription" and was never a constraint).** The word does not go in the object. The trampoline already knows its own remaining arity as a COMPILE-TIME CONSTANT -- it is emitted per partial-application site and the row above already accepts a family of trampolines per site, since the existing one hard-codes its capture count in the shift. Under the ruled stack-passed count the comparison is caller-count against that immediate, so nothing has to read an arity out of the object: the code pointer at offset 0 already identifies the trampoline and therefore the arity. The word is redundant with the code it points at. That buys the ruled BEHAVIOUR (re-close on under-application, rather than refuse it) at zero bytes per closure and a few bytes of code per SITE, which is static. **So this row's original "independently gateable" first step is GONE -- there is no layout change to gate -- and the replacement first step is the CALLING CONVENTION, which is the half that carries the real risk.** Every closure entry pushes the count it is passing before `call rax` and drops it after; no trampoline reads it yet; the object is untouched. That isolates five entry sites, one of which (`X86_64Helpers.codex` `__task_entry`) enters a task closure that may not return, and one of which is the effect-handler resume stub -- not a trampoline at all, and unaffected only because the CALLER cleans the word up rather than the callee. Caller-cleanup is also why the exact-arity path needs none of the return-address sliding this row previously specified: the count sits BENEATH the return address, the trampoline just jumps, and the target's `ret` lands correctly. The encoder gains `push-i32` because there is no free register at the moment of the push (`rax` is the target, `r11` the closure, `rdi`-`r9` the arguments), which is the same register famine that killed the register carrier. Alignment was checked before building rather than after: no aligned SSE form (`movaps`/`movdqa`/`movapd`) is emitted anywhere in `codex/compiler/Emit`, so the odd push cannot fault. Step 2 is the trampoline family: for a site with capture count `c` and target arity `A`, trampolines `T_c..T_{A-1}`, each comparing the pushed count against its own immediate `A - c` and, when it is short by `m`, allocating a fresh closure of `c + m` captures pointing at `T_{c+m}`. A site whose remaining arity is 1 has no under-application case and keeps exactly today's trampoline. Over-application through a closure (count GREATER than remaining arity) is NOT addressed and keeps today's behaviour; it is a different shape and needs its own row. **BUILT AND GREEN 2026-08-25 (blu), AND MEASUREMENT (1) IS CLOSED WHILE MEASUREMENT (2) IS NOT.** Same program, same runner, side by side: the fixed compiler prints `flat: 42` / `split: 42`, the depot seed prints `flat: 42` and then faults. Guarded by `codex/test/ops/closure-under-apply`, which was checked BOTH ways before being believed -- five rows correct under the fix (6, 42, 42, 10, 10, including a four-arity closure entered one argument at a time) and a hard fault under the seed, so it is a test that fails on the compiler it is meant to catch. **THE ROW'S CENSUS OF FIVE CLOSURE-ENTRY SITES WAS INCOMPLETE; THERE ARE SEVEN**, and under callee-consumes a missed one is not cosmetic, because the trampoline would slide a return address over a word nobody pushed. The two additions are `emit-effect-op-call` (`X86_64Compound.codex`), whose effect-op table slot is filled by `emit-store-to-handler-table` with the clause body's VALUE, which is a closure; and `__proc_entry` (`X86_64ProcessHelpers.codex:201`), which enters a process closure exactly as `__task_entry` enters a task one. **THE CALLER MUST NOT CLEAN UP, AND THIS COST A RED GATE WORTH RECORDING.** Caller-cleanup was tried first as a smaller step-1 and it puts the count at `[rbp+16]`, which IS the seventh-argument slot: 181 functions in the compiler read that slot, stage 2 died at `current+0x40` on the inlined `list-at` bounds trap with the pushed word arriving as a `ParseState` pointer, and the row's original return-address slide -- discarded as unnecessary -- was the thing preventing it. The diagnosis was measured, not argued: one sentinel value proved the word was read as DATA rather than a stack imbalance, then five per-site sentinels named `emit-indirect-call` in a single build (L-STATES). **MEASUREMENT (2) IS A DIFFERENT DEFECT AND SURVIVES THIS FIX.** `((even-fn 4) 20) 22` still answers a heap address, 6291496 under the fix against 6291488 under the seed -- it moved by exactly one allocation, so the re-close does engage and then the chain still fails to finish. The control the row asked for is now run: the FLAT form `flat-fn (n) = add3 5` answers **47 under BOTH compilers**, so the mutual-recursive shape is load-bearing on its own and this is not an arity-representation defect. It smells like result-type inference through mutual recursion rather than codegen, it needs its own row, and COMPILER-18 must not be closed as though it covered this. **CLOSED 2026-08-25 (blu) BY COMPILER-20, AND THE DIAGNOSIS ABOVE WAS WRONG IN BOTH HALVES: it is codegen, not inference, and mutual recursion is not the variable.** See COMPILER-20 for the cause and the fix. Two things this row got wrong are worth keeping. **The control was VACUOUS (L-CONTROL).** `flat-fn (n) = add3 5` answers 47 under both compilers because `inline-leaf-calls` deletes `flat-fn` outright and the call site becomes a flat `add3 5 20 22`, so the arm never entered a closure at all and could not have failed; it was read as proof the mutual-recursive shape was load-bearing. **And the shape it named was one of three that fail**, which a two-arm probe could not see: a plain SELF-recursive `rec-fn` fails in the split spelling, and a function with no recursion anywhere fails in both, so the defect was never about recursion. **INDEPENDENT CORROBORATION, absorbed from Steve Howell's PR 86 (red, 2026-08-25).** Working against public seed `6CF4A8E0`, before the fix and without seeing it, Steve built the smallest entry point yet -- `choose : Integer -> (Integer, Integer -> Integer)` with two return paths and NO recursion -- and measured the silent form (`stepwise` printing heap address `0x600038`) and the loud form (`choose 0 2 3` faulting) in one five-line file, concluding the variable was "something about multiple return paths, not recursion": the same narrowing this row's closure records, reached from the other side of the mirror. His file is `codex/plugs/test-input/overapply.codex`, re-measured GREEN on bare metal at head (all five lines match the zig plug), and plugs-backlog 1.59 carries its corpus role. |
| COMPILER-19 | **The two top-level scans are three-function mutual TAIL recursion, so every plug and bare metal pay a frame per definition** | From the zig-plug ladder, 2026-08-24 (`contrib/README.md`; ladder `findings/README.md` 37, instrument `stack_probe.py`). `scan-top-level` / `try-scan-type-def` / `try-scan-def-header` (`Syntax/Parser.codex`, the streaming header scan) and `parse-top-level` / `try-top-level-type-def` / `try-top-level-def` (the parse) each go once around per top-level definition, and **every edge is a tail call** -- no frame in either cycle is live when the next is entered. Measured on the compiler's own back-end unit bundled as one document (2,503,544 bytes, 4,511 top-level definitions), compiled by a native binary built through the zig plug: 2,393 nested `scan_top_level` frames at the shallower cliff, 3,385 nested `parse_top_level` at the deeper one. **Nothing in the fleet flattens mutual tail calls** -- `is-self-call` is self-only here, in the python plug, and in the zig plug -- so this is paid on x86-64 as well, where it is invisible only because the kernel stack is generous and frames are small. **The change is source-shaped and needs no emitter work anywhere:** have the two `try-*` functions RETURN what they found (a small record: the accumulated lists plus the new state) instead of tail-calling the loop, and the loop tail-calls ITSELF, which every existing TCO already flattens. That is what this PR's one commit does. **Measured with exactly that change applied:** the minimum thread stack the transpiled compiler needs for that document falls from 32 MB to **4 MB**, and what remains on the failing trace is `desugar_expr_at` at 297 frames -- structural recursion over one expression's nesting depth, not growth in the size of the document. **Behaviour preserved, at two depths.** The restructured parser compiled that unit to IR, through the plug to a native binary, and both of the ladder's `ir_to_x86` rung outputs are byte-identical to its Update 49 bank. A full ladder sweep then ran green 14/14 (1716 s), with the diagnostics census unmoved and 13 of the 14 recorded truths byte-identical to that bank; the single mover is the `parse` rung, whose own subject program IS the edited `Parser.codex`, and its diff is exactly four removed definitions, three added, two added type-defs and 50 line shifts, with **no existing definition changing params, anns or slug**. Both arms agree on the new dump, which is the rung that most directly tests the change. **One detail a re-implementer must keep:** when `parse-type-def` fails, the definition parse re-reads from the ORIGINAL state, not from the state the type parser returned -- the two functions this replaces did that, and merging them makes it easy to lose. |
| COMPILER-20 | **A saturated call whose RESULT is a function type is emitted as a partial application OF THE CALLEE, so the callee is never called** | Split out of COMPILER-18 measurement (2) and **FIXED 2026-08-25 (blu)**, one line in `Emit/X86_64Compound.codex`. **THE CAUSE, read out of the IR rather than guessed.** `emit-apply` dispatches on the apply''s RESULT type. Its `is FunTy` arm (`:166-169`) asks only whether the callee name is a local: if it is not, it emits `emit-partial-application` over that name with a fabricated `total-arity` of `list-length args + 1 + fun-type-arity rt`. Nothing on that arm consults the callee''s DECLARED arity, which `emit-apply` has already computed and holds in scope as `user-arity` at `:152`. So for `mk : Integer -> (Integer, Integer -> Integer)` with `mk (n) = ...`, the saturated call `mk 4` -- one argument against arity one -- is turned into a closure wrapping `mk` with capture `[4]` and a believed arity of 3. `mk` is never entered. Feeding that object its two remaining arguments re-closes once and then jumps to `mk` with three arguments in `rdi/rsi/rdx`; `mk` reads only `n`, returns its own closure, and the caller prints that heap address as an Integer. **The neighbouring arms were already correct and are what make the omission visible:** `:154` sends MORE arguments than the arity to `emit-over-apply`, and the `SumTy` and `otherwise` arms both go to `emit-call-or-effect-op`, which respects arity. Only the `FunTy` arm has the `emit-partial-application` escape, and only it is missing the equal case. **THE FIX** is the missing case, on that arm, ahead of the escape: `else if user-arity > 0 & list-length (flat.args) == user-arity then emit-call-or-effect-op st flat user-arity`. A genuine partial application (fewer arguments than arity) still falls through to `emit-partial-application`; builtins and effect ops are excluded by `user-arity > 0`. **R-COST: strictly negative on both axes.** Each site this repairs REPLACES a heap-allocated closure plus a trampoline family with a direct call. No loop, no recursion, no accumulator added. Measured through the gate rather than asserted: the internal gate is green with `SUT === stage1` in one pass. **WHY THE FLAT SPELLING HID IT, which is the whole reason this sat inside another row for a day.** `((mk 4) 20) 22` does not reach the defect: `flatten-apply` collapses it to one apply carrying three arguments, `3 > user-arity 1` takes the `:154` over-apply path, and that path calls `mk` properly first. Every spelling that binds the intermediate to a `let` -- which is what the Rulebook''s one-at-a-time model produces -- takes the broken arm. **MEASURED, and the census is what corrects COMPILER-18''s framing.** Under the depot seed `D74D8855`, seven of the nine rows of the guard below are wrong; all nine are correct under the fix. The failing shapes are (a) a saturated arity-1 call returning a function, split, (b) the same entered with all remaining arguments at once, (c) an arity-2 callee, (d) a SELF-recursive callee, (e) a mutually recursive pair. Recursion is incidental to all of it: it changes only whether the FLAT spelling also fails, because a self-recursive call is caught by `is-self-call` at `:148` and never reaches the broken arm, while a call to a sibling (`odd-fn`) does. That single asymmetry is the entire reason measurement (2) appeared to be about mutual recursion. **GUARD: `codex/test/ops/saturated-call-returning-function`, checked BOTH ways before being believed** -- seven wrong rows under the seed, nine correct under the fix, values hand-computed. Its `arity-two` arm is only real because `mk2` has two call sites: with one, `inline-leaf-calls` deletes it and the arm answers correctly under the broken compiler, which is the same vacuous-control trap COMPILER-18 fell into (L-CONTROL). **NOT ADDRESSED:** over-application through a closure (entering with MORE than the remaining arity) is untouched and keeps today''s behaviour, as COMPILER-18 recorded. Seed-affecting. |
| COMPILER-21 | **The tier-0 Unicode branch tests the LOW BYTE of the code point rather than the code point, so the fifteen Cyrillic code units print as unrelated ASCII -- on THREE print paths, not one** | Raised by fester 2026-08-25 (unowned, CurrentPlan); **FIXED 2026-08-25 (blu)**. **THE MECHANISM, read from the source and confirmed against the table.** `cce-to-unicode-byte-table` is `for x in cce-to-unicode-table -> int-mod x 256`, the LOW bytes only, and the hi table `cce-to-unicode-hi-table` sits at a separate rodata offset. Each print loop indexed the low table, compared THAT byte against 128, and took the single-byte ASCII arm when it was under -- reading the hi table only on the other arm. So any tier-0 code unit whose Unicode has a nonzero high byte AND a low byte under 128 printed as that low byte. `CCE.codex:122-123` puts exactly fifteen such entries at indices 113..127 (U+0430, U+043E, U+0435, U+0438, U+043D, U+0442, U+0441, U+0440, U+0432, U+043B, U+043A, U+043C, U+0434, U+043F, U+0443): every one has hi byte 4 and a low byte in 48..67, so all fifteen took the ASCII arm. The accented block at 97..112 is the control and was always correct, because its low bytes are 224..252, over the threshold. **MEASURED both ways.** Under main seed `966EF113` the probe prints `cyrillic-113-127: 0>58=BA@2;:<4?C` where `аоеинтсрвлкмдпу` belongs; under the fix it prints the Cyrillic, with the accented and digit rows byte-identical across both. **THE ROW SAID ONE SITE AND THERE ARE THREE**, all reading the same low-byte table with the same test: `emit-print-text-loop` (`X86_64IO.codex`, the serial path both `emit-print-text` and `emit-print-text-no-newline` delegate to, which is what fester measured), and `__uefi_print` and `__uefi_print_no_nl` (`X86_64Helpers.codex`), which `X86_64Builtins.codex:716-729` selects instead whenever the exit is `ExitUefi`. Fixing only the serial path would have left the same wrong bytes on the UEFI console (L-PARTIAL). The repair is the same in all three: combine the hi byte in BEFORE the `cmp 128`. **THE TWO UEFI LOOPS ARE NOT THE SERIAL LOOP AND THE DIFFERENCE COST A BUILD.** The serial loop pushes the CCE CODE UNIT before its table lookup, so popping it back gives the hi table its index; the UEFI loops push the TEXT INDEX (the loop counter) instead, and the CCE code unit is live only in `rax` between the text load and the table add. A first attempt reused the popped register the way the serial fix does, indexed the hi table by the text index, and produced a build whose UEFI arm was still wrong -- caught only because the arm was actually run rather than reasoned about. The shipped form saves the code unit to `r11` and the low byte to `r15`, both dead in these helpers. **WHY NO SUITE SAW IT, and it is L-CONSTRUCT.** `codex/test/apps/console-test.expected` is the ONLY non-ASCII expectation in `codex/test`, and every character in it (`é ó ñ è ê`) is from the accented block -- the half that works. Not one test in the tree spelled a Cyrillic code unit. **GUARD: `codex/test/ops/tier0-cyrillic-print`**, checked both ways (wrong under main seed 966EF113, correct under the fix, controls unmoved). **It covers the SERIAL path only**: no test in `codex/test` runs under `-uefi`, and the sidecar set has no way to ask for it, so the two UEFI loops remain ungated (L-NOGATE). The UEFI arm was run by hand (`compile.ps1 -Uefi` plus `codex-vm -uefi -headless`) and the fifteen characters go from literal ASCII to the same unreadable-glyph signature the known-correct accented row produces through the emulator console conversion; that conversion means the exact code points cannot be read off that surface, so the UEFI half is verified to "non-ASCII, one UTF-16 word per character" and no further. **STILL BROKEN, SAME CLASS, NOT FIXED HERE:** three more readers of the low-byte table with no hi-byte combine anywhere -- `__cce_to_unicode` (`X86_64Helpers.codex`), which returns the low byte AS the code point and is emitted into every binary while **nothing in the tree calls it** (L-UNCALLED); `__text_to_unicode_bytes`; and `__uefi_read_file`, whose filename conversion truncates the same fifteen. Each needs its own decision about what a truncated value means to its consumer, so none is a one-line repair and none is in this CL. **R-COST: negligible on both axes and no allocation.** Per printed character the fix adds one rodata address load and four ALU ops on a path that already ends in a `call __serial_put` (or a UTF-16 store); emitted code grows by roughly twenty bytes per print site, static. Seed-affecting: converged in two passes, seed `7298BBFA`. |
| COMPILER-22 | **The last three low-byte readers, and a fourth defect in the REVERSE direction that is not one of them** | Continuation of COMPILER-21 (blu, 2026-08-25). That row named three remaining readers of `cce-to-unicode-byte-table` that combine no hi byte; all three are **FIXED here**, and after this CL **every read of the low table in `Emit/` is paired with a hi-byte combine** -- five pairs in `X86_64Helpers.codex` and, in `X86_64IO.codex`, two low loads feeding the one shared `emit-print-text-loop` that carries the combine. **`__text_to_unicode_bytes` is the one that mattered**, because it is the emitter for the live builtin `text-to-unicode-bytes : Text -> List Integer` (`Types/Builtins.codex:211`), so the truncation was reachable from ordinary Codex and returned wrong INTEGERS rather than wrong glyphs. Measured against main seed `7298BBFA`: `text-to-unicode-bytes` over CCE 113..127 answered `48 62 53 56 61 66 65 64 50 59 58 60 52 63 67` where `1072 1086 1077 1080 1085 1090 1089 1088 1074 1083 1082 1084 1076 1087 1091` belongs; the accented, digit and uppercase rows were correct before and after. **`__uefi_read_file`** truncates the same fifteen in the FILENAME it converts to UTF-16, so a path containing one opened the wrong name; same repair, `rbx` and `r11` are both dead in that loop. **`__cce_to_unicode` is fixed but remains UNCALLED** -- one definition in `X86_64Helpers.codex`, one in `codex/plugs/riscv/RiscVRuntime.codex`, and after a whole-tree search no caller in either, while every `.map` in the tree shows it emitted. It is repaired rather than deleted because deletion is a design call and the too-narrow-search direction that licenses deletion is the expensive one; the L-UNCALLED observation stands and whoever wants it gone should take that decision on its own. **GUARD: `codex/test/ops/text-to-unicode-bytes-tier0`**, checked both ways (low bytes under seed `7298BBFA`, exact code points under the fix, three control rows byte-identical across both). It is a better instrument than COMPILER-21''s print guard because its output is plain ASCII integers, so it reads the code points directly instead of through a console conversion. `__uefi_read_file` and `__cce_to_unicode` remain ungated: the first needs a UEFI boot with a file and nothing in `codex/test` runs under `-uefi`, the second has no caller to drive it. **A FOURTH DEFECT, MEASURED HERE, NOT FIXED, AND NOT A LOW-BYTE READER: `unicode-bytes-to-text` round-trips nothing but ASCII.** `text-to-unicode-bytes (unicode-bytes-to-text [233])` answers `0`, and reading the intermediate text''s CCE units directly shows `unicode-bytes-to-text [233]` producing the TWO units `192 169` where CCE 97 (one unit) belongs -- `[1072]` likewise gives `196 176`, and `[65]` correctly gives the single unit `41`. The mechanism is a different one: `__unicode_bytes_to_text` sends every value at or above 128 to `__unicode_to_cce_tier1` and then encodes whatever comes back as a two-byte CCE sequence unconditionally, with no single-byte case, so a tier-0 answer under 128 is emitted non-canonically as two units. It is the inverse half of the same builtin pair and it fails for all 31 non-ASCII tier-0 code units, accented and Cyrillic alike. **It needs its own row and its own decision** (whether the tier-1 reverse should answer a tier-0 code at all, and whether the caller should branch on the width), which is why the guard above deliberately does NOT assert the reverse direction: baking today''s answers into an `.expected` would enshrine the defect. **R-COST: negligible, no allocation.** One rodata address load and four ALU ops per converted character on three paths, two of which run once per filename; roughly twenty bytes of emitted code per site, static. Seed-affecting: converged in two passes. |
| COMPILER-23 | **The CCE tier-1/tier-2 surface in the native x86 backend indexes rodata tables the image does not contain, and the `text-to-unicode-bytes` / `unicode-bytes-to-text` builtin pair is ASCII-only** | Split out of COMPILER-22 (blu, 2026-08-25). **MEASURED, NOT FIXED: the repair is a design decision and is named at the end.** **THE ORACLE IS IN THE TREE AND IT IS CORRECT.** `codex/foreword/core/CCE.codex` `from-unicode` tries tier 0, then tier 1, then tier 2, and `to-unicode` inverts it; compiled and run against seed `B773CC58` the pair round-trips every tier exactly: u=65 -> 41 -> 65, u=233 -> 97 -> 233, u=1072 -> 113 -> 1072, u=192 -> 192 -> 192, u=960 -> 592 -> 960, u=8364 -> 41616 -> 8364. Nothing below is a defect in the foreword. **THE BUILTIN PAIR DISAGREES WITH THAT ORACLE FOR EVERY NON-ASCII INPUT.** `text-to-unicode-bytes (unicode-bytes-to-text [u])` answers `u` for 65 and 48 and answers **0** for 233, 1072, 192, 960 and 8364 alike, so the pair round-trips ASCII and nothing else. Reading the intermediate text units directly names the reverse half error precisely: `unicode-bytes-to-text [233]` yields the TWO units `192 169`, which decode to CCE 41, where `from-unicode` mandates the single tier-0 unit 97; `[1072]` yields `196 176` (CCE 304) against the mandated 113. Those are legitimate TIER-1 encodings of the right characters rather than garbage -- the helper skips the tier-0 reverse for any value at or above 128 and goes straight to `__unicode_to_cce_tier1` -- but they are not what the spec says and they do not survive the trip back. **AND IT IS LOSSY, WHICH IS THE PART THAT MATTERS MOST:** `[192]` and `[8364]` BOTH yield `192 128`, so two distinct characters become the same text and cannot be told apart afterwards. **THE MECHANISM UNDER ALL OF IT: THE TABLES ARE NOT IN THE BINARY.** `X86_64Chapter.codex:634` writes `init-rodata` and nothing else, and `data-len` is `list-length init-rodata`. `init-rodata` is `cce-to-unicode-byte-table` (128) & `unicode-to-cce-byte-table` (128) & `tier1-block-rodata` (32) & `tier2-rodata` (60) & `cce-to-unicode-hi-table` (128) = **476 bytes**, and its three hardcoded offsets (256, 288, 348) are mutually consistent with exactly that layout. But `X86_64State.codex` also defines `cce0u2-lo` and `cce0u2-hi` (2,176 entries each) and `cce-t2-rodata` (240 bytes), **none of which is concatenated into `init-rodata` anywhere in the tree**, and `__cce_print` (`X86_64Helpers.codex`) reads them at `cce0u2-rodata-offset` = 288, `cce0u2-hi-rodata-offset` = 2464 and `cce-t2-rodata-offset` = 4640. The first **collides with `tier2-rodata`**, which is what actually sits at 288; the other two are past the end of a 476-byte data section. So the multi-byte print path reads whatever follows, and the reverse builtin inherits the same absent tables. **CONSISTENT WITH THE PRINT MEASUREMENT:** tier-1 `code-to-char 192` prints A-grave correctly while `code-to-char 592` prints `>` where U+03C0 belongs, so the surface is not uniformly dead -- it is right wherever the bytes it happens to read are right, which is the worst shape to leave in place. **WHY NOTHING CAUGHT IT (L-CONSTRUCT, again).** Every non-ASCII character anywhere in `codex/test` is tier 0, so every fixture is a single CCE unit and no test in the tree has ever built a tier-1 or tier-2 one. COMPILER-21 and COMPILER-22 both landed against corpora made entirely of the half that works. **THE ASK, AND IT IS A DESIGN DECISION RATHER THAN A PATCH.** Two repairs are available and they are not equivalent. (1) Make the tables real: concatenate `cce0u2-lo`, `cce0u2-hi` and `cce-t2-rodata` into `init-rodata` and re-derive all the offsets, which adds about 4.6 KB to every emitted binary and re-lays the seed data section. (2) Delete the hand-rolled helpers and route the two builtins through the foreword `from-unicode`/`to-unicode`, which are already correct and already compiled, removing the duplicate rather than repairing it. The second is smaller and kills a whole class, but it changes how a builtin is lowered. **Not blocking anything measured today: no shipping app prints tier-1 or tier-2 text, which is why this has been latent.** Seed-affecting whichever way it goes. **CORRECTION 2026-08-25 (blu), BEFORE ANY CODE WAS WRITTEN AND AFTER THE RULING: THE MECHANISM PARAGRAPH ABOVE IS WRONG, AND THE RULING CITES IT.** The row said `__cce_print` reads the absent `cce0u2`/`cce-t2` tables and therefore "the multi-byte print path reads whatever follows". Both halves are false. **`__cce_print` IS NOT ON ANY LIVE PATH AND IS NOT EVEN EMITTED.** It is recorded by `emit-cce-to-utf8-helper` (`X86_64Helpers.codex:662`), and that function is called from nowhere in `Emit/`; `__cce_print` has ZERO entries in `build/output/Sut.map`, while `__cce_print_multi` has one. The live multi-byte print path is `__cce_print_multi` (`X86_64Helpers.codex:249`, called from `X86_64IO.codex:338`), and it reads `tier1-block-rodata-offset` (256) and `tier2-rodata-offset` (288) -- **both of which ARE in `init-rodata`**. So no live path indexes a missing table, and the absent-rodata story explains none of the measured failures. **The first search that produced this correction was itself wrong and proved nothing**: I grepped `emit-cce-print-helper`, a name that does not exist, got zero hits, and had to redo it against the real name before the claim stood. **WHAT SURVIVES, all still measured and unchanged:** the foreword `from-unicode`/`to-unicode` round-trip every tier exactly; the builtin pair round-trips ASCII and nothing else; it is LOSSY, since `[192]` and `[8364]` both yield `192 128`; and tier-1 print is half right, `code-to-char 192` correct and `code-to-char 592` printing `>`. **WHAT IS NOW UNKNOWN:** the cause of those failures. The dead `emit-cce-to-utf8-helper` and the three never-concatenated tables are a real but SEPARATE finding -- abandoned code referencing offsets for tables nobody ever built -- and they are not why the builtins are broken. **The ruling''s `__cce_print` half therefore targets dead code**; its builtin half may well still be right, but the justification under it has changed, so the re-derivation comes before any edit. **RE-DERIVED, AND THE CAUSE IS NAMED (blu, 2026-08-25): `char-to-text` TRUNCATES EVERY MULTI-BYTE CCE CODE TO ITS LOW BYTE.** `emit-char-to-text-builtin` (`X86_64Builtins.codex:269`) allocates 16 bytes, stores the length as the immediate **1**, and writes the code with a single `mov-store-byte`. It never consults `cce-encode-length`. So a Char whose CCE code needs two bytes becomes a one-byte text holding `code mod 256`. **Measured against seed 7AF7CEF5**: `cce-encode-length` answers 2 for CCE 192, 592 and 2000, while `text-length (char-to-text (code-to-char c))` answers **1** for all three and the stored unit is 192, **80** and 208 -- the low bytes. `code-to-char` is NOT at fault: `char-code (code-to-char 592)` answers 592 exactly, so the Char carries the full code and the Text step loses it. **THIS CLOSES THE TIER-1 PRINT SYMPTOM EXACTLY, which is what makes it the cause rather than another candidate.** CCE 592 truncates to byte 80; byte 80 is a single-byte CCE unit; `cce-to-unicode-table[80]` is 62; and 62 is `>`, which is the character the row above measured. The chain predicts the observed glyph with no freedom left in it. **AND IT INVALIDATES FIXTURES, INCLUDING MINE.** Every text this row built with `char-to-text (code-to-char n)` for a multi-byte n was malformed at construction, so any conclusion drawn from those texts measured the truncation and not the thing under test. **STILL OPEN, NOT EXPLAINED BY THIS:** `unicode-bytes-to-text [233]` builds its two units WITHOUT `char-to-text` and `text-to-unicode-bytes` still answers 0 on the result, so the builtin round-trip has at least one defect of its own and must be re-measured on correctly built text before anything is concluded about it. **The fix is contained** -- encode per `cce-encode-length` and set the length to match -- but it lands in `X86_64Builtins.codex`, which reek claimed for the sized-vector repair, so it sequences with that claim rather than racing it. **THIRD CORRECTION, SAME DAY, AND THIS ONE SPLITS THE ROW INTO THREE SEPARATE DEFECTS (blu, 2026-08-25). THE HEADLINE "THE PAIR ROUND-TRIPS ASCII AND NOTHING ELSE" IS WRONG.** It was measured with the text in one position and is an artifact of that position. **DEFECT A, POSITIONAL: `text-to-unicode-bytes` answers 0 whenever its input text is the TOPMOST allocation.** Control, same seed `7AF7CEF5`, same program, two arms differing only in whether one small list is allocated after the text: topmost answers `0` for u=233, 1072, 192 and 8364, while buried answers **233, 1072, 192** correctly. ASCII (u=65) is right in both, which is why every earlier reading looked like an ASCII-only pair. This is the `list-push` topmost/extend/copy aliasing shape that `DevelopersGuide.md` documents and that `builtin-alloc`''s push-topmost and push-buried arms exist for, reaching a builtin that should not have it. **DEFECT B, TIER 2, AND IT IS THE ONLY SURVIVING LOSSINESS: even BURIED, u=8364 answers 192**, the same as u=192, and `unicode-bytes-to-text [8364]` emits the units `192 128`, identical to `[192]`. So the ENCODE half collapses a tier-2 code point onto a tier-1 one; that half of the original row stands, narrowed from "all non-ASCII" to "tier 2". **DEFECT C is `char-to-text` truncation**, recorded above, and it is independent of both. **WHY THIS TOOK THREE CORRECTIONS, WHICH IS THE PART WORTH KEEPING.** Every wrong version came from a probe with ONE arm: one helper name assumed to be the live one, one text position, one construction route. The version that finally held is the one with a CONTROL that differs in exactly one thing. A single-arm probe on a positional defect does not report "sometimes"; it reports whichever position it happened to use, with total confidence, and reads as a property of the builtin. **NOTHING HERE IS SAFE TO ACT ON UNTIL DEFECT C LANDS**, because a fixture built with `char-to-text` is malformed and any arm over multi-byte text built that way measures the truncation instead of its subject. **FOURTH CORRECTION, 2026-08-25 (blu), AND IT RESCOPES DEFECT C FROM A PATCH TO A CONTRACT DECISION. THE "THE FIX IS CONTAINED" SENTENCE ABOVE IS WRONG.** Encoding per `cce-encode-length` is correct for a Char that means a CCE CODE POINT, and `char-to-text` is very largely not used that way. **Census, whole tree, `codex/` and `apps/`, 3,648 files walked: 361 call sites, of which 169 are byte-identity shape** (`char-to-text (char-at s i)` and the bare-byte forms). Those loops walk a text one BYTE at a time and rebuild it, and they are correct today only because the builtin is an identity on 0..255. Under the proposed fix every framing byte at or above 128 re-encodes to two bytes and the rebuilt text is corrupt. **THE COMPILER ITSELF IS ON THAT PATH, IN FOUR PLACES:** `IRTextEmitter.codex:76` (`ir-quote-char`), `CodexEmitter.codex:804,811` (`escape-one-char`, `escape-char`), `ChapterScoper.codex:123-125,451-453` (slugify and cite-key) and `Lexer.codex:299-306` (`decode-escapes-loop`). `ir-quote-char` runs over EVERY string literal in text mode, which the gate compares. **MEASURED, NOT PREDICTED.** `codex/test/validation-rules.codex:57,61` hold a three-letter and a one-letter Greek string literal, Greek being tier 1, and they are the only multi-byte literals in the tree. Compiling that chapter to IR CCE against the DEPOT seed `E0347775284B1048` emits 74,719 bytes containing exactly 8 bytes at or above 128: `199 129 199 130 199 131` at offset 64,448 and `199 129` at 65,117, which is three and then one two-byte CCE unit, lead 199 with continuations 129/130/131. Each of those six bytes was rebuilt one at a time by `ir-quote-char` through `char-to-text (code-to-char c)` with c in {199,129,130,131}. `cce-encode-length` answers 2 for every one of them, so the narrow fix turns a six-byte literal into twelve bytes of garbage and moves text-mode output. **THE FIRST CENSUS WAS WRONG AND THE CONTROL CAUGHT IT:** a `codex/**/*.codex` glob reported 67 sites and silently missed all thirteen compiler ones listed above; a real recursive walk found 361. Quote the walk, not the glob (L-COUNT, and the negative-width half of check-your-own-instrument). **THE ASK.** `char-to-text` cannot serve both classes: for a value in 128..255 the byte class needs one byte out and the code-point class needs two, and no single behaviour satisfies both. So the question is which contract the builtin has, and it is a design decision rather than a patch: (a) leave it byte-level and give the code-point class a separate builtin, (b) make it code-point-level and route the 169 byte-identity sites onto a byte primitive such as `raw-bytes-to-text`, or (c) split it into two named builtins and retire the ambiguous one. **NOT STARTED, DELIBERATELY.** Landing the narrow fix reds the gate on `validation-rules` at best and corrupts literal round-tripping silently at worst. **L-CONSTRUCT CUTS THE OTHER WAY HERE:** the tree's near-total absence of multi-byte literals is what makes the narrow fix LOOK safe, and the one chapter that does spell them is what makes it measurably unsafe. **THE ASK ABOVE IS ANSWERED AND DEFECT C IS BUILT AND ON MAIN. THE ROW READ "NOT STARTED, DELIBERATELY" UNTIL 2026-08-26 AND HAD BEEN WRONG SINCE 19579.** The contract decision was taken as **option (a): `char-to-text` STAYS BYTE-LEVEL and the code-point class gets its own builtin, `char-encode`.** That is why the 169 byte-identity sites, the four compiler sites named above (`IRTextEmitter`, `CodexEmitter`, `ChapterScoper`, `Lexer`) included, are deliberately untouched and must STAY on `char-to-text`: under (a) they are correct, and a future agent who "finishes the migration" by moving them to `char-encode` reintroduces exactly the corruption this row measured. **What landed:** 19579 the `char-encode` builtin; 19588 its guard `codex/test/ops/char-encode-bands`, 12 arms; 19611 arms for the five plugs that can be verified on this box (python, javascript, zig, csharp, wasm); 19636 the foreword's 14 encode-meaning callers, which FIXED A LIVE DEFECT; 19672 the remaining 27, after which the encode-meaning shape is gone from the tree. **RE-MEASURED 2026-08-26 rather than carried forward (L-COUNT), and with the control the negative needs (check-your-own-instrument):** a real recursive walk of `codex/` and `apps/`, 3,650 `.codex` files excluding `build-output`, finds **0** sites of `char-to-text (code-to-char (from-unicode ...))`, and the control pattern `code-to-char (from-unicode ...)` finds **36**, so the zero is a real zero and not a dead pattern. A first attempt at this census matched `char-to-text (code-to-char ...)` WITHOUT the `from-unicode` and reported 76: those are the byte-identity sites option (a) keeps, so the too-WIDE pattern manufactured a false positive exactly as the too-narrow one manufactures a false negative. **THEREFORE THE "NOTHING HERE IS SAFE TO ACT ON UNTIL DEFECT C LANDS" HOLD ABOVE IS DISCHARGED, and defects A and B are actionable** (blu, 2026-08-26, per Damian's re-ruling that day via red: blu takes the open compiler bugs). **WHAT REMAINS OF C IS NOT IN THIS ROW: it is CORE-8's residue** in `codex/foreword/core/core-backlog.md`, the call-site policy for `from-unicode` answering -1. The JSON half is fixed (19662); the encoders still accept -1 outside JSON and emit unit 255. **A AND B RE-MEASURED AGAINST THE CURRENT COMPILER, 2026-08-26 (blu), seed C45E5825, kernel digest read off the compile rather than assumed.** Every measurement in this row above predates defect C, so it was taken through an encoder that truncated multi-byte codes; re-measuring first is L-SAMEVER, and it moved the picture. **The oracle control passes on all six values** (u=65, 233, 1072, 192, 960, 8364 round-trip through `from-unicode`/`to-unicode` exactly), and so does a stronger control this row did not have: **`char-encode` and `cce-decode-at` AGREE on all six, tier 2 included**, with `text-length` equal to `cce-encode-length` every time. So the foreword surface plus the new builtin is correct end to end, and every failure below is the hand-rolled helper pair. **DEFECT A IS CONFIRMED AND IT MASKS THE OTHERS.** Topmost, `text-to-unicode-bytes` answers **0** for 233, 1072, 192, 960 and 8364 alike; burying the text under ONE later allocation answers 233, 1072, 192 and 960 correctly. That single allocation is the only difference between the two arms. So the pair does NOT "round-trip ASCII and nothing else": it round-trips tier 0 and most of tier 1 fine, and the ASCII-only reading was the aliasing talking. **DEFECT B IS CONFIRMED AND IS INDEPENDENT OF A**, since it survives burying: `unicode-bytes-to-text [8364]` yields units `192 128`, byte-identical to `[192]`, and the buried decode of the 8364 text answers **192**. The reverse half is also non-canonical below that: `[233]` yields `192 169` where `from-unicode` mandates the single tier-0 unit 97, and `[1072]` yields `196 176` against 113. **TWO FURTHER DEFECTS IN THE FORWARD HALF, NOT PREVIOUSLY RECORDED, AND THE SECOND IS NEWLY REACHABLE BECAUSE DEFECT C'S FIX MADE IT SO.** (i) The 2-byte tier-1 decode is wrong at the LOW end of the range and right above it: bytes `193 128` (cce 192, foreword-decoded 192) come back as **256**, while `199 144` (cce 592) comes back as 960 correctly, so this is a base or boundary error in the tier-1 lead handling rather than a dead table. (ii) **A 3-byte tier-2 unit is not readable at all**: `char-encode` now emits `233 168 144` for cce 41616, which `cce-decode-at` reads back exactly, and `text-to-unicode-bytes` returns **two** values whose first is 1640, so it mis-frames rather than refusing. Before defect C landed nothing in the tree could produce a 3-byte unit, which is why this arm has never been exercised (L-CONSTRUCT, a third time on this surface). **WHAT THIS SETTLES FOR THE RULING.** Repair 1 was to make the absent rodata tables real; the 2026-08-25 correction already showed that story explains none of the failures, and the two forward defects above are arithmetic and framing in the helper, not a missing table. Repair 2 was to delete the hand-rolled helpers and route both builtins through the foreword, and the FORE-OK control above is direct evidence that the destination is correct on every value the helpers get wrong, tier 2 included. Damian ruled repair 2 on the since-retracted mechanism and red is re-presenting; this is the measurement that re-presentation wants, and it is recorded here rather than acted on. Still seed-affecting. Probes are `cce23probe` and `cce23probe2` in blu's scratchpad, four arms each with the oracle and the buried control as the two controls. |
| COMPILER-24 | **`==` on a RECURSIVE variant crashes the compiler.** `Wrap Leaf == Wrap Leaf` over `Nest = \| Leaf \| Wrap (Nest)` dies in `alloc-temp+0xAF` with `!EXC=06`, invalid opcode, compiling for x86-64 against seed 7AF7CEF5 (fester, 2026-08-25). **The control is the same type with no `==`, and it compiles clean**, so it is the comparison and not the declaration. `emit-eq-op` sends a `SumTy` operand to `emit-sum-structural-eq`, which inlines a field compare per constructor through `emit-eq-op` again (`X86_64.codex:2985`); a field declared at the sum's own type re-enters with the same type and nothing bounds it. Legal source, no diagnostic, a crash rather than a refusal. The wasm plug emits one function per variant type and a self-call, so it answers this shape, which means there is currently no oracle to grade a recursive `==` against. **THE CRASH IS FIXED ON MAIN 2026-08-26 (blu, main 19845, seed 591EEA7B) BUT THE SHAPE IS REFUSED RATHER THAN SUPPORTED, AND THAT DISTINCTION IS THE WHOLE OF WHAT IS LEFT.** New diagnostic **CDX2097 RecursiveStructuralEq**, raised from `emit-eq-op` AND `emit-neq-op`, which carried the identical recursion and which this row did not name. Reproduced first against seed 0D4724DD with the row's own control: the subject died at `alloc-temp+0xAF`, exit 4, and **retrying at 8192MB crashed identically, so it was never heap exhaustion**; the same declaration with no `==` compiled clean. **WHY IT IS A REFUSAL AND NOT A FIX.** Structural equality on a recursive type has to recurse at RUNTIME, and no inlining bound is correct, because any bound compares only part of the value -- so there is no version of the current design that works. The wasm plug's answer (one function per variant type, self-call) needs machinery x86-64 does not have: `emit-all-defs` walks a list of IRDefs and the code buffer is append-only per function, so nothing can emit a function mid-expression. Doing it here needs a pre-pass that synthesises an eq helper per recursive sum, a registry, and forward calls through the existing `st-add-deferred-patch` machinery. **That is a design change and it is NOT done; it is the open half of this row.** Detection is a bounded walk of the constructor field graph carrying a visited-name list, so **mutual recursion through a second variant is caught too** and is not a second defect waiting; a fuel of 64 answers "recursive" rather than looping if it is ever exhausted, which is the safe direction. Guarded by `codex/test/errors/recursive-eq` expecting 2097, and it fails on the compiler it is meant to catch, since that program raised nothing and crashed instead. R-COST measured rather than inspected: gate 407.3s against 434.0s for the previous seed's final gate and `check-errors` 22.7s against 23.9s, so the per-`==` walk costs nothing measurable. |
| COMPILER-25 | **`emit-zero-region` emits its prefixes in the wrong order, so hardware zeroes HALF the region.** | `X86_64Boot.codex:2317` emits `[72, 243, 171]`, which is REX.W then F3 then AB. REX is ignored unless it sits immediately before the opcode, so silicon executes `rep stosd` while rcx holds a QWORD count: `qwords * 4` bytes are written and the upper half of the region keeps whatever it held. The `movsq` at `Boot:3047` uses the correct order `[243, 72, 165]`, which is both the proof of intent and the shape of the one-line fix (F3 before REX.W). Found by a read-only instruction census over `Emit/` (red, 2026-08-25) and verified against source; no runtime symptom is attributed to it yet, so it is latent, presumably because the regions it zeroes are already zero or their tail is never read before first write. That presumption is UNMEASURED: whoever takes this should first find the callers and answer whether a half-zeroed region is reachable with dirty memory. **FIXED AND ON MAIN 2026-08-26 (blu, main 19828, seed 0D4724DD).** One line, `[243, 72, 171]`, the order the `movsq` at `Boot:3047` already used. **The presumption this row asked to be tested is now ANSWERED for the bed and still OPEN for metal, and the bed half is the finding.** Ten call sites, all in `X86_64Boot`: the process page tables, the process, channel and identity tables, the identity key, the TSS and the AP report area. The page-table one is the sharp one, because `emit-build-process-page-tables` zeroes the region and then fills only SOME entries, so an unfilled slot holding a stale Present bit maps garbage rather than faulting. **NO BED ARM CAN EVER SEE THIS.** codex-vm backs guest RAM with `VirtualAlloc(MEM_COMMIT)`, which Windows zero-fills, so every region `emit-zero-region` touches is already zero in the bed and half-zeroing it is invisible: the bed has a blind spot shaped exactly like the production defect (L-BEDTRUE, one level down). There is no per-process reuse to dirty it either -- `emit-build-process-page-tables` has exactly ONE caller, at a fixed address, at boot. On metal the region holds what the firmware left, and **whether the fill loops overwrite the upper half is NOT measured**, which is the half of the original presumption that survives and the reason this stayed latent. **So no arm was written, deliberately: in the bed it could only pass vacuously** (L-FALSIF). **The instrument is a byte census of the artifact, and it is what proved the fix reaches the wire rather than only the source:** count `48 F3 AB` against `F3 48 AB` in a seed. Old seed 9 and 0; first-pass Sut 9 and 0, which is the two-pass signature rather than a failure, since this alters the EMITTED RUNTIME so the first Sut carries the code that emits the bytes without carrying them in its own body; converged seed 0 and 9, confirmed again by printing the seed from the DEPOT after the copy-up. That census is the runner this row does not otherwise have. |
| COMPILER-26 | **A record's implicit type parameters are derived by the IR TEXT EMITTER at serialisation time, not carried on the AST -- so every consumer of the AST that is not the wire sees an incomplete type** | Absorbed from Steve Howell's PR 90 (his public numbering COMPILER-20; renumbered on absorption, the internal register minted 20-25 independently the same day). Citations re-verified against head by red 2026-08-25: `codex/foreword/core/Sort.codex:20` declares `SortPartition = record { list : List a, pivot : Integer }` with no parameter list, the AST carries `tparams = []`, and `Emit/IRTextEmitter.codex:402-406` computes the inferred list at write time with `ir-emit-tparams-text:386-388` writing explicit-else-inferred; variants the same at `:407-409`. Steve's measurement: `emit-zig-chapter` handed the front end's own ATypeDef emits a monomorphic struct whose fields still mention `a` (zig that does not compile), while the SAME emitter through the text wire emits the generic form; use-site annotations are a second instance of the class (`(record-ty ... (args (tvar 30)))` on the wire, args absent in memory). The wire is load-bearing for semantics, not just transport, and any direct AST consumer inherits the incompleteness silently. Suggested direction (his, unattempted): carry the inferred parameters on the ATypeDef where the checker learns them and let the emitter serialise what the AST holds. **FIXED AND ON MAIN 2026-08-26 (blu, main 19889, seed C3181693).** Steve's direction taken as written: the collectors move to `Ast/AstNodes.codex` as `ast-*`, and the desugarer fills `tparams` at BOTH `ATypeDef` construction sites (`Ast/Desugarer.codex:392-393`, the only two in the front end) with the same explicit-else-inferred rule the emitter applied. Filling at CONSTRUCTION rather than at each consumption site is the whole value: there are **151** `is ARecordTypeDef` / `is AVariantTypeDef` destructure sites across `codex/` and `apps/` (measured 2026-08-26, matching lines equal to matching occurrences, so no multi-hit line is hiding in the count), and one construction-site fill makes all of them correct at once, where patching consumers would have fixed the two that were looked at and left the rest. An interim attempt that called the helpers at the emitter and the type checker instead is what the corrected version replaced. **THE TRAP THIS UNCOVERED IS THE PART TO KEEP, AND IT IS NOT ABOUT THIS ROW.** A bare reference to a generic record in a signature (`-> SortPartition`) resolves because `parameterize-walk` merges the record's own parameter names with the enclosing signature's **BY NAME**, which is sound ONLY while the declaration carries no parameter list. `codex/foreword/core/Sort.codex` was relying on a name coincidence: the record's field said `list : List a` and `sort-partition`'s signature also said `a`, so the two `a`s merged. The Codex text printer normalises a signature's own tvars to `a0`, so the moment the declaration carried a literal `(a)` the names stopped agreeing, the merge silently failed, and stage2 refused `sort-partition` with **CDX2087**. Fixed by spelling the use site, `-> SortPartition a`, which removes the merge from the path rather than relying on it. **The prose at `Emit/IRTextEmitter.codex:119-126` describes that merge without naming the condition it depends on, and should not be read as a guarantee.** The IR wire is unaffected: it recovers type arguments from field tvars, a separate mechanism. **THE INSTRUMENT WAS THE PHASE SPLIT, NOT A DEBUGGER:** `text-stage1` GREEN with `text-stage2` RED says the compiler accepts the source and rejects only its own printed form of it, which localises the defect to the printer without reading any of it. Worth reaching for whenever a text-mode phase fails. **The gate reds arrived in three layers and each one moved the failure forward**, so the first red (sem-equiv dropping `SortPartition`, because the printer now spells the inferred parameter and the source did not) was progress rather than a wrong turn. Verified: full gate green 642.3 s, no skips, `app-sweep` included; seed is a true fixed point of the shipped source (workspace seed == built Sut). |
| COMPILER-27 | **The deck reservation is advisory: overrunning `__heap-advance` is detected, printed, ignored, and then faults at 200%** | Absorbed from Steve Howell's PR 91 (his public numbering COMPILER-21; renumbered on absorption). Measured by Steve on the zig-emitted hosted tool, and the row's scope is that arm: shrink the reservation, feed a bigger subject, and the tracer prints headroom going negative on every allocation past the boundary while nothing acts on it, until a general-protection fault in `cx_list_at` at exactly 200% of the reservation -- a user sees a memory fault in a list accessor with no reason to suspect the deck. The cursors-meeting overrun IS refused by name, so the machinery can refuse; the reservation boundary is simply not one of the things it refuses on. No partial output is produced (the emitted source is one Text printed at the end), so the failure cannot read as a successful compile. Why it matters soon, his measurement: deck cost ~145 MB per MB of source, near-linear; the hosted compiler on its own bundle sits at 421 MB of the 512 MB reservation and every Update adds chapters. Suggested fix (his, unattempted): refuse in the allocator when the deck cursor would pass the reservation, using the numbers the tracer already computes. The bare-metal analog of the same boundary is UNMEASURED here and is the first question for whoever takes the row (relation to L-TAILGUARD's post-loop allocations noted). **BOTH HALVES OF THIS ROW ARE WRONG AT HEAD, MEASURED 2026-08-26 (blu). THE ROW'S OWN ARM IS ALREADY FIXED AND THE BARE-METAL HALF IS ALREADY MEASURED.** This is COMPILER-28's shape a second time: a row absorbed from an external PR describing behaviour that the tree had repaired before the row was written, and read as open because nobody re-measured it. **THE ZIG ARM REFUSES, AT THE ALLOCATOR, BY NAME, AT EXACTLY THE CEILING.** `cx_bump_alloc` (`codex/plugs/zig/ZigEmitter.codex:3343`) carries two panics: region exhaustion, and `cx_frontier_crosses` (`:3342`), which inside an extent tests `base < cx_bivy and base + len > cx_bivy`. Because `cx_deck_slack` is **0** in every shipped build (`:3369`), `cx_heap_advance` parks the main frontier exactly on the deck top, so `cx_bivy` IS the reservation ceiling and crossing the reservation IS the cursors-meeting refusal. That is precisely the fix this row calls unattempted; it landed 2026-08-21 with the cursors-meeting work, before absorption. **MEASURED, three points, both directions, one variable.** A probe that reserves N bytes with `__deck-set` + `__heap-advance`, opens an extent with `__deck-enter`, burns list allocations, and reports `__deck-pos - base`: at a 1 MB reservation, 1 round of 4,000 pushes spends 35,495 bytes and survives; 20 rounds spend 709,761 and survive; 100 rounds are **REFUSED**, `cx heap: the two cursors met -- alloc at 9433792 + 23552 crosses (hp=9433792 dptr=8389006 deck_base=8389006 bivy=9437582 nest=1)`, and `bivy - deck_base` is **1,048,576**, the reservation to the byte. **The isolation control is the half that makes it a finding:** the SAME 100-round burn against a 64 MB reservation survives and reports 3,548,807 bytes used, which both exceeds 1 MB (so the refused arm genuinely overran) and leaves the reservation as the only variable. **Do not read the first attempt's PASS as a result:** 4,000 pushes cost 35,495 bytes because `list-push` is amortised here, so the original "overrun" arm never reached the ceiling and both arms printed SURVIVED. That is a vacuous control, and the deck-usage readout is what exposed it. So the row's symptom is gone in both particulars: it refuses at 100 per cent of the reservation rather than faulting at 200 per cent, and it names the deck rather than dying in `cx_list_at`. **THE BARE-METAL HALF IS MEASURED, GUARDED, AND HAS A RUNNER.** `build/deck-floor-test.ps1` asserts four arms and PASSES against seed C3181693: `-Decks 5` and `-Decks 20` report CDX9002 from LEX, `-Decks 40` from DESUGAR, all three with no crash, and `-Decks 100` compiles, so the guard is not noise. **WHAT IS ACTUALLY OPEN IS THE OPPOSITE OF WHAT THE ROW SAYS, AND IT IS A COST DECISION RATHER THAN A DEFECT.** Bare metal's guards are PER-SITE calls to `deck-short-of` (`tokenize-collect`, `NameResolver.codex:386`, `ResolveTypes.codex:104`, `LambdaLifting.codex:45`), not a chokepoint in the allocator, and they are documented blind exactly where exhaustion happens: `deck-short-of` reads `__deck-pos`, which is FROZEN inside a phase-wide `deck-record` extent (`Core/PhaseAllocator.codex:109-125`, measured 2026-07-21 -- starving that floor to 16 MB "did not raise CDX9002; it crashed in `__text_compare` on a garbage pointer" with the guard compiled in), and `Lowering.codex:2143` states plainly that "an under-reserved floor does not raise `CDX9002` -- it dies in a `#GP` with no diagnostic". **So the HOSTED arm is now STRICTER than bare metal**, which is the inversion of this row's premise. Closing that means a check on the bare-metal allocation path, and the tree has already priced it: `PhaseAllocator.codex:23-25` chose the deck-reservation chokepoint precisely because it is "one compare per deck, thirteen per compile, against one per allocation" across 58 inline bump sites. That is a hot-path cost trade on the compiler, not a bug fix, so it is Damian's call and is NOT taken here. Unowned, and re-scoped to that question alone. |
| COMPILER-28 | **A RANGE ON ANY NON-INTEGER BASE IS SILENTLY UNENFORCED. `Char between 0 and 255` COMPILES CLEAN AND ACCEPTS 99999.** PRIORITY (Damian, 2026-08-25). **THE PAIR, one word apart, seed D9B5B170:** `take : Integer between 0 and 255 -> Integer` called with 99999 is refused, `error CDX2050: bounded parameter has bound 0..255`, no binary emitted. `take : Char between 0 and 255 -> Integer` called with `code-to-char 99999` compiles with NO error and NO warning, runs, and prints `99999`. The enforcement half is present and works; it simply never reaches this declaration. **THE MECHANISM IS NAMED AND IT IS NOT SPECIFIC TO Char.** `resolve-bounded-int-type` (`codex/compiler/Types/TypeChecker.codex:39`) resolves the base and answers `deck-record (IntegerTy lo hi mode)` for an `IntegerTy` base, the same for a `UnitTy` wrapping one, and **`is otherwise -> ErrorTy`** for everything else. So every base that is not an integer takes the ErrorTy arm, and that ErrorTy is SWALLOWED rather than raised: the bad declaration is accepted and the bound becomes decoration. Char is the proven instance; the arm is shared, so any other non-integer base is the same path and is untested only because nothing in the tree writes one. **WHY NOTHING CAUGHT IT (L-CONSTRUCT):** all 873 `between` declarations in `codex/` and `apps/` use an `Integer` base and there is not one `Char between` anywhere, so the corpus has never spelled the shape that fails. This is the same corpus blindness that hid the `char-to-text` truncation, one layer up in the type system. **WHAT IT BLOCKS:** the ruled repair for COMPILER-23 defect C is that `char-to-text` gains a bounded 0..255 domain so misuse is a compile refusal. That repair cannot be written today: `CodexType` has `IntegerTy (lo) (hi) (OverflowMode)` but a bare `CharTy` with no fields, no `BuiltinSpec` in the tree carries a bounded domain, and writing the bound anyway would produce exactly the silent no-op above. **THE OPTIONS, MEASURED.** Give `CharTy` bounds: 45 mentions, 28 in the compiler and 17 across 12 plugs. Declare the domain as a bounded `Integer` instead of `Char`: 212 `char-to-text` sites, 115 of which pass `char-at` directly and would become type errors. Carry the range in `BuiltinSpec` and check it there: contained to the compiler, no plug or caller change, but a new mechanism. Bound `code-to-char` instead: one builtin, 155 sites, and it is the single gate every code-point-to-Char conversion passes through, so it excludes -1 everywhere. **THE FIRST FIX IS SMALLER THAN ANY OF THEM AND SHOULD LAND FIRST: STOP SWALLOWING THE ErrorTy**, so a range on a base that cannot carry one is refused at the declaration instead of accepted and ignored. That is a diagnostic, not a type-system change, and it turns this from a silent hole into a loud one. **BOTH SWALLOW POINTS ARE LOCATED, AND SO IS THE THING THAT MAKES THE FIX AWKWARD.** The bound is lost twice over. First `resolve-bounded-int-type` (`Types/TypeChecker.codex:39`) answers `ErrorTy` for the Char base. Then `lint-narrowing-check` (`Types/TypeCheckerInference.codex:1151`), which is the code that would have enforced it, opens `when field-ty is IntegerTy (f-lo) (f-hi) (f-mode) -> ...` and falls through `is otherwise -> st` for everything else -- so an `ErrorTy` parameter is not merely unbounded, it is unchecked, and `ErrorTy` then unifies with anything so no downstream arm complains either. Repairing only the first would leave the second silent for any other route to a non-integer declared bound. **THE CONSTRAINT: THE WHOLE RESOLUTION LAYER IS PURE.** Every function in `TypeChecker.codex` from `resolve-type-expr` down is `... -> CodexType` with no `UnificationState` and no bag, and `resolve-type-expr` has NO caller outside that chapter, so there is nowhere in the existing path to raise from. The fix is therefore either to thread the bag through the resolution layer, which is invasive and touches every arm, or to add a separate validation walk over the declaration's `ATypeExpr` that runs where the bag already exists and reports an `ABoundedIntType` whose base does not resolve to an integer. The second is additive and changes no signature; the span is already carried on the `ABoundedIntType` node (`TypeChecker.codex:22` binds it as `(s)` and drops it). **NOTHING IN THE TREE RELIES ON THE BROKEN SHAPE, SO THE REFUSAL BREAKS NOTHING AND THERE IS NO MIGRATION LIST.** **THE NUMBER IS ABOUT 875 IN SOURCE**, and it took two censuses and a re-measure to get there. blu, 2026-08-25, `Get-ChildItem -Recurse` over `codex/` and `apps/` excluding `build-output`, matching `(\w+) between -?\d+ and -?\d+`: **875** base tokens, 873 of them `Integer` and 2 prose false-positives (`difference between`, `lands between`). red first answered **3,083**; on re-measure that was **869** by bare `Integer between` plus **2,157 copies living inside generated concatenations**, and the accompanying claim that generated files affected only the prose tally was wrong. The 875 is the honest source figure: 869 bare plus the variant forms. **The conclusion never moved and is the half the fix depends on: ZERO non-Integer bases and zero unit-wrapping bases**, which survives the correction because copies of an `Integer` base are still `Integer` bases. The generated trees carry **1,381** if anyone needs that number, and it is not the one to quote. Worth keeping as a method note: a census over a tree that contains generated concatenations of its own source counts the same declaration many times, and the tell was that two counts differed by more than 3x while agreeing perfectly on the classification. **FIXED AND ON MAIN 2026-08-25 (blu, main 19731, seed C45E5825): the first-fix above is built -- a range on a base that cannot carry one is now REFUSED at the declaration with new diagnostic CDX2054, instead of accepted and ignored.** It is the additive validation walk, not the invasive one: `check-linearity-def` already walks a def's declared `ATypeExpr` carrying `UnificationState` and raises through `add-unify-error`, and `check-all-defs` already has `tdm` in scope at the line that calls it, so no signature moved. It reuses `resolve-bounded-int-type` rather than re-deciding what counts as bounded, so the check cannot drift from the thing it checks. Verified three arms against the Sut shipped with it: `Char between 0 and 255` -> CDX2054 refused (was: accepted 99999); `Integer between 0 and 255` with 99999 -> CDX2050, unchanged; the same with 200 -> compiles, no diagnostic. **This row read "Unowned" at main head until 2026-08-26 and the fix had been on main since 19731, which sent the commander to re-assign work that was already done** (L-COUNT's shape one register over: the row, not a count, was carried forward unre-measured). **The SECOND swallow point is untouched and deliberately so:** `lint-narrowing-check` still opens on `IntegerTy` and falls through `is otherwise -> st`, which is now unreachable for this route because the declaration is refused earlier, and stays open for any other route to a non-integer declared bound. **What it unblocks is live: COMPILER-23 defect C's compile-time backstop half is no longer impossible**, so re-read this row before assuming a bounded domain cannot be declared. |
| COMPILER-29 | **An effect operation that takes an ARGUMENT returns a POINTER instead of its value on arm64, and produces no output at all on riscv. x86-64 is correct.** | Found 2026-08-26 (blu) while working COMPILER-9's class-B failures; it is NOT a precedence-table defect and does not belong to that row. **THE MINIMAL REPRODUCER, and it is 14 lines:** `effect One where oop : Integer -> [One] Integer`, then `opening` = `let r = with One (oop 6)` / `oop (x) (resume) = resume (x * 7)` printing `show r`. Expect `r=42`. **Measured: x86-64 `r=42` (correct), arm64 `r=1075847184`, riscv64 NO OUTPUT.** The same 14 lines with a ZERO-argument operation (`zop : [Zero] Integer`, `zop (resume) = resume 42`) PASS on all three, so operation arity is the whole variable. **THE HANDLER BODY IS NOT RUN AT ALL, which is the part that localises it:** a variant whose clause ignores its argument entirely (`oop (x) (resume) = resume 99`, expect 99) returns the SAME `1075847184`, so this is not argument passing -- the `with` expression yields a pointer regardless of what the handler computes. `1075847184` is `0x40202010`, a heap address in the QEMU virt RAM window at `0x40000000`; it is NOT `a64-effect-op-table-base`, which is `#40020000`. **THE STRUCTURAL CAUSE IS VISIBLE ON THE WIRE.** A handler clause WITH parameters reaches a plug as an unlifted `(lambda (params (param "x" ...)) ...)`; a zero-parameter clause reaches it as a bare `(apply (name "resume") ...)`. That is `lift-lambdas` running on the CDX path only, which is COMPILER-12, and x86-64 is correct precisely because it does not use this wire. arm64 knows about the wrapper and tries to handle it -- `a64-unwrap-clause-lambda` (`Arm64CodeGen3.codex:1767`) emits the clause as a separate `"__handler_" & op-name` function, registers it, and stores its address in the effect-op table -- so the defect is inside that path rather than a missing case. **A HYPOTHESIS THAT LOOKED RIGHT AND IS FALSE, recorded so nobody spends the same hour:** the `handle` node and its `let` binding both arrive typed `error` on the wire, which reads exactly like the COMPILER-13/15 class. It is not the cause. The PASSING zero-argument probe carries `let type: error` too. **Affects at least three of the arm64 bed's 18 wrong-answer failures** -- `effect-smoke` and `handler-smoke` both print `1075847184` where `42` is expected, the identical value from two unrelated tests being the fingerprint that started this (L-SUSPECT), and `handler-dotted-discharge` prints `2151694408` (`0x80404048`) where `21` is expected. **No test is pinned for this deliberately:** the bed baseline is 48 failures and every lane compares against that set, so adding a known-failing reproducer would move the baseline for everyone. Pin it in the same change that fixes it. Unowned. |
| COMPILER-30 | **A lambda has no source span, so the type the checker solved for its parameters cannot be handed to lowering and reaches every plug as `ErrorTy`.** | Found by the zig plug's ladder, 2026-08-27 (ladder tag `h2-lambda-span`), on `codex/test/roc-closure-captures-list`. **`infer-lambda` (`Types/TypeCheckerInference.codex:488-498`) computes the lambda's complete function type and returns it; lowering never asks.** `lower-lambda` (`IR/Lowering.codex:721-728`) peels its parameters off the type the CONTEXT expects (`:724`), `peel-fun-param` (`Types/CodexTypeHelpers.codex:4-10`) answers `ErrorTy` for anything that is not an arrow, and `lower-let` hands its bound value `ErrorTy` as the no-expectation sentinel (`:689`) -- so a `let`-bound lambda's every parameter peels to `ErrorTy` on a program the compiler reports clean. **THE ROOT CAUSE IS ONE MISSING FIELD.** `Syntax/SyntaxNodes.codex:23` declares `| LambdaExpr (List Token) (Expr)`, the only expression node in the CST with no span -- `ListExpr` has a span field, and `RecordExpr`/`FieldExpr`/`HandleExpr`/`WithTimeoutExpr`/`TryExpr` each carry their keyword token -- so `Ast/Desugarer.codex:55` has nothing to pass and writes `synthetic-span`. `is-synthetic-span` is `span.file-id == 0` and it gates BOTH ends of the side channel: `record-expr-type` (`Types/Unifier.codex:147`) stores nothing and `lookup-expr-type` (`:178`) returns `ErrorTy`. Every lambda in the language is filed under file-id 0, which is to say not filed, and the `lookup-expr-type`/`deep-resolve` pair that `lower-dict-placeholder` already uses at `IR/Lowering.codex:216` is structurally unavailable to lambdas. **This is the same asymmetry the prose at `IR/Lowering.codex:729-740` already describes and `lambda-recorded-ty` already fixed for the lambda's RESULT** ("every consumer of the IR inherited a variable the compiler had already resolved"); this row is its parameters. **`ErrorTy` doing double duty as the type-FAILURE atom and the no-expectation sentinel is a second, smaller gap this does not close** -- a plug reading `(param "x" error)` cannot tell a failed check from an unwritten answer, and in practice it is always the second. **Cost today, measured:** C# and Rust erase it (`object`, `Box<dyn Any>`), Ada and Fortran GUESS a 64-bit integer (`AdaEmitter.codex:134`, `FortranEmitter.codex:148`) and case f of the matrix -- a parameter whose true type is `Text` -- refutes the guess, so two shipped plugs silently miscompile it. **Fixed by this PR:** matrix `error` cells 6 of 11 -> 0 of 11, case f recovering `text` rather than defaulting to `Integer`, case g (never applied, so genuinely unconstrained) honestly a type variable, both controls byte-identical; `codex/test` clean 318 -> 324 with six programs going markers -> match against their `.expected` and none regressing, 570 of 577 emitted `.zig` byte-identical. **Verified on the zig arm and bare metal only** -- the ladder has no depot gate. Two compiler-generated lambdas still desugar synthetic and are NOT in this PR because the matrix does not measure them: `ForExpr`'s `map-list` lambda (`Desugarer.codex:78`, `var-tok` is available) and instance-method synthesis (`:1455`, `m.name` is available). |
