# Compiler -- open capabilities

Quire-domain backlog, same rules as the app registers: an entry says
what is still missing and nothing else, a closed entry is DELETED, and
a gap that is still real is never quietly dropped. There is no
platform-wide register; `docs/PM/CurrentPlan.md` carries the shape.

| # | Capability | State of the gap |
|---|---|---|
| COMPILER-51 | **A text literal opened at the end of a line is silently empty, with no diagnostic at any severity (Steve Howell, `FINDINGS.md` item 9)** | UNVERIFIED HERE, routed as a pointer and not acted on (safari intake, val 2026-09-02). His report: a `Text` literal whose opening quote is the last thing on the line compiles clean, zero diagnostics, exit 0, and the content the author wrote is dropped. He rates it medium and the reason is the shape rather than the case: a lexer that can silently delete a delimiter, a separator or a path, which landed on a newline only because that is what somebody happened to write. **This one was never sent upstream** -- it is in no PR and no issue, unlike items 1, 2, 4 and 6 -- so nothing outside this row is tracking it. The source is `apps/safari` provenance commit `571d7d08`, `FINDINGS.md` section 9, which carries his reproducer. Reproduce before believing it and before quoting a mechanism (L-ROUTE): what is recorded here is his claim, not our measurement. |
| COMPILER-52 | **Three of Steve Howell's submissions to us are in no register at all** | Found 2026-09-02 (val) during the safari intake, by listing every PR and issue number our two backlogs already cite and diffing that against the ones his `FINDINGS.md` says he sent. Tracked already: issues 109, 110, 113, 115 and PRs 100, 101, 103, 105, 111, 112, 114, 116. **Untracked: issue 106, PR 107 and PR 108.** (1) **Issue 106**, item 6, a `Real` literal wider than an i64 is read as a different number, silently; he places it in the FRONT END, says both arms print the same wrong constant and the value reaches the emitter already a bit pattern, and cites `ZigEmitter.codex:3885` stating the wrapping accumulator in our own comment. He notes any fix is constrained because plug and seed must agree bit for bit, which is why he opened it as an issue rather than a PR. (2) **PR 108**, item 2, the Cordic docstring claims about 0.1% accuracy; his model measures 0.54% of full scale and 3.68% relative over the whole turn, and the claim does not say which reading it means. He CORRECTED his own first draft here: `math-cordic-quadrants` does assert Cordic's accuracy, so "nothing tests it" was wrong, but its tolerance is 1.5 per cent and therefore cannot catch this. (3) **PR 107**, an addition rather than a defect: `real-atan` and `real-atan2` for `Gpu chapter DeviceMath`, measured at 6.7e-16 worst error over 60 values against zig 0.16.0, on the ground that no Real arc tangent exists anywhere in the foreword. All three UNVERIFIED here. The gap this row names is the routing, not the defects: somebody must triage them. **The GitHub MCP server failed to connect in the session that filed this, so their current state on the remote was NOT checked** -- one or more may already be merged or closed, and that is the first thing to establish. |
| COMPILER-49 | **`spawn-affinity-addr` is 0 at rest where boot stores -1, so the FIRST process a program spawns is pinned to the boot processor** | Found 2026-09-02 (val) by being the first shipped caller of `process-spawn` (`docs/Designs/Active/OS/PreemptiveScheduler.md` stage 1, which has the full account). **The behaviour, measured on seed 930B322B:** a guest that prints `atomic-load 36224` before spawning anything prints 0. `process-spawn` takes the new process's affinity from that cell and then resets it to -1 (`Emit/X86_64ProcessHelpers.codex:376-380`, the `aff0`..`aff4` block; `process-spawn-priority` the same at `:485-489`), and 0 is the boot processor rather than a wildcard, so the first child of a program can only be claimed by the BSP. A parent that keeps running starves it; a parent that blocks does not, which is why every test in `codex/test` passes over this: they all `process-wait` at once. Proof of the pinning rather than of a hang: under `process-wait` the first child runs to completion with `ap-dispatch-count-addr` (36200) still at 0, so it ran, and it ran on core 0. A second child spawned afterwards is claimed by an application processor. Three children spawned with the parent spinning throughout give statuses `1 2 2` whatever their bodies are. **What is established about the cause, and what is not.** `Emit/X86_64Boot.codex:2647-2649` stores -1 into the cell inside `emit-process-setup`, which IS called (`Emit/X86_64Chapter.codex:494`), and `X86_64Boot.codex:348` states the intended contract: "holds -1 at rest, so an ordinary process-spawn pins to no core". **That block demonstrably runs and runs PAST the affinity store**: the slice table written three instructions later at `slice-table-addr` 30008 reads back 0/6/3/1 in a guest, its intended non-zero values, from `slt0`..`slt8` in the same straight line. So the emitter reaches it, and the instructions after it take effect, and the cell is still 0. Two mechanisms were named and refuted before this row was written (L-MECHANISM): the wake IPI is emitted on every spawn path (`X86_64ProcessHelpers.codex:250,383,492`), and the boot initialisation is neither missing nor unreached. **The one thing that distinguishes this store from every neighbour that works is its VALUE**: `saff1` is `li reg-rax (-1)` and every other store in the block loads a small non-negative immediate. A `li` that mis-encodes a negative immediate is therefore the next thing to test, and it is a hypothesis and not a finding: it is not obviously consistent with the reset at `aff3` writing a -1 that DOES take effect, which the second spawn running unpinned says it does. **Settle that disagreement first, because one of those two observations is being read wrong.** Seed-affecting, takes the token. The arm and its shipped caller are shelved in val's CL 21810 and stay there until this is fixed: with the spawn present and the spawn removed they print the same line, which is the arm correctly reporting that its subject never runs. **FIXED (red, 2026-09-02), and the `li` hypothesis was wrong in the way the row predicted: the file's own prose at `X86_64Boot.codex:295` names the mechanism for a neighbouring cell.** 36224 is 0x8D80, inside the PML4 page at 0x8000, and `emit-build-process-page-tables` zeroes that page (`(2 + bare-metal-total-pd-count) * 512` qwords from 0x8000); `emit-process-setup` stored the -1 twelve instructions BEFORE calling it, so the store took effect and was erased, while the slice table at 30008 (0x7538) sits below the page and survives, which is exactly the "neighbours take effect and the store does not" reading. The runtime reset at `aff3` works because it runs after the tables exist. The store now follows `emit-demand-unmap`. Measured with a probe (`atomic-load 36224` before any spawn): 0 under seed FC01D8B7, -1 under the fixed kernel, slice table 0/6/3/1 both ways; val's arm `gopweb-spawn` on four cores prints `NO AP RAN THE SERVICE` under the old seed and `an ap ran the service` with `parent still running True` under the fix. Two-pass by construction (the boot code is in the compiler's own body), stage 2 == stage 3. |
| COMPILER-1 | **Runtime scope enforcement for effect grants** | Re-homed from CAPABILITY-REFINEMENT step 6 (moved to Done 2026-08-05). Compile-time scope enforcement is shipped and pinned (`lint-effect-scope` / `scope-grants` in the type checker; `codex/test/errors/scope-violation`, `scope-network-port`). **Correction 2026-08-11 (val): the earlier claim that "the runtime half was never built" is FALSE, verified against source.** Runtime scope enforcement IS built and sound on x86-64 for FileSystem (path prefix) and Network (host:port authority), and the effect-op path enforces the SCOPE, not just the cap bit. Read path: `__fs-read-servicer` (checks `cap-filesystem-read`) -> `fat16-servicer-read` -> `fat16-read-text` -> `fat16-read-bytes` -> `fat16-resolve-path`, which calls `fat16-scope-admits` reading `process-get-scope process-get-pid` (the CALLING process's scope; the servicer runs in-context, only flipping the elevation flag). Writes are symmetric via `fat16-create-file` (`fat16-create-directory`, `fat16-list-root`, `fat16-root-entries` gated too); network via `net-scope-admits` in `http-request` / `fetch-tls`. Scope is serialized into the CDX (`manifest-cap-bytes`: cap-id, direction, scope-len, scope-bytes) and loaded into the proc table (`proc-scope-offset` fs, `proc-net-scope-offset` net) on BOTH boot (`emit-set-boot-scope`) and verified-load (`ProcessCaps.apply-load-decision`); the load-time wiring is pinned by `loader-scope-test`, `loader-network-scope-test`, `process-caps-test`, `network-scope-{open,deny,spawn}`. **Real remnants:** (1) **ARM64 has no counterpart.** The whole process/capability kernel is absent on ARM64 (no proc table, no syscalls, no fs/net execution), so scope is silently unenforced there. This is a MAJOR effort and its own initiative, not a COMPILER-1 tail; a QEMU bed now exists. Full detail and the parity chain are tracked in `codex/plugs/plugs-backlog.md` 1.10. (2) **Runtime denial IS tested end-to-end (correction 2026-08-11; the earlier "no denial test" claim here was wrong, caught by reading the tests).** `scope-runtime-deny` boots a program granted `FileSystem.Read "OK"` on a real disk and asserts the out-of-scope write (False), read (<none>) and root listing (0) are all refused while in-scope ops succeed (re-run green here, seed AF4E14D9); `scope-runtime-spawn` asserts a spawned child inherits the scope and denial (10); `scope-runtime-open` is the no-scope control; `loader-scope-test` asserts the verified-load path installs the scope. No denial-test gap remains. The only combination not in a single test is a verified-load-NARROWED scope plus a runtime out-of-scope read in one program; it is covered compositionally (same `proc-scope-offset` cell, same `fat16-scope-admits`), and a composed integration test would be real but marginal. (3) **Architecture, revisit ONLY if it becomes structurally limiting (Damian ruling 2026-08-11):** enforcement is library-level -- the servicer routes through the gated `fat16-*` functions rather than the stub checking the path itself. There is no single chokepoint (`Fat16.codex:1073-1077` warns a new disk-reaching door needs its own gate), so a future ungated door is a silent hole with no test to catch it. A syscall-boundary chokepoint would collapse the N gates to one, but is a seed-affecting change that stays redundant with gates that must remain for direct in-process callers. KEPT AS-IS: the burden is a clarity/discipline cost, not a structural break. Not blocking anything current. |

| COMPILER-7 | **Reconsider the `bounded` check's one over-refusal, after it has been in use for a while** | Damian, 2026-08-16. The `growing` inference (Cost Model 3.3 step 2, blu) scores **10 of 11** on `codex/test/cost/accumulator-corpus`, measured by ablation against the previous seed: 5 of 5 on the quadratic half, 5 of 6 on the negatives, table in `CostModel.md` section 8b. **This row said 9 of 10 until 2026-08-17, which was the score before rule 3 landed at main 16118 and took the corpus from ten entries to eleven; the count was stale, the row is not.** Still exactly one LINEAR entry refused, `n-fixed-appends`, which appends to a growing accumulator in a self call exactly four times however large the input gets and so has the shape without the cost. Ruled the cheap direction under abstain-toward-refusal (`docs/Designs/Active/Features/CostModel.md` section 5 q2, section 8) and paid in a declaration an author can decline to write. **Revisit once real chapters carry `bounded` declarations:** count how many callers the over-refusal actually bites in the tree (a refusal that never fires costs nothing; one that makes authors drop the declaration costs the guarantee). Any rule that lifts it must keep 5 of 5 on the quadratic half of the corpus, measured by the corpus and not argued. Not before the declaration has been in the tree for a cycle.  **CENSUSED 2026-09-01 (blu) and the revisit condition is UNMET, so do not open this yet.** "Once real chapters carry `bounded` declarations" -- they do not: a walk of 3,791 `.codex` files finds **72 `bounded` declarations and ALL 72 are in `codex/test/**`**, zero in `apps/`, `codex/foreword`, `codex/os`, `codex/compiler` or `codex/plugs`. So the over-refusal bites no caller today and costs nothing. Note the corpus is NOT where to look: `accumulator-corpus.codex` carries no `bounded` line at all, because it scores the INFERENCE by ablation rather than declarations. **RE-VERIFIED 2026-09-02 (blu), condition STILL UNMET, so this row stays parked.** A walk of 3,736 `.codex` files (`codex` and `apps`, excluding `build-output` and the generated `codex/build` shell scripts) finds no `bounded` DECLARATION outside `codex/test`. A declaration-shaped pattern returns ten hits outside it and **all ten are PROSE** (`punctual: no heap, no recursion, bounded instruction count` in the six `codex/foreword/punctual` chapters, `Section: Bounded Slots`, `Section: Bounded Window`, and two sentences), which is the trap this re-measure existed to catch: the word is common in prose and the declaration is not. Verdict: OPEN, correctly parked, not stale. |
| COMPILER-9 | **The ARM64 shadow rule is derived from registration on both halves; the hand list that remains is the helper-versus-arm precedence table, and it is named that** | Census 2026-08-16 (reek), born from the `peek-16` root cause: `a64-check-builtin-collisions` warned from a hand chain of 126 names that missed 85 of the 104 the runtime registers. **Both halves are now derived (root, 2026-08-17, on `//Codex/root`; copy-up after MAIN OPEN).** The CHECK reads `func-names` past `runtime-funcs` (reek, main 16081). The DISPATCH in `a64-emit-direct-call` (`Arm64CodeGen2.codex`) now says: a USER def (`a64-find-name-index (st.func-names) name runtime-funcs >= 0`) wins over every emitter arm unless the name is inlined-here, which is the x86 rule and the one `RiscVCodeGen2.codex:836` already had; a user def named like an arm the old list did not carry no longer loses silently. **What the list turned out to be, measured, so nobody deletes it next:** for names the user never defines it decides helper-versus-arm (a registered name IN it is called by name, its runtime helper; a registered name absent from it takes the arm), and deriving that half as "every registered name" put eight arm64 bed answers wrong; it is kept as `a64-helper-by-name` with that prose. **A latent defect found by the same bed and closed in the same CL:** `a64-emit-user-fn-call` was a stale twin of the generic call tail without the more-than-eight-argument stack path (`a64-push-stack-args-pre`), reachable before only for listed builtins of small arity; the derived rule sent every user def through it and `goose-encode` faulted, `heap-frame-reclaim` read 15 for 43, six more; the twin is now the one call path and the tail delegates to it. Judged by the full arm64 bed against two controls (old dispatch with head `Arm64CodeGen3`): identical FAIL set (27, all pre-existing on head; blu's lane). Left, blu's lane: derive the precedence table itself, one name at a time with the bed as judge (which of helper and arm is right for each of the 105 registered names is unmeasured), and RISC-V has no such table because it always calls the registered helper by name. **SCOPED AND RE-MEASURED 2026-08-26 (blu). THREE OF THIS ROW'S NUMBERS WERE STALE AND THE TABLE IS AN EIGHTH OF ITS APPARENT SIZE.** Registered names are **109**, not 105. **THIS FIGURE WAS PUBLISHED AS 108 EARLIER THE SAME DAY AND THAT WAS ALSO WRONG; FOUR INSTRUMENTS GAVE 83, 103, 108 AND 109.** The count is the single most error-prone thing in this row, so take the method and not just the number. 83 came from a pattern matching only `a64-record-func st<N>`. 103 is the literals in `Arm64Runtime.codex`. 108 adds the two sites that pass a VARIABLE (`a64-emit-svc-stub`, `a64-emit-process-spawn-named`, carrying `block-sector-count`, `block-read-sector`, `block-write-sector`, `process-spawn`, `process-spawn-with-heap`) but was measured over that ONE FILE. 109 adds `__opening_print`, registered at `Arm64CodeGen3.codex:1570`, which a single-file census cannot see. **So: census the whole plug, exclude `a64-record-func-with-arity` (a `\b` after `func` matches it, and it is fed `def.name` from user IR rather than runtime helpers), and separate the DYNAMIC registrations** -- `Arm64CodeGen3.codex:1774` and `:1802` register `"__handler_" & op-name`, one per handler clause in the program being compiled, so they are per-program and not part of this table's domain at all. With `__opening_print` counted the split is **16 helper / 93 arm**, not 16/92; it is absent from the hand list, so the 106-inert result below is unaffected. **THE TABLE HELD 122 ENTRIES AND 106 OF THEM WERE INERT, NOW REMOVED (main pending).** A hit sends the name to `a64-find-func-offset`, which answers -1 for anything `func-names` does not carry (`Arm64CodeGen3.codex:1920`), so an entry naming an unregistered name falls through to the arm exactly as if absent -- and the spot-checked ones already had arms. **Measured, not reasoned:** all 106 deleted, plug rebuilt, full bed run -- identical FAIL set, identical classes, identical PASS_EXPECTED, nothing fixed and nothing broken. **The 16 that decide anything** are `__buf-read-bytes`, `__buf-write-byte`, `__buf-write-bytes`, `bit-and`, `bit-or`, `bit-shl`, `bit-shru`, `bit-xor`, `char-code`, `code-to-char`, `integer-to-text`, `list-snoc`, `print-line`, `text-concat-list`, `text-replace`, `text-split`; the other 92 registered names take the arm today. **THE BAR, AND IT IS BETTER THAN THE FEAR:** the bed is `build/test-cross-batch.ps1 -Arch arm64 -Jobs 8`, **502 tests** (its own header says 363), Renode at `C:\Renode`, ~14 min a run. Baseline is **48 FAIL**, not the 27 this row carries -- 29-31 in that era on a bed ~78 tests smaller, so it is not a regression and must not be read as one. **Two full runs at different box loads (13.9 and 17.9 min, compile phase 222 s against 365 s) produced IDENTICAL FAIL sets, identical per-test classes, zero movement**, so one run adjudicates an experiment and no averaging is needed. Compare the SET, never the count: a count lets one test start failing while another starts passing. **The 48 decompose into three classes and only one of them is this row's:** **A, 20 tests, `[UNSUPPORTED]` at compile** -- `char-encode` (11), `raw-bytes-to-text` (8), `vec-empty` (1). **These are NOT a precedence question and do not belong here:** both are BUILTINS (`Types/Builtins.codex:58` and `:214`) with arms in csharp, javascript, python, wasm and zig and NONE in arm64 or riscv, and a name with neither helper nor arm cannot be routed anywhere. That is a plug builtin-coverage gap for `codex/plugs/plugs-backlog.md`. **B, 18 tests, wrong answer** -- deterministic, and the only class where a helper-versus-arm mis-route could show; `effect-smoke` printing `args=1075847184` against an expected `42` has the shape of a pointer. **C, 10 tests, silent or starved** -- timing, and stable across both baselines rather than noisy. **So the remaining work is not a 108-name sweep.** One-at-a-time over 108 is ~25 hours of bed; the 18 class-B failures are a far better entry, because the bed already names which answers are wrong and the work runs backwards from a wrong answer to a mis-route instead of flipping names and hoping. `-Filter` takes a subset. **THE DERIVATION THIS ROW ASKED FOR IS DONE, AND THE ANSWER IS THAT THE TABLE IS NOT NEEDED (main pending, 2026-08-26, blu).** It did not take 108 experiments; it took two, because the question "which of helper and arm is right for each name" has a cheaper form: route them ALL one way and see whether the bed can tell. Removing the 106 inert entries changed nothing. Then emptying the remaining 16 as well, so every registered name takes its arm, changed nothing either -- **48 against 48, zero movement in either direction, on a baseline two prior runs had shown deterministic at the set level.** **The vacuity control is what makes that a result rather than an absence:** the 16 are heavily exercised, `bit-and` at 184 uses in the test corpus and 900 in the foreword, `integer-to-text` 410, `char-code` 91 and 244, so the bed reaches them constantly and still cannot distinguish the two routings. **THE ASYMMETRY IS THE FINDING.** Routing every name to its HELPER was measured 2026-08-17 and put eight answers wrong; routing every name to its ARM costs nothing. So the arm is the safe default and the helper is the exception, which is the reverse of what a hand list of 122 names implies, and it is why the earlier "derive it as every registered name" attempt failed -- it derived the dangerous direction. RISC-V arrives at the same place from the other side, with no table because it always calls the helper. **The mechanism is kept, the data is gone:** naming a registered function there still routes it by name, which is the escape hatch for an arm that turns out to be wrong, and a name goes in when a measurement wants it. **Stated honestly: the bed skips 135 tests, so this is "no routing difference the bed can observe", not "no difference".** **What is left of this row is the 15 unexplained class-B failures**, three having been split out as COMPILER-29 (an effect operation with an argument, and NOT a precedence defect) and FIXED on both arm64 and riscv 2026-08-27; the 15 named here never included those three, so this count is unchanged by that fix. The rest of this row's history (six-def clause, list-tail, VirtioPci `poke-16`) is closed in `docs/Designs/Done/Compiler/ForewordShadows.md`. | **BASELINE RE-ESTABLISHED AND THE CLASS-B SET NARROWED, 2026-08-27 (blu).** The bed is now **515 tests** (was 502) and the baseline is **49 FAIL**, which is NOT a regression: A=20 `FAIL_COMPILE`, B=19 `FAIL_OUTPUT`, C=10 (`FAIL_RUNTIME` 4 + `FAIL_STARVED` 6). **Compare the SET, never the count** -- the bed grew by 13 between 2026-08-26 and 2026-08-27 and the count moved with it. Of the 19 class B: three are COMPILER-29 (`effect-smoke`, `handler-smoke`, `handler-dotted-discharge`), one is `recursive-eq` which is x86-64-only by construction and now carries a `.no-cross` (plugs 1.90), and **the 15 this row owns are the seven `engine-*` plus `collate-order`, `kvstore-test`, `lang-smoke`, `net-io-clock`, `qemu-virt-board`, `real-approx-equality`, `tcp-reliability`, `tier0-cyrillic-print`.** **`real-approx-equality` is now fully diagnosed and is NOT this row's kind of defect: it is an arm64 plug algorithm error, written up as plugs 1.91 with the port that closes it.** **THE SEVEN `engine-*` ARE ONE SYMPTOM AND TWO HYPOTHESES ARE ALREADY REFUTED BY MEASUREMENT, so do not spend them again.** The symptom is TOO LITTLE GEOMETRY DRAWN, never garbage: `engine-near-clip` front total 4353 against 33880, `engine-software-render` drawn 16974 against 48614, and `engine-mesh-gen` sphere coverage **0** against 18155. **The sharpest instrument in the set is `engine-mesh-gen` and it is worth reading before anything else.** Its lines 1-10 PASS, so mesh generation's counts, bounds, seam and normal relations are all correct; then every FACING test flips to no and `lit samples` answers **1315870 1315870 1315870** where x86-64 gives **11307597 14068576 14068576**. Identical where you expected different is the fingerprint (L-SUSPECT), and the value decodes: **1315870 is `#14141E`, the scenes' own sky colour**, so all three centre pixels are unpainted and the renderer drew NOTHING for a sphere whose mesh is correct. **REFUTED, both with probes graded against x86-64 as the oracle and both PASSING on arm64, so neither is the cause:** (1) negative-value conversion and integer arithmetic -- `real-to-int` at -3.5/-3.9/-0.5, `/` and `int-mod` with a negative operand, and `<` against 0.0 all agree with x86-64 exactly; (2) the vector layer -- `vec3-dot` for parallel, antiparallel, orthogonal and general operands, plus `Real` field reads out of a record and `Real` multiply, all agree. So it is not the obvious shared primitive, and the arithmetic under it is sound. **The one measurement that does not fit a single story yet, and is the next thing to explain:** `engine-near-clip` renders the SAME `mesh-plane 3000 3000` correctly at z=0.0 (straddle, 29440, heavily clipped by the camera) and at z=4000.0 (behind, 0) but wrongly at z=-3200.0 (front), so for that test the variable is the plane's POSITION and not its triangle count, while `engine-mesh-gen` loses a many-triangle sphere entirely and a two-triangle plane never fails anywhere. Whoever takes this should instrument the rasteriser's own accounting -- how many triangles are submitted and how many survive clipping and backface rejection -- rather than probe another leaf primitive, because the leaves are now measured clean. **THE SEVEN `engine-*` ARE PINNED TO ONE ARM64 CODEGEN SHAPE, 2026-08-27 (blu), AND IT IS NOT THE RASTERISER.** Instrumenting the accounting the way this row asked answered the question by CLEARING every stage. For the `engine-mesh-gen` sphere, arm64 and x86-64 agree exactly on: frustum verdict, world bounds, triangles dropped by clipping (0 of 96), fans submitted (96), fans backface culled (44), the projected screen-coordinate checksum, the projected DEPTH checksum with its min and max, bounding-box pixels scanned (57910), barycentric-covered pixels (18729), and pixels painted through `r3d-rasterize-tri` (18155). `real-to-int` at 5e5, 6e5, 1e6, 1e9 and -1e6 agrees; `mat4-sqrt` agrees; `alloc-bytes` hands back 16 consecutive 614400-byte blocks that read back correctly at both ends; and `r3d-shade-phong` returns colours whose sum over the mesh is IDENTICAL to x86-64. What fails is `r3d-render-tris-flat` itself: 18155 painted on x86-64, 0 on arm64, in a 49-line standalone program. **The reproducer is 90 lines and carries its own controls, kept at `docs/Test/Active/Arm64StackArgClobber.codex` with its x86-64 answers in the sibling `.expected`, deliberately OUTSIDE `codex/test/` so it does not enter the cross bed.** Three functions with the IDENTICAL 12-parameter signature, `half-w` and `half-h` at positions 9 and 10 so both are STACK-PASSED under AAPCS, differing only in how the fourth argument of `r3d-raster-fan-sh` is written: `(r3d-shade-ctx-flat packed)` with `packed` a `let`-bound local answers **0**; `(r3d-shade-ctx-flat #C8A05A)` with a LITERAL answers 18155; `let cx = r3d-shade-ctx-flat packed in ... cx ...` answers 18155. x86-64 answers 18155 for all three. **The shape is therefore a nested call in ARGUMENT POSITION whose own argument is a local, inside a function that has stack-passed parameters, and it is SILENT: nothing is plotted rather than plotted wrong**, which is exactly why the family symptom is too little geometry and never garbage. **Five hypotheses were refuted by measurement and must not be spent again:** parameter count alone (the same body in a 12-parameter function PASSES when `half-w` and `half-h` sit in register positions 5 and 6); program size (400 padding functions, code 110932 against the failing 52108, funcs 606 against 219, passes); heap exhaustion; stack arguments read after a call (arguments 9 through 12 return correctly across a call, including a deep one); and `r3d-ctx-with-tex`, whose `__record-set` returns the correct `sc-mode`, `sc-g0`, `sc-sh-base` and `sc-amb`. **MECHANISM PROVEN AND FIXED, same day, read out of the emitted instructions rather than inferred.** `a64-alloc-temp` (`Arm64CodeGen.codex`) rotates a pool of FOUR registers, `a64-x12 + int-mod (next-temp - a64-x12) 4`, so x12 through x15; `a64-load-stack-args-to-scratch` (`Arm64CodeGen2.codex`) staged each stack argument into `a64-x10 + slot`, so with four stack arguments slots 2 and 3 ARE pool registers x12 and x13. Decoding `sBound` gave `mov x12, x15` (slot 2 staged) immediately followed by `ldr x12, [sp, #424]` (the next rotating temp IS x12, slot 2 destroyed) and then `str x12, [sp, #16]`, so stack slot 2 was stored with slot 3's value. In the 12-parameter recursive call that slot is `i + 3` and it received `n`, so the loop stopped after one triangle. The control decodes clean: `sInline` allocates one FEWER temp, the rotation sits one place earlier, and no staged slot is ever reallocated. **That is why a `let`-bound local failed where a literal passed: materialising a literal needs no temp and materialising a local needs one, which shifts the rotation.** **The fix is two edits in the plug and touches no encoder and no seed:** a new `a64-load-local-into` beside `a64-load-local`, and the staging loop loading each stack argument STRAIGHT into its scratch register, so the loop allocates no temp at all and the overlap cannot fire. **Measured on the full arm64 bed, comparing the SET: 49 FAIL against 43, six removed and nothing newly wrong.** `engine-mesh-gen`, `engine-near-clip`, `engine-shading`, `engine-shadow` and `engine-software-render` now PASS outright. `engine-render-heap` now answers CORRECTLY (`drawn at 320x240` is 48614, was 16974) and passes at `-RenoTimeout 90`: it fails the standing bed only because painting the geometry it was always meant to paint is about three times the pixel work over sixteen frames and overruns the 10 s Renode ceiling, so it has moved from class B to class C and is a bed-budget question, not a wrong answer. **`engine-culling-cost` IS CLOSED AND IT WAS NOT THIS DEFECT AND NOT CODEGEN AT ALL (main 21138, 2026-09-01, blu); THE ENGINE GROUP IS NOW FULLY CLOSED.** It had advanced from line 5 to line 7, and line 7 printed a raw `alloc-bytes` delta as an ABSOLUTE NUMBER into a `.expected` shared by every architecture. Measured on the bed: arm64 prints **2992** where x86-64 prints **2352**, plausible and positive rather than 0 or garbage, which was the falsifier set BEFORE the run. **Three independent readings say the magnitude is the only thing that differs:** line 2 is a boolean over the SAME two deltas (`cost-far - cost-base < cost-near - cost-base`) and already passed, so the culling relation is correct on arm64; `engine-texture-cost` uses the same `alloc-bytes` instrument, prints only booleans and derived colours, and passes; and `heap-bracket-shape` and `engine-render-heap` both run cross-arch unskipped and express every allocation fact as a boolean or a derived ratio (`coverage ratio in permille`), never an absolute byte count. **The convention already existed and this was the one test breaking it, on exactly the line that failed.** Line 7 now asserts `cost delta culled > 0` in the form `heap-bracket-shape` already uses; verified x86-64 output matches `.expected` exactly and arm64 PASSES. **`.no-cross` was NOT the answer and should not be reached for when this shape reappears:** its own sidecar doc rules it out, and it would have discarded six good cross-arch assertions to hide one bad one. **NO CAUSE IS CLAIMED FOR THE 640-BYTE DIFFERENCE.** Both targets are 64-bit, so it is not self-evidently benign; alignment or closure-environment layout would explain it, and neither is measured. **`recursive-eq` also left the FAIL set and that is NOT this fix:** its `.no-cross` sidecar from main 20030 arrived in the workspace with the merge-down. **A latent limit of the same family is left deliberately unfixed (R-ONE), and THIS ROW DESCRIBED IT WRONG IN BOTH DIRECTIONS; corrected 2026-09-01 (blu) by tracing the emitter at head, NOT by a bed run and NOT by a decode, so treat the threshold below as READ AND NOT MEASURED.** The scratch base is still `a64-x10 + slot` and still unbounded (`Arm64CodeGen2.codex:1634`), so slots 6, 7 and 8 do land on x16, x17 and x18 as the old sentence said. **That is not a hazard, because the emitter never has a live value in those three at a call site.** No `a64-x16`, `a64-x17` or `a64-x18` constant exists; the sole path that allocates them is the literal `16` in `a64-alloc-local` (`Arm64CodeGen.codex:306`), reachable only when `next-local` is x12, and every FRAMED entry sets `next-local` to `a64-x19` and counts upward only (`Arm64CodeGen2.codex:1078`, `:1122`; `Arm64CodeGen.codex:215`, `:733`; `Arm64CodeGen3.codex:1798`). The one path that starts at `a64-x9` and so walks through 16, 17, 18 is frameless (`Arm64CodeGen3.codex:1400`), and `a64-can-be-frameless` (`Arm64CodeGen3.codex:1162`) caps parameters at 8 and admits no real call at all, so a frameless function never reaches `a64-push-stack-args-pre`. **Slots 9 through 17 are safe for a weaker and more fragile reason: they write x19 through x27, which hold `saved.locals[0..8]`, and those are already dead by then** -- `a64-move-args-to-regs` has moved locals[0..7] into x0-x7 before staging begins (`Arm64CodeGen2.codex:1306`, ordered ahead of `:1308`) and locals[8] is consumed at slot 0. **THE REAL CLIFF IS SLOT 18, WHICH IS x28, AND x28 IS `a64-heap-reg` (`Arm64CodeGen.codex:38`, 46 uses across the plug): the globally live heap pointer.** Slot 19 is the frame pointer, slot 20 the link register, slot 21 the SP/XZR encoding. **So the threshold is 27 parameters and not fifteen, and what breaks at it is a corrupted heap base rather than a scratch collision**, which is a worse failure than the one this row advertised and a much deeper one to reach. **The fix is also not the one the old sentence implies.** Widening or relocating the staging bank only moves the cliff; the shape that removes it is to emit the `sub sp` FIRST and then load-and-store each stack argument through a SINGLE scratch register, which requires `a64-load-local-into` to take the stack adjustment in its spilled-local case, since that case addresses off `sp` (`Arm64CodeGen.codex:678`). **Also noted while tracing, and worth deleting when someone is next in this file: `a64-preload-stack-args`, `a64-store-preloaded-stack-args` and `a64-push-stack-args` (`Arm64CodeGen2.codex:1610`, `:1618`, `:1643`) have no callers at head, and `a64-preload-stack-args` stages through `a64-alloc-temp`, which is precisely the rotation bug this row proved and fixed** -- dead code that still embodies the defect is how it comes back. **Nothing known reaches 27 parameters today, so this stays latent; whoever takes it must build the reproducer and decode it before changing an instruction, because everything in this paragraph is a reading.** **WHAT ACTUALLY BLOCKS THE SEVEN REMAINING CLASS-B FAILURES, 2026-09-01 (blu): this row records THAT they fail and never WHAT THEY PRINT.** Diagnosis runs backwards from a wrong answer, and that input does not exist for any of them, which is why each one has historically cost a bed run of its own. **One filtered batch over just those seven captures every wrong output at once and unblocks all of them; do that before opening any single one.** **Free triage already done, so do not repeat it:** all seven `.expected` files were read, and NONE prints a raw allocation count -- `collate-order`, `kvstore-test`, `lang-smoke`, `net-io-clock`, `qemu-virt-board`, `tcp-reliability` and `tier0-cyrillic-print` emit only architecture-independent logic, text and protocol values. **So the `engine-culling-cost` shape does not generalise and none of the seven can be dismissed that way.** **Two of them carry falsifiable PREDICTIONS from reading alone, recorded as predictions and NOT as findings, each answered by output that batch already captures.** (1) `qemu-virt-board` writes to the live UART on the board it runs on: `qemu-a64-uart-putc 65` stores `A` to `#09000000`, which on the arm64 `virt` board IS the PL011 data register, and `test-a64-uart-linear` writes `H` and `I` to `qemu-a64-pl011-dr`. On x86-64 those stores land nowhere and only the sum `8` appears; on arm64 they inject characters into the very stream the harness compares. Predicted actual: `8` polluted with stray `A`/`H`/`I`. **Falsifier: a bare integer other than 8, which would make it a real defect in one of the eight sub-checks.** If it holds it is architecture-INAPPROPRIATE rather than miscompiled, and the class-B set is not homogeneous. (2) `tier0-cyrillic-print` runs all three lines through `code-to-char` and `char-to-text` out via `print-line-uni`; line 3 is pure ASCII and lines 1-2 are Cyrillic and accented Latin. Predicted split: line 3 matches, lines 1-2 differ, localising the defect to the multi-byte CCE-to-UTF-8 encoder at the I/O boundary. **Falsifier: line 3 also differs, or all three match.** **Routing is already ruled out for it by a measurement in this row** -- `code-to-char` and `char-code` are both among the 16, and emptying all 16 moved nothing, 48 against 48 -- so do not spend a run re-testing that. **ALL SEVEN NOW MEASURED IN ONE PASS, 2026-09-01 (blu), AND THE SEVEN ARE SIX DISTINCT CAUSES OF WHICH ONE IS NOT CODEGEN.** `-Filter` is a single `-like "*x*"` substring and cannot select seven unrelated names, so this was seven sequential `build/test-cross.ps1 -Arch arm64 -Test <name>` runs, ~5 min total; that script is the better instrument anyway because it prints every differing line as `exp=[..] act=[..]` and writes a per-test `diff.txt`. **BOTH PREDICTIONS ABOVE CONFIRMED EXACTLY, so the two paragraphs above stand as written.** (1) `qemu-virt-board` actual is **`AHI8`**: the sum is **8 and CORRECT**, all eight sub-checks pass, and the only defect is that the test writes `A`, `H` and `I` into the live PL011 it is itself running on. **IT IS NOT CODEGEN AND NOT THIS ROW; it is architecture-inappropriate and belongs to whoever owns the board tests.** The RISC-V `putc` to `#10000000` emits nothing on the arm64 `virt` board, as expected. (2) `tier0-cyrillic-print`: line 3 (ASCII) matches, lines 1-2 differ, exactly as predicted. **MECHANISM PROVEN, NOT INFERRED: arm64 emits `codepoint & 0xFF` instead of the multi-byte UTF-8 sequence.** Expected `U+0430 U+043E U+0435 U+0438 U+043D U+0442 U+0441 U+0440 U+0432 U+043B U+043A U+043C U+0434 U+043F U+0443`; actual is the ASCII run `0>58=BA@2;:<4?C`, whose bytes are `0x30 0x3E 0x35 0x38 0x3D 0x42 0x41 0x40 0x32 0x3B 0x3A 0x3C 0x34 0x3F 0x43` -- **the low byte of each expected code point, 15 of 15, checked by arithmetic rather than by eye.** Line 2 renders as replacement characters because a lone `0xE9` is invalid UTF-8, which is the same defect. The fix is in the CCE-to-UTF-8 encoder at the I/O boundary, not the emitter. **(3) `lang-smoke` IS ONE LINE AND THE HARNESS WAS HIDING IT.** It reported `FAIL (output mismatch)` with NO diff lines, because `test-cross.ps1` caps its diff report at `[Math]::Min($maxLines, 20)` and the only difference is at **line 21**. Diffing `runtime.actual` against `.expected` in full: `exp=[False] act=[True]`, and line 21 is `show (Circle 5 == Circle 7)`. **Line 20 (`Circle 5 == Circle 5`, True) and line 22 (`Circle 5 == Rect 3 4`, False) are both CORRECT, so structural equality on a tagged union compares the TAG and ignores the PAYLOAD on arm64.** **That 20-line cap is a real harness defect and it silently turns a one-line failure into an inscrutable one; it is a separate unit (the script is generated, so the generator under `codex/build/` and the script land together per `docs/Designs/Active/Build/Build.md`), left undone here under R-ONE.** **(4) `net-io-clock` AND `tcp-reliability` ARE ONE CAUSE, NOT TWO, and the arithmetic proves the link rather than suggesting it.** `net-io-clock` wants `unmeasured-ladder=141` and `clamped-ladder=288` and gets **-1 for both**; `idle-before-tick=100000` MATCHES; and `polls-to-giveup` is exactly the product, `288 * 100000 = 28800000` expected against `-1 * 100000 = -100000` actual. **The ladder is the only broken input.** `tcp-reliability` then follows: its `giveup141` IS that 141, so with the ladder at -1 the giveup never fires (`exp=[giveup141=CLOSED] act=[giveup141=ESTABLISHED]`) and every retransmit and backoff counter reads 0 for 1 (`rexmit3`, `backoff6`, `synrexmit`, `finrexmit`, `qrexmit`, and `qoldest=0` for 5001). **Fix the ladder and both tests are answered; do not open them separately.** **(5) `collate-order`: the comparators return the RAW DIFFERENCE instead of a normalised -1/0/1 sign.** `a/b` wants `-1|-1` and gives `-1|-17`, `t/a` wants `1|-1` and gives `19|-1`, `app/apple` wants `-1|-1` and gives `-57|-2`. Both sides of the pair are affected, and `apple/apple=0|0` is correct, so zero is right and only the sign normalisation is missing. **(6) `kvstore-test`: `size` answers Int64 MAX.** `9223372036854775807` where 2 and 3 are wanted and `9223372036854775806` where 1 is; every non-size field on those same lines (`name`, `version`, `x`, `has-a`, `has-b`, `b`) is CORRECT, so it is the size computation alone and not the store. **What this row still owns is therefore five causes, not seven tests**, and `qemu-virt-board` should be re-homed rather than counted here.
| COMPILER-11 | **`noalias-note` has a positive arm and no negative arm, so "proven unaliased" and "nobody asked" are one output; DECIDED: no note at the not-proven arm** | From PR 66 finding 10 (plugs-backlog 1.27; confirmed reek 16195; red routed to root 2026-08-17). `emit-record-set-builtin` (`Emit/X86_64Builtins.codex:863`) and the field-store site (`X86_64.codex:1222`) call `noalias-note` (`X86_64Compound.codex:1810`), which raises the INFO `cdx-noalias-proven` (CDX4011) only when the base is a unique linear owner (`noalias-slots`, from `def.unique-params`) and returns the state untouched otherwise; `fc-store-barrier` raises the same code for a store through a fresh allocation. **Measured on the corpus, 2026-08-17 (root):** compiling the whole compiler (`build/output/quine-check/compiler.ir.log`, the gate's own log) raises CDX4011 ZERO times: the compiler has no linear-parameter record updates, so the positive arm never speaks on the largest program we have. A note at the not-proven arm would fire at every ordinary in-place store: 401 `__record-set` sites in `codex/compiler` (415 in the compiler's IR, plus 69 field stores), 2,729 more in `apps/`, 11 in `codex/test`. Every one of those is the documented concession (`DevelopersGuide.md`, "`__record-set` is not a functional update"; `mutable` records are unique-owned by the checker, CDX2062), written knowingly, so an info there would be an instrument that says the same thing at every site and is read by nobody (L-FALSIF): 400-plus lines in every compiler compile log. **Decision: the not-proven case gets NO note.** What the finding actually asks for is that the ABSENCE of CDX4011 must not be read as a safety claim, and that belongs in the code's own description: `CdxCodes.codex:351` should say "absence means not proven, not unsafe"; a description string is compiled into the seed, so it is folded into the next seed-affecting CL that touches `CdxCodes.codex` rather than spending a seed cycle on a sentence. **What would change the decision:** a consumer. If the IndependentRechecker or an audit ever wants a per-site verdict, emit the negative arm under `wire-trace` only, at `__record-set` sites whose base is neither fresh, nor linear, nor a `mutable` record, which is the only class where the concession is load-bearing. Not seed-affecting as decided. |
| COMPILER-12 | **COMPILER-SIDE HALF CLOSED (blu, 2026-09-01): the two front doors were reconciled at main 20176 and this row and `OperatorsManual.md` both went on asserting the divergence afterwards. What is left is the PLUGS half.** `lift-lambdas` runs on the CDX path only, so the plug wire carries lambdas that close over enclosing locals, and the wire format says nothing about it** | PR 66 finding 6 (plugs-backlog 1.27; verified root 2026-08-17 against source, routed here when plugs 1.17 closed). `codex/compiler/IR/LambdaLifting.codex:18` `lift-lambdas` has exactly ONE caller, `opening.codex:833`, inside the `cdx-chapter` path (its own LIFT phase and deck budget); `emit-ir-cce` (`opening.codex:1694`) emits `fe.ir` from `compile-frontend-ir` after `ir-prune-unreachable-roots` and never lifts. So every plug either implements closure capture itself or mis-emits a body silently, and Steve's zig emitter paid for a free-variable analysis with a shadowing rule that lifted IR makes unnecessary (the pass rewrites the use site as a partial application, which that plug already handled). Two closes, and the first is the one to take: (a) lift on the wire path too, one call to `lift-lambdas` in `emit-ir-cce` before the prune (or after, measure which; the pass has a deck ceiling argument to thread), seed-affecting, token; its runner is a plug-side arm: `test-input/lambda.codex` through a plug with NO closure support of its own must transpile a lambda over an enclosing local as a call to a lifted def, and `plug-oracle-test.ps1` must stay green on the plugs that already handle closures; (b) declare the contract in the wire format so a plug can refuse (`!UNSUPPORTED`), which is the honest fallback if (a) costs a deck the wire path does not have. **DECIDED 2026-08-17 (root, red's routing): (b), DECLARE; the wire does not lift.** Measured: the wire for `test-input/lambda.codex` carries `(def "make-adder" ... (lambda (params (param "x" int-default)) (binary add-int (name "x") (name "n"))))`, the captured `n` a bare `name` with no marker, and the wire path is LOWER plus the named passes only (no RESOLVE, no LIFT: those are `compile-frontend-cdx` phases, `opening.codex:819-841`). Census of the 55 plugs: 52 handle `IrLambda`; six carry their own capture/lifting machinery (`arm64`, `riscv`, `zig`, `t3isa`, `pascal`, `python`) and the rest lean on the target's own closures, which is correct for every closure-native target and is where the plugs that emit today actually are. Lifting on the wire path would hand every plug lifted `__lambda_N` defs and rewrite every capture site into a PARTIAL application, so it trades a burden six plugs already carry for a partial-application burden on all 55, and a plug that handles closures but not under-application would go from right to silently wrong (L-CAPABILITY-LOST shape); nothing measured asks for it. So the contract is declared where plug authors read, `DevelopersRulebook.md` codex.plugs ("What the wire carries"), and a plug for a closure-less target lifts or captures itself, as the six do. Re-open (a) only if a plug author measures a case the declaration cannot serve. Two adjacent findings from the same measurement, each its own row: `typescript` collapses the curried over-application `(make-adder 10) 32` to `make_adder(10, 32)` while `python` and `rust` emit `make_adder(10)(32)` (plugs-backlog 1.35); and the directly-applied inline lambda `(\x -> x + 100) 5` reaches the wire with `(param "x" error)`, a type the checker never resolved (COMPILER-13). **THIS ROW IS STALE AS OF MAIN 19558 AND ITS THREE CENTRAL FACTS ARE NOW FALSE (blu, 2026-08-27, verified against source and by `p4 annotate`).** `lift-lambdas` has TWO callers, not one: the `cdx-chapter` call (CL 4454) and **`opening.codex:1717` inside `emit-ir-cce`, landed at main 19558 by fester on 2026-08-25** to fix a wasm value-position-lambda defect (plugs 1.70). So close (a) was taken after all, the decision recorded above as "(b), DECLARE; the wire does not lift" no longer describes the compiler, and **the wire DOES lift**. **The casualty this row predicted in those exact words -- "a plug that handles closures but not under-application would go from right to silently wrong" -- was COMPILER-29, found the next day and FIXED 2026-08-27 by teaching both native plugs to follow the lifted clause; that row is closed and deleted.** arm64 is on this row's own list of six plugs carrying their own capture machinery; it implements effects by stripping `resume` syntactically from an inline clause lambda, lifting turns that clause into a partial application of `__lam_N` over its captures, the strip no longer matches, and the handler installed in the effect-op table is a closure builder. **`emit-ir-uni` (`opening.codex:1683`) still does NOT lift, so `-IrUni` and `-IrCce` now carry DIFFERENT IR for the same program** -- which is why COMPILER-29's first two recorded causes were both wrong: they were read off `-IrUni`, and the plug consumes `-IrCce`. That trap is in `docs/OperatorsManual.md`. Whoever picks this up decides whether the two front doors are reconciled (lift in both, or neither) rather than left divergent, and whether the other five self-capturing plugs are checked for COMPILER-29's shape.  **CLOSURE, MEASURED 2026-09-01 (blu), seed 42ACED00.** Main 20176 (2026-08-27 11:28, blu's own lane) reconciled the front doors: both now call ONE helper, `lift-ir-for-emit` (`opening.codex:1799`), from `emit-ir-uni` at `:1815` and `emit-ir-cce` at `:1840`, so the decision this row asked for ("lift in both, or neither") was taken as LIFT IN BOTH and jonquil keeps `no-lift` as the deliberate exception. **The three cited line numbers (1683, 1694, 1717) are stale and the sentence "`emit-ir-uni` still does NOT lift" is FALSE at head.** Measured with a control on both doors (`-RawFlags 'no-lift'`, jonquil's flag): a lambda capturing an enclosing local emits `(def "__lam_0" ...)` as a top-level def under `-IrUni`, and disabling lifting moves BOTH -- `-IrUni` 8 defs against 6, `-IrCce` 2,149 bytes against 1,969. **The row was corrected on 2026-08-27 to say the doors diverge, and the fix landed the SAME DAY from the same lane, and neither this row nor the OperatorsManual entry was updated** -- so a warning telling readers not to trust `-IrUni` outlived its cause by five days. `OperatorsManual.md` corrected at main 21409. **WHAT REMAINS AND IT IS NOT COMPILER WORK:** whether the other five self-capturing plugs carry COMPILER-29's shape (a handler clause whose `resume` strip no longer matches once lifting turns the clause into a partial application). That is a plugs question and belongs in `codex/plugs/plugs-backlog.md`, not here. |
| COMPILER-13 | **No arm would catch the multi-parameter lambda defect coming back** | The defect itself is CLOSED: root did the single-argument case (main 16326) and blu the nested-apply case (main 16374, seed 1E6BED13), so `((\x y -> x + y) 1) 2` now types every parameter instead of only its first. The account is CL 16371. **What is left is that nothing guards it.** Measured 2026-08-17: the OLD compiler ran all three shapes correctly, 105, 3 and 6, exactly as the new one does. The defect was never visible in x86-64 BEHAVIOUR at all; it lived only in the IR type annotations that cross the plug wire, where a parameter read `error`. So no x86 test can pin it and a behavioural test would be one pretending to guard something it cannot, which is why none was added. **The arm that would work is plug-level** -- emit the same source through a plug under both compilers and diff -- and it is UNMEASURED. Same shape `check-plug-types` records for its own subject: x86-64 never crosses this wire, so no x86 test will catch it. **Measured 2026-08-17 (blu), depot seed 1E6BED13: the wire is CLEAN for all three shapes.** `codex/plugs/test-input/lambda.codex` through `compile.ps1 -IrUni -Passes 'text-plug'` carries `(param "x" int-default)` at every lambda, including the directly-applied inline `(\x -> x + 100) 5` that COMPILER-12's row recorded as reaching the wire with `(param "x" error)`; that adjacent defect is closed by 16374 too, and COMPILER-12's row should not be read as still open on it. **The guard is still absent, and the diff-against-the-old-compiler arm the row asks for cannot be the guard: it expires the moment the old seed is replaced, which has already happened.** A durable arm is behavioural and belongs where the wire is already run rather than inspected -- `plug-oracle-arith.codex` has no lambda section, and `test-input/lambda.codex` is run by NOTHING automated (its only use in the tree is one manual measurement, plugs-backlog 605). **GUARD LANDED 2026-08-18 (blu): `plug-oracle-arith` has a Lambdas section and it is behavioural, so this row's gap is closed.** Seven rows: `lam-one` (single-parameter inline apply), `lam-two` (`((\x y -> x + y) a) b`, the exact shape of this defect), `lam-three` (catches a plug that types the first two parameters and not the third), `lam-mixed`, and `lam-capture` / `lam-capture-two`. **`lam-mixed`'s second parameter is a `Boolean` deliberately, and that choice is the whole point of the row:** with every parameter typed `Integer` a wrong or missing annotation on any parameter but the first still runs correctly, which is exactly how this class stayed invisible, so a same-typed row would be a guard pretending to guard. The capture rows exist because COMPILER-12 DECIDED the wire does not lift, which makes capture the plug's job and made it contract that nothing exercised. x86-64 truth measured, not predicted: 105, 3, 14, 7, -7, 15, 300; sidecar at 40 lines; gate green (`check-plug-builtins` and `check-plug-types` both clean, `SUT === stage1`, seed unchanged). **The census on its first run, all six wired plugs built and none skipped or stale: javascript and typescript PASS 40 of 40, and FOUR plugs fail, each a distinct defect nothing could see before because no lambda had ever been RUN through them.** Two passing is what says the subject is sound rather than impossible. `python` emits an N-ary lambda and applies it curried (`(lambda x, y: (x + y))(a)(b)`, `TypeError`), hitting all four multi-parameter rows and neither single-parameter one. `wasm` emits the lambda body inline referencing `local.get $x` for locals it never declares, so the WAT will not assemble; wasm has no native closures, so that plug must lift and does not. `csharp` raises CS0149 at six sites. `zig` captures `const k = 10` as `@TypeOf(k)`, which is `comptime_int`, so the env struct is comptime-only and has no runtime size. **Read the counts, not the line totals: the harness merges stderr, so a traceback shifts every later line and python reports 74 differing lines for one crash.** Routing: python, wasm and csharp are the plugs lane; **`codex/plugs/zig/**` is Steve Howell's and not the fleet's to change**, so that one is red's to route upstream. `plug-oracle-test.ps1` is standalone and NOT in the standing gate, so these four do not make main red.  **CLOSED (blu, 2026-09-01): the guard is landed and verified present.** `codex/test/plug-oracle-arith.codex` has its `Section: Lambdas` at :148 with `lam-one`, `lam-two`, `lam-three`, `lam-mixed` and the two capture rows, so this row's own subject -- that nothing guarded the defect -- is discharged. The four failing plugs are NOT this row: python, wasm and csharp are the plugs lane and `codex/plugs/zig/**` is Steve Howell's, as the routing below already says. **One thing worth a reader's eye: `plug-oracle-test.ps1` is still in no gate, so the guard exists and nothing runs it (L-NOGATE).** Wiring it in would turn main red on four known plug defects, so it is a plugs-lane sequencing question, not a unilateral one. |
| COMPILER-14 | **`emit-record` had two record layouts and every reader used only the first; the second is now a refusal** | From Steve Howell, PR 67 (`gh pr view 67 -R damiant3/NewRepository`), routed by red 2026-08-17. `emit-record` (`Emit/X86_64Compound.codex:1751`) laid a record literal out by `build-cce-byte-offsets` (width-prefixed sort key, cumulative widths) when `resolve-constructed-ty` answered a `RecordTy`, and otherwise by `emit-store-record-fields-by-list` (name-sorted rank times 8); `emit-field-access` and `emit-record-set-builtin` read by the first rule unconditionally, so a record built through the fallback with any field narrower than eight bytes was read at the wrong offsets (PR 67 probe: `Box = record { flag : Boolean, items : List Integer }`, `b.items` reading the boolean). Steve could not reach the fallback through `opening.codex`; his harness reached it only by skipping RESOLVE. **Root, 2026-08-17: the fallback is replaced by the refusal its neighbour `emit-field-access` already makes** (`st-add-error cdx-ir-error "emit-record: unresolved type for record literal"` and `ud2`), and `emit-store-record-fields-by-list`, `field-local-names`, `RecordLayoutPair` are deleted. Reachability was measured by that refusal under the gate: the compiler self-compile and the BVT never take it (a taken branch is a red gate). Not measured beyond the gate: the full battery and the apps; a red there with this message means the branch IS reachable and the close becomes "lay out by `cce-byte-offset-and-type` so the two rules cannot drift", not a restore of the by-list layout.  **THE APPS HALF IS STILL NOT MEASURED, and a green `-Internal` does NOT discharge it (blu, 2026-09-01).** `-Internal`'s `app-sweep` passes `-Sample 30`, so it sweeps a strided **30 of the 270** entry chapters; only the release gate sweeps all of them (`build/build.ps1` above `Measure-Phase 'app-sweep'`). Reading a green internal gate as "the apps are covered" overstates it ninefold (L-DENOM: the cap is not the corpus). So this row still waits on a release gate or a battery, neither of which is an agent command. **RE-VERIFIED 2026-09-02 (blu): the gating claim is exact and the row is correctly parked.** `build/build.ps1:1119` reads `if ($Internal) { $sweepArgs = @('-Sample', '30') }`, so the internal gate really does sweep a strided sample and only a release gate or a battery sweeps every entry chapter. Both are outside what an agent may run, so nothing in this lane can discharge this row. Verdict: OPEN, blocked on a non-agent action, not stale. |
| COMPILER-16 | **The compiler's own self-compile emits 21 warnings that nothing gates and no retained log carries, and one of them was a live miscompilation for as long as it has been there** | Found 2026-08-18 (blu) while investigating issue 70's supposed CDX2064 blind spot. **The premise was half wrong and the correction is the finding.** CDX2064 DID fire on `X86_64Boot.codex:3216`, correctly, on every build: measured by compiling the pre-fix concatenated source (`build/output-20260818-012801/Codex.codex`, retained by chance) and capturing diagnostics, which gives `:22326:39: warning CDX2064: Field 'code-len' is read from 'st7' ...`, and line 22326 of that file is exactly `in let st9 = patch-jcc-at st8 (st7.code-len) loop-top`. The checker was right and had been saying so all along; nobody read it. **A warning nobody reads is not a guard** (L-BODY: if a thing is gated only by prose or by a human noticing, it is not gated). Two separate gaps follow. **(1) VISIBILITY, and it needs a ruling: nothing gates compiler warnings and `build/output` retains no diagnostic log for the self-compile**, only `canary-compile.log` and `cdx-sign.log`. The 21 are 13 x CDX6020, 7 x CDX3005, 1 x CDX2064. Whether CDX2064 should fail the build, or the log should merely be retained, is Damian's call and is in the rulings queue. **(2) WAS "A GENUINE BLIND SPOT". IT IS CLOSED, AND THIS ROW WAS THE STALE HALF (measured 2026-09-01, blu, depot seed 278D8D7F).** The row said `scan-stale`''s `AApplyExpr` arm walks both arguments with the SAME consumed set. It does not. It computes `arg-mutations summary f` and adds that to the consumed set before scanning the argument (`Types/TypeChecker.codex:3761-3764`), and since application is curried, that IS the left-to-right threading this row asked for. `arg-mutations` (`:3738`) walks the spine of the already-evaluated part, and the prose at `:3730` records the very 1111-against-1101 evaluation-order measurement this row cites as the reason the extension WOULD be sound. **`staleprobe.codex` exists nowhere in the tree**, so the row''s named instrument was scratch that never landed, which is exactly why nothing ever re-ran it and the row could go stale unobserved (L-NOGATE, and the COMPILER-28 shape one row over: a done fix left reading as open work). Re-measured with five arms, every state distinguishable. WARNS: the 2-argument sibling `two (addn ss 10) (ss.n)`; the 3-argument `three (addn s3 10) (s3.n) 5`, which is this row''s own `patch-jcc-at` shape with the read in the second of three; and the let-bound shape, the positive control proving the checker ran at all. SILENT: the hoisted `let c = sh.n in two (addn sh 10) c` this row requires stay quiet, and `flip2 (sr.n) (addn sr 10)`, a read placed BEFORE the mutation, which must not warn and does not. **SCOPE, stated rather than left to be assumed:** this measures the MECHANISM on synthetic arms. Whether all eight real sibling sites in `X86_64Boot` now warn is NOT measured and cannot be from here, because the self-compile retains no diagnostic log, which is part (1) of this very row. Do not read this as "the eight are covered". **UNAUDITED SURFACE, do not read this row as "one defect found and fixed":** the 13 CDX6020 (`mutation in constructor`) sites in the compiler are the SAME mutate-in-place family and each needs per-site judgement, not a sweep. Several look benign because the field read is an argument evaluated before the mutation applies (`13777`), and at least one reads a DIFFERENT binding than it mutates (`13846`: `state = __record-set st1 "load-local-toggle" (st.load-local-toggle + 1)` reads `st` while mutating `st1`). None of them has been judged. **ALL 13 ARE NOW JUDGED AND ALL 13 ARE BENIGN, 2026-09-01 (blu), depot seed D3A0C75A, and they are benign for ONE shared reason rather than thirteen.** The list was taken from the compiler itself rather than a pattern: compiling the retained concat with `-Log` gives exactly 13 CDX6020, matching this row. **THE SHARED INVARIANT: every read of the mutated record happens strictly BEFORE the mutation applies, and no sibling field reads the mutated record at all.** The read is either `__record-set`'s own value argument, which is evaluated before the set (`alloc-temp` X86_64State.codex:896, `alloc-local` :930, both `emit-*-call` sites X86_64Compound.codex:202 and :219, the induction warning TypeCheckerInference.codex:1721), or a `let` computed before the literal is entered (`slot` at X86_64State.codex:925, `scratch` at :959, `nl` at :918). The sibling field is in every case a local, a constant, or a field of a DIFFERENT record: `reg = __narrow reg`, `reg = reg-rax`, `reg = r.reg` where `r` is an `EmitResult` and the mutated value a `CodegenState`, and in the one non-`state` site (`attach-deriving`, Syntax--Parser) `state = st-deriv`, a `ParseState` unrelated to the `td` being mutated. **THIS ROW'S OWN SAFETY ARGUMENT FOR ITS CITED EXAMPLE IS WRONG AND SHOULD NOT BE REUSED.** It offers `state = __record-set st1 "load-local-toggle" (st.load-local-toggle + 1)` (now X86_64State.codex:965) as safe because it "reads a DIFFERENT binding than it mutates". That is not established: `st1` is `st-append-code st (...)` from the line above, and if `st-append-code` returns `st` mutated in place then `st1` IS `st` and the bindings alias. **It is safe because the read precedes the write, which holds whether or not they alias.** Judging these by "different binding" would pass a genuinely unsafe site the moment the aliasing went the other way. **THE CONSEQUENCE FOR PART (1)'S RULING, WHICH IS THE USEFUL HALF: CDX6020 IS A SHAPE DETECTOR, NOT A HAZARD DETECTOR.** `check-mutation-in-ctor-fields` (`Types/TypeCheckerInference.codex:1724`) fires when a constructor field's value MENTIONS `__record-set` anywhere, via the syntactic walk `aexpr-mentions-record-set`; it does not ask whether any sibling reads the mutated record. On the current corpus that is a **13 of 13 false-positive rate**, so making CDX6020 fail the build would fail it on thirteen correct sites and nothing else. **What actually keeps these correct is EVALUATION ORDER, and nothing gates order:** move `scratch`'s computation at X86_64State.codex:959 below the literal and site :965 breaks silently while CDX6020 emits the identical text it emits today. If this check is to be worth gating it has to compare the mutated record against what the sibling fields read; as written it cannot tell the safe thirteen from an unsafe one. **Re-measured while here: the warning total is 20, not the 21 this row states -- 13 CDX6020 + 7 CDX3005 + ZERO CDX2064**, the CDX2064 having been fixed since. The 7 CDX3005 are untouched and outside this row's ask. **Stated as scope: these are verdicts from reading the sites and their preceding lets, not from a runtime probe.**  **CDX6020 IS NOW A HAZARD DETECTOR, MEASURED (blu, 2026-09-01).** This row's own conclusion -- that the check "has to compare the mutated record against what the sibling fields read" -- is implemented. `check-mutation-in-ctor-fields` now extracts the mutated record's base name and warns only when a LATER field reads it, the write-after-read order being the property the safe sites actually rely on. **The self-compile goes 13 CDX6020 to 1, with CDX3005 unmoved at 7 as the control and zero errors.** The surviving one is `__record-set (restore-locals (r.state) saved-locals) "range-env" saved-range` (`X86_64Compound`), where the mutated record is a COMPUTED value rather than a named binding, so the base cannot be determined and the check keeps today's behaviour and warns -- the fallback is deliberately the conservative direction, and a value carrying more than one `__record-set` warns for the same reason, since picking one target would be a false NEGATIVE. **THE CONTROL IS WHAT MAKES THIS A RESULT: `docs/Test/Active/Cdx6020Hazard.codex` has a hazard arm (field `a` mutates, later field `b` reads) and a safe arm (read precedes write), and the old compiler warns on BOTH while the new one warns only on the hazard.** A change that silenced both would have made the check vacuous and passed unnoticed. **STILL NOT GATEABLE AND NOT GATED, and the reason is not the false positive:** there is no mechanism in this tree to pin a WARNING -- `codex/test/errors` holds no `.expected` files and no `.expected` anywhere declares a warning -- so this behaviour is measured and unguarded (L-NOGATE), which is also why the probe lives in `docs/Test/Active/` rather than `codex/test`. The 7 CDX3005 remain untouched and outside this row's ask. |
| COMPILER-17 | **The boot capability grant is emitted as a sign-extended imm32, so a capability at bit 31 would grant bits 31-63 and any bit at or above 32 is dropped silently** | Found 2026-08-18 (root) while scoping the HAL's next capability rows (I2C, ADC, Power would take bits 30, 31, 32: `cap-spi` is 29 and the table has no free bit below it). `emit-grant-cap-mask` (`Emit/X86_64Boot.codex:2506`) and `emit-grant-capability` (`:2514`) OR the mask into proc 0's cap word with `or-ri reg-rax mask`, and `alu-ri` (`X86_64Encoder.codex`) encodes the immediate as `write-i32` under REX.W, which the CPU sign-extends: a mask with bit 31 set becomes `FFFFFFFF80000000` (every capability from 31 up granted, L-CAPABILITY-LOST shape: the aggregate reads as "granted", nothing says too much was), and a mask whose only bit is 32 or higher writes its low 32 bits, zero, so the capability is never granted and every use of it is refused at runtime. Bit 30 (`I2c`) is the last safe one under the current emission. LATENT today: `cap-spi` 29 is the highest bit in the table, `capability-doors` proves the table and not the grant, and nothing in the tree declares a bit above 29. **The fix is small and seed-affecting:** load the mask with `li` (mov r64, imm64) into a scratch register and `or-rr`, in both emitters, plus the ARM64/RISC-V boot grants (`Arm64Runtime`/`RiscVRuntime` equivalents, unmeasured) -- and it is untestable until a capability sits at bit 31 or above, so it lands WITH the first such row (`Adc` at 31, `Power` at 32) and a runtime arm that reads proc 0's cap word after boot and shows bit 31 grants only bit 31 and bit 32 grants at all. **Sidestepped 2026-08-18 (root): I2c, Adc and Power took the table's unassigned bits 1, 2 and 13, so nothing sits above 29 and no emitter changed; the row stays LATENT for the next capability, which has no gap left below 30 (bit 30 is the last safe one).** The audit is wider than the two grant emitters: `emit-check-capability` and every `and-ri reg (shl-1 cap-bit)` check site share the imm32 shape, `ProcessCaps.codex read-process-grant` reads the word with `peek-32` (bit 32 and up invisible to the loader), and the RISC-V `rv-emit-or-into-cell` loads the mask into t0 with `rv-li`, whose own scratch for a #8xxxxxxx immediate is t0. Owner: root (HAL) when the next row needs it. **THE MARGIN IS GONE, MEASURED 2026-09-02 (blu), AND THIS ROW DOES NOT KNOW IT.** The text above says `cap-spi` 29 is the highest bit in the table, that nothing declares a bit above 29, and that bit 30 is the last safe one. At head `codex/foreword/core/Capability.codex` declares **`cap-rng : Integer = 30`**, and every bit from 0 to 30 inclusive is now assigned: 0 console, 1 i2c, 2 adc, 3 concurrent, 4/5 console read/write, 6/7 filesystem read/write, 8/9 network read/write, 10 block-device, 11 ipc, 12 process-create, 13 power, 14 capability-admin, 15 identity, 16 device, 17/18 gpu compute/memory, 19 camera, 20 microphone, 21 location, 22 sensors, 23 display, 24 flash, 25 audio, 26 process, 27 gpio, 28 uart, 29 spi, 30 rng. **So there is no free bit below 31 and the NEXT capability added lands on bit 31, which is exactly the sign-extended case this row describes: `or-ri` under REX.W writes the mask with `write-i32`, the CPU sign-extends, and a bit-31 mask becomes `FFFFFFFF80000000`, granting every capability from 31 up while the aggregate still reads as "granted" (L-CAPABILITY-LOST).** The row is therefore no longer latent-with-headroom; it is one capability away from firing, and the sidestep it records (I2c, Adc and Power taking bits 1, 2 and 13) cannot be repeated because there is nothing left to take. A first attempt at this census UNDERCOUNTED and briefly suggested bits 4 to 18 were free: a pattern anchored on `cap-<name> : Integer = N` at line start missed the read/write pairs and the gpu and admin bits, so the whole table has to be read rather than grepped. Nothing is broken today and no capability sits above 30; what changed is that the next one cannot avoid it. Owner unchanged: root (HAL). **TAKEN BY BLU 2026-09-02 from root, and BUILT (not yet gated).** What the row said needed fixing and what actually did are not the same set, so the differences are named rather than quietly absorbed. **FIXED, and these were genuinely broken for bit 31 and above:** `emit-grant-cap-mask`, `emit-grant-capability` and `emit-revoke-capability` all built their mask with `or-ri`/`and-ri`, which is `alu-ri` in `X86_64Encoder.codex` emitting `write-i32` under REX.W. Each now loads the mask with `li` (which selects `mov-ri64` above the imm32 range) into rdi and uses `or-rr`/`and-rr`, re-materialising the constant address for the store rather than claiming a third register is free in boot code. `emit-revoke-capability` was NOT in the row's fix list and shares the defect exactly: its `inv-mask` is `bit-xor (shl-1 cap-bit) (0 - 1)`, a full-width value that `and-ri` truncates. **THE ROW IS WRONG ABOUT THE CHECK SIDE AND THIS IS THE USEFUL CORRECTION:** it says "`emit-check-capability` and every `and-ri reg (shl-1 cap-bit)` check site share the imm32 shape". `emit-check-capability` does NOT. It emits `[72, 15, 186, 225, cap-bit]`, which is REX.W `0F BA /4 ib`, a `bt r/m64, imm8`, correct for bits 0 to 63 as written. Nothing was needed there and nothing was changed. Two `and-ri` sites DO carry the imm32 shape (`X86_64Boot.codex` GPU compute and GPU memory checks) and are **deliberately untouched**: their bits are `cap-gpu-compute` 17 and `cap-gpu-memory` 18, provably inside imm32, they sit in live syscall handlers, and rewriting working handler code to fix a defect that cannot fire there is the "while I am here" R-ONE forbids. **THE LOADER WAS BROKEN IN BOTH DIRECTIONS, not just the read the row names.** `write-process-grant` wrote the low 32 bits with `poke-32` and then explicitly ZEROED offset 4, so a bit at 32 or above was discarded at WRITE time; `read-process-grant` then read only the low word. Both now carry the full 64 bits as two halves, since no `peek-64`/`poke-64` builtin exists. Measured rather than assumed: `peek-32` emits `[139, 7]`, `mov eax, [rdi]`, which ZERO-extends, and `poke-32` emits `[137, 23]`, `mov [rdi], edx`, exactly 32 bits, so bit 31 alone was already surviving the loader round trip and only 32-and-above were lost. **CROSS LANES REFUSE RATHER THAN GUESS (root's instruction, and the row's reading of them is also wrong).** The row says riscv `rv-emit-or-into-cell` loads the mask into t0 with `rv-li` whose own scratch is t0. Both riscv and arm64 already use the REGISTER form (`rv-emit-li` then `rv-or`; `a64-emit-li` then `arm64-or`), so neither shares x86's imm32 defect; the residual risk is only whether their `li` is correct for a mask that wide, which is UNMEASURED and unmeasurable today because both beds are unavailable. So `rv-emit-boot-grant` and `a64-emit-boot-grant` now REFUSE a mask carrying bit 31 or above with an `[UNSUPPORTED]` report and **emit no grant at all**. The first draft of that guard recorded the message and then emitted the grant anyway, which is a guard that produces a value and is exactly L-BAILVALUE; it was caught and rewritten before it left the workspace. **THE FIXTURE ROOT ASKED FOR IS NOT CONSTRUCTIBLE TODAY AND THAT IS A MEASUREMENT, NOT A REFUSAL TO TRY.** A fixture that grants bit 31 end to end needs a capability AT bit 31, and none exists: the boot mask is derived from the declared effect rows (`pm-opening-cap-mask`), so no chapter can ask for a bit the table does not define. Faking it by poking a spare process slot does not work either, because boot zeroes exactly 512 bytes at `proc-table-base` (`emit-zero-region proc-table-base 512`), which is TWO 256-byte entries, pid 0 and pid 1, both live; there is no spare slot to write. And no chapter under `codex/test` cites a Compiler or Verify chapter, so the encoder bytes are not reachable from the established test pattern either. The two honest routes are: land the bit-31 capability that the table now forces anyway (bits 0 to 30 are all assigned, so the NEXT capability IS bit 31) and let its own row carry the fixture, or add a `peek-64`/`poke-64` pair and test the loader round trip directly. Both are root's call. **ROUTE (b) RULED BY ROOT 2026-09-02 AND BUILT.** **HALF OF IT WAS ALREADY IN THE TREE, which is why no `peek-64` was added:** `peek-qword` already exists as a builtin and its helper emits `[72, 139, 7]`, `mov rax, [rdi]`, a full 64-bit load. Adding a `peek-64` beside it would have been a second name for the same instruction, which is the duplicate-encoder shape COMPILER-23 spent a campaign deleting (L-LESS). Only the WRITE side was missing, so this adds **`poke-qword`** alone: a `BuiltinSpec` beside `peek-qword`, an `emit-poke-qword-helper` emitting `[72, 137, 23]` (`mov [rdi], rdx`) with the same GPU guard and the same rdi/rsi/rdx convention as `poke-32`, and one line wiring it into the helper chain. `write-process-grant` and `read-process-grant` are now one line each over the qword pair instead of two 32-bit halves. **THE TEST IS `codex/test/ops/cap-word-64`**, round-tripping bit 31 (2147483648, the first value an ALU imm32 sign-extends) and bit 40 (1099511627776, the first band a 32-bit access cannot represent at all) through a scratch buffer from `alloc-bytes`, never the live process table. **Its last row is the CONTROL and it is the reason the file is worth anything:** bit 40 driven through `poke-32`/`peek-32` answers 0, because the low 32 bits of 2^40 are zero, so a compiler that quietly narrowed the qword pair back to 32 bits FAILS that row while every other row still passes. The expected values are worked out by hand and the chapter says so, because a sidecar captured from the subject enshrines whatever the subject does, which is how COMPILER-41 nearly acquired a guard asserting its own printer defect. **WHAT IS STILL UNGRADED, AND ROOT REQUIRED THIS BE STATED: the EMITTER half.** `emit-grant-cap-mask`, `emit-grant-capability` and `emit-revoke-capability` now build their masks through `li` plus `or-rr`/`and-rr`, and nothing exercises that end to end, because a bit-31 grant needs a capability AT bit 31 and none exists: the boot mask comes from the declared effect rows, and boot zeroes exactly 512 bytes at `proc-table-base`, which is two entries, pid 0 and pid 1, both live, so there is no spare slot to fake one in. **The first real bit-31 capability to land OWES THAT FIXTURE, and its row carries the debt** (Damian's standing shape: the emitter fix lands before the capability that needs it, and the capability brings the proof). Do not read this row's green as covering the grant path. `TechnicalDetails.md` test-file count moved 1,716 to 1,717 for the new chapter. |
| COMPILER-18 | **A partial-application closure carries no remaining-arity, so entering it with fewer arguments than it still wants corrupts silently -- and one-at-a-time entry is what the Rulebook says the language does** | From Steve Howell's zig-plug ladder, 2026-08-24 (ladder tag `closure-arity`). Line numbers checked against main at `5b8091e2`; `git diff bdf0049b..5b8091e2 -- codex/compiler/Emit/` is empty, so they also hold at the Update 49 pin where this was measured. Lane: the native x86-64 backend, seed-compiled, run on bare metal under QEMU, no plug anywhere in the path. **MEASURED.** (1) *Two spellings of one computation, one line apart, and the working one is yours.* `let h = add3 10 in show ((h 20) 12)` prints **42**; that is the shape of your own fixture at `codex/plugs/test-input/partial.codex:9-10`. Splitting the same work into two steps -- `let j = add3 10 in let g = j 20 in show (g 12)` -- faults in the same program, immediately after the first line prints. (2) *A wrong number with nothing to notice.* With `even-fn (n) = if n == 0 then add3 5 else odd-fn (n - 1)` and `odd-fn (n) = even-fn n`, both at `Integer -> (Integer, Integer -> Integer)`, `show (((even-fn 4) 20) 22)` prints **6291488** where 47 belongs. That is `0x600020`, a heap address printed as an Integer: no crash, no CDX code, no register dump. (3) *Control.* `add3` returns an Integer, is named by a partial application in every program in the series, and is correct in all of them. **READ FROM SOURCE.** A partial application is a heap object `[code-ptr][capture...]` of `(1 + num-captures) * 8` bytes (`emit-partial-application`, `Emit/X86_64Compound.codex:715-733`, size at `:727`). Its code pointer is a trampoline that shifts the incoming argument registers up by the capture count (`:703-707`), loads the captures beneath them (`:709-713`), and `jmp rax` to the target (`:724-725`). It therefore only works when entered with every remaining argument at once, and cannot re-close when given fewer -- **and the object stores no count, so no call site can discover what it wants.** Both entry paths reach it: `emit-apply` sends a global with more arguments than its arity to `emit-over-apply-extras` (`:154`, `:253-274`), which by construction emits one `mov rdi, <arg>` / `call rax` per argument (`:266-270`) and stores each result back as the next closure (`:272-273`); and it sends a FunTy result whose callee is a local to `emit-indirect-call` (`:166-169`, `:205-219`), which passes exactly the arguments written at that site. Measurement (1) is the second of those. **THIS IS THE DECLARED MODEL, NOT AN EXOTIC USE.** `docs/DevelopersRulebook.md:258-260` states it: a def "emitted n-ary is called flat at that arity, under-applied with one arrow per missing parameter, **over-applied by applying the rest one at a time**". COMPILER-12 already records the same shape on the plug side, warning that `(make-adder 10) 32` can go "right to silently wrong". And `codex/plugs/test-input/lambda.codex:19` drives the over-apply path on every build and is green -- because that closure wants exactly one more argument. The tree tests the boundary case and nothing past it. **NOT ESTABLISHED.** Why (2) needs the mutual recursion at all: the flat form `f (n) = add3 5` applied the same way prints 47, so something about the recursive shape is load-bearing and the ladder cannot say what. And separately, why appending a third statement to an unrelated two-statement program stops its *first* statement from printing. Both are real and neither is explained here. **THE ASK, and it is a ruling rather than a patch.** Repairing one caller only moves the boundary, since the arity is missing from the object rather than from any caller: the choice is whether the closure gains a remaining-arity word so the trampoline can re-close on under-application -- which is what the Rulebook's model above requires -- or whether under-application is instead refused where it is emitted. That is a representation decision and not a contributor's to make. On coverage, the shape belongs beside `codex/plugs/test-input/lambda.codex`, whose one-remaining case already passes; this PR does not add it, because accepting a red fixture is yours to decide. Seed-affecting. **RULED 2026-08-24: "give it the word" (Damian, via red). SCOPED AND MEASURED 2026-08-25 (blu), NOT IMPLEMENTED -- a second representation decision is open and is named at the end.** REPRODUCED first, on the seed compiler: the flat spelling prints 42 and the split spelling faults immediately after with `!EXC=08 CR2=fffffffffffffff8`. **WHY THE TWO SPELLINGS DIFFER, which the row above records as a mystery and is not one:** `flatten-apply` collapses `((h 20) 12)` into ONE flat apply carrying both arguments, which is exactly what the trampoline expects, while `let g = j 20` is an apply with one argument and the trampoline still shifts for two. The working spelling works by an accident of flattening, not by design, so it is not evidence the path is sound. **THE OBJECT ALONE CANNOT FIX IT.** x86-64 does not tell a callee how many arguments it received, so a remaining-arity word in the closure gives the trampoline something to compare against and nothing to compare WITH. The count has to reach it from the call site, and that is the open decision. **THE OBVIOUS CARRIER IS A LANDMINE: `r10` IS THE LIVE BUMP ALLOCATOR** (`X86_64Builtins.codex:589-593` advances it as a heap cursor; `X86_64Boot.codex:1287` saves it per process; `__alloc` itself is `mov rax, r10` / `add r10, rdi`). Putting an argument count there would corrupt the allocator silently. There is no other free register either: `rax` is the call target, `r11` the closure, `r15` has 99 uses across nine files, and `rbx`/`r12`/`r13`/`r14` are the tree emitter's locals. **TAGGING THE CLOSURE POINTER'S LOW BITS DOES NOT WORK, AND THE REASON IS WORTH KEEPING: heap pointers are NOT 8-byte aligned.** `__alloc` advances the cursor by the exact byte count requested with no rounding or masking, and `X86_64Helpers.codex:524-526` allocates `length + 8` bytes for text with no `shl`, so any odd-length text leaves the cursor unaligned and the next allocation returns an unaligned pointer. x86 tolerates unaligned 8-byte loads, which is why nothing has ever complained. The low three bits are real address bits. **ONE CHECK CAME BACK CLEAN and is recorded so it is not re-run:** the only non-partial-application shape reaching `emit-indirect-call` is the effect-handler resume closure, whose whole stub is `mov rax, rdi` / `ret` (`X86_64.codex:1427`) and which never reads `r11`. Lambdas are not a separate shape: `IrLambda` surviving to x86 emission is a hard error (`X86_64.codex:1201`), so lambdas are lifted and become partial applications, as does a bare function used as a value. **BLAST RADIUS:** five closure-entry sites (`X86_64Compound.codex:214`, `:267`, `:288`, `X86_64Builtins.codex:378`, `X86_64Helpers.codex:2844`) plus a family of trampolines per partial-application site, because the existing trampoline hard-codes its capture count in the shift and cannot re-close for a different one. **THERE IS NO OPEN DECISION. ONE DESIGN SURVIVES, and this paragraph said the opposite until 2026-08-25.** It called the carrier "Damian's decision" and left a compile-time alternative standing beside it. Both were wrong. The ruling in `CurrentPlan.md`'s ruled-items list is that **the trampoline re-closes on under-application**, which excludes re-closing at compile time from the callee's static type -- that is the call site re-closing, not the trampoline, and it traps rather than re-closes wherever the arity is not static. The register route and the tagged-pointer route are excluded by the two measurements above. What is left is one design and it is the one to build. **PASS THE COUNT ON THE STACK:** the caller does `push <count>` before `call rax`; at trampoline entry `[rsp]` is the return address and `[rsp+8]` is the count; the exact-arity path slides the return address up over the count, drops 8 from `rsp`, and jumps to the target exactly as today, using `rax` as the scratch since it is reloaded with the target anyway; the under-application path reads the count and re-closes. It is reentrant, needs no free register, and assumes nothing about alignment -- which is what makes it the only survivor rather than merely the preferred one. **Common ground under both, already ruled and independently gateable: the object gains the word and captures move from `[8 + i*8]` to `[16 + i*8]`.** The reproducer is four lines and is in the row's measurement (1); write it before touching anything. **BUILT 2026-08-25 (blu) AND IT IS RED, ON COST. THE WORD IN THE OBJECT IS NOT AFFORDABLE AND THE MEASUREMENT IS THE FINDING.** The layout half is correct: the reproducer still prints `flat: 42` under the new layout and R10 lands 16 bytes further on, exactly two closures' worth. It is the SIZE that fails. `build/build.ps1 -Internal` goes red at the STAGE-2 compile with `CDX9002: Deck overflow in LOWER`, isolated by compiling one snapshot of the concatenated source under three kernels: seed and Sut (both old-layout runtimes, since Sut is seed-emitted) agree to the byte at `LOWER used=189,324,144`, and stage 1 -- the first binary that actually CARRIES the new layout -- needs `335,997,424`. `-Decks 200` clears it, which is what says budget rather than corruption. **A scaling probe falsified the obvious explanation and is worth more than the red (L-SABOTAGE).** Re-running the whole chain with a THREE-word header instead of two predicted another jump of the same size in LOWER and did not get one: LOWER went 189,324,144 -> 335,997,424 -> 337,231,552, so the second 8 bytes cost 1,234,128 where the first cost 146,673,280 and LOWER's growth is mostly not per-object. The phases that ARE linear name the real cost exactly, the two deltas identical to the byte: **CHECK-RESOLVE 353,424,952 -> 488,626,600 -> 623,828,248, +135,201,648 per header word, which is 16,900,206 partial-application objects allocated in that one phase**, and CHECK +20,876,576 per word, another 2,609,572. **So the compiler allocates roughly twenty million partial applications per self-compile, and the great majority must be ZERO-CAPTURE -- a bare function used as a value, whose object today is a single 8-byte word.** The ruled word does not add 8 bytes to a rare object; it DOUBLES the hottest small object in the compiler. R-COST assessed this change by inspection as "8 bytes per partial application, negligible" and that was wrong in the way R-COST exists to catch: the per-object delta was right and nobody asked how many objects. **THE DESIGN THAT COSTS NOTHING PER OBJECT, AND IT IS THE ONE BEING BUILT (Damian, 2026-08-25, on the measurement above: do the right thing given the new information, the ruling "was merely in agreement with your prescription" and was never a constraint).** The word does not go in the object. The trampoline already knows its own remaining arity as a COMPILE-TIME CONSTANT -- it is emitted per partial-application site and the row above already accepts a family of trampolines per site, since the existing one hard-codes its capture count in the shift. Under the ruled stack-passed count the comparison is caller-count against that immediate, so nothing has to read an arity out of the object: the code pointer at offset 0 already identifies the trampoline and therefore the arity. The word is redundant with the code it points at. That buys the ruled BEHAVIOUR (re-close on under-application, rather than refuse it) at zero bytes per closure and a few bytes of code per SITE, which is static. **So this row's original "independently gateable" first step is GONE -- there is no layout change to gate -- and the replacement first step is the CALLING CONVENTION, which is the half that carries the real risk.** Every closure entry pushes the count it is passing before `call rax` and drops it after; no trampoline reads it yet; the object is untouched. That isolates five entry sites, one of which (`X86_64Helpers.codex` `__task_entry`) enters a task closure that may not return, and one of which is the effect-handler resume stub -- not a trampoline at all, and unaffected only because the CALLER cleans the word up rather than the callee. Caller-cleanup is also why the exact-arity path needs none of the return-address sliding this row previously specified: the count sits BENEATH the return address, the trampoline just jumps, and the target's `ret` lands correctly. The encoder gains `push-i32` because there is no free register at the moment of the push (`rax` is the target, `r11` the closure, `rdi`-`r9` the arguments), which is the same register famine that killed the register carrier. Alignment was checked before building rather than after: no aligned SSE form (`movaps`/`movdqa`/`movapd`) is emitted anywhere in `codex/compiler/Emit`, so the odd push cannot fault. Step 2 is the trampoline family: for a site with capture count `c` and target arity `A`, trampolines `T_c..T_{A-1}`, each comparing the pushed count against its own immediate `A - c` and, when it is short by `m`, allocating a fresh closure of `c + m` captures pointing at `T_{c+m}`. A site whose remaining arity is 1 has no under-application case and keeps exactly today's trampoline. Over-application through a closure (count GREATER than remaining arity) is NOT addressed and keeps today's behaviour; it is a different shape and needs its own row. **BUILT AND GREEN 2026-08-25 (blu), AND MEASUREMENT (1) IS CLOSED WHILE MEASUREMENT (2) IS NOT.** Same program, same runner, side by side: the fixed compiler prints `flat: 42` / `split: 42`, the depot seed prints `flat: 42` and then faults. Guarded by `codex/test/ops/closure-under-apply`, which was checked BOTH ways before being believed -- five rows correct under the fix (6, 42, 42, 10, 10, including a four-arity closure entered one argument at a time) and a hard fault under the seed, so it is a test that fails on the compiler it is meant to catch. **THE ROW'S CENSUS OF FIVE CLOSURE-ENTRY SITES WAS INCOMPLETE; THERE ARE SEVEN**, and under callee-consumes a missed one is not cosmetic, because the trampoline would slide a return address over a word nobody pushed. The two additions are `emit-effect-op-call` (`X86_64Compound.codex`), whose effect-op table slot is filled by `emit-store-to-handler-table` with the clause body's VALUE, which is a closure; and `__proc_entry` (`X86_64ProcessHelpers.codex:201`), which enters a process closure exactly as `__task_entry` enters a task one. **THE CALLER MUST NOT CLEAN UP, AND THIS COST A RED GATE WORTH RECORDING.** Caller-cleanup was tried first as a smaller step-1 and it puts the count at `[rbp+16]`, which IS the seventh-argument slot: 181 functions in the compiler read that slot, stage 2 died at `current+0x40` on the inlined `list-at` bounds trap with the pushed word arriving as a `ParseState` pointer, and the row's original return-address slide -- discarded as unnecessary -- was the thing preventing it. The diagnosis was measured, not argued: one sentinel value proved the word was read as DATA rather than a stack imbalance, then five per-site sentinels named `emit-indirect-call` in a single build (L-STATES). **MEASUREMENT (2) IS A DIFFERENT DEFECT AND SURVIVES THIS FIX.** `((even-fn 4) 20) 22` still answers a heap address, 6291496 under the fix against 6291488 under the seed -- it moved by exactly one allocation, so the re-close does engage and then the chain still fails to finish. The control the row asked for is now run: the FLAT form `flat-fn (n) = add3 5` answers **47 under BOTH compilers**, so the mutual-recursive shape is load-bearing on its own and this is not an arity-representation defect. It smells like result-type inference through mutual recursion rather than codegen, it needs its own row, and COMPILER-18 must not be closed as though it covered this. **CLOSED 2026-08-25 (blu) BY COMPILER-20, AND THE DIAGNOSIS ABOVE WAS WRONG IN BOTH HALVES: it is codegen, not inference, and mutual recursion is not the variable.** See COMPILER-20 for the cause and the fix. Two things this row got wrong are worth keeping. **The control was VACUOUS (L-CONTROL).** `flat-fn (n) = add3 5` answers 47 under both compilers because `inline-leaf-calls` deletes `flat-fn` outright and the call site becomes a flat `add3 5 20 22`, so the arm never entered a closure at all and could not have failed; it was read as proof the mutual-recursive shape was load-bearing. **And the shape it named was one of three that fail**, which a two-arm probe could not see: a plain SELF-recursive `rec-fn` fails in the split spelling, and a function with no recursion anywhere fails in both, so the defect was never about recursion. **INDEPENDENT CORROBORATION, absorbed from Steve Howell's PR 86 (red, 2026-08-25).** Working against public seed `6CF4A8E0`, before the fix and without seeing it, Steve built the smallest entry point yet -- `choose : Integer -> (Integer, Integer -> Integer)` with two return paths and NO recursion -- and measured the silent form (`stepwise` printing heap address `0x600038`) and the loud form (`choose 0 2 3` faulting) in one five-line file, concluding the variable was "something about multiple return paths, not recursion": the same narrowing this row's closure records, reached from the other side of the mirror. His file is `codex/plugs/test-input/overapply.codex`, re-measured GREEN on bare metal at head (all five lines match the zig plug), and plugs-backlog 1.59 carries its corpus role.  **CLOSED (blu, 2026-09-01), AND WHAT SHIPPED IS NOT WHAT WAS RULED.** Measurement (1) is fixed and its guard `codex/test/ops/closure-under-apply` answers 5 of 5 against seed 42ACED00 -- full 6, flat-two 42, split-one-at-a-time 42, split-four 10, half-then-one 10 -- the last two being the spellings that faulted under the old compiler, so it fails on the compiler it was written to catch. Measurement (2) went to COMPILER-20. **The ruled design, "the closure gains a remaining-arity word", WAS NOT BUILT, and the register said otherwise until today.** `emit-partial-application` (`Emit/X86_64Compound.codex:791`) still sizes the object `closure-size = (1 + num-captures) * 8` at `:802` and the emitter has ZERO mentions of `remaining-arity`, `arity-word` or `pa-arity`. What shipped is a TRAMPOLINE FAMILY (`emit-trampoline-family`, `:798`, with `total-arity` at `:795`): the remaining arity is encoded in WHICH trampoline the closure points at, so the object never grew. **That is exactly why L-PEROBJECT's blow-up never reached main** -- the word would have DOUBLED a zero-capture partial application, whose whole body is one 8-byte code pointer, at ~20 million allocations per self-compile, taking LOWER's deck 189 MB to 336 MB and refusing with CDX9002. The family pays code size once per arity instead of 8 bytes per object, and L-PEROBJECT's own question -- how small is this object and how many exist -- is what chose it. |
| COMPILER-19 | **CLOSED. The fix is in the tree and this row outlived it; re-verified at head 2026-09-02 (blu).** All four functions this row names as the defect, `try-scan-type-def`, `try-scan-def-header`, `try-top-level-type-def` and `try-top-level-def`, are ABSENT from `Syntax/Parser.codex`, against a control (`scan-top-level`, 20 hits) proving the file and the search both work. The remaining mutual edge is one hop per EFFECT DECLARATION and is explicitly not this row's defect. **The two top-level scans are three-function mutual TAIL recursion, so every plug and bare metal pay a frame per definition** | From the zig-plug ladder, 2026-08-24 (`contrib/README.md`; ladder `findings/README.md` 37, instrument `stack_probe.py`). `scan-top-level` / `try-scan-type-def` / `try-scan-def-header` (`Syntax/Parser.codex`, the streaming header scan) and `parse-top-level` / `try-top-level-type-def` / `try-top-level-def` (the parse) each go once around per top-level definition, and **every edge is a tail call** -- no frame in either cycle is live when the next is entered. Measured on the compiler's own back-end unit bundled as one document (2,503,544 bytes, 4,511 top-level definitions), compiled by a native binary built through the zig plug: 2,393 nested `scan_top_level` frames at the shallower cliff, 3,385 nested `parse_top_level` at the deeper one. **Nothing in the fleet flattens mutual tail calls** -- `is-self-call` is self-only here, in the python plug, and in the zig plug -- so this is paid on x86-64 as well, where it is invisible only because the kernel stack is generous and frames are small. **The change is source-shaped and needs no emitter work anywhere:** have the two `try-*` functions RETURN what they found (a small record: the accumulated lists plus the new state) instead of tail-calling the loop, and the loop tail-calls ITSELF, which every existing TCO already flattens. That is what this PR's one commit does. **Measured with exactly that change applied:** the minimum thread stack the transpiled compiler needs for that document falls from 32 MB to **4 MB**, and what remains on the failing trace is `desugar_expr_at` at 297 frames -- structural recursion over one expression's nesting depth, not growth in the size of the document. **Behaviour preserved, at two depths.** The restructured parser compiled that unit to IR, through the plug to a native binary, and both of the ladder's `ir_to_x86` rung outputs are byte-identical to its Update 49 bank. A full ladder sweep then ran green 14/14 (1716 s), with the diagnostics census unmoved and 13 of the 14 recorded truths byte-identical to that bank; the single mover is the `parse` rung, whose own subject program IS the edited `Parser.codex`, and its diff is exactly four removed definitions, three added, two added type-defs and 50 line shifts, with **no existing definition changing params, anns or slug**. Both arms agree on the new dump, which is the rung that most directly tests the change. **One detail a re-implementer must keep:** when `parse-type-def` fails, the definition parse re-reads from the ORIGINAL state, not from the state the type parser returned -- the two functions this replaces did that, and merging them makes it easy to lose.  **ALREADY DONE IN THE TREE, VERIFIED 2026-09-01 (blu); this row outlived its own fix and the four function names it cites no longer exist.** `try-scan-type-def`, `try-scan-def-header`, `try-top-level-type-def` and `try-top-level-def` are ABSENT from `Syntax/Parser.codex`. Both loops now do exactly what this row prescribes -- the helper returns a small record and the loop tail-calls ITSELF: `scan-top-level` self-recurses at `:1995`-`:2019` and takes `item.si-headers` / `item.si-type-defs` at `:2019`; `parse-top-level` self-recurses at `:1474`-`:1517` and takes `item.ti-defs` / `item.ti-type-defs` at `:1503`, `:1511`, `:1515`. **The file says so itself at `Parser.codex:2035`-`:2040`**, prose recording the change and the 2,393-frame measurement, which is stronger evidence than a CL description and is why no archaeology is quoted here (the restructure predates the last 20 revisions of the file). **ONE MUTUAL EDGE REMAINS ON EACH SIDE AND IT IS NOT THIS ROW'S DEFECT:** `scan-top-level-effect` (`:2002` out, `:2032` back) and `parse-top-level-effect` (`:1487`). That is a single hop per EFFECT DECLARATION, not the frame-per-definition growth this row measured, so the cliff it describes is gone; anyone wanting the last edge should say so as a new and much smaller row. |
| COMPILER-20 | **A saturated call whose RESULT is a function type is emitted as a partial application OF THE CALLEE, so the callee is never called** | Split out of COMPILER-18 measurement (2) and **FIXED 2026-08-25 (blu)**, one line in `Emit/X86_64Compound.codex`. **THE CAUSE, read out of the IR rather than guessed.** `emit-apply` dispatches on the apply''s RESULT type. Its `is FunTy` arm (`:166-169`) asks only whether the callee name is a local: if it is not, it emits `emit-partial-application` over that name with a fabricated `total-arity` of `list-length args + 1 + fun-type-arity rt`. Nothing on that arm consults the callee''s DECLARED arity, which `emit-apply` has already computed and holds in scope as `user-arity` at `:152`. So for `mk : Integer -> (Integer, Integer -> Integer)` with `mk (n) = ...`, the saturated call `mk 4` -- one argument against arity one -- is turned into a closure wrapping `mk` with capture `[4]` and a believed arity of 3. `mk` is never entered. Feeding that object its two remaining arguments re-closes once and then jumps to `mk` with three arguments in `rdi/rsi/rdx`; `mk` reads only `n`, returns its own closure, and the caller prints that heap address as an Integer. **The neighbouring arms were already correct and are what make the omission visible:** `:154` sends MORE arguments than the arity to `emit-over-apply`, and the `SumTy` and `otherwise` arms both go to `emit-call-or-effect-op`, which respects arity. Only the `FunTy` arm has the `emit-partial-application` escape, and only it is missing the equal case. **THE FIX** is the missing case, on that arm, ahead of the escape: `else if user-arity > 0 & list-length (flat.args) == user-arity then emit-call-or-effect-op st flat user-arity`. A genuine partial application (fewer arguments than arity) still falls through to `emit-partial-application`; builtins and effect ops are excluded by `user-arity > 0`. **R-COST: strictly negative on both axes.** Each site this repairs REPLACES a heap-allocated closure plus a trampoline family with a direct call. No loop, no recursion, no accumulator added. Measured through the gate rather than asserted: the internal gate is green with `SUT === stage1` in one pass. **WHY THE FLAT SPELLING HID IT, which is the whole reason this sat inside another row for a day.** `((mk 4) 20) 22` does not reach the defect: `flatten-apply` collapses it to one apply carrying three arguments, `3 > user-arity 1` takes the `:154` over-apply path, and that path calls `mk` properly first. Every spelling that binds the intermediate to a `let` -- which is what the Rulebook''s one-at-a-time model produces -- takes the broken arm. **MEASURED, and the census is what corrects COMPILER-18''s framing.** Under the depot seed `D74D8855`, seven of the nine rows of the guard below are wrong; all nine are correct under the fix. The failing shapes are (a) a saturated arity-1 call returning a function, split, (b) the same entered with all remaining arguments at once, (c) an arity-2 callee, (d) a SELF-recursive callee, (e) a mutually recursive pair. Recursion is incidental to all of it: it changes only whether the FLAT spelling also fails, because a self-recursive call is caught by `is-self-call` at `:148` and never reaches the broken arm, while a call to a sibling (`odd-fn`) does. That single asymmetry is the entire reason measurement (2) appeared to be about mutual recursion. **GUARD: `codex/test/ops/saturated-call-returning-function`, checked BOTH ways before being believed** -- seven wrong rows under the seed, nine correct under the fix, values hand-computed. Its `arity-two` arm is only real because `mk2` has two call sites: with one, `inline-leaf-calls` deletes it and the arm answers correctly under the broken compiler, which is the same vacuous-control trap COMPILER-18 fell into (L-CONTROL). **NOT ADDRESSED:** over-application through a closure (entering with MORE than the remaining arity) is untouched and keeps today''s behaviour, as COMPILER-18 recorded. Seed-affecting. |
| COMPILER-21 | **The tier-0 Unicode branch tests the LOW BYTE of the code point rather than the code point, so the fifteen Cyrillic code units print as unrelated ASCII -- on THREE print paths, not one** | Raised by fester 2026-08-25 (unowned, CurrentPlan); **FIXED 2026-08-25 (blu)**. **THE MECHANISM, read from the source and confirmed against the table.** `cce-to-unicode-byte-table` is `for x in cce-to-unicode-table -> int-mod x 256`, the LOW bytes only, and the hi table `cce-to-unicode-hi-table` sits at a separate rodata offset. Each print loop indexed the low table, compared THAT byte against 128, and took the single-byte ASCII arm when it was under -- reading the hi table only on the other arm. So any tier-0 code unit whose Unicode has a nonzero high byte AND a low byte under 128 printed as that low byte. `CCE.codex:122-123` puts exactly fifteen such entries at indices 113..127 (U+0430, U+043E, U+0435, U+0438, U+043D, U+0442, U+0441, U+0440, U+0432, U+043B, U+043A, U+043C, U+0434, U+043F, U+0443): every one has hi byte 4 and a low byte in 48..67, so all fifteen took the ASCII arm. The accented block at 97..112 is the control and was always correct, because its low bytes are 224..252, over the threshold. **MEASURED both ways.** Under main seed `966EF113` the probe prints `cyrillic-113-127: 0>58=BA@2;:<4?C` where `аоеинтсрвлкмдпу` belongs; under the fix it prints the Cyrillic, with the accented and digit rows byte-identical across both. **THE ROW SAID ONE SITE AND THERE ARE THREE**, all reading the same low-byte table with the same test: `emit-print-text-loop` (`X86_64IO.codex`, the serial path both `emit-print-text` and `emit-print-text-no-newline` delegate to, which is what fester measured), and `__uefi_print` and `__uefi_print_no_nl` (`X86_64Helpers.codex`), which `X86_64Builtins.codex:716-729` selects instead whenever the exit is `ExitUefi`. Fixing only the serial path would have left the same wrong bytes on the UEFI console (L-PARTIAL). The repair is the same in all three: combine the hi byte in BEFORE the `cmp 128`. **THE TWO UEFI LOOPS ARE NOT THE SERIAL LOOP AND THE DIFFERENCE COST A BUILD.** The serial loop pushes the CCE CODE UNIT before its table lookup, so popping it back gives the hi table its index; the UEFI loops push the TEXT INDEX (the loop counter) instead, and the CCE code unit is live only in `rax` between the text load and the table add. A first attempt reused the popped register the way the serial fix does, indexed the hi table by the text index, and produced a build whose UEFI arm was still wrong -- caught only because the arm was actually run rather than reasoned about. The shipped form saves the code unit to `r11` and the low byte to `r15`, both dead in these helpers. **WHY NO SUITE SAW IT, and it is L-CONSTRUCT.** `codex/test/apps/console-test.expected` is the ONLY non-ASCII expectation in `codex/test`, and every character in it (`é ó ñ è ê`) is from the accented block -- the half that works. Not one test in the tree spelled a Cyrillic code unit. **GUARD: `codex/test/ops/tier0-cyrillic-print`**, checked both ways (wrong under main seed 966EF113, correct under the fix, controls unmoved). **It covers the SERIAL path only**: no test in `codex/test` runs under `-uefi`, and the sidecar set has no way to ask for it, so the two UEFI loops remain ungated (L-NOGATE). The UEFI arm was run by hand (`compile.ps1 -Uefi` plus `codex-vm -uefi -headless`) and the fifteen characters go from literal ASCII to the same unreadable-glyph signature the known-correct accented row produces through the emulator console conversion; that conversion means the exact code points cannot be read off that surface, so the UEFI half is verified to "non-ASCII, one UTF-16 word per character" and no further. **STILL BROKEN, SAME CLASS, NOT FIXED HERE:** three more readers of the low-byte table with no hi-byte combine anywhere -- `__cce_to_unicode` (`X86_64Helpers.codex`), which returns the low byte AS the code point and is emitted into every binary while **nothing in the tree calls it** (L-UNCALLED); `__text_to_unicode_bytes`; and `__uefi_read_file`, whose filename conversion truncates the same fifteen. Each needs its own decision about what a truncated value means to its consumer, so none is a one-line repair and none is in this CL. **R-COST: negligible on both axes and no allocation.** Per printed character the fix adds one rodata address load and four ALU ops on a path that already ends in a `call __serial_put` (or a UTF-16 store); emitted code grows by roughly twenty bytes per print site, static. Seed-affecting: converged in two passes, seed `7298BBFA`. |
| COMPILER-22 | **The last three low-byte readers, and a fourth defect in the REVERSE direction that is not one of them** | Continuation of COMPILER-21 (blu, 2026-08-25). That row named three remaining readers of `cce-to-unicode-byte-table` that combine no hi byte; all three are **FIXED here**, and after this CL **every read of the low table in `Emit/` is paired with a hi-byte combine** -- five pairs in `X86_64Helpers.codex` and, in `X86_64IO.codex`, two low loads feeding the one shared `emit-print-text-loop` that carries the combine. **`__text_to_unicode_bytes` is the one that mattered**, because it is the emitter for the live builtin `text-to-unicode-bytes : Text -> List Integer` (`Types/Builtins.codex:211`), so the truncation was reachable from ordinary Codex and returned wrong INTEGERS rather than wrong glyphs. Measured against main seed `7298BBFA`: `text-to-unicode-bytes` over CCE 113..127 answered `48 62 53 56 61 66 65 64 50 59 58 60 52 63 67` where `1072 1086 1077 1080 1085 1090 1089 1088 1074 1083 1082 1084 1076 1087 1091` belongs; the accented, digit and uppercase rows were correct before and after. **`__uefi_read_file`** truncates the same fifteen in the FILENAME it converts to UTF-16, so a path containing one opened the wrong name; same repair, `rbx` and `r11` are both dead in that loop. **`__cce_to_unicode` is fixed but remains UNCALLED** -- one definition in `X86_64Helpers.codex`, one in `codex/plugs/riscv/RiscVRuntime.codex`, and after a whole-tree search no caller in either, while every `.map` in the tree shows it emitted. It is repaired rather than deleted because deletion is a design call and the too-narrow-search direction that licenses deletion is the expensive one; the L-UNCALLED observation stands and whoever wants it gone should take that decision on its own. **GUARD: `codex/test/ops/text-to-unicode-bytes-tier0`**, checked both ways (low bytes under seed `7298BBFA`, exact code points under the fix, three control rows byte-identical across both). It is a better instrument than COMPILER-21''s print guard because its output is plain ASCII integers, so it reads the code points directly instead of through a console conversion. `__uefi_read_file` and `__cce_to_unicode` remain ungated: the first needs a UEFI boot with a file and nothing in `codex/test` runs under `-uefi`, the second has no caller to drive it. **A FOURTH DEFECT, MEASURED HERE, NOT FIXED, AND NOT A LOW-BYTE READER: `unicode-bytes-to-text` round-trips nothing but ASCII.** `text-to-unicode-bytes (unicode-bytes-to-text [233])` answers `0`, and reading the intermediate text''s CCE units directly shows `unicode-bytes-to-text [233]` producing the TWO units `192 169` where CCE 97 (one unit) belongs -- `[1072]` likewise gives `196 176`, and `[65]` correctly gives the single unit `41`. The mechanism is a different one: `__unicode_bytes_to_text` sends every value at or above 128 to `__unicode_to_cce_tier1` and then encodes whatever comes back as a two-byte CCE sequence unconditionally, with no single-byte case, so a tier-0 answer under 128 is emitted non-canonically as two units. It is the inverse half of the same builtin pair and it fails for all 31 non-ASCII tier-0 code units, accented and Cyrillic alike. **It needs its own row and its own decision** (whether the tier-1 reverse should answer a tier-0 code at all, and whether the caller should branch on the width), which is why the guard above deliberately does NOT assert the reverse direction: baking today''s answers into an `.expected` would enshrine the defect. **R-COST: negligible, no allocation.** One rodata address load and four ALU ops per converted character on three paths, two of which run once per filename; roughly twenty bytes of emitted code per site, static. Seed-affecting: converged in two passes. |
| COMPILER-23 | **The CCE tier-1/tier-2 surface in the native x86 backend indexes rodata tables the image does not contain, and the `text-to-unicode-bytes` / `unicode-bytes-to-text` builtin pair is ASCII-only** | **CLOSED. REPAIR 2 WAS BUILT AND LANDED 2026-08-26 AT CL 19813, AND EVERY WORD BELOW THIS SENTENCE, THE 2026-09-02 RULING INCLUDED, PREDATES IT OR WAS WRITTEN WITHOUT IT.** Verified at head 2026-09-02 (blu) rather than taken from the CL: `codex/test/ops/unicode-bytes-roundtrip` compiled against depot seed `BE8B04B5` and run under codex-vm matches its `.expected` on all 14 lines, tier 2 included, topmost and buried arms equal. 19813 moved `text-to-unicode-bytes` and `unicode-bytes-to-text` out of the builtin table into Foreword chapter CCE as ordinary Codex, deleted the x86, arm64 and riscv hand-rolled copies, and fixed all four defects A, B, (i) and (ii). The guard chapter the ruling asks to land with it landed with it, at 19813. **THE GUARD IS NOT VACUOUS AND THAT WAS MEASURED, NOT ASSUMED:** sabotaging the tier-2 lead in `cce-encode-into` from `bit-or 224` to `bit-or 226` moves exactly the two tier-2 arms (units lead 233 becomes 235, back 8364 becomes 65533) and no others. The sabotage was CHOSEN by arithmetic against the actual inputs first: `bit-or 225` is a free pass, because the lead nibble for cce 41616 is 9 and `224|9` and `225|9` are both 233 (L-SABOTAGE). **THE ROW WAS RE-PRESENTED ON 2026-09-02 AND RULED THE SAME DAY FOR WORK ALREADY ON MAIN**, because this body ends at the 2026-08-26 measurement and says "recorded here rather than acted on", which was true when written and stopped being true hours later. A row that does not record its own landing costs a ruling round trip, and this is the third time in two sessions a compiler row has outlived its own fix. **A CONTROL THAT LOOKS RIGHT AND MEASURES NOTHING, worth keeping because it will be reached for again:** compiling this chapter with the PRE-FIX seed `C45E5825` and running it PASSES. That is not evidence the guard is weak. Head's source defines the pair in the foreword, so both kernels compile the fixed foreword code and the deleted builtin emitters are never reached; the pre-fix seed alone does not reconstruct the pre-fix world, since the fix was half source and half compiler (`p4 print //Codex/blu/codex/foreword/core/CCE.codex#6` contains none of `text-to-unicode-bytes`, `unicode-bytes-to-text`, `to-unicode-with`, `from-unicode-with` or `cce-encode-into`). Vary the SOURCE, not the seed (L-SAMEVER, L-VACUOUS). **RE-PRESENTATION FOR DAMIAN (red, 2026-09-02). Your 2026-08-25 ruling for repair 2 cited the absent-rodata mechanism, which blu retracted the same day; this is the same question asked on the measurements that stood afterwards, all against seed C45E5825, the detail below.** WHAT IS BROKEN: the hand-rolled x86-64 builtin pair `text-to-unicode-bytes` / `unicode-bytes-to-text`, four independent defects: (A) the forward half answers 0 whenever its input text is the topmost allocation (aliasing; it masks the rest and produced the wrong "ASCII only" headline); (B) the reverse half is non-canonical for tier 1 and LOSSY for tier 2, u=8364 and u=192 both becoming `192 128`; (i) the 2-byte tier-1 decode is wrong at the low end, `193 128` reading 256 for 192; (ii) a 3-byte tier-2 unit mis-frames instead of refusing. WHAT IS CORRECT: the foreword `from-unicode` / `to-unicode` and the `char-encode` / `cce-decode-at` pair, on every value the helpers get wrong, tier 2 included; `char-to-text` stays byte-level per option (a) and is not part of this. COST OF LEAVING IT: latent; no shipping app prints tier-1 or tier-2 text and the compiler's own text path does not go through the pair. THE CHOICE: repair 1 (make the rodata tables real, about 4.6 KB per binary, re-lay the seed data section) fixes nothing measured, because no live path reads an absent table, so it is off the table. Repair 2: delete the helper pair and lower both builtins through the foreword's `from-unicode` / `to-unicode`, which every binary already carries; one CL removes four defects and a duplicate encoder; seed-affecting; the builtins' lowering becomes a call into foreword code where a helper was open-coded. Repair 3, new: fix the four defects in the helper and add tier-2 framing, keeping the duplicate; four smaller CLs, and the duplicate stays to drift again. RECOMMENDATION: repair 2, guarded by a `codex/test/ops` chapter carrying the six-value round trip, the topmost-versus-buried control, and a 3-byte unit, since the tree has never built one (L-CONSTRUCT, three times on this surface). Owner: blu holds the open compiler bugs per your 2026-08-26 re-ruling. THE ASK IS ONE WORD: repair 2, yes or no.** **RULED 2026-09-02 (Damian, via red): repair 2, YES. blu builds it; the guard chapter in `codex/test/ops` lands with it.** Split out of COMPILER-22 (blu, 2026-08-25). **MEASURED, NOT FIXED: the repair is a design decision and is named at the end.** **THE ORACLE IS IN THE TREE AND IT IS CORRECT.** `codex/foreword/core/CCE.codex` `from-unicode` tries tier 0, then tier 1, then tier 2, and `to-unicode` inverts it; compiled and run against seed `B773CC58` the pair round-trips every tier exactly: u=65 -> 41 -> 65, u=233 -> 97 -> 233, u=1072 -> 113 -> 1072, u=192 -> 192 -> 192, u=960 -> 592 -> 960, u=8364 -> 41616 -> 8364. Nothing below is a defect in the foreword. **THE BUILTIN PAIR DISAGREES WITH THAT ORACLE FOR EVERY NON-ASCII INPUT.** `text-to-unicode-bytes (unicode-bytes-to-text [u])` answers `u` for 65 and 48 and answers **0** for 233, 1072, 192, 960 and 8364 alike, so the pair round-trips ASCII and nothing else. Reading the intermediate text units directly names the reverse half error precisely: `unicode-bytes-to-text [233]` yields the TWO units `192 169`, which decode to CCE 41, where `from-unicode` mandates the single tier-0 unit 97; `[1072]` yields `196 176` (CCE 304) against the mandated 113. Those are legitimate TIER-1 encodings of the right characters rather than garbage -- the helper skips the tier-0 reverse for any value at or above 128 and goes straight to `__unicode_to_cce_tier1` -- but they are not what the spec says and they do not survive the trip back. **AND IT IS LOSSY, WHICH IS THE PART THAT MATTERS MOST:** `[192]` and `[8364]` BOTH yield `192 128`, so two distinct characters become the same text and cannot be told apart afterwards. **THE MECHANISM UNDER ALL OF IT: THE TABLES ARE NOT IN THE BINARY.** `X86_64Chapter.codex:634` writes `init-rodata` and nothing else, and `data-len` is `list-length init-rodata`. `init-rodata` is `cce-to-unicode-byte-table` (128) & `unicode-to-cce-byte-table` (128) & `tier1-block-rodata` (32) & `tier2-rodata` (60) & `cce-to-unicode-hi-table` (128) = **476 bytes**, and its three hardcoded offsets (256, 288, 348) are mutually consistent with exactly that layout. But `X86_64State.codex` also defines `cce0u2-lo` and `cce0u2-hi` (2,176 entries each) and `cce-t2-rodata` (240 bytes), **none of which is concatenated into `init-rodata` anywhere in the tree**, and `__cce_print` (`X86_64Helpers.codex`) reads them at `cce0u2-rodata-offset` = 288, `cce0u2-hi-rodata-offset` = 2464 and `cce-t2-rodata-offset` = 4640. The first **collides with `tier2-rodata`**, which is what actually sits at 288; the other two are past the end of a 476-byte data section. So the multi-byte print path reads whatever follows, and the reverse builtin inherits the same absent tables. **CONSISTENT WITH THE PRINT MEASUREMENT:** tier-1 `code-to-char 192` prints A-grave correctly while `code-to-char 592` prints `>` where U+03C0 belongs, so the surface is not uniformly dead -- it is right wherever the bytes it happens to read are right, which is the worst shape to leave in place. **WHY NOTHING CAUGHT IT (L-CONSTRUCT, again).** Every non-ASCII character anywhere in `codex/test` is tier 0, so every fixture is a single CCE unit and no test in the tree has ever built a tier-1 or tier-2 one. COMPILER-21 and COMPILER-22 both landed against corpora made entirely of the half that works. **THE ASK, AND IT IS A DESIGN DECISION RATHER THAN A PATCH.** Two repairs are available and they are not equivalent. (1) Make the tables real: concatenate `cce0u2-lo`, `cce0u2-hi` and `cce-t2-rodata` into `init-rodata` and re-derive all the offsets, which adds about 4.6 KB to every emitted binary and re-lays the seed data section. (2) Delete the hand-rolled helpers and route the two builtins through the foreword `from-unicode`/`to-unicode`, which are already correct and already compiled, removing the duplicate rather than repairing it. The second is smaller and kills a whole class, but it changes how a builtin is lowered. **Not blocking anything measured today: no shipping app prints tier-1 or tier-2 text, which is why this has been latent.** Seed-affecting whichever way it goes. **CORRECTION 2026-08-25 (blu), BEFORE ANY CODE WAS WRITTEN AND AFTER THE RULING: THE MECHANISM PARAGRAPH ABOVE IS WRONG, AND THE RULING CITES IT.** The row said `__cce_print` reads the absent `cce0u2`/`cce-t2` tables and therefore "the multi-byte print path reads whatever follows". Both halves are false. **`__cce_print` IS NOT ON ANY LIVE PATH AND IS NOT EVEN EMITTED.** It is recorded by `emit-cce-to-utf8-helper` (`X86_64Helpers.codex:662`), and that function is called from nowhere in `Emit/`; `__cce_print` has ZERO entries in `build/output/Sut.map`, while `__cce_print_multi` has one. The live multi-byte print path is `__cce_print_multi` (`X86_64Helpers.codex:249`, called from `X86_64IO.codex:338`), and it reads `tier1-block-rodata-offset` (256) and `tier2-rodata-offset` (288) -- **both of which ARE in `init-rodata`**. So no live path indexes a missing table, and the absent-rodata story explains none of the measured failures. **The first search that produced this correction was itself wrong and proved nothing**: I grepped `emit-cce-print-helper`, a name that does not exist, got zero hits, and had to redo it against the real name before the claim stood. **WHAT SURVIVES, all still measured and unchanged:** the foreword `from-unicode`/`to-unicode` round-trip every tier exactly; the builtin pair round-trips ASCII and nothing else; it is LOSSY, since `[192]` and `[8364]` both yield `192 128`; and tier-1 print is half right, `code-to-char 192` correct and `code-to-char 592` printing `>`. **WHAT IS NOW UNKNOWN:** the cause of those failures. The dead `emit-cce-to-utf8-helper` and the three never-concatenated tables are a real but SEPARATE finding -- abandoned code referencing offsets for tables nobody ever built -- and they are not why the builtins are broken. **The ruling''s `__cce_print` half therefore targets dead code**; its builtin half may well still be right, but the justification under it has changed, so the re-derivation comes before any edit. **RE-DERIVED, AND THE CAUSE IS NAMED (blu, 2026-08-25): `char-to-text` TRUNCATES EVERY MULTI-BYTE CCE CODE TO ITS LOW BYTE.** `emit-char-to-text-builtin` (`X86_64Builtins.codex:269`) allocates 16 bytes, stores the length as the immediate **1**, and writes the code with a single `mov-store-byte`. It never consults `cce-encode-length`. So a Char whose CCE code needs two bytes becomes a one-byte text holding `code mod 256`. **Measured against seed 7AF7CEF5**: `cce-encode-length` answers 2 for CCE 192, 592 and 2000, while `text-length (char-to-text (code-to-char c))` answers **1** for all three and the stored unit is 192, **80** and 208 -- the low bytes. `code-to-char` is NOT at fault: `char-code (code-to-char 592)` answers 592 exactly, so the Char carries the full code and the Text step loses it. **THIS CLOSES THE TIER-1 PRINT SYMPTOM EXACTLY, which is what makes it the cause rather than another candidate.** CCE 592 truncates to byte 80; byte 80 is a single-byte CCE unit; `cce-to-unicode-table[80]` is 62; and 62 is `>`, which is the character the row above measured. The chain predicts the observed glyph with no freedom left in it. **AND IT INVALIDATES FIXTURES, INCLUDING MINE.** Every text this row built with `char-to-text (code-to-char n)` for a multi-byte n was malformed at construction, so any conclusion drawn from those texts measured the truncation and not the thing under test. **STILL OPEN, NOT EXPLAINED BY THIS:** `unicode-bytes-to-text [233]` builds its two units WITHOUT `char-to-text` and `text-to-unicode-bytes` still answers 0 on the result, so the builtin round-trip has at least one defect of its own and must be re-measured on correctly built text before anything is concluded about it. **The fix is contained** -- encode per `cce-encode-length` and set the length to match -- but it lands in `X86_64Builtins.codex`, which reek claimed for the sized-vector repair, so it sequences with that claim rather than racing it. **THIRD CORRECTION, SAME DAY, AND THIS ONE SPLITS THE ROW INTO THREE SEPARATE DEFECTS (blu, 2026-08-25). THE HEADLINE "THE PAIR ROUND-TRIPS ASCII AND NOTHING ELSE" IS WRONG.** It was measured with the text in one position and is an artifact of that position. **DEFECT A, POSITIONAL: `text-to-unicode-bytes` answers 0 whenever its input text is the TOPMOST allocation.** Control, same seed `7AF7CEF5`, same program, two arms differing only in whether one small list is allocated after the text: topmost answers `0` for u=233, 1072, 192 and 8364, while buried answers **233, 1072, 192** correctly. ASCII (u=65) is right in both, which is why every earlier reading looked like an ASCII-only pair. This is the `list-push` topmost/extend/copy aliasing shape that `DevelopersGuide.md` documents and that `builtin-alloc`''s push-topmost and push-buried arms exist for, reaching a builtin that should not have it. **DEFECT B, TIER 2, AND IT IS THE ONLY SURVIVING LOSSINESS: even BURIED, u=8364 answers 192**, the same as u=192, and `unicode-bytes-to-text [8364]` emits the units `192 128`, identical to `[192]`. So the ENCODE half collapses a tier-2 code point onto a tier-1 one; that half of the original row stands, narrowed from "all non-ASCII" to "tier 2". **DEFECT C is `char-to-text` truncation**, recorded above, and it is independent of both. **WHY THIS TOOK THREE CORRECTIONS, WHICH IS THE PART WORTH KEEPING.** Every wrong version came from a probe with ONE arm: one helper name assumed to be the live one, one text position, one construction route. The version that finally held is the one with a CONTROL that differs in exactly one thing. A single-arm probe on a positional defect does not report "sometimes"; it reports whichever position it happened to use, with total confidence, and reads as a property of the builtin. **NOTHING HERE IS SAFE TO ACT ON UNTIL DEFECT C LANDS**, because a fixture built with `char-to-text` is malformed and any arm over multi-byte text built that way measures the truncation instead of its subject. **FOURTH CORRECTION, 2026-08-25 (blu), AND IT RESCOPES DEFECT C FROM A PATCH TO A CONTRACT DECISION. THE "THE FIX IS CONTAINED" SENTENCE ABOVE IS WRONG.** Encoding per `cce-encode-length` is correct for a Char that means a CCE CODE POINT, and `char-to-text` is very largely not used that way. **Census, whole tree, `codex/` and `apps/`, 3,648 files walked: 361 call sites, of which 169 are byte-identity shape** (`char-to-text (char-at s i)` and the bare-byte forms). Those loops walk a text one BYTE at a time and rebuild it, and they are correct today only because the builtin is an identity on 0..255. Under the proposed fix every framing byte at or above 128 re-encodes to two bytes and the rebuilt text is corrupt. **THE COMPILER ITSELF IS ON THAT PATH, IN FOUR PLACES:** `IRTextEmitter.codex:76` (`ir-quote-char`), `CodexEmitter.codex:804,811` (`escape-one-char`, `escape-char`), `ChapterScoper.codex:123-125,451-453` (slugify and cite-key) and `Lexer.codex:299-306` (`decode-escapes-loop`). `ir-quote-char` runs over EVERY string literal in text mode, which the gate compares. **MEASURED, NOT PREDICTED.** `codex/test/validation-rules.codex:57,61` hold a three-letter and a one-letter Greek string literal, Greek being tier 1, and they are the only multi-byte literals in the tree. Compiling that chapter to IR CCE against the DEPOT seed `E0347775284B1048` emits 74,719 bytes containing exactly 8 bytes at or above 128: `199 129 199 130 199 131` at offset 64,448 and `199 129` at 65,117, which is three and then one two-byte CCE unit, lead 199 with continuations 129/130/131. Each of those six bytes was rebuilt one at a time by `ir-quote-char` through `char-to-text (code-to-char c)` with c in {199,129,130,131}. `cce-encode-length` answers 2 for every one of them, so the narrow fix turns a six-byte literal into twelve bytes of garbage and moves text-mode output. **THE FIRST CENSUS WAS WRONG AND THE CONTROL CAUGHT IT:** a `codex/**/*.codex` glob reported 67 sites and silently missed all thirteen compiler ones listed above; a real recursive walk found 361. Quote the walk, not the glob (L-COUNT, and the negative-width half of check-your-own-instrument). **THE ASK.** `char-to-text` cannot serve both classes: for a value in 128..255 the byte class needs one byte out and the code-point class needs two, and no single behaviour satisfies both. So the question is which contract the builtin has, and it is a design decision rather than a patch: (a) leave it byte-level and give the code-point class a separate builtin, (b) make it code-point-level and route the 169 byte-identity sites onto a byte primitive such as `raw-bytes-to-text`, or (c) split it into two named builtins and retire the ambiguous one. **NOT STARTED, DELIBERATELY.** Landing the narrow fix reds the gate on `validation-rules` at best and corrupts literal round-tripping silently at worst. **L-CONSTRUCT CUTS THE OTHER WAY HERE:** the tree's near-total absence of multi-byte literals is what makes the narrow fix LOOK safe, and the one chapter that does spell them is what makes it measurably unsafe. **THE ASK ABOVE IS ANSWERED AND DEFECT C IS BUILT AND ON MAIN. THE ROW READ "NOT STARTED, DELIBERATELY" UNTIL 2026-08-26 AND HAD BEEN WRONG SINCE 19579.** The contract decision was taken as **option (a): `char-to-text` STAYS BYTE-LEVEL and the code-point class gets its own builtin, `char-encode`.** That is why the 169 byte-identity sites, the four compiler sites named above (`IRTextEmitter`, `CodexEmitter`, `ChapterScoper`, `Lexer`) included, are deliberately untouched and must STAY on `char-to-text`: under (a) they are correct, and a future agent who "finishes the migration" by moving them to `char-encode` reintroduces exactly the corruption this row measured. **What landed:** 19579 the `char-encode` builtin; 19588 its guard `codex/test/ops/char-encode-bands`, 12 arms; 19611 arms for the five plugs that can be verified on this box (python, javascript, zig, csharp, wasm); 19636 the foreword's 14 encode-meaning callers, which FIXED A LIVE DEFECT; 19672 the remaining 27, after which the encode-meaning shape is gone from the tree. **RE-MEASURED 2026-08-26 rather than carried forward (L-COUNT), and with the control the negative needs (check-your-own-instrument):** a real recursive walk of `codex/` and `apps/`, 3,650 `.codex` files excluding `build-output`, finds **0** sites of `char-to-text (code-to-char (from-unicode ...))`, and the control pattern `code-to-char (from-unicode ...)` finds **36**, so the zero is a real zero and not a dead pattern. A first attempt at this census matched `char-to-text (code-to-char ...)` WITHOUT the `from-unicode` and reported 76: those are the byte-identity sites option (a) keeps, so the too-WIDE pattern manufactured a false positive exactly as the too-narrow one manufactures a false negative. **THEREFORE THE "NOTHING HERE IS SAFE TO ACT ON UNTIL DEFECT C LANDS" HOLD ABOVE IS DISCHARGED, and defects A and B are actionable** (blu, 2026-08-26, per Damian's re-ruling that day via red: blu takes the open compiler bugs). **WHAT REMAINS OF C IS NOT IN THIS ROW: it is CORE-8's residue** in `codex/foreword/core/core-backlog.md`, the call-site policy for `from-unicode` answering -1. The JSON half is fixed (19662); the encoders still accept -1 outside JSON and emit unit 255. **A AND B RE-MEASURED AGAINST THE CURRENT COMPILER, 2026-08-26 (blu), seed C45E5825, kernel digest read off the compile rather than assumed.** Every measurement in this row above predates defect C, so it was taken through an encoder that truncated multi-byte codes; re-measuring first is L-SAMEVER, and it moved the picture. **The oracle control passes on all six values** (u=65, 233, 1072, 192, 960, 8364 round-trip through `from-unicode`/`to-unicode` exactly), and so does a stronger control this row did not have: **`char-encode` and `cce-decode-at` AGREE on all six, tier 2 included**, with `text-length` equal to `cce-encode-length` every time. So the foreword surface plus the new builtin is correct end to end, and every failure below is the hand-rolled helper pair. **DEFECT A IS CONFIRMED AND IT MASKS THE OTHERS.** Topmost, `text-to-unicode-bytes` answers **0** for 233, 1072, 192, 960 and 8364 alike; burying the text under ONE later allocation answers 233, 1072, 192 and 960 correctly. That single allocation is the only difference between the two arms. So the pair does NOT "round-trip ASCII and nothing else": it round-trips tier 0 and most of tier 1 fine, and the ASCII-only reading was the aliasing talking. **DEFECT B IS CONFIRMED AND IS INDEPENDENT OF A**, since it survives burying: `unicode-bytes-to-text [8364]` yields units `192 128`, byte-identical to `[192]`, and the buried decode of the 8364 text answers **192**. The reverse half is also non-canonical below that: `[233]` yields `192 169` where `from-unicode` mandates the single tier-0 unit 97, and `[1072]` yields `196 176` against 113. **TWO FURTHER DEFECTS IN THE FORWARD HALF, NOT PREVIOUSLY RECORDED, AND THE SECOND IS NEWLY REACHABLE BECAUSE DEFECT C'S FIX MADE IT SO.** (i) The 2-byte tier-1 decode is wrong at the LOW end of the range and right above it: bytes `193 128` (cce 192, foreword-decoded 192) come back as **256**, while `199 144` (cce 592) comes back as 960 correctly, so this is a base or boundary error in the tier-1 lead handling rather than a dead table. (ii) **A 3-byte tier-2 unit is not readable at all**: `char-encode` now emits `233 168 144` for cce 41616, which `cce-decode-at` reads back exactly, and `text-to-unicode-bytes` returns **two** values whose first is 1640, so it mis-frames rather than refusing. Before defect C landed nothing in the tree could produce a 3-byte unit, which is why this arm has never been exercised (L-CONSTRUCT, a third time on this surface). **WHAT THIS SETTLES FOR THE RULING.** Repair 1 was to make the absent rodata tables real; the 2026-08-25 correction already showed that story explains none of the failures, and the two forward defects above are arithmetic and framing in the helper, not a missing table. Repair 2 was to delete the hand-rolled helpers and route both builtins through the foreword, and the FORE-OK control above is direct evidence that the destination is correct on every value the helpers get wrong, tier 2 included. Damian ruled repair 2 on the since-retracted mechanism and red is re-presenting; this is the measurement that re-presentation wants, and it is recorded here rather than acted on. Still seed-affecting. Probes are `cce23probe` and `cce23probe2` in blu's scratchpad, four arms each with the oracle and the buried control as the two controls. |
| COMPILER-24 | **`==` on a RECURSIVE variant crashes the compiler.** `Wrap Leaf == Wrap Leaf` over `Nest = \| Leaf \| Wrap (Nest)` dies in `alloc-temp+0xAF` with `!EXC=06`, invalid opcode, compiling for x86-64 against seed 7AF7CEF5 (fester, 2026-08-25). **The control is the same type with no `==`, and it compiles clean**, so it is the comparison and not the declaration. `emit-eq-op` sends a `SumTy` operand to `emit-sum-structural-eq`, which inlines a field compare per constructor through `emit-eq-op` again (`X86_64.codex:2985`); a field declared at the sum's own type re-enters with the same type and nothing bounds it. Legal source, no diagnostic, a crash rather than a refusal. The wasm plug emits one function per variant type and a self-call, so it answers this shape, which means there is currently no oracle to grade a recursive `==` against. **THE CRASH IS FIXED ON MAIN 2026-08-26 (blu, main 19845, seed 591EEA7B) BUT THE SHAPE IS REFUSED RATHER THAN SUPPORTED, AND THAT DISTINCTION IS THE WHOLE OF WHAT IS LEFT.** New diagnostic **CDX2097 RecursiveStructuralEq**, raised from `emit-eq-op` AND `emit-neq-op`, which carried the identical recursion and which this row did not name. Reproduced first against seed 0D4724DD with the row's own control: the subject died at `alloc-temp+0xAF`, exit 4, and **retrying at 8192MB crashed identically, so it was never heap exhaustion**; the same declaration with no `==` compiled clean. **WHY IT IS A REFUSAL AND NOT A FIX.** Structural equality on a recursive type has to recurse at RUNTIME, and no inlining bound is correct, because any bound compares only part of the value -- so there is no version of the current design that works. The wasm plug's answer (one function per variant type, self-call) needs machinery x86-64 does not have: `emit-all-defs` walks a list of IRDefs and the code buffer is append-only per function, so nothing can emit a function mid-expression. Doing it here needs a pre-pass that synthesises an eq helper per recursive sum, a registry, and forward calls through the existing `st-add-deferred-patch` machinery. **That is a design change and it is NOT done; it is the open half of this row.** Detection is a bounded walk of the constructor field graph carrying a visited-name list, so **mutual recursion through a second variant is caught too** and is not a second defect waiting; a fuel of 64 answers "recursive" rather than looping if it is ever exhausted, which is the safe direction. Guarded by `codex/test/errors/recursive-eq` expecting 2097, and it fails on the compiler it is meant to catch, since that program raised nothing and crashed instead. R-COST measured rather than inspected: gate 407.3s against 434.0s for the previous seed's final gate and `check-errors` 22.7s against 23.9s, so the per-`==` walk costs nothing measurable. | **THE OPEN HALF IS DONE AND THE SHAPE IS SUPPORTED RATHER THAN REFUSED (blu, 2026-08-27, Damian's ruling that morning: "if it is valid syntax then yes").** CDX2097 is GONE from the tree, code and registry both, and `codex/test/errors/recursive-eq` is replaced by `codex/test/recursive-eq` with an `.expected`, which is the same shape one direction over: a refusal test that no longer refuses is not a weaker test, it is a test of a thing that no longer exists. **The design is the one this row named as needed, minus the machinery it predicted.** No pre-pass registry and no forward-call plumbing were required, because a helper is synthesised as an ordinary two-parameter `IRDef` and appended to the definition list, so `emit-all-defs` gives it the standard frame and `emit-call-to` already patches call targets by NAME at finalize, forward references included. `arg-regs` is `[rdi, rsi, ...]`, which is exactly what `emit-helper-call-2-regs` loads, so not one line of calling-convention code exists in the change. The helper's own `==` on the recursive field reaches `emit-eq-op`, takes the same recursive arm, and emits a call to ITSELF: the recursion moves from emit time to RUNTIME, where the data terminates it, which is the whole of why no inlining bound was ever correct. **The collection walk mirrors the INLINER, and that is the part that is easy to get wrong.** Collecting helpers from the `==` SITES alone is insufficient: `emit-sum-fields-eq` calls `emit-eq-op` again per field, so a NON-recursive sum holding a recursive one reaches the helper arm too, and the miss is a call to a function nobody emitted. So the walk pushes a request at a recursive sum and stops, and descends through a non-recursive one. **THE TRAP, and it cost the session's only crash: a COMPUTED call target does not survive `emit-all-defs`.** `emit-call-to` stores the target name in `cp-targets`, one of the eleven pre-sized accumulators that are pushed IN PLACE and carried across the per-function `__heap-save`/`__heap-restore`. Every pre-existing caller hands it a string LITERAL -- `"__str_eq"`, `"__list_cons"` -- which lives in the constant pool and cannot be reclaimed; `"__eq_" & sn` is the first computed one, was allocated on the per-function bivy, and was freed the moment that function finished. The accumulator invariant covers the LIST and says nothing about the STRINGS it points at. It surfaced as `error CDX2040: Unresolved call to` followed by mojibake, and with the helper also being emitted it crashed the compiler outright in `offset-table-lookup-probe+0x76`, `!EXC=06`, `RDI=0`. `deck-record ("__eq_" & sn)` is the one-word fix. **The split that found it is worth keeping: stub the synthesis to return no defs while leaving the collection and the call site alone.** The crash vanished and the garbage NAME appeared in a diagnostic, which named the defect outright; reasoning about the crash site had produced three wrong theories first (a full offset table, the `__` builtin namespace, a deck-extent lifetime), and the diagnostic settled it in one run. **Verified against a compiler built from this source (`D23EFA0A1A6868F4`): 11 of 11 rows, both directions.** `Wrap Leaf == Wrap Leaf` eq, `Wrap (Wrap Leaf) == Wrap (Wrap Leaf)` eq, `Leaf == Leaf` eq, `Wrap Leaf == Leaf` ne, `Wrap (Wrap Leaf) == Wrap Leaf` ne, and `/=` correct in both directions. The `Chain = | End | Link (Integer) (Chain)` rows are the ones that make it evidence rather than a function that answers True: `Link 1 (Link 2 End) == Link 1 (Link 3 End)` is `ne`, which a helper comparing only the recursive field would get wrong, and `== Link 1 End` is `ne`, which one ignoring shape would. **What is NOT exact, stated rather than discovered later:** a field declared at a TYPE PARAMETER is compared by POINTER, because the helper is keyed on the sum NAME and carries declared field types with no instantiation to substitute. That is what the inlining path already does when a comparison site carries no type arguments, and what the wasm plug does. A generic recursive sum instantiated at `Text` therefore compares its parameter field by pointer; that shape did not compile at all before this, so it is a new answer and not a changed one, and making it exact means keying helpers on name-plus-arguments, which is not done. **MEASURED LIVE AT HEAD AFTER COMPILER-34 (blu, 2026-09-01, seed 278D8D7F, main 20888), AND IT IS A SILENT WRONG ANSWER RATHER THAN A REFUSAL.** `Box a = \| Empty \| Cell (a) (Box a)` compiles clean at exit 0 with no diagnostic, and `Cell "12" Empty == Cell (integer-to-text 12) Empty` answers `ne`. The one-variable control is the same shape with the field declared at a CONCRETE type, `Named = \| Nil \| Node (Text) (Named)`, whose `Node "12" Nil == Node (integer-to-text 12) Nil` answers `eq`; and `"12" == integer-to-text 12` answers `eq` on its own, which is the instrument check that the runtime-built text is a SEPARATE ALLOCATION carrying equal content. A literal on both sides would have proved nothing, because a pooled literal compares equal by pointer for the wrong reason. `Cell 1 Empty == Cell 1 Empty` is `eq` against `Cell 2 Empty`'s `ne`, consistent with a raw word compare on an immediate. **COMPILER-34 could not have moved this**, and the resting-state guess that closing this row might be a measurement rather than a fix was wrong: 34 changed WHEN a helper is derived, not how field types are instantiated. The mechanism is two lines. `gen-eq-def` (`Ast/Desugarer.codex:810`) builds the helper declared type from a bare `ANamedType tname` with no type arguments, so inside the helper a parameter field has no instantiation; and `type-name-of` (`IR/Lowering.codex:677`) answers `n.value` for a `ConstructedTy (n) (args)`, DISCARDING `args`, so `lower-eq-dispatch` has nothing to key on and falls to `IrBinary OpEq`. **The type arguments are present and resolved at the call site**, which is what makes this tractable rather than a rewrite: the candidates are monomorphising helpers on name-plus-arguments, or dictionary-passing one eq function per type parameter. Neither is started, and there is no `__eq_Text` to pass today, so the dictionary route needs a wrapper for the primitive cases. Not taken on 2026-09-01 because COMPILER-28 carries Damian's PRIORITY; **that condition EXPIRED the same day, COMPILER-28 having been verified closed and its stale header cleared.** **DESIGN SETTLED 2026-09-01 (blu), host-side under the release pin, NO CODE WRITTEN. DICTIONARY-PASSING, AND THE ANSWER IS THAT THE TREE ALREADY DOES IT FOR SHOW AND ORD AND EQ IS THE ONE THAT NEVER GOT IT.** **Monomorphising on name-plus-arguments is RULED OUT, and cheaply:** there is no general monomorphiser in the tree, and the only "one function per concrete type" idiom is `synth-specialized-methods` (`Ast/Desugarer.codex:1463`), which is driven by an explicit `instance` declaration at desugar time. A generic sum has no such driver, so monomorphising needs an instantiation-collection pre-pass and a registry -- **exactly the machinery the closed half of this row recorded as NOT needed and deliberately avoided** -- and it multiplies helpers per instantiation. **EVERY PIECE THE DICTIONARY ROUTE NEEDS IS ALREADY IN THE TREE, WITH A WORKING PRECEDENT:** (1) dictionary-passing itself is `lower-constrained-apply` and `lower-derived-apply` (`IR/Lowering.codex:465-471`, `:488-493`), which derive a dictionary name from the type, look it up, build `IrName dict-rn` as a VALUE and `IrApply` it ahead of the real argument; (2) **the primitive wrappers this row says do not exist DO exist for the other two classes**, `prim-show-wrapper` and `prim-compare-wrapper` (`Ast/Desugarer.codex:1225`, `:1237`), and `prim-eq-wrapper` is a direct copy of the compare one returning `Boolean`; (3) passing a named top-level function as a value already works, `codex/test/lang-smoke.codex` hands `my-combiner` to `list-fold-indexed`, so no closure or calling-convention work exists in this change; (4) `TypeDef` already carries `type-params : List Token` (`Syntax/SyntaxNodes.codex:151`), so `gen-eq-def` has what it needs to add one dictionary parameter per type parameter. **THE ONE HAZARD, AND IT IS NEUTRALISED BY MAKING AN INCONSISTENT LINE CONSISTENT.** An `__eq_Text` wrapper cannot simply be added, because `lower-eq-dispatch` (`:665`) probes `"__eq_" & tn` for EVERY type and would then route every plain `Text == Text` through a CALL instead of the inline `IrBinary OpEq`, which is a regression on the most common comparison in the compiler. **Its two siblings already guard against exactly this and it does not:** the compare dispatch (`:441`) and the show dispatch (`:641`) both read `if text-length tn == 0 | is-primitive-type-name tn then ErrorTy else lookup ...`, and `lower-eq-dispatch` is missing the `is-primitive-type-name` clause. Adding it makes the three consistent and lets the primitive dictionaries exist. **DO NOT CHANGE `type-name-of` (`:678`) to stop discarding `args`**, which is the obvious-looking repair: it has TEN callers (`:232`, `:234`, `:264`, `:440`, `:465`, `:476`, `:489`, `:509`, `:640`, `:664`) and every one of them is a NAME-keyed lookup that wants the bare name. `lower-eq-dispatch` should read `args` off the resolved `ConstructedTy` itself. **THE EDIT LIST:** `prim-eq-wrapper` beside its two siblings; `gen-eq-def` (`:805`) to build `tref` WITH its type parameters and to prepend one `p -> p -> Boolean` parameter per type param; `gen-eq-body` to compare a field declared at a type PARAMETER by calling that parameter's dictionary rather than `==`, with the recursive self-call forwarding the dictionaries; and `lower-eq-dispatch` to gain the primitive guard and to pass one dictionary per resolved type argument, mirroring `lower-derived-apply`. **NOT WRITTEN DELIBERATELY:** this touches the desugarer and lowering and cannot be run under the pin, and an unverified shelved CL that sits through a release is how a fix gets landed on trust later. The design is the deliverable; the code wants a box and the 11-row corpus in the closed half above as its judge, plus a new generic row (`Box a` at `Text`, the `Cell "12" Empty` case) which is the one this half exists for. **CORRECTION, SAME DAY (blu): THE DESIGN ABOVE NAMES THE WRONG LAYER, OR AT LEAST CANNOT BE SHOWN TO NAME THE RIGHT ONE, AND THE CODE FOR IT WAS WRITTEN AND THEN REVERTED RATHER THAN SHELVED.** Writing it surfaced a mechanism neither this row nor that design accounts for: **there are TWO helper generators and they mint the SAME NAME.** The desugarer's `gen-eq-def` emits an `ADef` called `__eq_<T>` for any type that is `deriving Eq` OR self-recursive (`Ast/Desugarer.codex:663`), and the x86 emitter independently synthesises an `IRDef` of exactly the same name from an `EqHelperReq` (`Emit/X86_64.codex:3257-3274`, `eq-helper-name` at `:2912`). **The emitter's is the one the closed half above describes** -- "an ordinary two-parameter IRDef appended to the definition list" is `eq-helper-def` verbatim, `params` being `__eqa` and `__eqb` at `:3263-3266`. **AND THE EMITTER SIDE ALREADY HAS WHAT THE DICTIONARY DESIGN WAS INVENTED TO DELIVER.** `emit-eq-op` binds the sum's type arguments as `args` (`:2920`) and `emit-sum-structural-eq` threads a `type-args` list into `emit-sum-full-eq`, where **`subst-field-type` (`:2981-2985`) already replaces a `TypeVar (id)` with `list-at args id`.** That is instantiation, already written, already working -- on the NON-recursive path. On the recursive path (`:2921-2922`) the branch calls `eq-helper-name (sn.value)` and **`args` is bound and never used**, and `eq-helper-def` builds the helper from the declared ctors with no `type-args` at all, so inside it the parameter field is still a `TypeVar` and falls to a raw word compare. **That is a complete and sufficient explanation of `Cell "12" Empty == Cell (integer-to-text 12) Empty` answering `ne`, and it is three lines from `subst-field-type` rather than a new dictionary protocol.** **WHAT IS NOT SETTLED, AND IT IS ONE MEASUREMENT:** whether the desugarer's `__eq_Box` or the emitter's `__eq_Box` is the one that actually runs for a self-recursive generic sum, since both are produced for the same type and only one name can win. Until that is known, neither the dictionary design above nor the `subst-field-type` route can be called correct. **The disambiguating run is cheap:** compile the `Box a` subject with `-Log`, and look for whether the desugarer's ADef or the emitter's synthesised IRDef reaches the wire, or stub one and see which answer moves -- the same stub-and-see split that found the computed-call-target crash recorded in the closed half. **Do that before writing a line of either design.** **DONE, AND THE MECHANISM IS MEASURED RATHER THAN READ (blu, 2026-09-01). FOUR MODELS DERIVED BY READING WERE EACH REFUTED; TWO INSTRUMENTED RUNS SETTLED IT.** The instrument is `docs/Test/Active/EqGenericDiscriminator.codex`, built so that a specific outcome REFUTES rather than confirms, plus a temporary `st-add-info` trace in `emit-eq-op` and `emit-sum-fields-eq` (written, used, reverted; it emits no code, so it cannot perturb what it measures). **THE LINE THAT SETTLED IT:** `EQTRACE ty=Ctor:Pair[Text] resolved=Sum:Pair[Ctor:a]/ctors1 recursive=no`. **TWO FACTS, AND TOGETHER THEY EXPLAIN WHY EVERY READING LOOKED CORRECT.** (1) **The SITE type carries the right instantiation, `Pair[Text]`, and `resolve-to-sum-with-defs` DISCARDS it**, answering args of `Ctor:a`, the declaration's own formal parameter. (2) **The `EQFIELD` arm of the trace NEVER FIRED, and that silence is the other half:** it was gated on a field whose declared type is a `TypeVar`, and no field ever was one. **A type parameter in this tree is a `ConstructedTy "a" []`, a NAMED reference, not a de Bruijn `TypeVar`** -- and `subst-field-type` (`Emit/X86_64.codex`) matches ONLY `is TypeVar (id)`. So substitution is a SILENT NO-OP for the only case it exists to serve, the field stays `Ctor:a`, and `emit-eq-op` falls through to `emit-comparison`, a word compare, which is the pointer comparison. **Every function on that path is individually correct; the join between them is what fails, which is exactly why four static readings could not find it.** **THE FIX SHAPE:** keep taking the args from the SITE (`ty`), which the existing code already does and which one of the refuted models would have BROKEN by preferring the resolved type's formal parameters; and make substitution match a type-parameter reference **BY NAME**, zipping the resolved type's formal args against the site type's actual args. Not by index. **A REFUTED MODEL WORTH NOT REPEATING:** feeding `resolved`'s args into `emit-sum-structural-eq` was written, gated green, measured, and moved NOTHING (all four arms byte-identical), then reverted -- because `resolved`'s args are `[a]` and substitution ignores them anyway. **THE ROW IS SPLIT: what remains HERE is the non-recursive generic sum only.** `Box` and `Named` produce NO trace at all, so a RECURSIVE sum never reaches `emit-eq-op`; that is a separate defect on a separate path and is now COMPILER-43. The pair is diagnostic: `Named`, recursive with a CONCRETE field, is CORRECT, while `Box`, recursive with a parameter field, is wrong. |
| COMPILER-34 | **CLOSED 2026-08-31 (blu). `==` on a self-recursive variant is structural on every backend and every plug.** The fix is NOT the hoist this row originally asked for. `lower-eq-dispatch` (`IR/Lowering.codex`) already rewrites `==` into a call to `__eq_<T>` whenever that definition EXISTS, which is exactly why `deriving Eq` reached every plug while the x86-64 emitter's recursive helper did not. So the cheaper fix is to make the desugarer the SINGLE PRODUCER: `push-derived-for-type` (`Ast/Desugarer.codex`) now derives `Eq` when `deriving-has ... "Eq" | td-self-recursive td`, so a self-recursive sum gets a real source-level `__eq_<T>` whether or not it asked, the checker types it, and no `IrEq` on that type ever reaches a backend. `td-self-recursive` walks the type expression structurally rather than name-matching the field head, so `List Tree` and `Tree -> Tree` count as recursion. **The name collision this row warned about cannot arise: the condition is an `|`, so a type that both derives Eq and is recursive yields exactly ONE definition.** **The x86-64 helper (`Emit/X86_64.codex`, `X86_64Chapter.codex:1150`) was deliberately NOT removed.** With no `IrEq` left on these types its request walk collects nothing, so it is inert rather than a second producer, and deleting a working path in the same change that introduces its replacement is L-FALLBACK. Removing it is a separate, later CL. **MEASURED, and the attribution is CONTROLLED rather than assumed** (which is the COMPILER-32 mistake this row's author had just made): `codex/test/recursive-eq` passes 11 of 11 on x86-64, arm64 and riscv64, and `codex/test/deriving-eq-recursive` 6 of 6 on all three. The zig plug now emits real `__eq_Nest` / `__eq_Chain` functions and calls them instead of a bare `==`, and the built binary answers 11 of 11. **The negative control is the load-bearing half:** the SAME arm64 arm on the OLD depot seed `2B69CDD2` fails with six `ne` where `eq` is expected, against the new seed `278D8D7F` passing, one variable. That is the failure `recursive-eq.no-cross` had recorded and the sidecar is DELETED with this CL, since its stated reason ("arm64 answers ne where eq is expected") no longer holds and a `.no-cross` is never the right home for a target gap anyway. Gate green, hard fixed point in one pass. Steve Howell's issue 97 can be closed; the zig side he offered to do himself is closed by this instead. |
| COMPILER-25 | **`emit-zero-region` emits its prefixes in the wrong order, so hardware zeroes HALF the region.** | `X86_64Boot.codex:2317` emits `[72, 243, 171]`, which is REX.W then F3 then AB. REX is ignored unless it sits immediately before the opcode, so silicon executes `rep stosd` while rcx holds a QWORD count: `qwords * 4` bytes are written and the upper half of the region keeps whatever it held. The `movsq` at `Boot:3047` uses the correct order `[243, 72, 165]`, which is both the proof of intent and the shape of the one-line fix (F3 before REX.W). Found by a read-only instruction census over `Emit/` (red, 2026-08-25) and verified against source; no runtime symptom is attributed to it yet, so it is latent, presumably because the regions it zeroes are already zero or their tail is never read before first write. That presumption is UNMEASURED: whoever takes this should first find the callers and answer whether a half-zeroed region is reachable with dirty memory. **FIXED AND ON MAIN 2026-08-26 (blu, main 19828, seed 0D4724DD).** One line, `[243, 72, 171]`, the order the `movsq` at `Boot:3047` already used. **The presumption this row asked to be tested is now ANSWERED for the bed and still OPEN for metal, and the bed half is the finding.** Ten call sites, all in `X86_64Boot`: the process page tables, the process, channel and identity tables, the identity key, the TSS and the AP report area. The page-table one is the sharp one, because `emit-build-process-page-tables` zeroes the region and then fills only SOME entries, so an unfilled slot holding a stale Present bit maps garbage rather than faulting. **NO BED ARM CAN EVER SEE THIS.** codex-vm backs guest RAM with `VirtualAlloc(MEM_COMMIT)`, which Windows zero-fills, so every region `emit-zero-region` touches is already zero in the bed and half-zeroing it is invisible: the bed has a blind spot shaped exactly like the production defect (L-BEDTRUE, one level down). There is no per-process reuse to dirty it either -- `emit-build-process-page-tables` has exactly ONE caller, at a fixed address, at boot. On metal the region holds what the firmware left, and **whether the fill loops overwrite the upper half is NOT measured**, which is the half of the original presumption that survives and the reason this stayed latent. **So no arm was written, deliberately: in the bed it could only pass vacuously** (L-FALSIF). **The instrument is a byte census of the artifact, and it is what proved the fix reaches the wire rather than only the source:** count `48 F3 AB` against `F3 48 AB` in a seed. Old seed 9 and 0; first-pass Sut 9 and 0, which is the two-pass signature rather than a failure, since this alters the EMITTED RUNTIME so the first Sut carries the code that emits the bytes without carrying them in its own body; converged seed 0 and 9, confirmed again by printing the seed from the DEPOT after the copy-up. That census is the runner this row does not otherwise have. |
| COMPILER-26 | **A record's implicit type parameters are derived by the IR TEXT EMITTER at serialisation time, not carried on the AST -- so every consumer of the AST that is not the wire sees an incomplete type** | Absorbed from Steve Howell's PR 90 (his public numbering COMPILER-20; renumbered on absorption, the internal register minted 20-25 independently the same day). Citations re-verified against head by red 2026-08-25: `codex/foreword/core/Sort.codex:20` declares `SortPartition = record { list : List a, pivot : Integer }` with no parameter list, the AST carries `tparams = []`, and `Emit/IRTextEmitter.codex:402-406` computes the inferred list at write time with `ir-emit-tparams-text:386-388` writing explicit-else-inferred; variants the same at `:407-409`. Steve's measurement: `emit-zig-chapter` handed the front end's own ATypeDef emits a monomorphic struct whose fields still mention `a` (zig that does not compile), while the SAME emitter through the text wire emits the generic form; use-site annotations are a second instance of the class (`(record-ty ... (args (tvar 30)))` on the wire, args absent in memory). The wire is load-bearing for semantics, not just transport, and any direct AST consumer inherits the incompleteness silently. Suggested direction (his, unattempted): carry the inferred parameters on the ATypeDef where the checker learns them and let the emitter serialise what the AST holds. **FIXED AND ON MAIN 2026-08-26 (blu, main 19889, seed C3181693).** Steve's direction taken as written: the collectors move to `Ast/AstNodes.codex` as `ast-*`, and the desugarer fills `tparams` at BOTH `ATypeDef` construction sites (`Ast/Desugarer.codex:392-393`, the only two in the front end) with the same explicit-else-inferred rule the emitter applied. Filling at CONSTRUCTION rather than at each consumption site is the whole value: there are **151** `is ARecordTypeDef` / `is AVariantTypeDef` destructure sites across `codex/` and `apps/` (measured 2026-08-26, matching lines equal to matching occurrences, so no multi-hit line is hiding in the count), and one construction-site fill makes all of them correct at once, where patching consumers would have fixed the two that were looked at and left the rest. An interim attempt that called the helpers at the emitter and the type checker instead is what the corrected version replaced. **THE TRAP THIS UNCOVERED IS THE PART TO KEEP, AND IT IS NOT ABOUT THIS ROW.** A bare reference to a generic record in a signature (`-> SortPartition`) resolves because `parameterize-walk` merges the record's own parameter names with the enclosing signature's **BY NAME**, which is sound ONLY while the declaration carries no parameter list. `codex/foreword/core/Sort.codex` was relying on a name coincidence: the record's field said `list : List a` and `sort-partition`'s signature also said `a`, so the two `a`s merged. The Codex text printer normalises a signature's own tvars to `a0`, so the moment the declaration carried a literal `(a)` the names stopped agreeing, the merge silently failed, and stage2 refused `sort-partition` with **CDX2087**. Fixed by spelling the use site, `-> SortPartition a`, which removes the merge from the path rather than relying on it. **The prose at `Emit/IRTextEmitter.codex:119-126` describes that merge without naming the condition it depends on, and should not be read as a guarantee.** The IR wire is unaffected: it recovers type arguments from field tvars, a separate mechanism. **THE INSTRUMENT WAS THE PHASE SPLIT, NOT A DEBUGGER:** `text-stage1` GREEN with `text-stage2` RED says the compiler accepts the source and rejects only its own printed form of it, which localises the defect to the printer without reading any of it. Worth reaching for whenever a text-mode phase fails. **The gate reds arrived in three layers and each one moved the failure forward**, so the first red (sem-equiv dropping `SortPartition`, because the printer now spells the inferred parameter and the source did not) was progress rather than a wrong turn. Verified: full gate green 642.3 s, no skips, `app-sweep` included; seed is a true fixed point of the shipped source (workspace seed == built Sut). |
| COMPILER-28 | **CLOSED. FIXED ON MAIN 19731, 2026-08-25 (blu). DO NOT ASSIGN THIS ROW.** Re-verified at head 2026-09-01 against the SOURCE and not this row: `check-bounded-bases` (`Types/TypeChecker.codex:3229`) raises `cdx-bounded-base-not-integer` (`Core/CdxCodes.codex:129`, code 2054, `sev-error`) whenever `resolve-bounded-int-type` answers `ErrorTy`. **THE ONLY THING LEFT IS THE SECOND SWALLOW POINT, DESCRIBED AT THE END OF THIS ROW, AND IT IS DELIBERATELY OPEN AND UNREACHABLE BY THIS ROUTE.** Was: **A RANGE ON ANY NON-INTEGER BASE IS SILENTLY UNENFORCED. `Char between 0 and 255` COMPILES CLEAN AND ACCEPTS 99999.** Marked PRIORITY (Damian, 2026-08-25), **and that tag was written the SAME DAY the fix landed and was never cleared, which is the whole reason this row keeps recruiting agents to finished work.** It has now done so TWICE: the body already recorded the 2026-08-26 misassignment, and the tag sent a second agent here on 2026-09-01. **A ROW WHOSE HEADER CONTRADICTS ITS OWN BODY COSTS A FULL RE-READ EVERY TIME, AND THE HEADER IS THE HALF PEOPLE ACT ON.** When a fix lands, clear the priority tag in the same edit. **THE PAIR, one word apart, seed D9B5B170:** `take : Integer between 0 and 255 -> Integer` called with 99999 is refused, `error CDX2050: bounded parameter has bound 0..255`, no binary emitted. `take : Char between 0 and 255 -> Integer` called with `code-to-char 99999` compiles with NO error and NO warning, runs, and prints `99999`. The enforcement half is present and works; it simply never reaches this declaration. **THE MECHANISM IS NAMED AND IT IS NOT SPECIFIC TO Char.** `resolve-bounded-int-type` (`codex/compiler/Types/TypeChecker.codex:39`) resolves the base and answers `deck-record (IntegerTy lo hi mode)` for an `IntegerTy` base, the same for a `UnitTy` wrapping one, and **`is otherwise -> ErrorTy`** for everything else. So every base that is not an integer takes the ErrorTy arm, and that ErrorTy is SWALLOWED rather than raised: the bad declaration is accepted and the bound becomes decoration. Char is the proven instance; the arm is shared, so any other non-integer base is the same path and is untested only because nothing in the tree writes one. **WHY NOTHING CAUGHT IT (L-CONSTRUCT):** all 873 `between` declarations in `codex/` and `apps/` use an `Integer` base and there is not one `Char between` anywhere, so the corpus has never spelled the shape that fails. This is the same corpus blindness that hid the `char-to-text` truncation, one layer up in the type system. **WHAT IT BLOCKS:** the ruled repair for COMPILER-23 defect C is that `char-to-text` gains a bounded 0..255 domain so misuse is a compile refusal. That repair cannot be written today: `CodexType` has `IntegerTy (lo) (hi) (OverflowMode)` but a bare `CharTy` with no fields, no `BuiltinSpec` in the tree carries a bounded domain, and writing the bound anyway would produce exactly the silent no-op above. **THE OPTIONS, MEASURED.** Give `CharTy` bounds: 45 mentions, 28 in the compiler and 17 across 12 plugs. Declare the domain as a bounded `Integer` instead of `Char`: 212 `char-to-text` sites, 115 of which pass `char-at` directly and would become type errors. Carry the range in `BuiltinSpec` and check it there: contained to the compiler, no plug or caller change, but a new mechanism. Bound `code-to-char` instead: one builtin, 155 sites, and it is the single gate every code-point-to-Char conversion passes through, so it excludes -1 everywhere. **THE FIRST FIX IS SMALLER THAN ANY OF THEM AND SHOULD LAND FIRST: STOP SWALLOWING THE ErrorTy**, so a range on a base that cannot carry one is refused at the declaration instead of accepted and ignored. That is a diagnostic, not a type-system change, and it turns this from a silent hole into a loud one. **BOTH SWALLOW POINTS ARE LOCATED, AND SO IS THE THING THAT MAKES THE FIX AWKWARD.** The bound is lost twice over. First `resolve-bounded-int-type` (`Types/TypeChecker.codex:39`) answers `ErrorTy` for the Char base. Then `lint-narrowing-check` (`Types/TypeCheckerInference.codex:1151`), which is the code that would have enforced it, opens `when field-ty is IntegerTy (f-lo) (f-hi) (f-mode) -> ...` and falls through `is otherwise -> st` for everything else -- so an `ErrorTy` parameter is not merely unbounded, it is unchecked, and `ErrorTy` then unifies with anything so no downstream arm complains either. Repairing only the first would leave the second silent for any other route to a non-integer declared bound. **THE CONSTRAINT: THE WHOLE RESOLUTION LAYER IS PURE.** Every function in `TypeChecker.codex` from `resolve-type-expr` down is `... -> CodexType` with no `UnificationState` and no bag, and `resolve-type-expr` has NO caller outside that chapter, so there is nowhere in the existing path to raise from. The fix is therefore either to thread the bag through the resolution layer, which is invasive and touches every arm, or to add a separate validation walk over the declaration's `ATypeExpr` that runs where the bag already exists and reports an `ABoundedIntType` whose base does not resolve to an integer. The second is additive and changes no signature; the span is already carried on the `ABoundedIntType` node (`TypeChecker.codex:22` binds it as `(s)` and drops it). **NOTHING IN THE TREE RELIES ON THE BROKEN SHAPE, SO THE REFUSAL BREAKS NOTHING AND THERE IS NO MIGRATION LIST.** **THE NUMBER IS ABOUT 875 IN SOURCE**, and it took two censuses and a re-measure to get there. blu, 2026-08-25, `Get-ChildItem -Recurse` over `codex/` and `apps/` excluding `build-output`, matching `(\w+) between -?\d+ and -?\d+`: **875** base tokens, 873 of them `Integer` and 2 prose false-positives (`difference between`, `lands between`). red first answered **3,083**; on re-measure that was **869** by bare `Integer between` plus **2,157 copies living inside generated concatenations**, and the accompanying claim that generated files affected only the prose tally was wrong. The 875 is the honest source figure: 869 bare plus the variant forms. **The conclusion never moved and is the half the fix depends on: ZERO non-Integer bases and zero unit-wrapping bases**, which survives the correction because copies of an `Integer` base are still `Integer` bases. The generated trees carry **1,381** if anyone needs that number, and it is not the one to quote. Worth keeping as a method note: a census over a tree that contains generated concatenations of its own source counts the same declaration many times, and the tell was that two counts differed by more than 3x while agreeing perfectly on the classification. **FIXED AND ON MAIN 2026-08-25 (blu, main 19731, seed C45E5825): the first-fix above is built -- a range on a base that cannot carry one is now REFUSED at the declaration with new diagnostic CDX2054, instead of accepted and ignored.** It is the additive validation walk, not the invasive one: `check-linearity-def` already walks a def's declared `ATypeExpr` carrying `UnificationState` and raises through `add-unify-error`, and `check-all-defs` already has `tdm` in scope at the line that calls it, so no signature moved. It reuses `resolve-bounded-int-type` rather than re-deciding what counts as bounded, so the check cannot drift from the thing it checks. Verified three arms against the Sut shipped with it: `Char between 0 and 255` -> CDX2054 refused (was: accepted 99999); `Integer between 0 and 255` with 99999 -> CDX2050, unchanged; the same with 200 -> compiles, no diagnostic. **This row read "Unowned" at main head until 2026-08-26 and the fix had been on main since 19731, which sent the commander to re-assign work that was already done** (L-COUNT's shape one register over: the row, not a count, was carried forward unre-measured). **The SECOND swallow point is untouched and deliberately so:** `lint-narrowing-check` still opens on `IntegerTy` and falls through `is otherwise -> st`, which is now unreachable for this route because the declaration is refused earlier, and stays open for any other route to a non-integer declared bound. **What it unblocks is live: COMPILER-23 defect C's compile-time backstop half is no longer impossible**, so re-read this row before assuming a bounded domain cannot be declared. |
| COMPILER-30 | **`ErrorTy` is BOTH the type-failure atom and lowering's no-expectation sentinel, and the IR drops types the checker did resolve, so a plug cannot tell "the checker failed" from "nobody wrote it down"** | Steve Howell's note "Zig as the demanding customer" (2026-08-27, via Damian and red); both halves verified against source and by measurement by blu the same day. This is the named residue behind `codex/plugs/plugs-backlog.md` 1.96, where the Ada and Fortran ErrorTy arms guess a 64-bit integer. **Half one, the overload, is a source fact:** `IR/Lowering.codex:689` and `:707` (`lower-let`, `lower-let-rest`) pass `ErrorTy` as the EXPECTED-type argument to `lower-expr-at`, meaning "no expectation", and `:55` (`AFieldAccess`) does the same, while `ErrorTy` is simultaneously the atom the checker produces for a genuine type failure. One atom, two meanings, and the wire carries no marker separating them. **Half two, the fidelity gap, is measured on `codex/test/roc-fold-empty`**, which compiles clean (exit 0) and prints its expected `42`: its IR carries `(param "xs" (list error))` on the lambda parameter while THE SAME NAME inside that lambda's body carries `(name "xs" (list int-default))`, so the checker computed the element type and the IR failed to carry it to the declaration site. The same def also shows `(let "total" error ...)` against `(name "total" int-default)` two nodes later, and the empty literal itself as `(list-expr (elems) error)`. **By the discriminator Steve's note states -- did the checker compute an answer the IR failed to carry? -- this is UPSTREAM, in the compiler, not the plug's work.** Distinct from COMPILER-15, which is two specific `is otherwise -> []` sites in codegen with a residue measured as bounded at 2; this is a source-level empty literal in argument position, a third producer. Distinct from COMPILER-13, whose multi-parameter lambda defect is closed and guarded. **Note before re-measuring: `-IrUni` and `-IrCce` carry different IR since main 19558 (see COMPILER-12), so a measurement of the WIRE must not be taken from `-IrUni`; the figures above are `-IrUni` and are about what the CHECKER resolved rather than about the wire.** **THAT NOTE IS STALE AS OF blu's LIFT UNIFICATION (main 20176); measured 2026-08-27 by fester against seed `4341370C8FE5BAD6`, the two paths now carry the SAME IR and `-IrUni` is a sound wire measurement.** Not a length proxy: for four programs the `-IrCce` bytes were aligned position-by-position against the `-IrUni` characters and the map checked in BOTH directions, giving a clean bijection with zero inconsistencies (`lambda-param-type` 1153 positions / 42 distinct characters, `parametric-sum-pattern-binding` 1637, `empty-list-element-type` 1127, `bounded-int-derived-range` 1151; CCE is one code per character over this content and the single trailing byte is the newline, CCE 1). The lambda program is COMPILER-12's own discriminating subject, the one that would diverge if only the CDX path lifted: both paths now emit the lifted `__lam_0`. A stated blocker is the one sentence nobody re-tests, because its whole function is to stop people looking, so this was re-tested rather than inherited. The reserved work in this row is untouched by this note. **THE LAMBDA-PARAMETER CARRIER IS FIXED (Steve Howell's PR 93, absorbed by red in the CL carrying this edit; the design and every measurement are his).** `LambdaExpr` carries its lambda token through the parser's three construction sites, the desugarer passes `token-span` instead of `synthetic-span`, `infer-lambda` records the solved function type under that span, and `lower-lambda` asks for it through `lambda-expected-ty` ONLY when its expectation is `ErrorTy`, so the expectation-present path is untouched by construction. His measurements: matrix `error` cells 6 of 11 to 0 of 11 with case f (`Text`) recovering `text` rather than a default, case g honestly a type variable, both controls byte-identical; corpus clean 318 to 324, no program regressing, 570 of 577 emitted files byte-identical. **STILL OPEN in this row, none of it closed by the PR:** the `ErrorTy` overload itself (his explicit question upstream: should `ErrorTy` on the wire be reserved for actual type failures? -- a design call, rulings-queue shaped); the empty-list carrier (`roc-fold-empty` above); and two lambda sites still desugaring synthetic, `ForExpr`'s `map-list` lambda (`Desugarer.codex:78`, `var-tok` available) and instance-method synthesis (`:1455`, `m.name` available), left out of the PR because his matrix does not measure them. **RESERVED 2026-08-27 (red, from Steve Howell's comment on PR 93, ruled by Damian: grab his work, suspend ours): DO NOT TAKE THESE UP.** Steve reports measured fixes on his branch for the empty-list carrier (recorded under the literal's span, asked for only when the context supplies no list type; `roc-fold-empty` goes markers to match) and for the instance-method site (`token-span (m.name)`; his measurement shows `typeclass-smoke`/`typeclass-poly` refusing today, with a method lambda disagreeing with itself inside one node), plus two more in the same family: `subst-type-vars-from-arg` (`IR/LoweringTypes.codex`) learns through only its `TypeVar`/`ListTy`/`FunTy`/`TypeApply` arms while `SumTy`, `RecordTy`, `ConstructedTy`, `LinkedListTy`, `VectorTy`, `UnitTy` and `LinearTy` all land on `is otherwise -> target`, so the learning direction is blind where the applying direction (`subst-type-var-in-target`) already walks every variant; and `lower-nonempty-list` consults its elements only when the context offers NO type, so a wrong context wins unopposed (his `hamt-test` reading). All four are fixed and measured on his corpus arm with a bare-metal sweep running; they arrive as a second PR after the mirror push moves the pin, and red answered "send all four" on the PR. The ForExpr `map-list` site (`Desugarer.codex:78`) stays here, unmeasured on both sides. **THE ERRORTY OVERLOAD IS RULED (Damian, 2026-08-27 afternoon): SPLIT NOW.** `ErrorTy` on the wire is reserved for genuine type failures; a distinct no-expectation marker takes the sentinel meaning at every site that currently passes `ErrorTy` to mean "no expectation" (`IR/Lowering.codex:689`, `:707`, `:55` per the measurement above). Every plug inherits the answer; Steve Howell's second PR rebases onto the new wire and was told so on PR 93. When the split lands, `build/ir-fidelity` gains a new invariant for free: a clean compile carries zero `ErrorTy`. **THE SPLIT IS LANDED (fester, main 20316 compiler and seed, 20327 the plug arms) AND THAT FREE INVARIANT DOES NOT HOLD.** `NoExpectTy` took the sentinel meaning and `ErrorTy` kept only genuine type failures, but the promise in the sentence above was written from reading and is false in the tree. **Measured against the landed seed over all 607 programs in `codex/test`: 596 compile clean and 23 of those carry `error`, 251 occurrences of it at ONE site** -- `list-expr[2]` on an EMPTY list literal, which is `lower-empty-list`'s `is otherwise -> deck-record (IrList [] ErrorTy sp)` at `IR/Lowering.codex:1319`, Steve Howell's reserved fix and deliberately untouched here. A `let` binding one of those also records `error` as its own type, so the two sites travel together. The instrument is `build/ir-fidelity -Census` and the attribution is measured rather than read: ablating `:1319` alone to a concrete type removes both sites and moves nothing else. **`-Census -Calibrate` is the part that makes a reading worth anything** -- a positive arm that must find the atom and only in the program with no type expectation on its empty list, and a negative arm asking the same corpus for an atom outside the wire vocabulary, which must find nothing. A census that reports "no sites" is otherwise indistinguishable from a reader that cannot see. **Also worth knowing before quoting a number here: the constrained sibling is CLEAN, because `:1318` takes the `ListTy` arm and records the element type.** So the defect is an empty list literal whose type is not constrained at that point, not empty lists in general. Remaining open here: Steve's second PR, the `ForExpr` `map-list` site (`Desugarer.codex:78`, unmeasured on both sides), and the invariant itself once `:1319` is fixed. Open work, unowned. **CORRECTION 2026-08-28 (Steve, PR 93 comment 20:34Z, absorbed by root): the instance-method half of the reservation is WITHDRAWN.** He built `token-span (m.name)` and it does not hold: a method lambda is a record FIELD VALUE whose declared type supplies the expectation, so the no-expectation arm never fires there and his `typeclass-smoke` readings were UNCHANGED. The instance-method site stays unmeasured; the empty-list-carrier fix stays expected in his second PR. His replacement lead, convergent with our issue-94 reply's `Desugarer.codex:1377` pointer: `Showable-dict-Integer` carries `(args (tvar 511))` where the Boolean sibling instantiates, in a unit with no foralls at all. **THE RESERVATION IS CLOSED: THREE OF THE FOUR LANDED (Steve Howell, PR 101, absorbed by red 2026-09-01), THE FOURTH WITHDRAWN BY HIM AS MEASURED (above).** (1) The empty-list carrier: `infer-list` now files the fresh variable under the literal's span (`record-expr-type`), and `lower-empty-list` asks for it through `empty-list-element-source` ONLY when the context supplied no list type, so the `ListTy`/`LinkedListTy` arms are byte-for-byte what they were. (2) `subst-type-vars-from-arg` learns through `SumTy`, `RecordTy`, `ConstructedTy` (name-guarded, positional args), `LinkedListTy`, `VectorTy`, `UnitTy` (name-guarded) and `LinearTy`, so the learning direction walks what `subst-type-var-in-target` already walked. (3) `lower-nonempty-list` lets a witness from element 0 win over a context element type that still carries type variables, when the witness has no `error` and admits no widening (`list-elem-witnessed`). ONE DEPARTURE FROM THE PR, on R-COST grounds: his version lowered element 0 as a witness on EVERY non-empty literal, so a fully typed list lowered its first element twice (and a lambda there would lift twice); here the witness is computed only when the context carries type variables, which is the only case in which it can win, so every fully typed list is unchanged. MEASURED against a kernel built from this source (`Sut101`, unsigned, 45755F2C): `roc-fold-empty` wire `error` 6 to 0 with `(param "xs" (list error))` now `(list int-default)`, program still prints 42; `hamt-test` loses `tvar 25` (type variables with no `forall` in scope 17 to 16, three defs changed) and still matches its `.expected`; the changed compiler compiles its own source to a byte-identical binary in one pass; diagnostic census unchanged (CDX3005 7, CDX6020 13). STILL OPEN HERE: the ForExpr `map-list` site (`Desugarer.codex:78`, unmeasured on both sides); the `build/ir-fidelity` invariant "a clean compile carries zero `ErrorTy`" is now measurable and unmeasured; and a NEW instance of the family Steve names in the PR, `lower-apply-lambda-chain` after main 20706 typing `let "total"` in `roc-fold-empty` as a three-argument function on the wire (`(let "total" (fn (list int-default) (fn int-default ...)))`) while the name reads back an integer, which he reports bare metal reproduces and which refuses in his zig arm as `expected type i64, found CxFn3`; that site is COMPILER-32's and is not touched by this CL. His standing offer: a whole-IR property, "for every expression the checker resolved, does the wire carry the answer", built on his non-erasing arm, reported as one list rather than a sixth finding. |
| COMPILER-33 | **NULLARY HALF FIXED (blu, 2026-09-01, main 21348, seed E5425317); the COMPOUND half is still open and is what remains of this row.** `InstanceDef.type-name` is a single `Token`, so a compound instance head is discarded at PARSE time and a dictionary's type argument is whatever the method bodies happen to pin. **WHAT THE NULLARY HALF ACTUALLY WAS, and the plan recorded below for it was wrong in two ways, both refuted by measurement rather than argument.** The cause was ONE LINE: `synth-class-type-defs` (`Ast/Desugarer.codex:1005`) gave the Dict record a type parameter only when the class had more than one instance, while `synth-class-fields` copies the class method signatures verbatim and those mention `a` regardless. So a single-instance class produced a record type declaring `(tparams)` EMPTY and then referring to a name nothing bound, and the checker invented a fresh variable at each use: measured, `EquatableDict` carried `(args (tvar 298))` while its own rec-def said `(tparams)`. Binding it unconditionally types both dicts at `int-default`, which is exactly the outcome Steve Howell's lead was reaching for, WITHOUT his declared-type change. **(1) His lead does NOT produce the `unresolved type variable of Sortable-dict-Integer` refusal this row predicts** -- measured on this head, both fixtures compile clean and pass; what it produces is `forall 52 (SortableDict (tvar 52))` on the one dict with a superclass, which is that refusal one step short of firing and is a consequence of the unbound `a`. **(2) "add a declared type AND give the super field one" is wrong about the super field:** `Equatable` has one instance, so `EquatableDict` was nullary and the field at `:1000` was correctly applied. That hypothesis was written down and refuted before any code was written. **STILL OPEN, LATENT, NOT TOUCHED: `__super-Equatable` is declared as a bare `EquatableDict` (`:1000`) while `EquatableDict` now takes a parameter.** It compiles and both fixtures pass, so it is latent rather than live, but it is an under-applied constructor. **CENSUS RE-MEASURED 2026-09-01 (L-COUNT) and the row's numbers HOLD: 16 instance declarations and exactly ONE compound head**, over 3,790 files. A first pass reported 18; two of those are record FIELDS named `instance` in `Lwm2m.codex`, so the too-wide pattern would have filed a correction against a row that was already right. `type-name-of` still has ten callers, all in `Lowering.codex`; a careless grep folds in `zig-type-name-of`, a different function, and reports twenty. | From Steve Howell's issue 94 (2026-08-27), verified against main at head 20354 before this row was written. `Syntax/SyntaxNodes.codex:134` types the head as `Token`; `Syntax/Parser.codex:1571-1576` (`parse-instance-type-head`) answers the FIRST token inside a parenthesized head and `skip-to-close-paren`s the rest, so `instance Showable (List Integer)` arrives as the bare token `List` with its argument gone; `Ast/Desugarer.codex:1369-1380` builds the dictionary name from `token-text (id.type-name)` and emits `declared-type = []`, so the head's type never reaches the definition. MEASURED (his one-respect pair, fixtures public in his ladder repo): `instance Tellable Integer` with `tell (x) = "int"` yields `args (tvar ..)` FREE while `instance Tellable Boolean` with `tell (b) = if b then "y" else "n"` yields `args boolean` CONCRETE -- same class, same arity, same return type, differing only in whether the body pins, which is impossible if the head reached the dictionary. Explains every instance in `codex/test/typeclass-smoke.codex` without exception. His LEAD, deliberately not shipped by him: a declared-type for provably nullary heads moves the `-Integer` dicts to `int-default` with controls unmoved, but `typeclass-poly` gains an `unresolved type variable of Sortable-dict-Integer` refusal -- the one subject with a superclass, and the `__super-` field is built as an UNTYPED name expression at `Desugarer.codex:1377`, which is where that refusal plausibly lives (unmeasured). The compound half needs `InstanceDef` to carry a type expression and the parser to stop skipping: a parser and node change, argued before written. Related: the free-vs-solved wire marker in CurrentPlan's rulings queue. **DESIGN ARGUED 2026-09-01 (blu), host-side under the release pin, NO CODE WRITTEN, which is what this row asked for.** **THE NODE AND PARSER CHANGE IS THE SMALL HALF AND IT IS NOT THE HARD PART.** The parser already carries everything: `TypeExpr` (`Syntax/SyntaxNodes.codex:71`) has `AppType (TypeExpr) (List TypeExpr)`, exactly the `List Integer` shape, and `parse-type` (`Syntax/Parser.codex:35`) already produces it. `parse-instance-type-head` (`:1597-1602`) is four lines that return `current st1` and `skip-to-close-paren` the rest; it should call `parse-type`, and `InstanceDef.type-name : Token` (`SyntaxNodes.codex:134`) becomes a `TypeExpr`. **THE NODE CHANGE ALONE WOULD BREAK THE LOOKUP, AND THIS IS THE FINDING.** A dictionary's name is mangled in TWO places that must agree, and only one of them sees the syntax head. The desugarer builds it from the head (`class-name & "-dict-" & type-name`, `Ast/Desugarer.codex:1411`), while LOWERING builds the same name from the RESOLVED type through `type-name-of` (`constrained-dict-rn pname (type-name-of arg-ty)`, `IR/Lowering.codex:465`; `prefix & type-name-of arg-ty`, `:489`). **`type-name-of` (`:678`) answers `n.value` for `ConstructedTy (n) (args)` and DISCARDS `args`**, so a desugarer that starts emitting a richer name produces a dictionary nobody ever looks up: the defect moves from a silently-wrong argument to an unresolved call. **That is the SAME LINE COMPILER-24 names as the root of its own generic half, and neither row can be closed by changing it in place** -- it has ten callers and every one wants the bare name -- so both need a second, args-aware key computed beside it, and whoever takes either row should read the other first. **A LATENT COLLISION, MEASURED RATHER THAN ASSUMED: `List Integer` and `List Text` both mangle to `List`**, so two compound instances of one class collide silently on a single dict def. It is not live today: a census of the whole tree finds **16 instance declarations and exactly ONE compound head**, `instance Showable (List Integer)` in `codex/test/typeclass-smoke.codex`. The corpus has never spelled a second one, which is the same corpus-blindness that hid COMPILER-28's non-Integer `between` base (873 declarations, zero of the failing shape) and this row's own head-discarding for as long as it has been there. **THE NULLARY HALF'S REFUSAL NOW HAS A NAMED MECHANISM, still unmeasured but no longer a guess.** In the dict record built at `Desugarer.codex:1414-1422`, every method field is a closure over the instance's own methods, and the `__super-` field is **the only field whose value CROSS-REFERENCES ANOTHER DICT DEF** -- `ANameExpr (super-name & "-dict-" & type-name)` at `:1417`, emitted with no declared type at all. `typeclass-poly` is the only subject in the corpus with a superclass (`class Equatable a => Sortable`), which is exactly the one subject Steve's lead broke. So when a declared type is added for the head, that single field is the one thing in the record the checker has nothing to pin, which is the shape of `unresolved type variable of Sortable-dict-Integer`. **The nullary half therefore is not "add a declared type": it is "add a declared type AND give the super field one", and a fix that does only the first will reproduce his refusal exactly.** |
| COMPILER-32 | **REOPENED, and smaller than it was: `lower-lazy` records the no-expectation sentinel as the thunk lambda's type, so `noexpect` reaches the wire for `lazy` and nothing else** | The row was closed at 0 of 601 on 2026-08-28 and is reopened the same day, because the fix for THIS site shipped a runtime defect and had to be reverted (main 20762). **What is still closed and must not be re-derived:** the `handle[4]`, `with-timeout[5]`, `apply[3]` and propagated `let[2]` sites are FIXED and stayed fixed -- `lower-handle` and the with-timeout arm stopped recording the incoming expectation verbatim (main 20620), and `lower-apply-lambda-chain` stopped building the curried type ending in a sentinel (main 20706). The measured arc was 12 carriers / 108 occurrences, to 5/68, to 1/44, to 0/0, and the revert returns only the last step. **What is open is `lazy-smoke` alone, at `fn[2]` and the `let[2]` that reads it.** **THE REVERTED ATTEMPT AND WHY, so it is not tried again the same way:** `lower-lazy` builds the thunk lambda and recorded `ty` for it, which for `let c = lazy (x + 1)` is `NoExpectTy` because `lower-let` hands its bound value no expectation. Replacing that with a real `FunTy int-ty-default empty-row val-ty` DID clear the census to zero and DID break the program: unforced thunk pointers escaped as values, `call`/`memo1`/`memo2` answering heap addresses 6291624/6291720/6291784 where 47 was expected, `nested` 6292248 for 99, and `variant` empty. Controlled: `lazy-smoke` passes on seed #715 and fails on #717, one variable. Two of the three edits in that CL were innocent and REMAIN IN THE TREE -- `inner-ty`'s fallback and `val-ty` taken from the lowered body; only the `lam-ty` construction was reverted. So the open question is narrow: what type may the thunk lambda carry such that the call site still forces it? **DO NOT ACCEPT A CENSUS AS THE ACCEPTANCE INSTRUMENT FOR THIS ROW AGAIN.** The census reads emitted IR and never executes, and `-Internal` COMPILES `codex/test` chapters without running them, so both instruments were blind to this defect by construction and it reached main and blocked a release (L-NOGATE's third instance, now recorded on that row). The next attempt runs `lazy-smoke` and the other twelve carriers as RUNNING arms against the built kernel before it is believed; the recipe is `build/compile.ps1 -Kernel <new kernel>` then `build/test-run.ps1 -OutFile` and a compare against the `.expected`. Open work, blu. **THE MECHANISM IS FOUND, MEASURED BY RUNNING (blu, 2026-09-01), AND THE SENTINEL WAS HIDING A DIFFERENT DEFECT.** First, the loop this row needs and did not name: `build/concat-codex-self.ps1 -OutFile X` then `build/compile.ps1 -Src X -Out k.cdx -Kernel seed\Codex.cdx` builds a kernel in 7 s, so `lazy-smoke` runs against a real built kernel in about 15 s rather than a 400 s gate. Run the unmodified source first; the baseline passing 12 of 12 is what makes a later red mean anything. **The reverted attempt reproduces EXACTLY**, same heap addresses this row records (6291624 / 6291720 / 6291784, `nested` 6292248, `variant` empty), which is the control that the loop is faithful. **The failures are not arbitrary: every one is a lazy whose body is a CALL** (`expensive 5`, `pick-color 1`, and the inner `lazy` of `lazy (lazy 99)`); every literal, arithmetic, text, record, list, `if` and `let` body passes. **Cause, read off the emitted IR rather than the source:** `inner-ty` falls back to `lookup-expr-type` BY THE SPAN OF THE INNER EXPRESSION, and for a call that returns the CALLEE's function type, not the call's result. So `val-ty` becomes a function type, the thunk lambda is typed `Integer -> (Integer -> Integer)`, and `emit-apply`'s `is FunTy` arm turns the saturated force into a partial application, which is the closure pointer that gets printed. **`noexpect` is not a `FunTy`, so the sentinel was CONCEALING a wrong `val-ty`** -- that is why removing it broke four rows at once, and why this row read as "the sentinel is load-bearing" when it never was. **The fix is one line**: take `inner-ty` from the LAZY node's own recorded type and peel one arrow (`peel-fun-return (deep-resolve (ctx.ust) (lookup-expr-type (ctx.ust) s))`), which is what the checker already computes at `Types/TypeCheckerInference.codex:1709`. Wire census over `lazy-smoke`'s twelve value bindings: **three wrong function types at head (the two `expensive 5` rows and `variant`) go to zero**, `lazy-smoke` stays 12 of 12 including `nested`, and behaviour is unchanged. **WHAT IS STILL OPEN, AND IT IS NO LONGER A LOWERING BUG.** `nested`'s value binding is `error` on the wire before AND after this fix, so an error type still reaches the wire for the lazy-of-lazy shape. And once the lambda carries a real `FunTy` the `nested` case breaks in EMIT rather than in lowering: its value type is then legitimately a function, and the forcing call takes `emit-apply`'s `is FunTy` arm, which is COMPILER-18 and COMPILER-20 territory, not this row's. So the sentinel cannot come off `lower-lazy` until the function-valued force is right in the backend. Removing it without that is the same red as 20762. |
| COMPILER-35 | **Nothing sweeps the front end for a diagnostic placed BELOW an early return that the guarded input is the one thing that triggers** | From the safari port (Steve Howell, PR 114), 2026-08-31, absorbed by red; the one known instance is fixed by the CL carrying this row. `scan-string-body` (`Syntax/Lexer.codex`) returned early when the scan advanced nothing, four lines above the `terminated` test that raises CDX7 -- so the ONE input producing an empty scan, a quote opening at end of line, was the one input that never reached the check. **Measured at head before the fix rather than inferred:** such a chapter compiles exit 0, with no CDX7 and no other diagnostic, and prints the empty string; the author's text is gone. Two changes are needed and both are load-bearing: removing the early return alone leaves `stop - 1` pointing at the OPENING quote, so `terminated` answers True and it is still silent, and the `entry` binding is what makes `stop > entry` mean what it says. **Inert on every valid program by case analysis:** the patched body differs only when `stop == entry`, where `new-col` and the offset write are both no-ops, and `scan-string-end` can never return below its input, so the only state delta in the change is the added diagnostic. **Swept for self-compile risk rather than asserted:** 4,230 `.codex` files across `codex`, `apps`, `build`, `bench`, `shaders`, `tools` and `docs`, 8,676,225 code lines after skipping column-2 prose, which is EXACTLY one leading space -- ZERO sources carry the silent shape and exactly one carries the loud shape, `codex/test/errors/unterminated-text.codex:5`, the deliberate fixture. So the token stream over the compiler's own source is bit-identical across this change. **A new fixture was required because none of the 203 could see it:** the existing `unterminated-text.codex` has content after its quote, takes the LOUD path, and refuses identically before and after, so nothing in the tree discriminated the fix. `codex/test/errors/text-open-at-eol.codex` is the silent shape; refusals go 203 to 204. **What is fixed is that instance; what is not is the class.** The lexer's four sibling string scanners were read and are safe for a reason that does not generalise -- each ends at `in st` with nothing guarded below it. **And the class has a live second member, found while checking the PR's claim that the lexer is otherwise clean: `scan-char-literal` (`:512-529`) has NO unterminated diagnostic at all.** Its non-escape path does `advance-char` unconditionally, so for a quote at end of line it consumes the NEWLINE as the character value, then takes a closing quote only if one happens to be there, and emits a `CharLiteral` either way with nothing raised. Verified at head. Same family, different shape, still open. **Not swept at all:** the parser, the desugarer and the checker have the same `if <nothing-changed> then <state>` shape available and no arm here has looked. The general question is whether a guard can be shown to be reachable on the input it guards; a diagnostic that cannot fire is indistinguishable from one with nothing to report, and both read as green. **What the contributor did not run, stated in his PR body:** not the battery, not the seed rebuild, not the non-zig plugs. All of that is carried by the absorbing CL. |
| COMPILER-36 | **A declared trapping multiply wraps silently: `Integer` is `IntegerTy i64-min i64-max OvError` and lowering discards the mode, so no back end can honour it** | From Steve Howell's issue 109 (2026-08-30), verified against source by red 2026-09-01. `IRBinaryOp` has ONE integer multiply, `IrMulInt` (`IR/IRChapter.codex`), and `IR/LoweringTypes.codex` `OpMul` selects it for every integer type with no branch on the mode: `OvError` and `OvWrapping` (`Types/CodexType.codex`) both fall through, so the mode is gone before any plug or the x86-64 emitter is handed anything. His measurement: `big = 4000000000`, `show (big * big)` prints `-2446744073709551616`, exit 0, no diagnostic, and the zig and C# plugs agree character for character with each other because they receive the same information-free opcode, not because anyone decided. The contrast one line down is the tell: Real trapping and saturating DO survive lowering as distinct opcodes (`IrMulRealTrapping`, `IrMulRealSaturating`), so the integer side is the odd one. Two shapes, Damian's call: give lowering an opcode that carries the mode (and every back end an arm, x86-64 first, since `imul` wraps too), or diagnose at the declaration site that `OvError` is not delivered so the promise stops being made. Either beats silence; a negative product of two positive numbers will be believed by whatever consumes it. Seed-affecting either way. **RULED 2026-09-01 (root, per CurrentPlan's narrow test): the opcode carries the mode.** The Real side already lowers trapping and saturating to distinct opcodes, so the integer side is the odd one and a declaration-site diagnostic would retire a promise the type system makes rather than keep it. Shape, revised the same hour on red's objection: `Integer`'s DEFAULT mode is `OvError`, so a new `-trapping` spelling would relabel every integer op in every program and force twenty plug parsers to add arms or refuse everything. So **`mul-int`/`add-int`/`sub-int` ARE the trapping default** (x86-64 emits the op then a trap on OF), and `-wrapping` spellings are added only for `OvWrapping`. Plugs keep parsing; a plug that still wraps on `mul-int` is now a defect against a STATED contract rather than blindness, so the contract is written where the IR wire is specified, and one `plugs-backlog.md` row names the gap per plug family (wasm's is reek's parity work). Multiply is the unit; add/sub follow as separate units in the same arc. Fixture: Steve's `4000000000 * 4000000000` on bare metal, expected a diagnosed trap, and a wrapping control on an `OvWrapping` type. red's, AFTER COMPILER-38. **SIZED BEFORE BUILDING (red, 2026-09-01, handed off unbuilt), and the size is not the `jo`:** (1) `int-ty-default` IS `IntegerTy i64-min i64-max OvError`, so every unannotated `Integer` in the tree is trapping, and the foreword's wrap-by-design arithmetic (SHA-512 and the hashes, the RNGs, `__text_to_double`'s accumulator, CRC, the CCE arithmetic) will TRAP under x86 until each is declared `wrapping`; that annotation sweep is the bulk of the work and is measured FIRST by building the trapping kernel in scratch (concat to the scratchpad, patch, `compile.ps1 -Repl -Kernel seed`) and running `codex/test/apps/sha512-test`, `ed25519-test`, `crc32-test`, `forewords/foreword-random`, the `cce-*` tests and the compiler's own self-compile against it, counting `!EXC=06`. (2) The wire ALREADY carries the mode in TYPES: `IRTextEmitter` prints `int-default` for the trapping default and `(int lo hi ov-wrap)` otherwise, so the `-wrapping` spellings are for the reader's convenience and a plug could decide from the operand type today. (3) `Simplify.codex` `simp-fold-int` folds `a * b` in the compiler's own arithmetic "wrap for wrap", so a trapping compiler would trap while folding a user's overflowing literal product: guard the fold (fold `mul` only when both operands lie within +-2^31, `add`/`sub` by sign test) or leave it for the runtime trap. (4) The immediate paths `emit-binary-add-imm`/`sub-imm` use `lea`, which sets no flags, so trapping needs the `add-ri`/`sub-ri` forms there; `emit-binary-mul-imm` (`imul-rri`) sets OF and only needs the check; the register forms are `emit-binary-op`, `inplace-int-op`, the reg-left path and the fused compare paths. A scratch probe patching seven register-form sites cost 1,721 bytes, 0.06 per cent of the seed, so the gate's 5 per cent drift guard is not the constraint. `cc-no` is not defined in `X86_64Encoder.codex` (`cc-b` 2 ... `cc-g` 15; `jno` is condition 1, `jo` is 0); the house trap shape is `jcc <cc> 2` over `[15, 11]` (`emit-real-trapping-arith`). **GO (Damian, 2026-09-02, "yes do it"): red, starting with the scratch trapping-kernel measurement above.** **MULTIPLY UNIT BUILT 2026-09-02 (red).** The 64-bit `wrapping` band is accepted: `bounds-are-hw-width` answers True for the exact i64 band (its lo IS writable as a literal; the prose that said otherwise was wrong, u64 stays refused). Mode reaches the operation by the LEFT operand: lowering already picks the opcode from the left type, and `arith-result-ty` now keeps the LEFT mode when both bands are equal, so `h * k + v` stays wrapping across the `+`; a literal on the left is trapping. The IR carries the mode in the node TYPE (`(int i64-min i64-max ov-wrap)`) and the text wire spells the op `mul-int-wrapping` (`bin-op-atom-typed`); `codex/plugs/common/IRTextParser.codex` accepts the spelling as `IrMulInt`, so every plug still parses and still wraps: that gap is `plugs-backlog.md` 2.21. NO new IRBinaryOp variant, deliberately: plug bundles carry `IR Chapter`, and a new variant reaches 56 `when op` sites. x86-64: `int-trap-after node-ty` (`jno +2; ud2`, the house shape) after every IrMulInt emit, keyed on the BINARY NODE type, never the left operand: the single-caller inliner substitutes a literal or a global for the parameter and the node type is what survives (measured: `w64-mul big big` inlined into `opening` trapped on the left-operand rule). The LIR leaf path refuses wrapping muls (`lir-op-wraps`) and traps its `LoMul` unconditionally; `proven-mul-range` answers the full band for a wrapping node; `simp-fold-int` folds a trapping product only when `mul-fold-fits` and never a wrapping one; a trapping mul is no longer `simp-op-trapfree`. Annotated: `Unifier.codex` `memo-probe`, `tcons-probe`, `cons-probe`, `cons-mix`; `Token.codex` `hex-text-value` (a hex literal with bit 63 set accumulates `acc * 16`, found only by the trap-carrying stage compiling itself); `opening.codex` `disk-fnv` through the new `Foreword chapter Wrap64` `w64-mul`; foreword `Random`, `ElasticHash`, `FunnelHash`, `Lz4`, `Noise`, `Perlin`, `Steering`, `DiamondSquare`, `CellularAutomata`, `WorldGen`. Bootstrapped in two local stages because the depot seed refuses the band: a landing-1 kernel (acceptance only, one-pass fixed point BDAB6FBD) compiled the full source, and the trap-carrying stage reproduced itself byte for byte; that stage is the seed this CL carries. Fixture `codex/test/ops/int-mul-wrapping` (funnel, wrapping parameter, in-band and small controls); Steve's `4000000000 * 4000000000` dies `!EXC=06` with `RDI=de0b6b3a76400000`, and 32 subjects across the crypto, CCE and mixer chapters match their sidecars under the new kernel. **Two traps for the next unit (add/sub), both paid here:** `emit-binary` pattern-binds `(n) (ty) (sp)` for a name on the RIGHT and shadowed the node-type parameter, so every `x * name` trapped while `x * literal` wrapped; and a cite line inserted by a loop that aborted midway left four chapters uncited, invisible to the self-compile (the unit is globbed, L-SUBSET) and caught only by their focused tests. Add and sub are the next units: the same shape, with the `lea` immediates replaced by flag-setting forms, and `Sha512.codex` `w64-add` is the one signature SHA-512 needs. **The first gate was RED on the BVT (2026-09-02): ten repo, checkout, colophon and disk-facts subjects died in `Hamt.codex` `hamt-djb2-loop` (`h * 33 + c`), a DJB2 accumulator my census of ten-digit constants could not see. A second census over `* 31|33|37|131` found the same shape in `BloomFilter`, `CountMinSketch`, `os/net/LoadBalancer`, `os/net/MessageQueue`, and in apps `Proxy`, `GopText` (`GtLine.gl-entropy`), `NameForge`, and codexmagic `Auth`, `ChainCore` (`Hash256` fields) and `TransactionValidator`; all annotated in the same CL and the ten subjects match again. The app sites are compile-checked by the gate and run only by the battery, so a trap left there is a release finding, not a gate one; `apps/landing/web/compile/Codex.codex` is a generated bundle and carries the old Hamt until the public page refresh regenerates it.** 
**MEASURED 2026-09-01 (reek, taking red's lane): the multiply sweep is SMALL, and
the sizing step as written would have measured it as EMPTY.** red sized this
unbuilt and named the first step: build a trapping kernel in scratch and count
`!EXC=06` over `sha512-test`, `ed25519-test`, `crc32-test`, `foreword-random`,
the `cce-*` tests and a self-compile. That was done. The probe is the house trap
shape, `jcc 1 2` over `[15, 11]`, appended at the seven `IrMulInt` sites; the
kernel is 3,094,535 bytes against the seed's 3,091,608, so 2,927 bytes, and the
gate's 5 per cent drift guard is nowhere near it.

**Calibrated first, both directions.** Steve's own fixture
`4000000000 * 4000000000` traps under it, with `RSI=de0b6b3a76400000` in the
dump, which is the wrapped product the issue reports being printed silently;
`1000 * 1000` prints `1000000` and does not trap.

**Results, multiply only:**

| subject | result |
|---|---|
| the compiler's own self-compile | NO TRAP, exit 0, one pass |
| `sha512-test`, `ed25519-test`, `crc32-test` | no trap, output matches |
| `cce-roundtrip`, `cce-tier1`, `cce-tier2`, `cce-bounds-smoke`, `cce-builtin-bounds` | no trap, output matches |
| `forewords/foreword-random` | no trap, output matches, AND THE ROW IS VACUOUS |
| `Foreword chapter Random`, `mix-bits 12345 7`, called directly | **TRAPS** |

**The last two rows are the finding.** `codex/test/forewords/foreword-random`
CITES the Random chapter and prints `"Foreword/Random OK"`; it never calls the
RNG. So its green says nothing about the RNG, which is L-NAMED at the exact
point where the sizing step depends on it. Called directly, `mix-bits` traps at
once, and it must: it multiplies by 2654435769, 6364136223846793005 and
1442695040888963407, which is wrap-by-design and the clearest annotation
candidate in the tree.

**So the count to plan from is not zero and not "the bulk".** For MULTIPLY the
compiler itself is clean across a whole self-compile, and the hashes and CRC
are untouched because they add and xor rather than multiply, so a multiply-only
probe cannot speak to them at all. What needs `wrapping` is the mixers.
**Size the remaining sweep by probing the wrap-by-design SITES directly, never
by running tests named after them**, and note that add/sub are unprobed here and
are where SHA-512 and CRC will actually land.

**BLOCKED 2026-09-01 (reek), and it is a question for Damian, not a workaround.
THERE IS NO WAY TO SPELL 64-BIT WRAPPING ARITHMETIC TODAY, and 64-bit is what
every site this row must annotate needs.**

The ruled shape is `mul-int` trapping by default with `-wrapping` spellings for
`OvWrapping`. Reaching `OvWrapping` from source requires a BOUNDED band
(`Parser.codex` `parse-bound-between` is the only producer), and
`check-wrapping-band` refuses any band that is not exactly a hardware width.
`bounds-are-hw-width` (`Types/CodexTypeHelpers.codex:91`) answers True for u8,
i8, u16, i16, u32 and i32, and **False for HwU64 and HwI64**.

That refusal is deliberate and its reasoning is sound for a bounded FIELD, in
the prose beside it: the exact 64-bit bands are not writable as literals (u64's
hi and i64's lo both fall outside the signed 64-bit literal range) and a 64-bit
store narrows nothing, so there is no truncation for the band to coincide with.

**But this row does not want a store property, it wants an ARITHMETIC MODE**,
and the sites it must annotate are all full-width 64-bit: `mix-bits` multiplies
by 6364136223846793005 and 1442695040888963407 and relies on i64 wraparound, and
every other mixer in the list is the same shape.

Measured, not assumed. `Integer between -9223372036854775808 and
9223372036854775807 wrapping` is refused with CDX1073. A bare `Integer wrapping`
does not mean the mode at all: it parses as a type APPLICATION and fails
CDX2003 "Arithmetic operator requires Integer or Real" with CDX2001
"Type mismatch: Con:Integer[T271] vs Integer".

**So the annotation half cannot be written for a single one of the eighteen
sites, and without it the trapping half cannot land** (turning on the trap
without annotating the mixers red-lines the tree). The IR plumbing was built and
proven separately -- opcode, mode-selecting lowering, wire spelling both ways,
and arms at all 28 routing sites, compiling clean -- and was then REVERTED
rather than landed as dead code, since nothing can emit the new opcode at 64-bit
until this is answered.

**The choice is Damian's.** Allow a 64-bit wrapping band and solve the literal
spelling; or give the mode its own spelling that does not go through a band
(a bare `Integer wrapping` currently means something else, so it would have to
be taken); or re-shape the row. Whichever is chosen, add and sub inherit it.

**THE ANNOTATION SWEEP IS SIZED, 2026-09-01 (reek), and the syntax half is
already done.** `wrapping` is a REAL spelling today: `Parser.codex:188` maps the
token to `OvWrapping`, `CodexEmitter` prints it back, and `IRTextEmitter` already
distinguishes `ov-wrap` in the type. So nothing needs inventing at the source
level; what is missing is that lowering discards the mode.

**The tree declares it exactly ONCE**: `RankedTextSet.codex:144`,
`pos : Integer between 0 and 4294967295 wrapping`. The other eight matches for
the word in the foreword are prose using it in an English sentence, which is
worth naming because a grep for `wrapping` reads as nine adopters and is one.

**The multiply sites that will need it**, taken by searching for a multiply by a
constant large enough to overflow rather than by chapter name: the mixers and
LCGs. `Random` (`mix-bits`, three), `ElasticHash` (three), `FunnelHash` (three),
`Noise`, `Perlin`, `DiamondSquare`, `CellularAutomata`, `Steering`, `WorldGen`,
`SpatialHash`, `ImageTensor`, `Lz4`. Roughly eighteen expressions across twelve
chapters.

**Excluded deliberately, and this is where a careless sweep would go wrong:**
byte and word assembly multiplies (`TrueType` `a * 16777216 + b * 65536`,
`Gguf`/`SafeTensors` `hi * 4294967296`) cannot overflow for the byte-sized
inputs they take, and the fixed-point scalings (`Trig`, `Geodesic`, `Sampler`,
`GpuProxy`) are bounded by their callers. Annotating those would relabel correct
trapping arithmetic as wrapping and retire a real guarantee, which is the
opposite of what this row is for.

So the multiply unit is: an opcode carrying the mode, lowering selecting on it,
an x86-64 arm, and about eighteen annotations. Not a tree-wide sweep.

**PROBED 2026-09-02 (red), ADD/SUB/MUL TOGETHER, seed BE8B04B5, and two of the rows above are corrected.** Kernel: every IR-driven integer add, sub and mul site in `Emit/X86_64.codex` and `X86_64Compound.codex` (the `lea` immediates replaced by `mov`+`add-ri`/`sub-ri` so the flags exist) followed by `jno +2; ud2`, 29 sites, 3,151,761 bytes against the seed 3,149,077. Calibrated both directions: a small-value control prints, `4000000000 * 4000000000` and `9223372036854775807 + 1` each die `!EXC=06`. Results: `sha512-test` TRAPS, `ed25519-test` TRAPS (it cites Sha512); `crc32-test`, the five `cce-*` tests and `foreword-random` match their `.expected` with no trap. **The "self-compile NO TRAP" row above is VACUOUS (L-VACUOUS): a kernel compiled BY THE SEED carries no traps in its own code, so running it over the source tests nothing about the compiler's arithmetic.** The kernel it PRODUCES carries them, and that one compiling the same source dies at `memo-probe+0x3B` (invalid opcode) at 88 MB of heap: `Types/Unifier.codex:952`, `a * 2177342782468422677`, a Fibonacci hash of an address. **So the compiler is NOT clean; its mixers are on the list.** Sites measured or read, by chapter: `Unifier.codex` 930, 952, 993 (the same multiply, three sites) and 986 (`cons-mix`, `h * 1099511628211 + v`); `opening.codex:2205` (`disk-fnv`); `Sha512.codex:18` `w64-add` (EVERY SHA-512 add funnels through it, so the chapter is ONE signature, and Ed25519 reaches it by citation); `Random.codex:70-74` `mix-bits` (three multiplies and one add). The `sha512-test` trap RIP was not mapped to a site; `w64-add` is the only add in the chapter that can overflow. What the probe cannot say: whether Ed25519's own field arithmetic traps independently of Sha512 (the trap could be either), and what else in the compiler traps after `memo-probe` (the first trap ends the run; expect `cons-mix` and `disk-fnv` next).

Caveats on this measurement, so it is not read wider than it is: multiply only;
the self-compile is ONE pass against the trapping kernel and not a re-proved
fixed point; nothing was submitted and no seed moved.

**ADD/SUB UNIT BUILT 2026-09-02 (red), the same shape as multiply.** Plain `+` and `-` trap on overflow on x86-64 at every IR-driven site. The immediate paths drop `lea` for `mov`+`add-ri`/`sub-ri` followed by `jno +2; ud2` when the node type traps and keep `lea` when it wraps (`emit-binary-add-imm`/`sub-imm`, and the two tail-call immediate paths through `tail-add-imm`/`tail-sub-imm` in `X86_64Compound.codex`); the register forms (`emit-binary-op`, `inplace-int-op`, `emit-tco-binop`, `emit-binary-standard`, the staged pop-add) append `int-trap-after` on the node type, and `emit-reg-right-inplace` now receives the node type rather than the left operand's (the inliner rule the multiply unit paid for). The LIR refuses wrapping add/sub (`lir-op-wraps`), traps `LoAdd`/`LoSub` unconditionally in both ALU forms, loses its two `lea` fusions (they set no flags), and its aliased subtract parks the right operand in R8 and subtracts the right way round instead of `neg; add`, which raises OF on `r == i64-min` where `l - r` does not. `simp-fold-int` folds a trapping sum or difference only when it fits (`add-fold-fits`/`sub-fold-fits`) and never a wrapping one, and add/sub are no longer `simp-op-trapfree`. Wire: `add-int-wrapping`/`sub-int-wrapping` from the node type, parsed by `IRTextParser` to the plain ops (plugs 2.21). `Foreword chapter Wrap64` gains `w64-add`/`w64-sub`; `Sha512.codex` cites it and its own masked `w64-add`, the one signature every SHA-512 add funnels through, is gone. **No other annotation was needed**: the stage-2 self-compile (the trap-carrying compiler compiling the source, the run L-VACUOUS says is the only one that counts) ran clean the first time, because every accumulator the multiply unit annotated already carries the mode on its left operand and the `+` inherits it. Measured against stage 2 (stage 2 == stage 3 byte for byte, one pass; `sha512-test`, `ed25519-test`, `crc32-test`, `foreword-random`, the five `cce-*`, `hamt-test`, `mix-bits`, `int-mul-wrapping` and the new `codex/test/ops/int-add-wrapping` (funnel, parameter, immediate, in-band and small arms for both ops) match their sidecars; `9223372036854775807 + 1` in a plain function dies `!EXC=06` with `RDI=8000000000000000 RSI=7fffffffffffffff` after the small control printed. Size: stage 2 is 3,191,651 bytes against the seed's 3,155,718, 1.1 per cent, 4 bytes per trapping add or sub. The app sites are compile-checked by the gate and run only by the battery, as for multiply. **GATED AND LANDED (red, 2026-09-02): seed F185CB2E (3,191,641 B, signed, self-verified), `-Internal` green in 599 s, one-pass fixed point on the merged head at main 21784, BVT 408 lines 0 drift, oracle-vector 130/130. One red on the way, the multiply unit's shape again (L-CENSUS): `build/oracle-vector.ps1`'s generated `ballast` sums the IEEE bit patterns of negative doubles with plain `+` as a live-value sink, and the third term carries the sum past i64-min (trap at `ballast+0x191`, RSI holding the wrapped sum); it goes through `w64-add` now (blu's generator, authorized by root). The first green gate was then invalidated by reek's seed 21767 landing under it, and the unit was re-gated on the merged head.** **SPELLING UNIT (red, 2026-09-02, Damian's 10:35 ruling): a bare `Integer wrapping` is the i64 band in wrapping mode, hooked in `parse-type-args` before the identifier would be taken as a type argument (`is-bare-wrapping`, base spelled `Integer`, next token spelled `wrapping`); `-9223372036854775808` is a literal because the checker's unary arm accepts the one-past-max magnitude directly under the minus (`is-int-min-literal`) while the bare literal stays CDX2071, and lowering already folds the negation without a subtraction (`IrIntLit (-v)`, where `-v` of i64-min is i64-min); `vec-lane-literal` negates with unary minus rather than `0 - v` so a pathological lane index cannot trap the compiler. Fixture `codex/test/ops/int-wrapping-spelling` (both literal positions, bare-band add, sub and mul, a small control).** **A third census miss, found by fester's plug-selftest at 21846 (L-CENSUS): the evidence plug's `ev-u64-loop` assembled a little-endian u64 as `acc + byte * mul` with `mul * 256`, which reaches 2^64 on the eighth byte; a multiply trap latent since 21676, invisible to the gate because plug-selftest runs only when the plug changes and app-sweep only compiles. It is `bit-or`/`bit-shl` now (main 21855). A byte assembler is a shift, and the shape to grep for is a loop parameter multiplied by a power of two and fed back.** **A fourth, from reek's hosted runs the same afternoon: both classic-games subjects died in `rng-next`, the LCG `state * 1103515245 + 12345` on a plain `Integer` record field (`apps/games/classic/Rng.codex`), reproduced on bare metal at `rng-next+0x2e`. A census by CONSTANT over `apps/` found the same LCG in nine more chapters (codexmagic Token and DungeonRun, magic CardGen, spark SparkDemo and ParticleSystem, fontexplorer FontModel, fontai FontTrain, fireworks, fishtank), each annotated on the operand that carries the state (a record field or the seed parameter) in the sweep CL. The app sites the multiply unit deferred to the battery are these; the gate compiles them and only a runner that executes them sees the trap.**
|
| COMPILER-44 | **`==` on an instantiated generic recursive sum is still wrong on every back end except x86-64** | Registered by root's ruling on COMPILER-43 (2026-09-01) as the follow-on, not a fork: option A fixed x86-64 because the eq-helper machinery lives only in `Emit/X86_64.codex`, and riscv, arm64 and wasm are plugs that receive IR and compare a constructor by POINTER. The backend-independent answer is a POST-RESOLVE SPECIALIZATION PASS minting `__eq_Box@Text` per instantiation, which is what the desugarer cannot do (it runs before resolution and has no instantiations) and what lowering cannot do (`LowerCtx` has no def accumulator and `lower-expr` returns an expression, so a helper cannot be minted without threading an accumulator through all of expression lowering -- that is the cost of this row). Acceptance: `codex/test/eq-generic-recursive` passes cross and its `.no-cross` sidecar is DELETED. **Unmeasured on the cross lanes rather than known-red** -- Renode was barred for memory when 43 landed (root, 2026-09-01), so nobody has run those arms; measure before sizing. Open, unowned. |
| COMPILER-38 | **Lexical scoping of `let` is left to each back end: the IR names bindings by source spelling, so a shadowing inner `let` and an outer one are the same IR name, and wasm answers wrong where x86-64, zig and C# each invented a different repair** | From Steve Howell's issue 113 (2026-08-30). Four corpus programs (`let-shadow-scope`, `act-let-scope`, `scope-let-arm-global`, `inline-single-caller`) give silent wrong answers through the wasm plug: an inner `let v` clobbers the outer `v` (want 9, wasm 13), and a `let glob` hides the top-level `glob` for the whole function (want `"GLOBAL"`, wasm `""`); `inline-single-caller` is the sharpest, a spliced body's reference to a global resolving to a neighbour's local. Three shipped back ends solve it three ways in their own emitters, which is how a fourth plug invents a fourth answer. **RULED 2026-09-01 (root): the IR decides scoping ONCE. Lowering renames only the COLLIDING binders inside a definition (`v`, `v_1`, ... or a stable suffix scheme; the choice is the implementer's, the property is that two live bindings never share an IR name), and the wire format is unchanged: no second name field (red's reading (a), confirmed; L-LESS). Diagnostics run before lowering, so the source spelling survives for free. Every back end then consumes the unique name and its private renaming becomes dead code to DELETE in the same arc, not leave.** The two parity reds reek has agreed are red's (`act-let-scope`, and `bacnet-encode`'s `Real` family via PR 111) hang on this, which is why it precedes COMPILER-36. Seed-affecting; the text-mode pingpong is the fixed-point proof and the four corpus programs are the fixtures, graded against bare metal. red's. **BUILT 2026-09-01 (red).** `LowerCtx` carries `binders`, a source-to-emitted map per definition; every binder (`let`, act bind, act-let, lambda parameter, pattern variable, list-match head and tail) goes through `bind-local`, which keeps the source spelling when it is free against the enclosing scope AND the top-level names, and otherwise takes the first free `name_1`, `name_2`, ...; the overlay is keyed by the emitted name and `lower-name-normal` resolves a use through the map before the chapter renames, so a local shadows a global as the language says. Pattern variables and lambda parameters read their emitted names FROM the bound context (`bound-name`), so the binding walk and the emitting walk cannot disagree. Definition parameters are not renamed (they are the first binders; a parameter spelled like a top-level name stays, since every back end resolves parameters before globals). MEASURED against a kernel built from the source (`Sut38`, 93031B25): it compiles its own source to a byte-identical binary in one pass; the four fixtures match their `.expected` on x86-64 under it; the wire shows exactly the collision renamed (`scope-let-arm-global`: `(let "inner" ...)` is `(let "inner_1" ...)` where a top-level `inner` exists, every other name unchanged); and the wasm plug agrees with x86-64 on all four with NO emitter change, `scope-let-arm-global` included, which was the one still red after reek's per-binding slots. Diagnostic census unchanged (CDX3005 7, CDX6020 13). What this does NOT rename, by design: sibling scopes may reuse a spelling (never live together); parameters; a local spelled like a BUILTIN (`wat-try-builtin` still wins in apply position, a separate row if it bites). The plugs' private repairs: wasm's shadow stack is dead code once this lands (deletion coordinated with reek, who is mid-edit there); zig's `renamed-from/to` also serves zig's own no-shadowing rule for nested functions and is assessed separately; the C# plug has none. **LANDED main 20995 (dev 20993), seed DE664C4E, gate green 478 s with test-compile FULL and every plug phase.** The wasm deletion is the follow-up recorded in `plugs-backlog.md` 2.18. |
| COMPILER-43 | **FIXED (blu, 2026-09-01), x86-64 only.** A recursive GENERIC compared its parameter field by pointer. **The route was measured, and it is NOT where this row and the reproducer both said to look:** `lower-eq-dispatch` (`IR/Lowering.codex`) looks `__eq_<T>` up by NAME, `type-name-of` having already dropped the arguments, so every instantiation shared one DESUGARER-generated helper whose field stays at the declaration's own type variable, and a `tvar` operand falls through to a word compare on two pointers. The IR names the generator outright: the helpers carry `gen-eq-def`'s `__ex`/`__ey` parameters, never `eq-helper-def`'s `__eqa`/`__eqb`, so the emitter `EqHelperReq` path this row blamed is not on the route at all and "thread type-args into the helper request" would have been a day spent on a function that never runs for `Box`. The earlier trace was not wrong, it was GATED ON THE TYPE NAMES: `emit-eq-op` is reached for these types, but on the `tvar` operand inside the helper, never on a `Box`-named one. Fix: lowering sends an INSTANTIATED type to the emitter, and the emitter's helper key carries the type arguments (`eq-helper-name-inst`, `eq-req-name`) with a deep substitution of actuals into field types, so `Box Text` and `Box Integer` get separate helpers. Test `codex/test/eq-generic-recursive` (nine rows, `.no-cross` sidecar); the arms that MOVE are `box-text-eq` and `box-nested`, and `box-int-eq` answers `yes` on both compilers because a word compare on two integers is accidentally right -- it is there to catch the key collision, not the original defect. The cross lanes are unchanged and still wrong; that is COMPILER-44. | Split out of COMPILER-24 on 2026-09-01 (blu) once measurement showed the two are not one defect. **Measured, not inferred:** a temporary `st-add-info` trace in `emit-eq-op`, gated on the subject type names, fired for the non-recursive `Pair` and produced **NO line at all for `Box` or `Named`**, both of which are recursive. So a recursive sum's comparison takes a different route entirely and COMPILER-24's fix cannot touch it. **The diagnostic pair is what makes this its own row rather than more of 24:** `Named = | Nil | Node (Text) (Named)`, recursive with a CONCRETE field, answers `eq` CORRECTLY; `Box a = | Empty | Cell (a) (Box a)`, recursive with a PARAMETER field, answers `ne` for equal Text built at runtime. Same route, one right and one wrong, so the route works and only the instantiation is lost. **WHERE TO LOOK, and it is NOT `emit-eq-op`:** two generators mint the name `__eq_<T>` -- the desugarer's `gen-eq-def` for a `deriving Eq` or self-recursive type (`Ast/Desugarer.codex:663`) and the emitter's `eq-helper-def` from an `EqHelperReq` (`Emit/X86_64.codex`) -- and which one is operative for a self-recursive generic is STILL UNMEASURED. Settle that first, the same way this row was settled: the trace is cheap and reading has now been wrong four times on the sibling row. **A HAZARD FOR WHOEVER FIXES IT:** `eq-req-has` dedupes helper requests by NAME ONLY, so `Box Text` and `Box Integer` share one helper. Adding substitution WITHOUT an args-aware key would build that helper from whichever instantiation arrived first and compare an Integer field with `__str_eq` -- **a worse failure than the pointer compare it replaces**, which is why the same change was written and reverted rather than landed on the sibling row. The key must come first or with it. **The reproducer is `docs/Test/Active/EqGenericDiscriminator.codex`**, whose `box-generic` arm is this row and whose `named-concrete` arm is the control that must stay `yes`. **CLOSED ON x86-64: landed main 21314, seed D6ED6F35, guarded by `codex/test/eq-generic-recursive` (nine rows, `.no-cross` sidecar). The trailing "Open, blu." this row carried until 2026-09-02 contradicted its own opening word, which is FIXED, and is what a reader scanning tails would have acted on. The cross-lane half is COMPILER-44 and is unmeasured because Renode is barred for memory, not because it is known red.** |
| COMPILER-41 | **CLOSED. FIXED 2026-09-01 AT CL 21214 (copy-up 21215), AND THE ROW BELOW NEVER RECORDED IT.** The split moved from 16 to 24 at all four sites that carry it; verified at head 2026-09-02 (blu) against depot seed `BE8B04B5`: `codex/test/real-show-wide` matches its `.expected` exactly, `d17` printing `12345678901234566.0` and `d18` `123456789012345680.0`, which are the two arms this row measured wrong. The guard is not vacuous: those two rows are precisely the ones that moved, and every other row in it was correct before the fix as well. **STILL GENUINELY OPEN AND NOT FIXED BY 21214: whether the PLUGS share the defect**, which the body below flags as unchecked and which nobody has measured; the printer is hand-rolled in `Emit/X86_64TextHelpers.codex` and the plugs format reals through their target language, so the 16/24 split is x86-specific by construction, but that is an argument and not a measurement. **`show` ON A `Real` CORRUPTS A DIGIT ONCE THE INTEGER PART PASSES SIXTEEN, AND THE PARSE IS FINE** | Found 2026-09-01 (blu) while capturing the sidecar for COMPILER-37's boundary test against Sut `9B73E281`. **The defect is in the PRINTER, not the parser, and the probe separates them.** `docs/Test/Active/RealLiteralProbe.codex` reports each literal twice, through `real-to-int` (the parsed bits) and through `show`: **d16 `1234567890123456` prints `1234567890123456.0`, correct. d17 parses `12345678901234566` and prints `02345678901234566.0`. d18 parses `123456789012345680`, which IS the nearest double, and prints `103456789012345680.0`.** So at seventeen digits the leading `1` becomes `0`, and at eighteen the second digit `2` becomes `0`, while `real-to-int` on the very same value is right both times. Small values are unaffected: `0.5`, `1.5`, and everything to sixteen digits round-trip. **THIS IS NOT COMPILER-37 AND MUST NOT BE FOLDED INTO IT.** 37 is the parser accumulating every digit into one wrapping i64, now refused at the literal by CDX2073; this row is the formatter mangling a value the parser got right, and it fires WELL INSIDE the range 37 leaves legal. **IT NEARLY BECAME GOLD, WHICH IS THE PART WORTH KEEPING.** The plan was to capture `real-literal-boundary.expected` from the pre-change seed, since the fix only adds a diagnostic and cannot alter parsing of an in-range literal, and that reasoning was sound. root asked for the capture to run against the NEW Sut instead so the sidecar could never predate the change; that is what put the wrong digits on screen where they could be read. **A sidecar captured from a run is only as true as the thing it captures, and nothing in the harness would have contradicted it** -- the value would have been enshrined, the test would have passed forever, and the printer defect would have acquired a guard asserting it. `codex/test/real-literal-boundary` therefore reports through `real-to-int` and says in its own prose why. **WHERE TO START:** the parse side is clean, so the subject is the Real-to-text path, not `__text_to_double`. Note the corruption is a DIGIT SUBSTITUTION toward `0` at a fixed position rather than a truncation or a rounding, which is the shape of a place-value or divisor error in the digit loop and not of precision loss; the 2^53 significand limit is a separate and documented thing (`Emit/X86_64TextHelpers.codex:505`) and it does not substitute digits. **Unmeasured and worth knowing before a fix: whether the plugs share it.** COMPILER-37's zig-plug coupling does not apply here unless the printer is mirrored the way `text-to-double-bits` is; that is unchecked. The plugs question is what remains; the x86-64 printer is closed. |
| COMPILER-40 | **FOUR `roc-fold-*` TESTS REGRESSED PASS TO FAIL ON ARM64 AND NO GATE SAW IT FOR FIVE DAYS (L-NOGATE).** Found 2026-09-01 (blu) while verifying an unrelated plug fix on the full arm64 bed. `roc-fold-count`, `roc-fold-empty`, `roc-fold-product` and `roc-fold-sum` were `PASS_EXPECTED` in the last full bed (`test-output-cross/history/arm64_20260827-120229.md`) and are `FAIL_OUTPUT` now. **ALL FOUR PRINT THE IDENTICAL VALUE `1075847232` against four DIFFERENT expected answers (4, 42, 24, 10), which is the L-SUSPECT fingerprint, and the value is pointer-shaped: it is 48 away from the `1075847184` this file already records for `effect-smoke`.** **THE PLUG IS CLEARED BY A CONTROL, SO THE SEED IS THE ONLY AXIS.** Measured: with the 21158 plug fix reverted and the plug rebuilt to a byte-identical `E9D41346` binary, all four still print `1075847232`; the positive control in the same arm, `tier0-cyrillic-print`, failed there and passes with the fix, so the arm really was the old code. **THE FLEET BASELINE IN THE COMPILER-9 ROW IS STALE AND MUST NOT BE USED: it says 43, the last actual full bed was 40**, and the bed has since grown from 515 to 524 so counts are not comparable across it at all -- compare the SET. Current is 46, and of the six additions FOUR are brand-new tests absent from the old bed failing at compile on pre-existing `[UNSUPPORTED]` gaps (`desk-hover`, `desk-root-guard`, `desk-span`, `fat16-lfn-record`), not regressions. **WHY NOTHING CAUGHT IT, AND THIS IS THE PART THAT MATTERS (L-NOGATE):** the arm64 bed is not in `build/build.ps1 -Internal` and is not in any standing gate, so it runs only when somebody asks for a slot. **The seed moved TWELVE times between the baseline and the discovery** (`p4 changes //Codex/main/seed/Codex.cdx`: 20762, 20783, 20812, 20822, 20824, 20888, 20906, 20944, 20984, 20995, 21038, 21071) **and every one of those landed green under its own gate, because no gate observes this bed.** An assertion with no runner is not a test, and for five days these four were exactly that. **NEXT ACTION IS A BISECT OVER THOSE TWELVE SEED REVISIONS, NOT A CODE READ.** It is cheap and needs no plug rebuild: `p4 sync //Codex/blu/seed/Codex.cdx@<CL>` then `build/test-cross.ps1 -Arch arm64 -Test roc-fold-sum`, about 40 s a probe, four probes by binary search, then confirm the culprit against all four subjects. **The prior suspicion to test first rather than last is any CL touching closure or thunk lowering, because these are fold tests and a fold is a closure**; `COMPILER-32` (21071, blu) changed `lower-lazy` and is the author own prior suspect, which is a reason to probe it, not a reason to assume it. **BISECTED 2026-09-01 (blu). THE CULPRIT IS CL 20706, "COMPILER-32 step 2", blu's own lane, and the prior suspect 21071 IS CLEARED** (it fails on both sides of itself). Hash-verified either side, because the changelist numbers alone are NOT a safe axis: seed `87B8600D` (main@20684) passes all four, seed `39CE158F` (main@20706) fails all four. **A TRAP THAT COST A WRONG ATTRIBUTION MID-BISECT AND WILL COST THE NEXT PERSON ONE: `p4 sync //Codex/<agent>/seed/Codex.cdx@<CL>` USES MAIN'S CHANGELIST NUMBER AGAINST THE DEV STREAM, WHICH RECEIVES MAIN ONLY AT MERGE-DOWN, so several main CLs collapse onto one dev-stream revision.** Probes at 20706 and 20736 returned the IDENTICAL seed hash, which is the tell. Bisect by `p4 print -q -o <file> //Codex/main/seed/Codex.cdx@<CL>` and HASH each revision first; distinct CLs must give distinct hashes before any of them is worth a guest run. **THE MECHANISM.** 20706 replaced `ty` with `want = expected-or-recorded-ty ty ctx sp` in `lower-apply-lambda-chain` (`codex/compiler/IR/Lowering.codex`), in BOTH the curried type it builds and the result type it folds with. `expected-or-recorded-ty` answers the type RECORDED AT `sp` when `ty` is `ErrorTy` or `NoExpectTy`. For an immediately-applied lambda literal the recorded type at that span is the LAMBDA'S FUNCTION TYPE, not the application's result, so `build-curried-fun-ty` produces an OVER-CURRIED `a -> b -> c -> (a -> b -> c -> Integer)` and `apply-chain-fold` folds to a function result. The chain still believes it holds a function after consuming every argument, so what reaches `show` is the closure pointer, which is why all four print one identical pointer-shaped value. **This is the same span-collision family as 21071's own fix**, which changed `lookup-expr-type (aexpr-span inner)` to `lookup-expr-type s` and added `peel-fun-return` for exactly this reason, so the repair shape is likely to peel the recorded type once per applied argument rather than to revert. **WHY 20706 SHIPPED GREEN, AND IT IS THE WHOLE OF L-NOGATE: THE CL'S OWN EVIDENCE WAS A STATIC IR CENSUS AND IT SAYS SO IN ITS DESCRIPTION, "all four roc-fold carriers gone", carriers 5 to 1, occurrences 68 to 44.** It counted the sentinel's SIGNATURE disappearing from the IR and read that as the defect being fixed; it never RAN the four roc-fold programs, which is how the signature left the IR while the answers turned into a pointer. A census of a symptom is not a test of the behaviour, and the four tests it named by name are the ones it broke. |
| COMPILER-42 | **CLOSED AND REFUSED 2026-09-03 (Damian). MORATORIUM: no agent discusses `list-push`, `list-snoc`, `&` or list-append semantics again, and this row is not to be reopened.** | The premise was that `list-push` mutating its argument in place is a defect to repair. It is not. **It is the contract, and it is what makes this compiler fit its memory budget: the compiler took more than 4 GB before appends grew in place.** In Damian's ruling: `list-push` in no case should ever copy the list, ever; and **if a holder has an alias to a list that is going to mutate, deal with it AT THE HOLDER** -- the one caller needing the old value copies it there, where one caller pays, rather than every call site paying through the allocator. Every attempt at the other reading has been measured and has failed: capacity equal to length killed `apps/brotli-hostile` and `apps/deflate-hostile`; a build that copied at unproven call sites could not compile a three-line source. **All of it is ripped back out of the seed (blu, 2026-09-03): the `list-push-copy` builtin, `__list_snoc_copy` and its x86 emission, the wasm `$list_push_copy`, and the whole `own-report`/`own-rewrite` ownership apparatus.** `codex/compiler/Types/Builtins.codex` now states the contract at the builtin table, because the ABSENT contract is what made the behaviour read as a bug and pulled two agents into rebuilding it. The design doc is kept only as the account of what was refused and why. |
| COMPILER-39 | **A declared export is never erased: `inline-single-caller` (and any pass that removes a definition) exempts every name a chapter declares exported, on every target** | From Steve Howell's issue 110 (2026-08-30), ruled by Damian 2026-09-01 ("exports exempt from single-caller is fine"), posted on the issue the same day. The pass is correct inside a whole program: `collect-once-defs` / `keep-single-caller` (`IR/Lowering.codex`, the `:2353`/`:2368` region at his pin) inline a definition referenced exactly once and it never reaches the output. It is wrong at a language boundary, where a zig shim outside the transpiled program can call only what survives, and which definitions survive depends on how many OTHER places call them, invisible at the definition. His measurement: nine definitions erased across three ported games (Num 1, Truck 7, GuardRail 1), eight of them single-caller, decidable from the emitted module because a body that still contains calls is not a leaf. **The mechanism is the export declaration PR 112 introduced for wasm** (`plugs-backlog.md` 2.18, `wasm-exports`, taken): a chapter names its exports, and that list's scope extends from the wasm emitter to the pass pipeline, so the rule is predictable from the definition you are looking at. Not a diagnostic on erasure; inlining at the call site stays allowed; the definition reaching the output is what is guaranteed. Acceptance is his two-roads harness (`passes=text-plug` against `default-ir-pipeline`): a declared export appears on both roads, and an undeclared single-caller definition still does not. Seed-affecting. Steve's queue lane (reek, red's lane absorbed 2026-09-01). |
| COMPILER-45 | **`compile.ps1 -EscapeCheck` crashes on the seed compiling the compiler, so `KEEP-STAT` and every escape-check reading of the selfhost are unreachable** | Registered as COMPILER-44 at 21317 and renumbered 45 by root the same hour, blu having taken 44 at 21314. Measured 2026-09-01 (red) against depot seed 83C9E0B1: the selfhost unit under `-EscapeCheck` dies in `copy-sx-pos+0x1E` (general protection, RDI non-canonical) at heap 128.5 MB, and identically under the pre-change compiler, so it is not a regression of the memory campaign. Only the selfhost unit was measured. The counts the mode would print (`KEEP-STAT`) are read from the `CHECK-TAIL` phase-trace note instead. Nothing in any gate runs `-EscapeCheck` (L-NOGATE), which is how it stayed unnoticed. Start from the crash site: `copy-sx-pos` is the span copier, and a non-canonical pointer that deep reads as a span taken from reclaimed memory under the escape walk.  **REPRODUCED AND NARROWED 2026-09-02 (blu), against depot seed `BE8B04B5`, two selfhost runs.** The crash reproduces exactly: GP in `copy-sx-pos+0x1E`, `RDI 0x0038281100000000`, heap 125.9 MB (red measured 128.5 MB against 83C9E0B1, so the heap figure moves with the seed and the crash does not). **THE HEADLINE IS TOO BROAD AND SHOULD NOT BE ACTED ON AS WRITTEN: `-EscapeCheck` IS NOT UNREACHABLE, ONLY THE SELFHOST UNIT DIES.** Compiling `codex/test/ops/unicode-bytes-roundtrip` under `-EscapeCheck` exits 0, produces a 104,523-byte CDX, and prints the full escape report for every phase (PARSE 2 deck pointers into bivy, DESUGAR 6, SCOPE 1162, CHECK 1, LOWER 211, with all four PRECISE lines at 0 ptr and reach 12,048 / 23,185 / 48,625 / 31,051). So escape-check readings of ordinary chapters are available today and only the selfhost reading is blocked. **THE PHASE IS NAMED: it dies after `PH:SCOPE` and before `PH:CHECK`.** A second run under `-EscapeCheck -DebugMode` prints `DBG:frontend src=3115049`, then `PH:LEX`, `PH:PARSE`, `PH:DESUGAR`, `PH:SCOPE`, then the fault. **THE FAULTING VALUE IS DETERMINISTIC AND CONTENT-DERIVED, NOT A WILD ADDRESS, AND THAT IS THE MOST USEFUL FACT HERE.** Across the two runs `RDI` and `RBX` are byte-identical at `0x0038281100000000` while every neighbouring register MOVED with the extra `-DebugMode` allocations (`R12`/`RSI` 0x0E0CEC75 to 0x0E0CF22D, `R14` 0x05E16B99 to 0x05E17151, `R10` 0x083E69F9 to 0x083E71F9, `R15` 0x0D7535B9 to 0x0D753D41). A pointer into reclaimed memory would have moved with them. The value is a 32-bit quantity sitting in the HIGH half of the word (`0x382811 << 32`, low 32 bits all zero), which is the shape of a field read at the wrong width or offset, or of a value that took a `shl 32` it should not have, rather than of a stale pointer. That is a different first suspect from the row above it: the row said a span taken from reclaimed memory, and the register evidence argues against that reading without settling what it is. **WHAT IS UNMEASURED AND IS THE NEXT STEP, NAMED SO IT IS NOT GUESSED:** which call in the SCOPE window actually reaches `copy-sx-pos`. `opening.codex:586-609` is that window and contains no `copy-sx-*` call at all; the nearest are the DESUGAR keep at `:561-567` (before the SCOPE marker) and the CHECK keep at `:706` (after it). The two candidates to separate first are `precise-escape-bag` at `:595`, which walks the AChapter graph through the runtime pmap table that only `-EscapeCheck` enables (`X86_64Compound.codex:1369,1513`), and a symbol misattribution in `seed/Codex.map` putting a neighbouring function under this name. Settle that before reading any more code, because both readings explain the symptom and only one survives a symbol check (L-MECHANISM: name the line the mechanism runs through, and grep it). Not chased further; blu had two box grants and spent them on the two runs above. |
| COMPILER-46 | **DISK compile mode fails with `CODEGEN-ERRORS:1` and NO diagnostic text: four empty lines where the message should be** | Found 2026-09-01 (fester) while answering whether the "four dead harnesses" row wanted a delete. `build/test-disk-compile.ps1` RUNS end to end and this is the only step that fails, so the harness is a working reproducer rather than a dead one (CurrentPlan, the harness row, has the full account). Reproducer, one command, about four minutes on QEMU: `pwsh build/test-disk-compile.ps1`, which compiles `codex/test/field-range-proven.codex` to CDX, lifts it to PE through the pe plug, builds an 8 MB FAT16 image through the img plug, boots that image under QEMU, and has the guest compile the source off its own disk. Everything up to the compile is healthy and says so: `DISK-SOURCE resolving [SOURCE.SRC] over 32 root sectors` then `DISK-SOURCE found cluster 178 size 651 sectors 2`, with the volume geometry printed above it (part 2048, bps 512, spc 2, data 2129, clusters 6087). Then four blank lines and `CODEGEN-ERRORS:1`. **The blindness is the first defect and it is separable from whatever the compile error is**: an error COUNT with no message cannot be diagnosed from the wire at all, and every consumer downstream inherits a number it cannot act on, which is why this is registered before the cause is known. Whoever takes it establishes first whether the diagnostic is not produced, produced and dropped on the DISK path's output route, or produced into a buffer nothing drains -- the DISK path prints through the same ring the DIAG lines above it came through, so the channel demonstrably carries text. NOT chased on discovery (fester was answering a different question). One thing IS established about the fixture and it is worth having: the SAME subject compiles clean to CDX in step 1a of the same run, which is what the image is built from, so this is not simply a broken source. TEXT mode was not tried on it, so `DISK-specific` is the working reading and not a measured one. |

| COMPILER-47 | **A `cites` name list is not validated: a name that resolves to nothing is silently accepted** | Found reviewing Steve Howell's PR 116 (reek, 2026-09-02), whose new cite line on Opening names `compile-flags-of`, a definition that exists nowhere in the PR, in its base, or in this tree. It survived his whole verification, a self-compile and a fixed-point comparison included, because nothing checks it. **Measured here with both controls, seed 930B322B.** Adding `no-such-definition-xyzzy` to the cite list of `codex/test/workflow-parse-int.codex` compiles clean, emits no diagnostic, and the CDX is BYTE-IDENTICAL to the control (91,324 bytes, same SHA-256), so the name has literally no effect. The POSITIVE control, a cite naming a chapter that does not exist, refuses at once: `error 3010: Unresolvable cite: Wflow chapter 'NoSuchChapterXyzzy'`, exit 8, no artifact written. So the CHAPTER half of a cite is checked and the parenthesized NAME half is not checked at all. This is L-ACCEPTED on our own front door: the list reads as a declaration and is decoration, so a typo, a renamed definition or a name left behind by a move is invisible. `check-subset-cites.ps1` does not close it, because it answers the other direction (is a USED name cited). Nothing measured is blocked: the names are advisory today. The fix is a refusal where the chapter check already runs. |

| COMPILER-48 | **Four dependencies on the deck discipline that nothing declares (Steve Howell, issue 115)** | **Instance (5) TAKEN by fester 2026-09-02** (root, after the bytes-loss row): measure the runtime consequence with a bounded probe first, then decide guard + poke, control both ways. Instances (1) to (4) are unclaimed and stay reek's row. Filed 2026-09-02. All four RE-MEASURED against head by reek before filing, and one does not hold here. **(1) VERIFIED.** `Syntax/SyntaxNodes.codex` carries exactly one cite, `Foreword chapter Maybe`, and uses 45 `for .. in .. ->` comprehensions, which desugar to `map-list`; in a bundle not otherwise carrying that chapter it is 20x CDX3002. **(2) DOES NOT HOLD AT HEAD.** The report says `Syntax/Lexer.codex` defines `copy-sx-diag` and so does `SyntaxNodes`, colliding as CDX3006. Measured: the definition exists ONCE, at `SyntaxNodes.codex:554`; `Lexer.codex:127` and `Parser.codex:930` CALL it, and `Lexer.codex:122` names it in prose. No duplicate and no collision. What IS true, and is the same shape, is that Lexer cites nothing while using a SyntaxNodes definition. **(3) VERIFIED.** `Syntax/Lexer.codex` has ZERO `cites` lines and uses `deck-record` 60 times. Whether `deck-record` is the intrinsic or a 4-byte identity is decided by the DEFINING CHAPTER in `Emit/X86_64Chapter.codex`, and a cite is what pulls that chapter into a bundle, so a bundle without it gets the identity with nothing said. **(4) VERIFIED as to the calling convention.** `check-chapter` issues its own `__deck-exit` at `Types/TypeChecker.codex:2388` and `__deck-enter` at `:2393`, so it exits an extent it assumes its caller opened, and `opening.codex` is the caller that opens one. Nothing above the signature says a caller owes that. The consequence Steve reports (called bare the nesting counter goes to -1, every `deck-record` inside the walk silently stops being one, and the damage surfaces in a LATER phase) is HIS measurement and is not re-measured here. **(5) VERIFIED, added by Steve 2026-09-02 and slightly WIDER than reported.** `emit-build` (`Emit/EmitAllocator.codex:13`) is `build` (`Core/PhaseAllocator.codex:34`) minus TWO things, not one: the `deck-reservation-guard` call as well as the `poke-qword-cell deck-reservation-top-cell (p + size)`. So an emit reservation never republishes the ceiling, and the chain that makes this matter is real at head: `build` writes cell **36344** when `deck-guard-enabled /= 0` and `hosted-kind == 0`; the emitter declares the same address as `deck-reservation-top-addr` (`Emit/X86_64Boot.codex:454`) and emits a load of it when arming the guard (`Emit/X86_64Builtins.codex:1096`). Nothing in `codex/compiler` or `codex/foreword` READS the cell in Codex; the only consumer is the code the emitter writes, which is why a source-only search reads as a dead cell. **MEASURED 2026-09-02 (fester), and it is worse than the row said: emit is UNGUARDED, not guarded against the wrong ceiling.** One bare-metal run of a probe compiler printing the armed cell against emit's own reservation at the moment `emit-build` takes it: `PROBE48 ceil=449847088 p=6627864 top=16392728`. The ceiling is **433 MB above emit's entire reservation, 27.4x its top**, so `R10` inside an emit extent is always far below it, the `jbe` in `deck-guard-code` always skips, and the `UD2` is unreachable. The margin is categorical, not marginal: no input can move `R10` those 433 MB, so this cannot be made to fire by reshuffling a workload. Emit is therefore the one phase where a deck overflow CORRUPTS where every other phase faults. **The opposite branch was refuted with no run at all:** had emit's reservation sat ABOVE the stale top, the first guarded allocation in any emit extent would `UD2` and a guarded self-compile could not finish -- it finishes on every gate, so the fault branch is dead by contradiction with a working artifact. Guard direction, read from `Emit/X86_64Helpers.codex:46`: the ceiling cell is tested against 0 and the whole check skipped when disarmed; otherwise `cmp R10, ceiling`, `jbe` past the trap, `UD2` on fall-through, so it faults when `R10 > ceiling`, and `emit-deck-arm` (`X86_64Builtins.codex:1092`) loads that ceiling from cell 36344 on the extent's 0-to-1 crossing. The structural half was confirmed by reading; the behavioural half is now measured rather than predicted. **The repair is NOT simply restoring the poke:** `Core/PhaseAllocator.codex`'s own prose records that arming the ceiling in the wrong place broke the guarded self-compile on 2026-08-27 (the guard fired on the first ordinary allocation after every reservation, `!EXC=06` at R10=0x14ADAECC), so the guard half wants reading before either line moves. **Read, and that history does NOT apply:** cell 36344 (reservation top) and cell 36336 (live ceiling) were ONE cell until 2026-08-28, which is exactly why arming at `build` time left the ceiling live outside extents where the bivy frontier legitimately sits above the reservation and tripped it. Since the split, writing 36344 is inert until `__deck-enter` arms from it, so `emit-build` may publish its own top by the same argument the split was made for. The two missing lines are also DIFFERENT mechanisms and only one is this defect: `deck-reservation-guard` (`PhaseAllocator.codex:84`) is a reservation-TIME check that pokes the guard page to fault immediately if `p + size` reaches it, nothing to do with the ceiling.

**FIX MEASURED, four arms, CL 22060 (fester, 2026-09-02).** `emit-build` takes both of `build`'s lines. (A) The fixed compiler compiles its own source to a HARD FIXED POINT, stage1 === stage2 byte-identical at `2E50AB28`, so emit does NOT exceed its own reservation and arming its real ceiling costs nothing -- the risk that this converts a working compile into a fault is refuted, not assumed. (B1) Fix plus a deliberately undersized emit reservation (`emit-build 65536` at `X86_64Chapter.codex:1248`) faults `!EXC=06` at `R10=0x091943B8`: the guard fires. (B2) **The same undersizing WITHOUT the fix exits 0, produces output, and raises no fault at all** -- emit overran its reservation by roughly 150 MB and nothing noticed. B1 against B2 isolates the behaviour to the two added lines, and B2 is the direct demonstration of the silent-corruption claim rather than an inference from the ceiling arithmetic. Remaining proof is the BVT as one granted run. The ask: teach `check-subset-cites` the dependency that is not a cite, since the emitter already computes the predicate; for (4) make `__deck-exit` refuse a non-positive counter, turning a silent -1 into a local failure; and for (5) decide whether an emit reservation should publish its own ceiling or deliberately inherit. All are COMPILER-47's shape: the mechanism degrades to a silent default instead of refusing. |

| COMPILER-50 | **`derive-deck-scale` clamps at 100 while a compiler-sized unit estimates 200, so the derivation cannot express the largest unit we have** | UNOWNED, raised by reek 2026-09-02 from the plugs 1.75 residue; read, not run. `derive-deck-scale` (`opening.codex:165`) is `est = unit-len * 100 * deck-scale-margin / deck-scale-anchor` with `deck-scale-margin` 2 and `deck-scale-anchor` 2,993,576, then `if est < 64 then 64 else if est > 100 then 100 else est`. A unit of anchor length estimates exactly **200** and is clamped to **100**, so every unit at or above HALF the anchor, **1,496,788 bytes**, gets the identical reservation: the derivation stops tracking length at half the anchor rather than at the anchor, and the file's own prose beside it says so. **Measured consequence, already paid:** the page's binary tab sent `CDX` with no `decks=`, derived, was clamped, and refused `CDX9002 Deck overflow in SCOPE` on a compiler-sized unit (`plugs-backlog.md`, the binary-tab 2.10). The page works around it with an explicit ladder, `DECKS = [12, 48, 125]` (`prism.html:693`), whose top rung is 25 per cent ABOVE the clamp the derivation can reach; so the only unit large enough to need more than the clamp is currently served by a hardcoded list in a web page rather than by the derivation. **What is NOT established here:** which phase binds. The prose names DESUGAR as the binding phase across 29 units above 150 KB (estimate-to-requirement 1.24 to 1.71), the page symptom was SCOPE, and the `PARSE-KEEP` overflow recorded in `plugs-backlog` 1.71 is STALE, having been measured on a wasm target with no deck at all. Naming the phase for a real compiler-sized unit wants the per-phase deck metrics a run already emits. **The repair is a decision, not a patch:** raise the clamp, make the estimate non-linear above half the anchor (the prose records CHECK usage per unit byte between 21x and 167x and not monotonic, which is why the margin exists), or rule that the ladder carries it and the derivation is for small units only. Seed-affecting whichever way it goes. |

## COMPILER-23 residue: `__cce_print` is DEAD WEIGHT, and it is not in any binary (reek, 2026-09-02)

Answered by reading and by the shipped artifact, no guest. The question was
whether the uncalled second printer is a replacement or dead weight (L-UNCALLED).

| symbol | emitter | emitter called? | symbol called? | in `Sut.map` |
|---|---|---|---|---|
| `__cce_print_multi` | `emit-cce-print-multi-helper` | YES, `X86_64Helpers.codex:1844` | YES, `X86_64IO.codex:350` | 1 of 5620 |
| `__cce_print` | `emit-cce-to-utf8-helper` | **NO** | **NO** | **0 of 5620** |

`emit-cce-to-utf8-helper` spans `X86_64Helpers.codex:926..1104`, **179 lines**,
and the only reference to it in the tree is its own signature and definition.
So `__cce_print` is not merely uninvoked: its bytes are never emitted, which the
compiler's own map confirms directly.

**This corrects the row above.** That analysis says the three rodata tables
never concatenated into `init-rodata` mean "the multi-byte print path reads
whatever follows". The live multi-byte path is `__cce_print_multi`, a DIFFERENT
symbol from a different helper. `__cce_print` is on no path at all, which is why
the corruption that analysis predicts was never observed.

**DELETED, ruled by Damian 2026-09-02.** The closure was re-measured at head
rather than carried forward (L-COUNT) and it is SIX definitions, not the three
this row first named: it missed `cce0u2-rodata-offset`, `cce0u2-hi` and
`cce-t2-rodata`. What went is `emit-cce-to-utf8-helper` (179 lines) and the
contiguous block `X86_64State.codex:493..533` (42 lines): `build-cce0u2`,
`cce0u2-vals`, `cce0u2-lo`, `cce0u2-hi`, `cce-t2-cbases`, `cce-t2-sizes`,
`cce-t2-ubases`, `le8-of`, `le8-cat`, `cce-t2-rodata` and the three offsets.
None is reached from `init-rodata`.

**Residue, deliberately not taken:** `cce-to-uni` and `cce-to-uni-t2` are now
unreferenced, but they sit in the tier-conversion family whose siblings serve
the live `utf8-to-cce` path, so peeling them is a separate change.

**The census instrument matters here.** The Grep tool silently skipped
`codex/compiler/` on this pattern and returned only the page bundle, which reads
as the symbol being absent from compiler source. A recursive
`Get-ChildItem | Select-String` over the 3,948 live `.codex` files is what
answered it.
| COMPILER-37 | **A Real literal wider than an i64 is read as a different number, silently, and the regression test that guards that parser cannot reach the wrap** | From Steve Howell's issue 106 (2026-08-30), verified against source by red 2026-09-01. `__text_to_double` accumulates the decimal digits as ONE wrapping i64, counts the places after the dot, converts once and divides once, so a literal past 19 digits wraps before it is ever a float: his measurement, `1152921504606846976.0` (2^60) reads back through `real-to-int` as `-691752902764108160` and `1000000000000000000.0` (1e18 written out) as `-844674407370955136`, exit 0, no diagnostic, identical on bare metal and through the zig plug, whose `zig-p-cx-text-to-double-bits` mirrors the seed deliberately (its own prose: "the bits land in IrNumLit, so they must match the seed's exactly; a parser that rounds better is a parser that diverges"), which is why he filed rather than patched: the two parsers must move together and the seed side is ours. `codex/test/ops/real-literal-rounding` guards the ROUNDING half of the same function (2026 repair, 106 of 580 literals) with twelve values all below ten, so it is structurally blind to the wrap. His cheap ask, separable from the fix: add 1e18 written out, 2^60 written out and an eighteen-digit value that fits, so the boundary is covered from both sides (the `.expected` has to record either today's wrong answers or a fix, which is why it is not a PR). Fix shape: parse the decimal into the float directly, or accumulate wider or checked and DIAGNOSE the overflow; failing loudly would be enough. It cost him a wrong gold file: a generator wrote a nineteen-digit literal and the oracle lied. Seed-affecting. Open, unowned. **HOST-SIDE GROUNDWORK, 2026-09-01 (blu), no code written.** Mechanism confirmed at the site: `emit-text-to-double-parse` (`Emit/X86_64TextHelpers.codex:552-557`) accumulates `acc = acc * 10 + digit` in ONE 64-bit register (`shl rax, 3` then two `add`s), so it wraps silently, and `cvtsi2sd` then converts the wrapped and possibly negative i64. The prose above it at `:505` already names the SEPARATE and accepted 2^53 precision limit; the wrap is not that. **THE FIX SHAPE THE ROW LISTS FIRST IS THE ONE THE FLEET CANNOT LAND ALONE.** Changing the accumulator (to a double, or wider, or checked) changes `__text_to_double`, and the zig plug's `zig-p-cx-text-to-double-bits` mirrors it deliberately so the two must move together -- and `codex/plugs/zig/**` is Steve Howell's and not the fleet's to change (COMPILER-13). That coupling is why he filed rather than patched, and it does not go away by being ignored. **DIAGNOSING AT THE LITERAL INSTEAD AVOIDS THE COUPLING ENTIRELY**, because the helper's behaviour is unchanged and a refused literal never reaches either parser; the row already sanctions this with "failing loudly would be enough". **THE HOOK EXISTS AND IS EXACT: `TypeCheckerInference.codex:1548` already reads `is ALitExpr (val) (kind) (s) -> infer-literal (check-int-lit-range st val kind s) kind`**, so there is a site carrying the UnificationState (the bag), the literal TEXT, the KIND and the SPAN, and it already performs precisely this class of check for INT literals. A `check-num-lit-range` beside it is additive and moves no signature. **Do NOT try to raise from `lower-literal` (`IR/Lowering.codex:334`): it is `Text, LiteralKind, SourceSpan -> IRExpr`, pure, with no bag**, which is the same wall COMPILER-28 hit in the resolution layer and solved with a separate walk where the bag already exists. **The boundary is 19 digits, not 20:** i64 max is `9223372036854775807`, itself 19 digits, so a 19-digit literal may or may not wrap and needs a comparison rather than a length test; 20 or more always wraps. **The row's "cheap ask" of adding the boundary tests is NOT separable in practice**, since the `.expected` would have to enshrine today's wrong answers; land the tests WITH whichever fix is chosen. |
| COMPILER-43 | **Update 54's check compact validates a type box by READING ITS TAG WORD out of raw memory, so on every HOSTED target it answers `ErrorTy` for the whole chapter with `check-errors 0`** | Found 2026-09-02 by Steve Howell's plug-oracle ladder, on the zig arm, against seed `FCBABF07479516DE`. Nine of the ladder's fourteen rungs are red and the nine are exactly the nine harnesses that call `check-chapter`; the five green ones never do. `check.truth` and the zig arm differ in three lines of fifty-four -- `tb fib fn` against `tb fib other` -- while `check-errors 0`, `substitutions 8`, `next-id 8` and `expr-types 11` agree, so the checker neither complains nor loses count. It binds every top-level function to `ErrorTy`, which `CodexEmitter` renders `Unknown`; `lower` then has no parameter type for `n`, `n + n` becomes an error node, and every rung downstream carries "arithmetic operand is neither a number nor an integer". **MECHANISM.** `check-batch-close` and `check-batch-rewrite-acc` are new in Update 54 (`git log -S`, both first appear in the release commit) and route every binding through `mcopy-type`, whose guard is `peek-qword (address-of ty) 0` ranged against the 29 `CodexType` tags (`Types/Unifier.codex:1387`). That is a bare-metal representation contract -- a box's first qword IS its tag -- and no plug can meet it: a hosted target's objects live in its host allocator while `address-of`, `peek-*` and `__heap-save` answer for a different region entirely. Measured by instrumenting the emitted zig: the peek returns `0x8000000000000000` and `0xAAAAAAAAAAAAAAAA`, untouched page fill, both negative, so every box takes the first unsafe escape. `mc-ceiling` also reads BELOW `mc-floor` in that space, so the floor and ceiling tests are noise as well; the tag guard is merely the one that fires first. **WHY IT IS SILENT.** `mcopy-type` answers `ErrorTy` rather than sharing, deliberately (`Unifier.codex:857`), and on bare metal the caller catches it by comparing the keep frontier against `mc-ceiling` and raising CDX9002. That is a second measurement in the same fictional address space, so on a hosted target the guard fails OPEN. **FIX, MEASURED.** The whole of `check-batch-close` is bare-metal memory management -- it moves the deck cursor twice, `__memset`-poisons the two spans it gives back, and rewinds the bivy -- so the guard belongs at the operation, not at the copier: guarding `mcopy-type` alone was tried and REFUTED by running it, `check` went green and the four rungs behind it died on a corrupt switch. `hosted-kind /= 0` at the top of `check-batch-close`, exactly as `PhaseAllocator.codex:38` and `:86` already guard raw-memory work for the same reason, takes the zig arm from 5/14 to **14/14 byte-identical** against a bare-metal bank re-measured under the changed compiler. Nothing is lost: the close exists to materialise the batch into the keep BEFORE the scratch is reclaimed, and a hosted target reclaims nothing. **State this in review rather than discover it later:** a skipped close leaves `cb-keep`, `cb-batches` and `cb-overflow` at their initial values, so `check-batch-note` reads zeros and CDX9002 cannot fire on a hosted arm -- both correct, since a hosted arm has no keep to fill and no reservation to overflow, but no ladder harness prints that note so nothing here measured it. Every non-bare-metal plug is affected, not only zig; the C# plug answers `0L` for `address-of` and takes a different escape from the same guard. Steve Howell's; PR attached. |

## COMPILER-53 -- CONTRIBUTED by Steve Howell (PR pending, 2026-09-06): cite resolution runs once per SLUG TRANSITION where once per unit would do, and the slug cache is one entry per definition

`scope-adefs-ll` rebuilds a chapter's rename map at every slug transition of
the defs walk, and `apply-cite-overrides-loop` resolves every citation each
time through `find-slug-for-cite-name`. That function depends on the quire, the
cite name, the assignments and the slug cache, and NONE of those changes while
the walk runs, so it recomputes identical answers at every chapter boundary.

The product is (slug transitions) x (citations) x (slug cache), and the cache
is one entry per DEFINITION rather than per chapter, which is what makes it
large. Compiling the compiler: 91 transitions, 106 citations, 6,431
assignments -- 9,646 resolutions each walking 6,431 entries with a character-
by-character suffix compare per entry.

`scope-achapter` now resolves each citation once, beside the cache that feeds
them, and carries the list down; `apply-cite-overrides-loop` reads
`list-at cite-slugs i`.

MEASURED, on the compiler compiling itself through a tree-walking interpreter
where the work is counted rather than timed:

    without   43,762,877,372 steps
    with      29,306,453,914 steps        a third of the compile

and the IR is byte-identical either way: 19,570,302 bytes over 6,890
definitions, against an arm that shares no code with the one that produced it.
Fourteen smaller subjects are byte-identical too, with their step counts
unchanged to a tenth of a percent -- a four-chapter bundle with one citation
resolves four times and saves three, so a small subject cannot show this and
should not be expected to.

WHAT IS NOT FIXED: the character loop underneath. A `text-contains` guard in
front of it was tried and reverted, because this bundle DEFINES `text-contains`
(`text-index-of s sub >= 0`), so the call site gets a Codex substring search
rather than the builtin and the compile gets dramatically slower. CDX3005 says
exactly that will happen and is printed on every build. `codex/test/slug-suffix.codex`
carries that finding with the eleven pairs a slug walk can present, including
the one that discriminates: `Foo--Bar--Baz` contains `--Bar` and does not end
with it.

Not seed-affecting; the compiler is on the path, so it takes the token.

Reported and prepared by Claude (Anthropic), working with Steve Howell.
